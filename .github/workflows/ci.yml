name: CI

on:
  push:
    branches: [main]
  pull_request:

# Cancel in-progress runs on the same PR / branch when a new commit lands.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  ci:
    name: Lint, typecheck, license, test, build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up pnpm
        # No `with.version` — pnpm/action-setup@v4 rejects when both
        # the action input AND package.json's `packageManager` field
        # are set. We pin via packageManager: "pnpm@11.3.0" in
        # package.json (npm-standard, more specific). The action
        # reads it automatically.
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          # Node 22 LTS — required by pnpm 11.3.0 (which uses node:sqlite,
          # added in Node 22.5). Bumped from 20 after pnpm/action-setup@v4
          # surfaced the version conflict. Node 20 goes EOL Apr 2026; 22
          # is the current LTS through Apr 2027.
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript typecheck
        run: pnpm typecheck

      - name: ESLint
        run: pnpm lint

      - name: ESLint Electron main
        run: pnpm lint:electron

      - name: Prettier
        run: pnpm format:check

      - name: License audit (ADR-008 / ADR-017)
        run: pnpm license-check

      - name: Vitest (unit + property + snapshot)
        run: pnpm test

      - name: Build web bundle
        run: pnpm build:web

      - name: Build Electron main process
        run: pnpm build:electron-main

      - name: Verify file-size discipline (coarse raw-line backstop)
        # ESLint's max-lines rule (eslint.config.mjs) is the real gate and runs
        # in the "ESLint" step above: 400 lines EXCLUDING blank and comment
        # lines (skipBlankLines + skipComments), per ADR-015. This step is only
        # a coarse backstop against catastrophic raw-line bloat. It counts EVERY
        # line, including the explanatory comments CLAUDE.md mandates, so its
        # threshold is deliberately looser than the 400 code-line rule.
        run: |
          MAX=600
          BAD=$(find src -name '*.ts' -o -name '*.tsx' | xargs wc -l | awk -v max=$MAX '$1 > max && $2 != "total" { print $2 " (" $1 " lines)" }')
          if [ -n "$BAD" ]; then
            echo "Files exceeding $MAX lines:"
            echo "$BAD"
            exit 1
          fi
