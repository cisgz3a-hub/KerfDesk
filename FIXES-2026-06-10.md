# Audit fix session — 2026-06-10

Disposition of every AUDIT-2026-06-10.md finding. Work landed on branch
`fix/audit-2026-06-10` (32 commits on top of `e3da1a1`), one finding per
commit, failing-test-first, full `pnpm test` / `lint` / `typecheck` green at
every commit (finished at 1325 tests / 186 files; baseline was 1210 / 172).

**PR note:** the description must carry
`Snapshot change acknowledged: new H15 corpus fixtures only — no existing snapshot modified.`

## Step 0 — C1 reconcile

Local main was fast-forwarded 88 commits to real GitHub main `e3da1a1`
(the PR #11 merge of `wip/checkpoint-2026-06-03`). Every finding was then
re-verified against the new tree by a 12-agent read-only pass before any fix
was written. The remaining C1 item — branch protection requiring green CI on
main — is a GitHub settings change only the maintainer can make.

## Fixed upstream by the reconcile (verified against e3da1a1, no action)

C2 (raster mirror — `orientRasterLumaForMachine`, orientation pinned in
compile tests), H1 (G92 fill/image preflight), H2 (hatch in scene space),
H4 (SVG transform stack), H5 (error:N terminal + ADR-041), H7 (center-origin
bounds), H10 (trace worker retirement), M2 (raster ETA), M8 (trace watchdog),
M9 (preview token race), M16 (autosave failures surfaced — quota ceiling
itself remains), M32 (Electron navigation policy), LC4 (deploy repo guard),
LU30 (origin-gated permissions), LU31 (unknown-font fallback toast).

## Fixed this session (29 fix/test/docs commits)

| Finding | Commit | Fix |
|---|---|---|
| C3 (remaining half) | 67807d5 | beforeunload/pagehide best-effort `RT_SOFT_RESET` mid-job |
| H3 | 60f82d4 | preview toolpath mapped back to scene frame (`toSceneCoords`, `jobOriginOffset`) |
| H6 (remaining half) | 134da06 | JogPad disabled while a job is active |
| H8 | 4791330 | grammar-aware arc-flag tokenizer (`a4 4 0 011 7`) |
| H9 (remaining half) | 9895996 | viewBox×physical-size unit scaling + 96 DPI px; **ADR-046** |
| H12 (mitigation) | fb1b614 | Start-time warning when burn grid exceeds stored luma |
| H13 | 042dced | job-aware dialogs: native alert/confirm/prompt degrade to toasts mid-job, confirm fails closed |
| H14 (remaining half) | f08fa50 | `import/no-cycle: error` enabled (tree is cycle-free); `--passWithNoTests` removed |
| H15 | 37418e3 | snapshot corpus through the shipped `emitGcode` composition: fill-donut+overscan, raster, mixed M3/M5/M4, user-origin, curves |
| H16 (remaining half) | b87cf3a | 250 ms trailing-debounce job estimate; frame action reads state at call time |
| M1 | ef5b99f | bounds failure near an edge now names overscan + remedy |
| M3 | 4571193 | negative scale engraves as the mirror the canvas shows |
| M4 | fdf540e | shared `isClosedEnough` — Convert to Bitmap fills what Fill fills |
| M7 | a18d335 | `INK_LUMA = 127` — converted bitmaps burn under Threshold |
| M10 | 8df5e83 | trace retry keeps `fixedPalette` (same backend; kills the IoU-0.25 degeneracy path) |
| M11 | 0dfaf02 | export header records assumed $30/$32; save gated on controller mismatch |
| M12 | 64582c4 | byte-per-char wire encoding — 0x85 jog-cancel is one byte (it previously did NOTHING) |
| M13 | 1dc56c1 | oversized-line refusal + 10 s ack-stall watchdog with safety notice |
| M14 | 619e8b4 | M3 S0 preamble documented truthfully (header, PROJECT.md, LIGHTBURN-STUDY §8.3) |
| M15 | 62cd10f | autosave recovery marks dirty + keeps slot until next manual save |
| M21 + LU6 | f741cc9 | sanitizer href ALLOWLIST (closes `//evil` + whitespace bypass) + the documented malicious corpus now exists with integration tests |
| M22 | 2f41384 | Ctrl+Return start / Ctrl+. stop; stop bypasses modal/editable gates |
| M23 | b3b350f | hiding an image layer hides its bitmaps |
| M25 | 78d0776 | layer inputs snap to the committed (clamped) value |
| M26 | fdab192 | PNG/JPG drag-drop routes through the image import pipeline |
| M29 | 01907f2 | core lint: console/process restricted + node-import ban |
| M33 (remaining half) | 7051caf | deploy checks out `workflow_run.head_sha` |
| M34 | 795ee58 | pnpm-aware license gate (14 prod packages covered vs 6) |
| M35 | 64451ac | preflight mirror rejection relaxed to rotation-only (output-pinned) |
| LU9/LU10/LU26/LU34 | 876c37f | four phantom/stale comments corrected |
| LU35 | d80aaca | self-contained `public/404.html` disables Pages' implicit SPA fallback |

## NOT fixed — needs maintainer decision (flagged, per audit instructions)

1. **H11 — potrace provenance.** Unchanged: no ADR, no provenance headers,
   GPL-source fingerprints, default backend. Legal/licensing call. The new
   license gate explicitly documents it cannot clear vendored source.
2. **C1 — GitHub branch protection** (require green CI before merge to main).
3. **stash@{0}** (unapplied): HD1 (change pause/stop write-failure status to
   `'errored'`), M36 (0–360 angle dead range), M37 (PNG re-encode data loss —
   needs an ADR), M38 (`pnpm audit --audit-level=low` hard gate), M39
   (deleted safety comments), LU23 (untested helper). §4's
   merge-with-fixes-in-slices verdict stands; note the stash no longer
   applies cleanly to the rewritten `laser-store.ts`.
4. **M31 — Playwright smoke layer** (new dependency + CI surface).
5. **Design-level remainders:** M16 (IndexedDB / payload-stripping for the
   ~5 MB autosave ceiling), M17 (schema v2 bump + migrators — file-format
   decision), M19 (dataUrl interning), H12 full fix (decouple engrave luma
   from the ADR-037 trace cap), M5 (retain curves, re-flatten at compile —
   needs an ADR), M28 (module-level mutable-cache policy: ADR exception +
   lint selector), LU16 (immediate 0x18 vs hold-then-reset — record the
   trade-off), LU28 (desktop update story).
6. **LC3 — Roboto Apache-2.0 license copy** must ship verbatim (bundle +
   About); quick once the canonical text is added.

## NOT fixed — still open, no decision needed (next session candidates)

M6 (text adaptive flattening), M18/M20 remainders (preview cache keys/LRU,
`sliceToolpath` copying), M24 (multi-select group move; the store comment
still overstates Move), M27 remainder (Preview toolbar button, empty hint,
OOB banner), M30 remainder (out-of-bed preflight fuzz; raster in mixed-job
fuzz), LC1, LC2, LU1–LU5, LU7, LU8, LU11–LU15, LU17–LU22, LU25, LU27, LU29,
LU32, LU33, LU37. (LU36 stays documented-intentional per the audit.)

## What was NOT verified (Karpathy's law)

- **No hardware burns.** C2's orientation fix, C3's unload write, M12's
  single-byte jog-cancel, M13's watchdog, and the M3/M35 mirror behavior are
  pinned structurally/unit-level only. The F.2.f burn checklist (asymmetric
  test image for orientation) remains the decisive check.
- **No live-browser rendering.** H3's registration is asserted on
  coordinates, not pixels; all dialog/shortcut fixes are jsdom-verified.
- **No LightBurn side-by-side.** LightBurn behaviors are cited from
  LIGHTBURN-STUDY.md and vendor docs.
- **No raster/fill perceptual fixtures yet** — the suite still cannot see
  engrave fidelity (audit §2 blind spot; Stage 0 item 3).
- `pnpm build:web` / `build:desktop` and the deploy workflow changes (M33,
  LU35) were not exercised against production.
