// Compatibility seam for the tracer. The fitter is shared geometry because
// vector stroke fonts use the same deterministic least-squares fairing.

export { fitCubicsThroughPoints, sampleCubics, type CubicBezier } from '../geometry';
