// ui/design-studio — the full-window on-canvas design surface (ADR-271,
// Phase N). App mounts the host; everything else is internal to the lazy chunk.

export { DesignStudioHost } from './DesignStudioHost';
export { useDesignStudioStore } from './design-studio-store';
