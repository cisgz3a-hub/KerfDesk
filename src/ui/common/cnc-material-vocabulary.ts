// One name for the no-recipe state.
//
// The same condition — no material recipe governs these numbers, they are
// hand-set — was called "Custom (manual feeds)" by the project picker and the
// Job Review stock card, but "Manual — verify feeds" by the layer card. An
// operator comparing the two surfaces could not tell whether they described
// one state or two. The wording that carries the instruction wins.

export const MANUAL_FEEDS_LABEL = 'Manual — verify feeds';
