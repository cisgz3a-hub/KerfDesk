/** Default physical width in millimetres for a newly imported relief. */
export const DEFAULT_RELIEF_WIDTH_MM = 100;
/** Default maximum cut depth in millimetres for a newly imported relief. */
export const DEFAULT_RELIEF_DEPTH_MM = 5;
