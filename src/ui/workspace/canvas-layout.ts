// Keep the fitted bed clear of the ruler strips. The six-pixel gap preserves
// the origin marker and bed-edge separation when ruler text is enlarged.
export const RULER_THICKNESS_PX = 24;
export const CANVAS_PADDING_PX = RULER_THICKNESS_PX + 6;
