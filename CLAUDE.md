# LaserForge agent compatibility

Use `AGENTS.md` in this checkout as the single active repository instruction source. The user's explicit task instructions take precedence over file and skill guidance.

Read only the task-relevant sections of `PROJECT.md`, `DECISIONS.md`, and `WORKFLOW.md`; do not load every manual by default. Preserve unrelated work and do not operate hardware, merge, deploy, or publish unless the user requests it.
