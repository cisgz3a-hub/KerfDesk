node_modules
dist
build
release
dist-electron
coverage
pnpm-lock.yaml
src/core/text/cnc-stroke-font-data.ts
# Generated by scripts/generate-project-library.mjs. JSON.stringify always
# expands short arrays and prettier always collapses them, so formatting these
# would make every regeneration dirty the tree.
src/ui/library/assets/projects
.claude
references

# Spec documents are hand-formatted. Re-enable per-file once we're ready to
# adopt prettier's markdown style across the docs.
*.md
