# LaserForge Roadmap 鈥?Verified Shipped Audit

**Audit date:** 2026-04-30
**Repo state at audit:** master at `7fb7b7f` (`feat(connection): auto-detect $32=0 with one-click remediation banner`)
**Method:** Static probe of the working tree against every ticket in `docs/ROADMAP.md`. Each row backed by code identifier search, file existence, test file existence, or explicit T-marker comment in source. Where a ticket has multiple parts, each part assessed.
**Companion file:** `docs/ROADMAP.md` (the master ticket list).

---

## How this file is maintained

This file is the **verified ledger** that pairs with `docs/ROADMAP.md`. It exists because the roadmap describes *what should be done* and this file describes *what has been done*. Without this pairing, audits drift and the same work gets re-proposed across sessions.

**Update rules:**

1. Every commit that ships a roadmap ticket must update this file in the same commit.
2. Add the ticket to the appropriate "Shipped" section with the actual commit hash. Use `b4769f0` in the editor; substitute after `git commit` reports the hash.
3. If a ticket was *partial* and the new commit completes it, move the row from "Partial" to "Shipped" and update the evidence column.
4. If a ticket was open and the new commit completes it, move the row from "Open" to "Shipped."
5. **Do not** delete rows. The history is part of the value.
6. **Do not** rewrite past evidence. If a previously-claimed-shipped ticket turns out to have a regression, add a new dated row noting the regression rather than altering the original.
7. When the score table at the end of `docs/ROADMAP.md` (`### Current classification`) crosses a meaningful threshold (e.g., a Gate 1 cluster fully closes), update that table in the same commit. Don't update for every micro-change.

**Re-audit cadence:**

- Full re-audit at every Gate transition (Gate 1 鈫?Gate 2, Gate 2 鈫?Gate 3).
- Spot re-audit any time a contributor isn't sure whether a ticket is shipped (cheaper to re-verify than to redo work).
- The previous full re-audit was 2026-04-30; the one before was 2026-04-27 (Gate 1 cluster journal entry).

---

## Headline numbers

| Tier | Total | Fully shipped | Partial | Open | Shipped + partial |
|---|---|---|---|---|---|
| 0 | 4 | **4** | 0 | 0 | **100%** |
| 1 | 94 | ~37 confirmed (Gate 1 cluster + verified-this-session + T1-6, T1-19 closed 2026-04-30; T1-17 partial — pass 1 shipped) | 6 | 3 confirmed | est. ~96% |
| 2 | 127 | **9** | **3** | **115** | ~9% |
| 3 | 89 | **2** | 0 | **87** | ~2% |
| 4 | 9 | **2** | 0 | **7** | ~22% |
| **Total** | **323** | ~49 | 6 | ~210 confirmed-or-likely | 鈥?|

**Gate status (per `docs/ROADMAP.md` 搂 Release Readiness):**

- **Gate 1 鈥?Private Technical Alpha: cleared.** Every Gate 1 cluster ticket (dirty-state, commercial credibility, diagnostic recovery, installer correctness, easy security wins, architectural safety) is shipped.
- **Gate 2 鈥?Public Beta: open.** Tier 2 architectural work is the path; ~115 tickets remaining of various sizes.
- **Gate 3 鈥?Paid Production Release: open.** Tier 2 commercial / release-engineering / supportability / security clusters all open.

---

## Tier 0 鈥?All 4 shipped

| Ticket | What | Evidence | Hash |
|---|---|---|---|
| T0-1 | Comment-stripping in start-job | No `startsWith(';')` filter at `src/ui/components/ConnectionPanelMain.tsx:578` | pre-session |
| T0-2 | Three unsafe gcode templates fixed | `Park near far corner` rename + `BED_WIDTH_MINUS_5` substitution at `src/core/plan/GcodeTemplates.ts:113,174`; `HOMING_REQUESTED_BUT_DISABLED` preflight at `src/core/preflight/Preflight.ts:98,210` and `src/core/preflight/rules/TemplatePreflight.ts:72`; `M300` demoted to migration-only `LEGACY_FOOTER_BODY__WITH_BEEP` at `src/core/plan/GcodeTemplates.ts:214`, only referenced from `src/core/devices/DeviceProfile.ts:401` for legacy migration | pre-session |
| T0-3 | `pauseResume` split | `pause()` and `resume()` at `src/app/MachineService.ts:775,780`; callers at `src/ui/components/ConnectionPanelMain.tsx:909,911` | pre-session |
| T0-4 | Wainlux removal | All bridge code gone; one comment-only ref at `src/core/devices/DeviceProfile.ts:77` ("e.g. Wainlux $23=3"); `scripts/wifi-bridge.mjs` is now a generic Falcon WiFi bridge, not Wainlux-specific | pre-session |

**Plus three pre-Tier-0 fixes** that pre-date the formal roadmap numbering:
- Electron permission handler tightened to `serial` only (`6b980a9`)
- `BaseGCodeStrategy.currentSpeed` reset on `generate()` (`6b980a9`)
- `compileGcode(scene)` helper extracted (`6b980a9`)

---

## Tier 1 鈥?Gate 1 cluster shipped, 5 confirmed open

### 鉁?Shipped (Gate 1 cluster + verified individually this session)

The Gate 1 cluster 鈥?required for Private Technical Alpha 鈥?is fully closed. Per the roadmap's own definition this means LaserForge can be distributed to a small group of technically competent testers running supervised jobs on scrap material.

#### Dirty-state cluster (5/5 shipped)

| Ticket | What | Evidence |
|---|---|---|
| T1-68 | Autosave awaits write before clearing dirty | `writeAutosaveAsync` at `src/app/autosavePersistence.ts:45` (`Promise<void>` via `persistAutosave`). Autosave interval in `src/ui/components/App.tsx:1252–1287`: `void writeAutosaveAsync(json).then(…)` clears dirty only after write resolves (1274–1278); failure preserves dirty (comment 1281–1282). Same-content short-circuit 1265–1267. Test `tests/autosave-dirty-flag-on-failure.test.ts` (not spec filename `autosave-awaits-write.test.ts`). **Ship hash:** not recorded — see `docs/ROADMAP.md` T1-68 Status. |
| T1-69 | Manual save needs acknowledgement | `handleKeyboardSave` in `src/ui/hooks/useFileHandlers.ts:45-67` awaits `saveSceneToFile`, then awaits `showConfirm('File saved?', …)` (app cannot confirm browser downloads). **`syncAutosaveAfterFileSave()` only on user `ok: true`**; cancel leaves dirty for retry. Comment block **52-56** cites T1-69. Pinned by `tests/manual-save-needs-acknowledgement.test.ts`. **Implementation hash not recorded** — predates 2026-05-02 audit cleanup; see `docs/ROADMAP.md` T1-69 Status. Formalized 2026-05-03. |
| T1-73 | Delete marks dirty | `tests/delete-marks-dirty.test.ts`; `App.tsx:handleDelete` routed through canonical `handleSceneCommit` |
| T1-74 | Text `patchTextGeometry` commits history | `tests/text-property-edits-undoable.test.ts`; `PropertiesPanel.tsx:patchTextGeometry` calls `onSceneCommit` directly |
| T1-75 | Undo/redo marks dirty + invalidates | `tests/undo-redo-invalidation.test.ts`; `App.tsx:handleUndo/handleRedo` route through `applyHistoryScene` |

#### Commercial credibility (4/4 shipped)

| Ticket | What | Evidence |
|---|---|---|
| T1-77 | `DEFAULT_TESTER_HMAC_SECRET` removed | `src/entitlements/testerKey.ts:4` comment ("the previous `DEFAULT_TESTER_HMAC_SECRET` export was removed") |
| T1-81 | CI check 鈥?no dev auto-unlock in production | `scripts/verify-production-build.mjs` checks `tier:'developer'` literal, legacy tester HMAC, debug API leakage, mock-entitlement leakage, vitest leakage, source map references; wired into `npm run build`. Closed by **T3-82** (`de3fbc7`). |
| T1-83 | Strip Electron source maps | `vite.config.ts:12` `sourcemap: 'hidden'`. Closed by **T2-105** (`b6a56ed`). |
| T1-85 | Remove `--dev` arg escape hatch | `electron/main.ts:12` "T1-85: the previous --dev arg escape hatch was removed"; line 213 "No more --dev arg path" |

#### Diagnostic recovery (2/2 shipped)

| Ticket | What | Evidence |
|---|---|---|
| T1-87 | Failed-start persists log | `src/app/MachineService.ts:523,541` (T1-87 markers). Closed by **T2-67** (`a1bb80f`). |
| T1-88 | Replay capture not Pro-gated | `src/app/MachineService.ts:484` "T1-88: replay capture is no longer Pro-gated" |

#### Installer correctness (2/2 shipped)

| Ticket | What | Evidence |
|---|---|---|
| T1-84 | Restrict `storage:clear` IPC | `electron/main.ts:357` "T1-84: storage:clear IPC was removed"; `electron/preload.ts:22` `storageClear removed` |
| T1-86 | npmRebuild decision | `package.json:24,28` `_npmRebuildRationale` + `npmRebuild: false` with full justification; `tests/native-deps-prebuild-check.test.ts` |

#### Easy security wins (6/6 shipped)

| Ticket | What | Evidence |
|---|---|---|
| T1-89 | Electron renderer `sandbox: true` | `electron/main.ts:145` `sandbox: true`; `tests/electron-renderer-sandbox.test.ts` |
| T1-90 | `setWindowOpenHandler` + `will-navigate` | `electron/main.ts:102,109,165,174`; `tests/electron-navigation-blocked.test.ts` |
| T1-91 | G-code template variable sanitization | `src/core/plan/GcodeTemplates.ts:87` `.replace(/[\r\n]+/g, ' ')`; `tests/gcode-template-sanitization.test.ts` |
| T1-92 | `dialog:open` size limit by extension | `electron/main.ts` `dialog:open` handler; `tests/dialog-open-file-size-limit.test.ts` |
| T1-93 | `dialog:open` returns basename only | `electron/main.ts:313,321` "T1-93: return basename only" + `path.basename(filePath)`; `tests/dialog-open-no-full-path.test.ts` |
| T1-94 | Falcon WS frame cap | `electron/falcon-wifi/FalconWebSocket.ts:45,46` `MAX_WS_FRAME_BYTES = 256 * 1024`, `MAX_WS_BUFFER_BYTES = 1024 * 1024`; buffer-overflow check at line 312; `tests/falcon-ws-frame-cap.test.ts` |

#### Architectural safety (3/3 shipped)

| Ticket | What | Evidence |
|---|---|---|
| T1-18 | Test-fire deadman service-owned | `TEST_FIRE_DEADMAN_MS = 5000` at `src/app/ExecutionCoordinator.ts:43`; service-owned timer + arming logic; `ExecutionCoordinator.beginTestFire` arms its own deadman per `ConnectionPanelMain.tsx:975` |
| T1-22 | Critical write awaitability | `writeCritical` and `writeByteCritical` in `src/communication/SerialPort.ts:16,21`, `src/communication/WebSerialPort.ts:63`; `tests/safety-write-failure-surfaces.test.ts` |
| T1-59 | Frame-before-start gate | `hasFramed` ref at `src/ui/components/ConnectionPanelMain.tsx:294`; gate at `Workflow.tsx`; reset on scene change at `ConnectionPanelMain.tsx:405,416,420` |

#### Other Tier 1 verified shipped this session

| Ticket | What | Evidence |
|---|---|---|
| T1-5 | `_stopOnError` per-profile configurable | `stopOnError?: boolean` field on `DeviceProfile` at line 161 |
| T1-6 | Classify `sendCommand` and gate dangerous (service-layer) | `MachineService.sendCommand` rejects user-source warn/dangerous lines. Originally took an `acknowledged: severity` flag (T1-6, `ef8ac92`); **superseded by T1-19** which replaced the flag with a single-use approval token. The runtime gate is the same wall, just with stronger guarantees on the token contract. |
| T1-7 | JobLog QuotaExceededError visibility | `tests/job-log-quota.test.ts` |
| T1-8 | Acceleration-aware power sanity bounds | `tests/plan-accel-sanity.test.ts` |
| T1-9 | Frame bed extents preflight | `tests/bed-height-resolver-parity.test.ts` |
| T1-10 | Wake lock during active jobs | `tests/wake-lock.test.ts` |
| T1-11 | Canvas 鈫?machine coord mismatch | `tests/scene-canvas-machine-coord-check.test.ts` |
| T1-12 | Preflight refired on every status tick | `samePreflightSummary` at `ConnectionPanelMain.tsx:101,376`; commit `4ad42d8`. **Note:** dep-coverage follow-up not shipped (firmware-homing read without listing in deps). |
| T1-13 | Double power attenuation in M4 raster | `appendBurnMoves2D` at `src/core/plan/PlanOptimizer.ts:539,670,767` |
| T1-14 | Max-update-depth crashes during fast resize | `lastSyncedValue` in `src/ui/components/NumberInput.tsx:40,45` |
| T1-15 | MachineService job lifecycle hardening | `tests/machine-service-job-lifecycle-safety.test.ts` |
| T1-16 | Render-loop crashes after job completion | `samePreflightSummary` + `preflightRef.current` in `ConnectionPanelMain.tsx` |
| T1-19 | Service-level approval tokens for dangerous commands | `MachineService.requestApproval(cmd)` mints command-bound, single-use, 30 s TTL `ApprovalToken`s for warn/dangerous classifications. `MachineService.sendCommand(cmd, source, approvalToken?)` enforces token presence + match + expiry + nonce-not-replayed with bounded consumed-nonce retention. UI at `ConnectionPanelMain.sendCmd` calls `requestApproval` after the confirm dialog and threads the token through. Structured `Error.blockReason: 'no-token' \| 'token-mismatch' \| 'token-expired' \| 'token-replayed'`. `tests/machine-service-user-sendcommand.test.ts` (46/46). Supersedes T1-6's simpler `acknowledged` flag. Shipped 2026-04-30 in `1a78fdf`. **Static guard owed** as follow-up (`tests/no-direct-controller-sendcommand-from-ui.test.ts`). |
| T1-20 | WCS no-listener fallback hardening | `allowHeadlessWcsAutoNormalize` on `GrblControllerOptions` at `src/controllers/grbl/GrblController.ts:52-54`; T1-20 doc comment **39-50**; `_placementUncertain` **175**, `_allowHeadlessWcsAutoNormalize` **181**, `getPlacementUncertain()` **213-215**; `_emitWcsPayload` no-listener branch **1034-1073** (default: mark placement-uncertain; opt-in restores pre-T1-20 auto-apply). UI/service refuse job start while uncertain. Tests: `tests/wcs-no-listener-blocks-job.test.ts`, `tests/wcs-no-listener-headless-flag.test.ts`. Shipped **`b0375fa`**; ROADMAP T1-20 Status formalized 2026-05-03. |
| T1-21 | Frame-dot try/finally safety scope | `frameDot` at `src/app/ExecutionCoordinator.ts:140` |
| T1-24 | Error/alarm handlers send laser-off | `_handleError` at `GrblController.ts:1127` and `_handleAlarm` at line 1214, both with T1-24 markers and `safetyOff` calls; `tests/error-handler-sends-safety-off.test.ts`. Commit `2600666`. |
| T1-95 | `ui-start-job-uses-ticket.test.tsx` frame-wait insufficient post-T1-59 | `tests/ui-start-job-uses-ticket.test.tsx:283` `await flush(1400);` covers worst-case `frameSafe` corner-streaming + idle-poll budget. Was the "known pre-existing failure" carried as baseline through Fixes #1-#3. Shipped 2026-04-30 in `05ce7b86`, bundled with T1-17 Pass 4a (workflow rule #4 violation, retroactively documented here for bisect hygiene). |
| T1-96 | Start-button readiness diagnostics panel | New `src/ui/components/connection/StartReadinessPanel.tsx`; structured `StartReadiness` payload built in `ConnectionPanelMain.tsx` from existing gate state; replaces single-string `startDisabledReason`. 8 gates: controllerConnected, gcodeCompiled, gcodeFresh, preflight, machineState, framing, laserState, wcsState. Collapsed = first-failing-gate headline; expanded = full list with status glyphs, action hints, preflight detail items. Pinned by `tests/start-readiness-panel.test.tsx` (6/6). Preempted T1-17 Pass 4b/4c because the only physical-hardware tester was blocked from burning. Shipped 2026-04-30 in `5e8cff96d983d30cac721b8d6717c9fbfac9d6df`. |
| T1-97 | Frame-before-start bypass override — **RETIRED 2026-05-02 in `b629293`** | Originally shipped 2026-04-30 in `24e5dd468a6939f679c880e0e56d15229ac6504d` as a Tier 1 safety relaxation (tester-blocker bandage). Per-session bypass of T1-59 frame-before-start gate, engaged via More Options confirm-dialog, auto-disengaging on scene change / disconnect / reload. Was deliberate weakening of safety invariant 3 in `.cursor/rules/laserforge.md`. **Retired** because the underlying defect was structurally fixed by T1-98 (dynamic frame idle timeout — the actual cause of "Start grey on 6-block boxes"), T1-99 (savedOrigin no longer compile-invalidating), and T1-100 (machinePlanBounds source uses lastResult pre-job-start). Hardware-verified by tester 2026-05-02: Start unlocks normally on the 6-block scene without engaging bypass. Retire commit removes `frameBypass` state, the More Options control, the canStartJob conjunct widening, the active-bypass banner, and `tests/frame-bypass-override.test.tsx`. T1-59 frame-before-start invariant restored to its default-and-only behavior. **Lesson recorded:** bandage fixes for misdiagnosed bugs should be retired the moment the underlying fix is verified — not left in production indefinitely. |
| T1-98 | Frame idle timeout: dynamic from corner travel distance | Root cause of the 6-block-vs-5-block Start-grey bug. `FRAME_IDLE_TIMEOUT_MS` raised 15s → 60s; new `estimateFrameIdleTimeoutMs(corners)` returns max(30_000, ceil(expectedTravelMs * 2 + 5_000)) using 3000 mm/min conservative feed; `ConnectionPanelMain.handleFrameSafe` passes the per-frame estimate through. Initial T1-97 ticket spec misdiagnosed this as a phantom commitSceneTransaction post-frame; external review identified the actual 15s timeout cause from `grblIdlePoll.ts`. **T1-97 (bypass override) is now superseded by this fix and should be reverted or demoted to productionMode-only once tester verifies 6-box frame completes without bypass.** Pinned by `tests/frame-idle-timeout-dynamic.test.ts` (7/7). Shipped 2026-04-30 in `b899f7b`. |
| T1-99 | savedOrigin removed from compile-invalidation dep set | `useCompileManager.ts` had `savedOriginX/Y` in three dep arrays (layout effect + 2 useCallbacks), but `computeGcodeOffset` (`src/core/output/GcodeOrigin.ts:39`) accepts savedOrigin as `_savedOrigin` (underscored, unused). For startMode='savedOrigin' the emission is byte-identical to 'current' mode; the physical origin is set by `G10 L20 P1 X0 Y0` at Set Origin click, not by recompile. Listing the value as a dep flipped gcodeStale on every Set Origin click for no content change, blocking Start via the `!gcodeStale` gate. Fix: remove from all three dep arrays and keep latest savedOrigin in a ref for explicit compile calls. Pinned by `tests/savedorigin-not-compile-invalidating.test.ts` (7/7). Shipped 2026-04-30 in `e3dffe0`. |
| T1-100 | `machinePlanBounds` source uses `lastResult` pre-job-start | App.tsx prop changed from `activeJobTransform?.plan.bounds ?? null` (only set during job execution) to a precedence chain using `lastResult.machinePlanBounds` when fresh compile exists. Removes the pre-Start-phase null that forced preflight onto the fragile `gcodeTravelScan` text-scan fallback at `Preflight.ts:323` (which can mis-handle G91 relative moves in current/head mode and produce false bed-bounds blockers). Pinned by `tests/machine-plan-bounds-source.test.ts` (7/7). Shipped 2026-04-30 in `243ad0f`. |
| T1-106 | Reset GRBL WCS when switching away from Origin mode | New `src/app/sendResetWcsCommand.ts` sends `G10 L2 P1 X0 Y0 Z0` (absolute WCS = machine origin) when `startMode` changes away from `'savedOrigin'`. Bed mode assumes WCS == machine coords; switching modes without this reset left a stuck WCS offset from a previous Set Origin click, causing subsequent Bed/Head jobs to burn from wrong physical coordinates. Integration in `App.tsx`'s mode-switch handler is conditional: only fires when `mode !== 'savedOrigin'` (the savedOrigin path keeps WCS for the next Set Origin click). **Note on G10 L2 vs L20:** uses `G10 L2` (absolute WCS-to-machine-origin) rather than the `G10 L20` used by `sendSetOriginWcsCommand` (relative WCS-to-current-position). Inverse operations, intentional. **Known limitation (carried forward, not regression):** the helper swallows `sendCommand` errors silently — same pattern as `sendSetOriginWcsCommand` and other WCS helpers. If the reset command is blocked or rejected, no UI signal. Tracked under the broader "UI state updates before hardware success is proven" umbrella from external code review (ChatGPT 14-item analysis, item #4 family). Originally authored on `fix/start-mode-clears-wcs` branch as `b7f8bc0`; cherry-picked to `box-joinery-v5-2-corner-preserve` on 2026-05-02 with conflict resolution: helper path kept at `src/app/` (not `src/ui/origin/` from original); `handleSceneCommit(..., 'start-position')` action metadata preserved from current branch. Pinned by `tests/start-mode-wcs-reset.test.ts` (3/3 — sends correct command on success, no-op on null/undefined controller, swallows sendCommand errors). Shipped 2026-05-02 in `d3b12b2`. |
| T1-101 | Box Library + Joinery v5 (umbrella retroactive paper trail) | Five-commit cluster shipped on `box-joinery-v5-2-corner-preserve` between Fix #8 (T1-100, `92e0946`) and the 2026-05-02 audit. Workflow rule #4 violation (no ticket numbers in commit messages) — same shape as T1-95, retroactively documented here for bisect hygiene. **Scope:** total ~2123 LOC new code: 21 box presets across 7 categories in `src/core/box/boxLibrary.ts` (526 LOC); supporting types and preview model in `src/core/box/boxLibraryTypes.ts` + `boxPreviewModel.ts`; joinery v5 engine in `src/core/box/boxGeometry.ts` (335 LOC, ~5x pre-cluster size, with kerf-aware tab/slot geometry, fit allowance, drawn-vs-physical depth math); 9 React components in `src/ui/components/box-library/` (~1100 LOC); full-page workspace `src/ui/pages/BoxStudioPage.tsx` (97 LOC); 2 new hooks (`useBoxLibraryState.ts`, `usePersistentBoxPreferences.ts`). `BoxGenerator.tsx` restructured 432 -> 116 LOC as a thin "Box Studio launcher" modal. **Cluster commits (chronological):** `6d74fb1` pro-grade box generator fit controls; `19779bc` full-page Box Studio workspace; `d584dce` clean-room joinery v2 engine; `669ab60` clean verified joinery contours; `58b3fce` preserve joinery panel corners. **Tests added in cluster:** `tests/box-library.test.ts`, `tests/box-library-filtering.test.tsx`, `tests/box-preset-preview-model.test.ts`, `tests/box-generator-library-integration.test.tsx`, plus expansion of `tests/box-geometry.test.ts` (~3x pre-cluster size). All passing as of 2026-05-02. **Why Tier 1 not Tier 4:** box generation is a primary product differentiator on the landing page; the cluster is flagship feature work, not polish. **Lesson recorded (companion to T1-95):** off-roadmap work shipped on a feature branch must still acquire ticket numbers retroactively when the audit doc is updated. Future box-library work should reference T1-101 or file a successor ticket. Filed retroactively 2026-05-02 in `fe9d5e0`. |
| T1-102 | Frame crosshair safety in current/head startMode — investigated, no bug | Hypothesis from ChatGPT 14-item analysis #9: `frameSafe` defaults `withCrosshair: true`; in current/head mode the crosshair leaves the head at design centroid, not original jog point, causing wrong-position burn. Hardware testing 2026-05-02 confirmed: frame traces the design rectangle plus a center crosshair, then Start burns correctly from the design's top-left reference. The crosshair is a visible center-alignment aid, not a position-affecting move. **Closed without code change, no ship hash.** Lesson recorded: ChatGPT hardware-behavior claims need physical verification; code-level claims are reliable (T1-98/T1-99/T1-100 all verified from code), hardware-level claims are weaker. |
| T1-103 | `runFrame` must fail if any frame command is blocked | ChatGPT 14-item analysis #1. `ExecutionCoordinator.runFrame` corner-streaming loop caught `ctrl.sendCommand` throws, logged via `console.warn`, and continued. End-of-loop idle poll returned `{ok: true}` because rejected commands produced no movement -> GRBL reported idle quickly -> timeout didn't fire. UI then set `hasFramed.current = true` after a partial-or-zero physical frame trace, passing the T1-59 frame-before-start gate without the laser actually having framed the design. Fix: first throw returns `{ok: false, reason: 'command-blocked', blockedError, blockedAtLine}`. `FrameResult` widened. `handleFrameSafe` and `handleFrameDot` both add a `command-blocked` branch surfacing line number and error to the messages console. `hasFramed.current` not set on failure. Side cleanup: removed residual "15s" literal string from `handleFrameDot` message (T1-98 cleanup miss). Pinned by `tests/run-frame-fail-fast-on-blocked-command.test.ts` (5/5). **First closure of a "UI state updates before hardware success" hole identified by ChatGPT analysis.** Shipped 2026-05-02 in `8cb3faa`. |
| T1-104 | Exact-idle gate cluster (Frame, Frame Dot, Test Fire, Jog, Set Origin) | Five surfaces tightened from permissive `(isConnected && !isRunning)` or ad-hoc per-state denials to positive `machineState?.status === 'idle'` checks matching the existing `canAutoFocus` precedent. Frame/Frame Dot: `canFrame` widened with idle. Test Fire: `beginTestFire` early-return rewritten; `MachineControls` gets a new `canFire` prop replacing the negative `isAlarm \|\| isFaulted \|\| isRunning` blocklist (which permitted hold/homing/check/door/unknown/connecting). Jog: `handleJog` adds idle gate + console rejection message. Set Origin: `handleSaveOrigin` adds idle gate (silent; T1-105 will surface message when it changes the function signature). All 5 surfaces previously allowed clicks during machine states from which the action would silently fail or partially execute, then update UI state as if the action succeeded — same family as T1-103. Pinned by `tests/exact-idle-gates.test.ts`. **T1-105 follows: state-after-confirm pattern fix for Jog `hasJogged` and Set Origin `savedOrigin` (UI updates only after hardware confirms accept).** Shipped 2026-05-02 in `e346c55`. |
| T1-107 | Preflight bed-bounds + visible-layer-for-output checks ignore `layer.output: false` (guide layers) | ChatGPT 14-item analysis #8. Two preflight rules (`runBoundsChecks` in `OutputBoundsPreflight.ts`, `runSceneChecks` in `ScenePreflight.ts`) filtered objects by `(obj.visible && layer.visible)` without considering `layer.output`. Guide-layer content (visible on canvas, `output: false`) contributed bounds to the bed-bounds check, producing false `OUT_OF_BOUNDS_MAX/MIN` errors that blocked Start for jobs that would not burn in those regions. The `NO_VISIBLE_LAYERS` check had the inverse problem: scenes with all-output-disabled layers but visible content passed the check, missing the "this job will produce no output" warning. Fix: both filters now require `layer.output !== false` in addition to visibility checks. Pattern matches `OptimizationPreflight`, `LayerSettingsPreflight`, `RasterPreflight`, and scene output-layer helpers which were already correct. Variable renames `visibleObjects -> outputObjects` and `hasVisibleObjects -> hasOutputObjects` for clarity. Pinned by `tests/preflight-output-layer-filter.test.ts` (7 contracts including positive regression checks that legitimate `output: true` violations still fire). Shipped 2026-05-02 in `95911e3`. |
| T1-23 | Pause emits explicit M5 + resume reasserts spindle mode | Pause was sending only feed-hold (`0x21`) and relying on GRBL `$32=1` firmware contract to disable the laser. On `$32=0` (CNC mode misconfiguration) or non-spec-compliant GRBL forks, feed-hold preserves the modal M3/M4 state and laser stays on. Resume's cycle-start (`0x7E`) then restored the modal state and re-engaged the laser without fresh user action. Fix: pause emits feed-hold THEN `M5 S0` via `writeCritical` (fire-and-forget; feed-hold has already halted motion if the M5 write throws). Resume re-asserts the captured `_lastSpindleMode` (`'M3'` or `'M4'`) with `S0` before cycle-start, so the modal state is correct when motion resumes; the gcode stream's next `S<n>` sets power. Spindle-mode tracking added via new `_trackSpindleMode(line)` method called from both `_writeLine` and `_writeSystemLine`. Substring match against the code portion of the line (parenthesized comments stripped). Implementation kept the controller interface synchronous; M5 and reassert use `void writeCritical(...).then(ok, err)` with error logging inside the sync method body. Pinned by `tests/pause-emits-m5-after-feed-hold.test.ts` (8 contracts: byte order, M3 reassert, M4 reassert, no-prior-mode case, `_trackSpindleMode` covers M3/M4/M5/comment-stripping). Closes firmware-dependency for `$32=0` and non-spec-compliant GRBL forks. **Hardware verification recommended:** press pause mid-job, observe laser turns off; press resume, observe burn continues from same position. Shipped 2026-05-02 in `87b0524`. |
| T1-27 | Remove unused `window.electronAPI.sendGcode` IPC bypass | Renderer-to-controller bypass that wrote raw lines to the serial port via `writeSerialLine`, skipping MachineService / ExecutionCoordinator / GrblController. IPC handler validated only string type, length <=127, and no CR/LF — no semantic safety check. **Verified dead before removal:** repo search found only the preload export, IPC handler, type declaration, and roadmap prose; no application code called it. Removed the `sendGcode` export from `electron/preload.ts`, the `ipcMain.handle('serial:send', ...)` block from `electron/main.ts`, and the `sendGcode?:` declaration from `src/types/web-serial.d.ts`. Removal-explanation comments left at each site for grep discoverability. Pinned by `tests/no-electron-sendgcode-export.test.ts` (4 contracts: preload has no `sendGcode` or `serial:send` outside comments; main has no `serial:send` handler; type declaration absent; no production reference in `src/`). Pattern mirrors `no-localstorage-in-core` (T2-2 phase 9) and `no-gcode-in-ui` (T2-4 phase 8). Highest value-to-effort ratio of any safety item per audit framing: zero functional impact (dead code), critical bypass closed. Shipped 2026-05-02 in `757f2c3`. |
| T1-21 | Frame-dot try/finally safety scope | `ExecutionCoordinator.runFrame` corner-streaming loop + idle wait wrapped in `try { ... } finally { if (laserMode === 'dot') await this.emergencyLaserOff(); }`. Closes the modal-laser-on hole between an accepted M4 (frame-dot dim outline) and the trailing M5: if the loop returned early via T1-103's command-blocked path or threw before the trailing M5, the laser stayed in modal M4 state. The finally now fires unconditionally for dot mode and routes through `emergencyLaserOff` (T1-22's two-stage safety: M5 critical-write -> soft reset on fail). Frame-safe (`laserMode === 'off'`) skips the finally because `buildFrameGcode` never emits M4 in that mode — `M5 S0` is emitted at every position instead. The finally has its own try/catch so a transport failure during safety-off doesn't mask the original return. Pinned by `tests/frame-dot-finally-emits-m5.test.ts` (5 contracts including: frame-safe success doesn't trigger safetyOff; frame-dot success does; frame-dot interrupted does; frame-dot blocked-at-index-0 does; safetyOff throwing in finally is logged but original return value preserved). Composes with T1-103 (fail-fast on blocked commands, makes the failure path explicit) — T1-21 makes the same failure path safe. **Audit framing:** lower realistic trigger frequency than initial audit suggested (most "interruption" scenarios are crash-level events where a try/finally may not run anyway), but defensible as defense-in-depth for the recoverable-exception case. Shipped 2026-05-02 in `ba903a9`. |
| T1-26 | Custom footer enforce-M5-at-send | Defense-in-depth M5 append in `BaseGCodeStrategy.encode` (`Output.ts`) at the end of gcode assembly. Scans last 5 non-empty lines; appends `M5 S0 ; T1-26 defense-in-depth laser-off` if no `\bM5\b` (case-insensitive) match. Idempotent for default footer (already emits M5 via `encodeLaserOff()`); remedial for custom-template footers that bypass or escape the `FOOTER_MISSING_M5` preflight validator. **Implementation choice:** placed in `BaseGCodeStrategy.encode` rather than `MachineService.startValidatedJob` so the safety net applies uniformly to all output paths — preview, file save, send to controller, replay capture. The user-downloaded gcode file matches what the controller receives. **Lenient on comments:** "M5 was here" in a comment counts as already-present and skips the append, to avoid double-appending when the user explicitly noted M5 in a comment; the strict check remains the `FOOTER_MISSING_M5` validator. Pinned by `tests/footer-m5-appended-at-send.test.ts` (7 contracts including the bug-fix proof: M5 only in header, append still fires because tail-scope misses it; total 2 M5s in result). Composes with T1-23 + T1-21 + T1-103: every path that could leave the laser modal-on now has an explicit M5 in the same family. **Audit framing note:** the original audit overstated the bug — `FOOTER_MISSING_M5` is already severity `error` in the validator, blocking job start, not just warning. T1-26 ships as belt-and-suspenders against validator regex bugs or future profile-bypass paths. Shipped 2026-05-02 in `e5fbec0`. |
| T1-9 | Frame bed extents preflight (audit close-out) | Bug fix landed earlier in commit `e427a0a` (Frame-vs-Burn divergence corrected to use `resolveBedHeightMm` instead of `scene.canvas.height`). T1-9 ticket called for a static-guard test to prevent regression. **At write-time verification:** zero `scene.canvas.width|height` references exist in `src/core`, `src/app`, `src/controllers`, or `src/communication` directories — the machine-coordinate codebase is already clean, this commit just locks it in. Pinned by `tests/no-scene-canvas-in-machine-coord.test.ts`: recursive walk of all four machine-coordinate directories, comment-stripped scan with line-level violation reporting. Pattern mirrors T1-27, T2-2 phase 9, T2-4 phase 8. Composes with `tests/bed-height-resolver-parity.test.ts` (resolver function correctness): parity test pins "the function is correct"; T1-9 guard pins "machine-coordinate callers use it instead of `scene.canvas.*`." `scene.canvas` remains the correct concept in `src/ui`, `src/import`, and viewport/import geometry helpers; the guard intentionally excludes those rendering/import-placement paths. Shipped 2026-05-02 in `e4fc24b`. |
| T1-105 | Jog + Set Origin: state-after-confirm pattern | Companion to T1-104. T1-104 added exact-idle gates; T1-105 closes the remaining UI-state-before-transport-accept hole on those surfaces. Signature changes: `MachineService.jog` / `ExecutionCoordinator.jog` from `void` to `{ ok, reason? }`; `ExecutionCoordinator.setOriginAtCurrentPosition` from `Promise<void>` to `Promise<{ ok, reason? }>`; `sendSetOriginWcsCommand` from `void` to `{ ok, reason? }`. UI handler updates: `handleJog` only sets `hasJogged.current=true` on `ok:true`; on failure surfaces "Jog failed" to messages console. `handleSaveOrigin` only writes `setSavedOrigin` + localStorage on `ok:true`; on failure shows `showAlert` modal. **Success semantic:** controller exists and `sendCommand` did not throw. Not GRBL ack-correlation; true firmware confirmation is out of scope. Existing tests widened for return-shape assertions and new `tests/jog-and-setorigin-state-after-confirm.test.ts` pins primitive success/null/throw paths plus simulator intent notification. Composes with T1-103, T1-104, T1-23, T1-21, and T1-26 to close the UI-vs-hardware divergence cluster on user-action surfaces. Hardware verification recommended but not blocking. Shipped 2026-05-02 in `4b1af8a`. |
| T1-66 | Jog catch must surface "command blocked" — closed (superseded by T1-105) | T1-66 specified: when `executionCoordinator.jog(...)` throws, the catch logs `console.warn` only; user gets no visible feedback. Spec proposed adding `appendMessage` to the catch block. **Superseded** by T1-105 (`4b1af8a`) which solved the same problem via a different mechanism: widened `executionCoordinator.jog()` from `void` to `{ ok, reason? }` and made `handleJog` read the return value, surfacing `"⚠ Jog failed: {reason}. Check connection and machine state."` to the messages console on `ok: false`. The user-visible behavior T1-66 wanted is delivered. Implementation difference: T1-105 routes through the return value (type-checked) rather than the catch path; `executionCoordinator.jog` no longer throws — failures return `ok: false`. **Closed without separate ship hash; work is in `4b1af8a`.** Original T1-66 spec mentioned a richer `reportError` form pending T2-65 (central error reporter); if anyone wants that escalation, it becomes a T2-65 follow-up, not a T1-66 reopen. Closed 2026-05-02. |
| T1-47 | Register `simulator-view-ymirror.test.ts` + registration drift guard | Audit 2F (2026-04-25) identified that `tests/simulator-view-ymirror.test.ts` existed on disk but was missing from `scripts/run-tests.mjs` — the test file never ran, could be silently broken or stale, no one would know. **Two-part fix:** (1) Added the orphan test to the runner near the end of the main section (after `frame-required-before-start.test.ts`, before E2E block). (2) New `tests/runner-registration-coverage.test.ts` walks `tests/` recursively for any `*.test.tsx?$` file (excluding `snapshots`, `helpers`, `fixtures`, `node_modules`), reads `scripts/run-tests.mjs`, asserts every on-disk path appears as a substring. Future contributors who add a test without registering get a CI failure. Pattern matches T1-9, T1-27, T2-2 phase 9 static guards. **Verification:** the orphan test's import (`mapMachineYToCanvasY` from `SimulatorView.tsx`) was confirmed extant before registration so the test passes on first run. **Limitation:** guard catches "on disk but not registered" (the audit's identified case) but not "registered but missing from disk" — different failure mode, separate ticket if anyone wants it. Shipped 2026-05-02 in `037bfdd`. |
| T1-37 | Hide offset fill UI option until implemented | `ConnectionPanelMain`'s per-layer engrave-mode fill-mode button row exposed an "Offset" button with no disable guard. `PlanOptimizer` doesn't implement offset fill and falls through to single-angle line fill. User clicked Offset, button highlighted as if accepted, output was line-fill instead. `LayerPanel` correctly disabled the offset option in its `<select>`; `ConnectionPanelMain` didn't mirror. **Fix:** mirrored LayerPanel's pattern — label `'Offset (coming soon)'`, `disabled: f.mode === 'offset'`, defensive onClick early-return, `title: 'Offset fill not yet implemented'`, `cursor: 'not-allowed'`, and `opacity: 0.5`. Line and cross-hatch buttons unchanged. Pinned by `tests/connection-panel-offset-button-disabled.test.ts` (6 contracts via source-level pinning since `ConnectionPanelMain` is large and requires the full dependency tree to mount). **Out of scope:** actual contour-parallel offset fill implementation in `PlanOptimizer`; separate ticket if/when prioritized. Shipped 2026-05-02 in `bfb1a6b`. |
| T1-72 | `APP_VERSION` wired to package.json via Vite define | `SceneSerializer.ts:27` had `const APP_VERSION = '0.1.0'` hardcoded; `package.json.version` was `1.0.0` — every saved `.laserforge` scene file was tagged with a stale version, weakening support diagnosis. **Fix:** Vite `define` injects `__APP_VERSION__` from `packageJson.version` at build time. New `src/types/global.d.ts` declares the build-time constant for TypeScript. `SceneSerializer.ts` uses the constant via a `typeof` guard with fallback `'0.0.0-tsx-fallback'` for the test environment, because tsx runs without Vite. **Fallback choice:** `'0.0.0-tsx-fallback'` rather than the spec's `'0.1.0-dev'`; the latter is too easy to confuse with the legacy hardcoded value. Pinned by `tests/app-version-from-package.test.ts` (file-save/autosave appVersion are non-empty strings; tsx env produces the fallback exactly; fallback is not legacy `'0.1.0'`). **Module syntax note:** uses `import packageJson from './package.json' with { type: 'json' }` (modern Node/Vite form). Shipped 2026-05-02 in `41bfa06`. |
| T1-67 | Hide hash values from user-facing validation messages | Three `validateTicket` failure paths in `MachineService.ts` previously embedded raw hash/controller details into user-facing `reason` strings, e.g. `"recompile to continue. (ticket scene hash a3f8d2bc, current 9c1e7afb)"`. User saw gibberish; the actionable instruction was buried. **Shipped Option B (cosmetic), not Option A (structural).** Spec proposed adding a `details: { cause, ticketHash, currentHash }` field to the result type; that only matters with a UI that renders it under "Advanced details," which does not exist. Shipping the structured field without a consumer is YAGNI. **What shipped:** rewrote the 3 reason strings as plain-English action ("The design changed after this G-code was created. Update G-code, then frame again before starting."). Hashes/details preserved in `console.warn` for support — added warns for the profile and controller-type sites that did not have them before, mirroring the existing scene-hash warn. Pinned by `tests/ticket-validation-message-translation.test.ts` plus updated stale-ticket behavioral assertions in `tests/validated-job-ticket-mismatch.test.ts`. **Future:** if a richer error UI gets built, the structured `details` field can be added then. Shipped 2026-05-02 in `f0e8d41`. |
| T1-48 | Remove `new Date()` from Output.createdAt for determinism | `Output.generate` previously called `new Date().toISOString()` for the `createdAt` return field and the default header's `; Date:` comment line. Same input + different time = different Output. The header line is normalized away by E2E helpers, but `createdAt` is not — this blocked metadata-level determinism and future cache-key-by-hash work. **Fix:** `GcodeGenerateOptions` gains `clock?: () => string`. `Output.generate()` resolves the injected clock once, uses it for `createdAt`, and passes the same string into `encodeHeader` for the `; Date:` line; no clock means wall-time fallback. Production paths do not pass `clock`, so there is no user-visible behavior change. Pinned by `tests/output-deterministic-with-clock-injection.test.ts` (same frozen clock plus deterministic IDs = byte-identical Output objects; different frozen clocks = different `createdAt`; no clock = parseable wall-time fallback within the execution window; header uses injected value). **Out of scope:** ticket `createdAt` in `PipelineService.ts` (`Date.now()`, separate concern). E2E normalization unchanged. Foundation for T2-23 (determinism gate). Shipped 2026-05-02 in `69f1221`. |
| T1-73 | Delete action must mark scene dirty — closed (superseded by T2-76 unified routing) | T1-73 specified: `handleDelete` in `App.tsx` pushed history and updated scene directly, bypassing the dirty flag — a real data-loss path because autosave skipped non-dirty state. Spec proposed replacing the manual push with `handleSceneCommit`. **Superseded** by T2-76 step 4 (unified mutation routing). At write-time verification: `handleDelete` in `src/ui/components/App.tsx` now calls `commitSceneTransaction(newScene, { kind: 'edit', action: 'delete' }, ...)`. `commitSceneTransaction` (`src/ui/scene/SceneTransaction.ts`) fires `deps.notifyDirty(true)` for `kind: 'edit'`. T2-76's routing is more comprehensive than T1-73's local patch — every mutation now uses the same dirty-firing path, not just delete. An in-source comment in `App.tsx` already documented that T1-73's concern was satisfied; this row formalizes the close-out. **Closed without separate ship hash; the work is in T2-76 commits.** Future T1-73-style bugs would only appear if a new mutation path bypasses `commitSceneTransaction`. Same close-out shape as T1-66 (superseded by T1-105). Closed 2026-05-02. |
| T1-82 | Remove `PRO_FLAG_KEY` legacy writes (no readers found) | Grep for `laserforge_pro` / `PRO_FLAG_KEY`: consumers only `EntitlementService` (migration, `deactivate`, constant) and `tests/entitlement-storage-migration.test.ts`. `App.tsx` hit is `laserforge_production_mode` (false positive). **Deleted** per-state `setState` writes to `laserforge_pro`; comment in `setState` documents T1-82. Migration list unchanged; `deactivate` still `remove`s the key. Pinned by `tests/no-live-pro-flag-writes.test.ts`. Shipped 2026-05-03 in **`b3266a4`**. |
| T1-3 | Profile-dependent negative-coord preflight severity (close-out) | Shipped pre-2026-05-02; formalized 2026-05-03. `DeviceProfile.allowsNegativeWorkspace?: boolean` at `src/core/devices/DeviceProfile.ts:153`; defaults `false` at `:436`, `:471`. `negativeCoordPreflightSeverity` + `NEGATIVE_COORD_SETTINGS_HINT` at `src/core/preflight/rules/sharedHelpers.ts:3–10` (`warning` if allowed, else `error`; recovery text points users to Machine settings). Pinned by `tests/preflight-negative-coords.test.ts` (header references T1-3; registered in `scripts/run-tests.mjs`). **Implementation hash not recorded** — predates audit-cleanup session. |
| T1-86 | `npmRebuild` decision (close-out) | Shipped pre-2026-05-02; formalized 2026-05-03. **Path B** — document/safeguard `npmRebuild: false`; `package.json` includes `_npmRebuildRationale` (T1-86 markers). Structural guard `tests/native-deps-prebuild-check.test.ts` (203 lines; registered in `scripts/run-tests.mjs`): sandbox asserts production `serialport` dep, `@serialport/bindings-cpp` prebuild layout, N-API / node-addon-api evidence, `node-gyp-build` resolver. Header documents prerequisites (1)–(4); packaged `SerialPort.list()` smoke is **T3-86** CI. Failures force re-evaluating **`npmRebuild`**. **Implementation hash not recorded.** Beyond spec (comment-only) — regression test is the authoritative rationale. Closed 2026-05-03. |
| T1-43 | Reassert G90/G91 after customStart + block mode flips in templates | **Validator:** widen `TEMPLATE_G91_IN_HEADER` to `header` + `customStart`; add `TEMPLATE_G90_IN_HEADER` (kept legacy G91 code for test pin). **Output.encodeHeader:** reassert matching `G90`/`G91` with T1-43 marker after non-empty customStart. Pins: `tests/customstart-mode-reassertion.test.ts`, extended `tests/gcode-template-validator.test.ts`. Ship hash **`6eb006b`**. |
| T1-65 | Frame inner loop must abort on first command failure — closed (superseded by T1-103) | T1-65 specified: frame inner loop must abort on first command failure, not continue partial frame. Spec proposed adding `'command-failed'` to `FrameResult.reason`. **Superseded** by T1-103 (`8cb3faa`, shipped 2026-05-02): `runFrame` widened to return `{ ok: false, reason: 'command-blocked', blockedError, blockedAtLine }` on first `sendCommand` throw, with reason union documented at `src/app/ExecutionCoordinator.ts:36-43`. Naming difference: T1-103 used `'command-blocked'` not T1-65's `'command-failed'` — functionally identical; name preserved because T1-103 did the work. Pinned by `tests/run-frame-fail-fast-on-blocked-command.test.ts`. Companion to T1-21 (frame-dot try/finally + safety-off in finally, also shipped 2026-05-02 in `ba903a9`). **Closed without separate ship hash; work is in `8cb3faa`.** Same close-out shape as T1-66 (superseded by T1-105), T1-73 (superseded by T2-76). Closed 2026-05-02. |
| T1-56 | Preflight `machinePlanBounds` reads wrong source — closed (superseded by T1-100) | T1-56 specified: change `App.tsx:1719` from `activeJobTransform?.plan.bounds ?? null` to `lastResult?.machinePlanBounds ?? null` so preflight bed-bounds checks see machine-space transformed bounds pre-Start instead of always-null. **Superseded** by T1-100 (`243ad0f`, shipped 2026-04-30): the precedence chain at `src/ui/components/App.tsx:1980-1982` reads `activeJobTransform?.plan.bounds ?? (!gcodeStale && currentGcode && lastResult ? lastResult.machinePlanBounds : null)`. Pre-Start, `activeJobTransform` is null and the chain falls through to `lastResult.machinePlanBounds` when a fresh compile exists — exactly T1-56's intent. The chain layer rather than a flat replacement preserves `activeJobTransform.plan.bounds` for the runtime overlay role T1-56 explicitly carved out. Inline comment at App.tsx:1977-1979 cites T1-100. Pinned by `tests/machine-plan-bounds-source.test.ts` (T1-100, 7/7). **Closed without separate ship hash; work is in `243ad0f`.** Same close-out shape as T1-65 (superseded by T1-103), T1-66 (superseded by T1-105), T1-73 (superseded by T2-76). Closed 2026-05-04. |
| T1-70 | Recovery failure must alert user, not just `console.error` | `useWizardHandlers.handleRecover` rewritten so the user sees what happened instead of a vanishing prompt: empty/missing autosave → `showAlert('Recovery unavailable', …)` then hide; corrupt scene JSON (`deserializeScene` throws) → `showAlert('Recovery failed', …)` with the error message and **prompt KEPT visible** so retry / send-for-support / discard remain reachable; success → `handleNewProject` + hide + clear time label. `console.error` preserved on the failure path for support diagnosis. `UseWizardHandlersParams` gains `showAlert`; `App.tsx` wires the existing modal `showAlert` through. `readAutosave` already swallows storage errors and resolves null, so the empty-payload branch covers read failures without a separate catch — keeps the surface tight. Pinned by `tests/recovery-failure-alerts.test.tsx` (12 contracts: alert title/count + `setShowRecover` side effect + `handleNewProject` side effect for each of the three branches; console.error preserved on failure). Shipped 2026-05-04 in **`bdedd58`**. |
| T1-71 | Recovery offered for non-empty autosave, not only object-having scenes | Eligibility logic extracted to a pure function `evaluateRecoveryEligibility(json) -> { shouldOffer, reasons }` in new `src/app/recoveryEligibility.ts`; App.tsx's startup `useEffect` now calls it. Recovery fires on any of: `scene.objects.length > 0`, `scene.layers.length > 1` (more than the single default `Cut` layer), populated `scene.material`, or populated `scene.machine`. Default-only scene (one layer, null material, null machine, no objects) still doesn't fire — opening the app and doing nothing won't surface a stale prompt. Malformed JSON and missing-scene blocks return `shouldOffer:false` because there's nothing meaningful to restore — T1-70 covers user-initiated retries on partly-broken data. The `reasons` field is reported back as `('objects' \| 'customLayers' \| 'material' \| 'machine')[]` so future "what's recoverable" UI (T2-69 / T2-70 follow-ups) can hang off the same predicate. Pinned by `tests/recovery-eligibility-non-empty-cases.test.ts` (16 contracts: each positive cause individually, multiple-cause aggregation, default-only negative, malformed/empty/missing-scene/null-scene negatives). Shipped 2026-05-04 in **`02b9706`**. |
| T1-52 | Auto-detect machine settings must include `$30 → maxSpindle` — minimum scope | Audit 3C Critical 3 + Priority 3: `handleAutoDetectMachine` copied bedWidth/Height, maxFeedX/Y, maxAccelX/Y from `grblMachineInfo` into the active profile but silently dropped `grblMachineInfo.maxSpindle`. Combined with `createBlankProfile`'s default `maxSpindle: 1000` and PipelineService's profile-wins precedence, a controller reporting `$30=255` paired with the default profile delivered `encodePowerValue(50) → S500 → controller clamps to 255 → 100% power`. **Shipped minimum scope:** added `maxSpindle: grblMachineInfo.maxSpindle > 0 ? grblMachineInfo.maxSpindle : current.maxSpindle` to the `updateActiveProfile` call. The positive-value guard preserves the current profile value when the controller hasn't reported `$30` yet, defending against a not-yet-parsed value zeroing the profile. **Out of scope for this commit:** the audit's extended Priority 3 (`$32` laser mode, `$22` homing enabled, `$20` soft limits, `$23` origin direction). `$23` is explicitly flagged dangerous in the audit ("only if mapping is safe and user-confirmed"); the others would need a new `laserModeEnabled` profile field plus user-confirmation flow. Filed as a follow-up; the headline T1-52 contract ("$30 → maxSpindle") is delivered. Pinned by `tests/auto-detect-includes-max-spindle.test.ts` (12 source-level contracts: maxSpindle copy with positive-value guard, fallback to current value, every existing field still copied, T1-52 marker present). Source-level pin because mounting App.tsx with stubbed grblMachineInfo + spying on updateActiveProfile would need the full App dependency tree; the structural pin guarantees the line cannot be silently dropped again. Shipped 2026-05-04 in **`41310f4`**. |
| T1-57 | Compile manager request-id guard for async result ordering | Audit 4A Race 1: `useCompileManager.compileGcode` could commit a stale result if compile A started, the user edited the scene (bumping sceneCompileTick), compile B started, B finished first, then A finished — A's `setLastResult` ran second and overwrote B's. Plus `lastCompiledRevisionRef.current = sceneCompileTickRef.current` was reading the **completion-time** tick, falsely marking A's older result as fresh against the newer tick (audit's exact framing). **Shipped:** new monotonic `compileRequestIdRef` (`useRef(0)`); every compile increments and captures its id at START, plus `sceneTickAtStart = sceneCompileTickRef.current` at the same moment. After `await`, if the captured id no longer matches the ref's current value, the result and its `setLastResult` / `setGcodeStale` / `setIsCompiling(false)` updates are dropped (with a diagnostic info log so the drop is visible during debugging). Catch and finally paths likewise gate `setLastResult(null)` and `setIsCompiling(false)` on `requestId === current`, so a stale failure cannot wipe a newer success. `lastCompiledRevisionRef.current = sceneTickAtStart` — never `sceneCompileTickRef.current` — fixes the falsely-fresh tick-tracking bug. Pinned by `tests/compile-race-guard.test.ts` (12 source-level contracts). Source-level pin chosen because `pipelineCompileGcode` is imported as a static ESM binding with no test-only override; a behavioral race-test would need module mocking the tsx runner doesn't supply. The structural contracts are tight enough that a regression would have to remove the request-id ref entirely or revert the tick-source line, both of which the test catches. Shipped 2026-05-04 in **`d28a57f`**. |
| T1-53 | Live `$30` overrides profile.maxSpindle when connected; mismatch is preflight blocker — closed (superseded by T1-33) | T1-53 specified the same fix shape T1-33 delivered: PipelineService precedence flipped so live `controllerMaxSpindle` wins when present and a blocking preflight rule when profile vs controller diverge. **Superseded** by T1-33 (`a5dbe91`, shipped 2026-05-04): precedence is `(controllerMaxSpindle != null && controllerMaxSpindle > 0) ? controllerMaxSpindle : (profile?.maxSpindle ?? 1000)`; `MACHINE_MAXSPINDLE_MISMATCH` rule fires at error severity when `ratio < 0.95 || ratio > 1.05`. T1-53's spec called for two resolution paths in the blocker dialog (Update profile / Continue with live value); T1-33 shipped the simpler form — the user reconciles via Settings or `$30=N`. The two-path UI is filed as a future T1-33 follow-up if blocker friction shows up in practice. Pinned by `tests/maxspindle-precedence-and-mismatch.test.ts` (T1-33, 22/22). **Closed without separate ship hash; work is in `a5dbe91`.** Same close-out shape as T1-65 (superseded by T1-103), T1-66 (T1-105), T1-73 (T2-76), T1-56 (T1-100). Closed 2026-05-04. |
| T1-54 | Block job start if GRBL output uses M4 and `$32 ≠ 1` — closed (superseded by T1-32) | T1-54 specified the same headline contract T1-32 delivered: blocking preflight when output emits M4 against a controller reporting `$32=0`. **Primarily superseded** by T1-32 (`8bc2f3a`, shipped 2026-05-04): `MACHINE_LASER_MODE_DISABLED` rule fires at error severity when `liveLaserMode === false && outputUsesM4`. **Scope difference:** T1-54's spec also called for an error when `laserMode === undefined` (connected but `$32` not yet read). T1-32 deliberately skipped that branch because `$$` is queried during connect handshake and parsed before the user can start a job; a connected-but-no-$32 state is rare and the `MACHINE_DISCONNECTED` preflight covers the disconnected case. If the rare "connected with malformed $$ response" path turns out to matter in practice, the tightening is a 5-line extension to the T1-32 rule (filed as a future T1-32 follow-up). T1-54's optional one-click "Enable laser mode ($32=1)" UX button is a separate future enhancement, not part of the headline contract. Pinned by `tests/preflight-rejects-m4-without-laser-mode.test.ts` (T1-32, 21/21). **Closed without separate ship hash; primary work is in `8bc2f3a`.** Closed 2026-05-04. |
| T1-79 | Box generator service-level entitlement check — closed (feature moved to free) | T1-79 was filed because `box_generator` was the only Pro feature without a service-level `requireFeature`/`assertFeature` gate, leaving a DevTools-bypass path where a free user could trigger the dialog and generate faces. The fix was specced as a one-line `assertFeature('box_generator')` inside `generateBoxFaces`. **Obsolete** — `box_generator` was subsequently removed from `PRO_FEATURES` (see `src/entitlements/types.ts:2-3`: "moved to free: finger-joint box generation is beginner-friendly and belongs in easy mode without a pro gate"). With the gate lifted entirely there is nothing to assert; T1-78 Phase 1's `assertFeature` API is still in place to make a ~5-line re-introduction trivial if the commercial decision ever reverses. **Closed without ship hash.** Same close-out shape as T1-65 (superseded by T1-103), T1-66 (T1-105), T1-73 (T2-76), T1-56 (T1-100). Closed 2026-05-04. |
| T1-62 | `jobModeLabel` shows operation order — static plan summary | Audit 4B Finding 9.2: a 12-minute mixed engrave+cut job displayed `▶ Running...` for the entire run with no indication of what operation was active or which was coming. The user could not tell mid-job whether the laser had switched from engrave to cut yet. **Shipped the static-plan-summary version** (the spec's recommended first path; live-line tracking via compile metadata remains the T2-63 path). New helper `jobModePlanSummary(scene): string \| null` in `src/ui/components/connection/jobModePlanSummary.ts` returns the canonical PlanOptimizer group order (engrave/score → cut, with `image` deduped to engrave because the planner treats them identically) for multi-mode jobs and `null` for single-mode or no-objects (where the existing `activeLabel` already names the operation). `Progress.tsx` accepts a new optional `planSummary` prop and renders it as a small `Plan: Engrave → Cut` subtitle under the active label while running and not paused. `ConnectionPanelMain.tsx` passes `jobModePlanSummary(scene)` through. The plan order is fixed (matches `OperationOrderer.ts:176-191`'s engrave/score → cut grouping) and independent of the user's layer order. Hidden / `output:false` / object-less layers are correctly excluded from the contributing set. Pinned by `tests/job-mode-plan-summary.test.ts` (13 contracts). Behavioral test on the pure helper rather than a Progress-mount integration — the contract is the mode-mapping logic; the Progress wiring is a one-prop forward. Shipped 2026-05-04 in **`04a4e66`**. |
| T1-61 | Start mode labels rewritten with full sentences + tooltips | Audit 4B Critical UX failure 6: start-mode buttons rendered as `'📍 Bed'`, `'🎯 Head'`, `'⚑ Origin'` — emoji plus one word, with no signal about what each mode actually controls. For the most safety-relevant decision in the run flow (*where will the laser burn relative to the machine?*), beginners could not predict the burn location from the labels. Workflow.tsx's `modes` array shape widened from `{ mode, label }` to `{ mode, short, long, tooltip }`: buttons now render `'Bed coordinates'` / `'From laser head'` / `'From saved origin'` (legacy emoji labels removed), surface `title=tooltip` for hover hints with the audit's "Best for: ..." framing, and the selected mode's `long` sentence renders in a small accent panel below the button row so the user always sees what their current selection means. Per-mode visual diagrams remain T3-70's scope. Pinned by `tests/start-mode-labels.test.ts` (17 source-level contracts: each mode's short/long/tooltip text + button rendering shape + selected-mode-only long display + legacy emoji labels removed). Source-level pin because the visible-render assertions need only the JSX structure of the modes array, and a React-mount integration would require the full ConnectionPanelMain dependency tree. Shipped 2026-05-04 in **`a5a6af9`**. |
| T1-76 | Active layer change — unified path (Option B, project state) | Audit 4E Critical 9: same conceptual action took two policies — `App.tsx`'s `handleActivateLayer` (canvas-click-to-activate) used `handleSceneChange` (no history), while `LayerPanel.tsx:157` used `onSceneCommit` (history). The user could click an inactive layer one way to get an undo entry and another way to silently skip the entry. **Shipped Option B** (project state, both routes commit) — `handleActivateLayer` now calls `handleSceneCommit({...prev, activeLayerId: layerId}, 'activate-layer')`. New `'activate-layer'` label added to the `SceneCommitAction` union so the action is honest in transitionLog / future T3-68 history rendering. Inline comment cites T1-76, the audit framing, and notes that schema-level "active layer is UI state" (Option A) remains the future T2-71/T2-73 path — Option B is the consistency fix today. Pinned by `tests/active-layer-history-consistent.test.ts` (8 source-level contracts: handleActivateLayer routes through commit, not change; uses `'activate-layer'` action; carries T1-76 marker; the SceneCommitAction union includes the new label; LayerPanel's onSceneCommit call shape unchanged). Source-level pin (rather than React-mount integration) since App.tsx is heavy and a canvas-click event would need full viewport plumbing — the assertion targets the handler shape so a future refactor cannot silently revert. **UX note carried in commit message:** clicking through inactive layers via the canvas now creates undo entries; the consistency benefit outweighs the extra Ctrl+Z presses. Shipped 2026-05-04 in **`4e5643a`**. |
| T1-30 | Tighten command enablement gates — centralized helper + ConnectionPanelMain integration | Audit 1E: every UI surface had its own `isConnected && !isRunning` ad-hoc gate for laser-on operations. Each surface re-derived "is it safe to fire?" from primitives but with subtle drift — `canFrame` at one site, `canAutoFocus` at another, `canFire` (test-fire) at a third, each missing different inputs (alarm state, laser modal state, active operation, error code, recovery pending). Safety should not depend on every UI site doing the same conjunction independently. Deps met: T1-22 (`laserOutputState`) pre-session, T1-24 (`2600666`), T1-29 (`67e1da2`), T2-11 (`24ce00a`). **Shipped:** new pure helper `src/app/computeCommandGates.ts` exports `computeCommandGates(input): CommandGates` taking `{ state: MachineState, laserOutput: LaserOutputState, activeOperation: ActiveOperationState \| null, recoveryPending: boolean }`. Returns 10-key gate map: `baseSafe` (AND of all five "safe to fire" conditions), `canJog` / `canFrameSafe` / `canFrameDot` / `canTestFire` (all = baseSafe today; named separately so refinements can diverge), `canPause` (status === 'run'), `canResume` (status === 'hold'), `canUnlock` (status === 'alarm'), `canStop` / `canEmergencyStop` (connection-only — gating on baseSafe would deadlock the recovery path). **ConnectionPanelMain integration:** new `gates` const computed once; `canAutoFocus` (line 439) and `canFrame` (line 605) replaced with `gates?.baseSafe` / `gates?.canFrameSafe`; `canFire: canFrame` alias becomes `canFire: gates?.canTestFire`; `canStartJob` keeps existing product-level conjuncts and adds `gates?.baseSafe` as defense-in-depth. activeOperation + recoveryPending read inline (not React state) because transitions are driven by machineState updates that already trigger re-renders, and the T2-11 mutex layer is the actual safety net — gates are UX preventing clicks that would just bounce off the mutex. **Frame-safe consent stance preserved** — spec disagreed with the audit on requiring consent for M5-only frame; helper keeps `canFrameSafe = baseSafe`. **Out of scope (deferred):** ad-hoc gates inside `MachineControls.tsx` (consumes props from panel, T1-104 + T1-30 close the surface together); `canSendRawCommand` (T1-19's approval-token gate is a different shape); `canStop` / `canEmergencyStop` button-site replacements (not safety-critical). Pinned by `tests/command-gates-honor-safety-state.test.ts` (69 contracts: pure-helper across every state/output/op/error/recovery combination + edge cases like canPause-while-mutex-held; source-level pins for T1-30 marker, helper imports, gates const, all four `gates?.<gate>` call-site reads, canStartJob baseSafe conjunct, OLD ad-hoc shapes removed, helper exports). Regression sweep clean across 7 UI tests (`start-readiness-panel`, `connect-button-mutex`, `exact-idle-gates`, `frame-required-before-start`, `connection-panel-offset-button-disabled`, `connection-panel-preflight-no-loop`, `jog-and-setorigin-state-after-confirm`). **Hardware verification: not required** (UI gating consolidation; refusing operations on non-safe state is intrinsically safe; no g-code emission change). Shipped 2026-05-04 in **`7afc2e3`**. |
| T1-29 | Persisted unsafe-prior-state flag across reconnects (job-running scope) | Audit 1D: T1-25 catches the case where firmware reports alarm/run/hold on next connect, but if firmware happened to finish the buffered job cleanly while the renderer was dead, firmware reports clean idle and T1-25's check passes — even though the previous session was an uncontrolled crash. Realistic scenario: user mid-burn → browser tab crashes → firmware finishes buffered moves → user relaunches → no signal anything went wrong. The workpiece may be partially burnt and the head may be in a dangerous position. **Shipped (job-running scope):** new `src/app/unsafePriorState.ts` module with `setUnsafePriorState` / `getUnsafePriorState` / `clearUnsafePriorState` wrapping `localStorage` with key `'laserforge_unsafe_prior_state'`. Storage failures swallowed with console.warn (graceful degradation). Set-side: `MachineService.startValidatedJob` calls `setUnsafePriorState({ kind: 'job-running', ticketId, startedAt: Date.now() })` synchronously at the top of the job-begin block — durable before any await. Clear-side: three sites — `tryFinalizeJobLog` at terminal status (completed / stopped / failed all count as clean exits), `disconnect` in the finally block (user-initiated halt is qualitatively different from a crash), and the failed-start catch (job never reached running). App.tsx startup useEffect reads `getUnsafePriorState()` and surfaces `showAlert('Previous session ended unexpectedly', ...)` modal naming started-time, optional ticketId, and "Inspect the machine and the workpiece BEFORE reconnecting" remediation. Modal overlay blocks the connect button until acknowledged. After dismissal the flag clears. T1-25's `getUnsafeAtConnect()` on the next connect provides the orthogonal safety net (catches non-safe firmware state); T1-29 covers the "firmware finished cleanly while we were dead" gap. **Out of scope (deferred):** the spec's `kind: 'unclean-disconnect'` differentiation for app-close-while-connected-without-explicit-disconnect (needs a beforeunload distinction that's hard to make reliably); integration with T1-25's destructive `forceSafeState()` after acknowledgement (T1-25 itself deferred the destructive primitive — regular connect-time check still runs). Both filed as future T1-29 follow-ups. Pinned by `tests/unsafe-prior-state-persistence.test.ts` (22 contracts: persistence module round-trip; malformed-JSON / wrong-kind / missing-ticketId tolerated as null; `setUnsafePriorState` swallows storage exceptions; source-level pins for T1-29 marker, helper imports, set-with-kind=job-running at job begin, three clear-call sites in MachineService, App.tsx reads the flag + surfaces named alert text + clears after dismissal). Regression checks: `machine-service-start-validated-job.test.ts` (11/11), `machine-service-job-lifecycle-safety.test.ts` (14/14), `job-start-deferred-simulator-fanout.test.ts` (11/11), `failed-start-persists-log.test.ts` (16/16) all green. **Hardware verification: not required** (recovery dialog is informational; clearing the flag does not mutate machine state; T1-25's connect-time check still runs on next connect). Shipped 2026-05-04 in **`67e1da2`**. |
| T1-25 | Reconnect safe-state handshake — connect-time check + preflight blocker | Audit Phase 1 + audit 1D Required Fix #7: `connect()` queried machine info and settings but did not verify the controller was in a known-safe state before allowing machine control. If a previous session ended uncleanly (cable pull mid-job, app crash, browser restart), the firmware could be in alarm / hold / run state with the laser still in modal M3/M4 — UI permitted frame, jog, test fire, job start. Audit's framing: "FAIL — best-effort safety, not provable safety." **Shipped (non-destructive scope):** `GrblController` records a connect-time verdict via the new `getUnsafeAtConnect(): UnsafeAtConnectState \| null` getter. New private state: `_unsafeAtConnect`, `_safeStateCheckArmed`, `_safeStateWatchdog`. Welcome handler calls `_armSafeStateCheck()` which sets the flag and starts a 5-second watchdog. The first status report after welcome runs through the `_handleStatusReport` hook, computes a verdict via `_classifySafeStateReason()` (mapping `_state.status` + FS field + alarm code to one of `alarm \| run \| hold \| check \| unsafe-residual-spindle \| no-status-response \| null`), records the snapshot, and disarms. The watchdog covers the no-status-response case (wedged firmware, dead cable). `disconnect()` + `connect()` entry both clear state. `LaserController` interface gains optional `getUnsafeAtConnect?(): ...`. **Preflight blocker:** new `MACHINE_UNSAFE_AT_CONNECT` code (severity: error, blocking) in `MachinePreflight.ts` raised when `connectedToMachine === true && unsafeAtConnect != null`. Reason-specific recovery messages: alarm → "Clear via $X"; run → "Soft-reset (Ctrl-X / 0x18)"; hold → "Cycle-start (~) or soft-reset"; check → "Toggle check mode off, $C"; no-status-response → "Power-cycle the controller"; unsafe-residual-spindle → "Send M5 and reconnect". `runPreflightSummary` takes new optional `firmwareUnsafeAtConnect` parameter, wired from `controllerRef.current?.getUnsafeAtConnect?.()` in `ConnectionPanelMain.tsx`. **Out of scope (deferred):** the spec's destructive `forceSafeState()` primitive (soft-reset → banner → status sequence for operator-initiated button + T1-29 recovery integration) — needs different design that mutates controller state and its own UX; non-destructive connect-time check is what closes the spec's main "FAIL" exposure. Spec's optional step 5 (opt-in profile setting to auto-send M5 after Idle confirmation as defense-in-depth) and inline UI surface naming the unsafe reason in the connection panel header are filed as future follow-ups. Pinned by `tests/connect-safe-state-handshake.test.ts` (43 contracts across two surfaces — controller-level state machine: idle+FS:0,0 → null; alarm/run/hold/idle+FS:1500,500 → reason verdicts with snapshot fields populated; disconnect clears; preflight rule: each non-null reason raises MACHINE_UNSAFE_AT_CONNECT with reason-specific recovery text; null verdict + connected → no blocker; source-level pins for T1-25 marker, all new fields and helpers, ControllerInterface declaration, preflight constant + parameter wiring, ConnectionPanelMain wiring). Regression checks: `controller.test.ts` (47/47), `controller-bounds-recheck.test.ts` (13/13), `grbl-handshake-rejects-bare-ok.test.ts` (10/10), `wcs-mutation-consent.test.ts` (12/12), `preflight.test.ts`, `preflight-rejects-m4-without-laser-mode.test.ts` (21/21), `preflight-blocks-when-maxspindle-unknown.test.ts` (13/13) all green. **Hardware verification: not required** (preflight blocker; refusing job start when controller state is uncertain is intrinsically safe, no g-code emission, no machine-state mutation). Shipped 2026-05-04 in **`797319c`**. |
| T1-35 | Image import max-dim cap raised 1000 → 4000 px + downscale warning | Cross-check during audit 2B verification: `useImport.ts:103` had `const maxDim = 1000;` — every import with longest dimension > 1000 px was silently downscaled. A 4000×3000 phone photo became 1000×750 (0.75 MP). For 200×150 mm engraves at 254 DPI the user lost half the available resolution to the cap and never knew — a hard-to-debug quality cliff (calibrate against soft input → produce worse engraving than source supports). **Shipped (minimum-viable scope):** new named constant `IMAGE_IMPORT_MAX_DIM = 4000`. 4000 px supports a 400 mm engrave at 254 DPI exactly without downscale. T1-17 Pass 1 already moved the grayscale loop to a Web Worker via `prepareImageGrayscale` (`023a341` + `0632b2b` + `b8f3dfb` + `05ce7b86`), so larger source bitmaps no longer block the UI on import — raising the cap is now safe per the spec's sequencing requirement. When a downscale still happens (image > 4000 px), `console.warn` surfaces original + downscaled dimensions + rationale ("Detail beyond the cap is lost; raise the limit if needed") for support diagnosis. **Out of scope (deferred):** the spec's `userPreferences.imageImportMaxDim` user-tunable preference + Settings UI surface; UI toast at import time. Filed as future T1-35 follow-ups; the headline silent-downscale-quality-cliff is closed by the 4× cap raise + warning. Pinned by `tests/image-import-max-dim-cap.test.ts` (10 contracts: T1-35 marker; constant declared as 4000; OLD `const maxDim = 1000` line removed; scale formula uses the new constant; downscale branch emits console.warn naming the failure mode; T1-17 worker call still threaded; cap supports 400mm × 254 DPI without downscale; regression guard against silent revert). Source-level pin chosen because `useImport` is a React hook with a deep dependency tree (scene, dialogs, IndexedDB storage, image-prep worker). Regression checks: `image-prep-grayscale-equivalence.test.ts` (18/18), `import-callback-identity-stable.test.tsx` (6/6). **Hardware verification: not required (import-time bitmap sizing only; no g-code, no machine state, no preflight semantic change).** Shipped 2026-05-04 in **`1ed3aaf`**. |
| T1-38 | Operation-aware default flattening tolerance | Audit 2C: SVG-imported path geometry used a hardcoded `0.5mm` flattening tolerance for all operation modes. A 20mm-diameter quarter-circle came out as ~9 points (a regular hexadecagon-equivalent), visibly faceted on small / detailed work — gears, decorative script, tight curves, inlays. **Shipped:** new `FLATTEN_TOLERANCE_BY_OPERATION: Record<OperationType, number>` const in `JobCompiler.ts`: cut=0.05, score=0.05, engrave=0.03, raster=0.5. `flattenObject` takes new `operationType: OperationType` parameter; threaded to `geometryToPoints` (defaults to `'cut'`); the path branch reads the constants table instead of the hardcoded `0.5`. OLD `// SVG-imported paths: default 0.5mm tolerance is fine for typical cutting at 100+ mm scale.` comment is gone (pinned by test). Same 20mm quarter-circle now produces 33 points at cut tolerance — smooth curves at the cost of ~3.7× more vertices (PlanOptimizer / output layers handle this fine; point counts in the 10K-100K range are routine). Text path unchanged — already used `TEXT_FLATNESS_MM = 0.05mm` at its call site. **Out of scope (deferred):** the spec's `LayerSettings.flatteningTolerance?: number` per-layer override field; filed as a future T1-38 follow-up if user-tunable tolerance becomes necessary. Pinned by `tests/flattening-tolerance-by-operation.test.ts` (15 contracts: tolerance constants shaped per spec; cut produces 33 points on the 20mm quarter-circle (vs 9 at legacy 0.5mm); engrave at least as tight as cut; default operationType = 'cut'; non-path geometry (rect) unaffected; source-level pins for T1-38 marker, constant declaration, signature changes, OLD legacy comment gone, path branch passes the new tolerance). Regression checks: `pipeline.test.ts` (98/98), `svg-import.test.ts` (141/141), `job-compiler-curve-flatness.test.ts` (2/2) green. **Hardware verification: not required (geometry flattening tightening only; output gcode line counts grow modestly for small/curve-heavy work, no semantic change to planner/output layers).** Shipped 2026-05-04 in **`c684f06`**. |
| T1-1 | Gate silent WCS + status-mask mutation behind consent — close-out | Spec called for: read G54 via `$#` and `$10` via `$$` at connect; if either is non-zero, prompt the user before mutating; on accept apply `G10 L2 P1 X0 Y0 Z0` + `$10=0`; on decline leave settings alone. Plus a `stashedWcsState` profile field for restore-on-disconnect. **Shipped pre-session:** primary work in `0310007` (`fix(grbl): prompt before overwriting user's WCS and status mask`) plus T1-20 hardening in `b0375fa` (`feat(safety): T1-20 - WCS no-listener fallback marks placement-uncertain`). At write-time verification: `GrblController._maybeNormalizeWcs` queries G54 + statusMask, auto-applies on zero/zero, fires `onWcsConsentNeeded` event otherwise. T1-20 hardened the no-listener fallback: pre-T1-20 silently auto-applied; now refuses without explicit consent and marks the controller `_placementUncertain = true` so the UI blocks job start until the user reconnects with a listener attached. UI side: `App.tsx:370-410` registers the consent listener, surfaces `showConfirmWithCheckbox` with both G54 + `$10` values rendered, plus a "Don't ask again for this profile" toggle that writes `profile.suppressWcsConsent = true` for the per-profile suppression case. Accept calls `applyWcsNormalization`, decline calls `skipWcsNormalization`. `allowHeadlessWcsAutoNormalize: true` is the test/headless escape hatch (preserves the pre-T1-20 auto-apply for tests that don't mount the UI). Pinned by `tests/wcs-mutation-consent.test.ts` (12 contracts: zero-G54-zero-mask auto-applies; non-zero G54 fires consent event with payload; decline path honored; no-listener default refuses + marks placement-uncertain; headless-flag opt-in restores auto-apply). **Out of scope (deferred):** the spec's `stashedWcsState` profile field for restore-on-disconnect — the safety contract (don't silently mutate WCS without consent) is delivered; the restore-the-old-values feature is a quality-of-life enhancement filed as a future T1-1 follow-up if requested. Same close-out shape as T0 retroactive close-outs (T0-1, T0-2, T0-3, T0-4) and the in-session T1-2 / T1-4 / T1-51 close-outs. Closed 2026-05-04 (no separate ship hash; primary work at `0310007` + `b0375fa`). |
| T1-60 | Device profile selector pinned to the connection panel — out of "More options" | Audit 4B Critical UX failure 3: the device profile selector lived inside the `moreSection` collapsed `showMore && (...)` block. A beginner connecting their machine had to click "More options ▼" to even see which profile was active. The active profile governs the most safety-critical settings (bed dimensions, origin corner, max spindle, homing, header/footer templates); a wrong profile makes every other setting wrong — top-tier "wrong place to burn" cause for beginners. **Shipped (minimum-viable scope):** lifted the `DeviceProfileSelector` mount out of the More options collapse and into a new `profileSection` rendered as a sibling of `MoveControls` inside the panel body — always visible when connected, no clicks needed. The selector itself is unchanged; only its render position moved. Subtle accent background (`rgba(0, 212, 255, 0.02)`) marks it visually as a structural panel section. The richer header treatment from the spec — collapsed summary card showing "Bed: 400 × 400 mm · Origin: front-left · Max spindle: 1000 · Homing: off" with click-to-open modal — is filed as a future T1-60 follow-up; the safety contract (user always sees which profile is active without clicking through More options) is delivered by the always-visible selector. Pinned by `tests/profile-selector-pinned-to-header.test.ts` (10 source-level contracts: T1-60 marker present; `profileSection` gated on `isConnected`; mounts `DeviceProfileSelector`; renders before `MoveControls`; OLD location inside `moreSection` is empty of the selector mount; marker comment cites the rationale; profileSection is a sibling of MoveControls in the scrollable panel body). Source-level pin chosen because mounting `ConnectionPanelMain` requires the full machine-service / controller / preflight tree — disproportionate for a contract that targets the panel structure. **Hardware verification: not required (UI relocation only; no g-code, no machine state, no preflight semantic change).** Shipped 2026-05-04 in **`255105f`**. |
| T1-58 | `PipelineService.compileGcode` and `compileToolpath` accept profile snapshot | Audit 4A Coupling 3 + Race 5 + Required Fix: `compileGcode` and `compileToolpath` read `getActiveProfile()` from global storage internally. Determinism bug: if the active profile flipped between the caller's decision-to-compile and the read (programmatic update, profile import, cross-tab storage event), the result was computed against a profile the UI didn't know was active. Untestable too — tests had to mutate global state to compile against a specific profile shape. **Shipped (smaller-than-spec scope):** `profile: DeviceProfile | null = null` added as a new positional parameter (8th on `compileGcode`, 3rd on `compileToolpath`); the internal `const profile = getActiveProfile()` calls are removed from both function bodies. Spec proposed an options-object refactor; positional addition delivers the same semantic guarantee with a fraction of the blast radius (only `useCompileManager` — the sole caller of both pipeline entry points — updates). The options-object refactor is filed as a future T1-58 follow-up if 8 positional parameters become unwieldy. `useCompileManager` updated to snapshot `getActiveProfile()` alongside the existing `requestId` and `sceneTickAtStart` snapshots at compile entry, then forward the snapshot through to `compileToResult`, `compileGcode`, and `compileToolpath`. The race window is now bounded to the synchronous block that takes the snapshot. T1-57's request-id guard catches the case where a newer compile started against a different profile overtakes an older one. Pinned by `tests/pipeline-compile-accepts-profile-snapshot.test.ts` (18 contracts: distinct profile snapshots produce distinct gcode (S-scale differs); T1-33 controller-vs-profile precedence preserved; profile=null fallback path works for both functions; source-level pins for T1-58 markers in PipelineService + useCompileManager, internal `const profile = getActiveProfile()` removed from both bodies, signature declares the new parameter, useCompileManager forwards `profileSnapshot` to all call sites). Existing `tests/pipeline.test.ts` (98/98) passing — backward-compatible default `profile = null` keeps any external consumers unbroken. **Hardware verification: not required (compile semantics unchanged in production paths because `useCompileManager` already used `getActiveProfile()` at compile entry — T1-58 just makes the read explicit and pinned at the call site instead of hidden in the pipeline; T1-33 maxSpindle precedence preserved).** Shipped 2026-05-04 in **`91d0873`**. |
| T1-55 | Preflight blocks job start when controller is connected but `$30` is unknown | Audit 3C Finding 7.1 / Critical 5 / Priority 7: when controller.maxSpindle is null but the controller is "connected," every laser-on path (PipelineService compile, test-fire, frame-dot, preflight maxSpindle reads) fell back to `profile?.maxSpindle ?? 1000`. Audit's framing: "for a laser controller, unknown should be treated as unsafe." Three different scenarios collapsed into the same `controller.maxSpindle === null` state — `$$` query failed, parse rejected the line, or connect raced ahead of `_queryMachineSettings`. In all three the S-scale was a guess. **Shipped (preflight-blocker scope):** new `MACHINE_MAXSPINDLE_UNKNOWN` PREFLIGHT_CODES constant + new rule in `MachinePreflight.ts` that fires at error severity (blocking) when `ctx.connectedToMachine === true && ctx.hasGcode === true && ctrlMax == null`. The `liveMachineInfo.maxSpindle` slot was already wired in by T1-33's earlier work; T1-55 reuses the same channel for the absent-value branch. Fix-it message tells the user to wait for settings detection or reconnect. Disconnected (offline / simulator / export-only) paths are explicitly skipped — `connectedToMachine === true` gate keeps profile-fallback working there. Pinned by `tests/preflight-blocks-when-maxspindle-unknown.test.ts` (13 contracts: connected + no $30 + has gcode raises with $30 + remediation text; connected + $30 reported passes; disconnected + no $30 passes (offline allowed); connected + no gcode skips (nothing to refuse yet); zero-value handling deferred to UI wiring; unknown + mismatch rules don't conflict; source-level pins for the constant, T1-55 marker, guard shape). **Out of scope:** UI button-disable for Test Fire / Frame Dot / Set Origin when connected-unknown — the preflight blocker is the structural guarantee at job start; UI gates are filed as a future T1-55 follow-up if friction surfaces. **Hardware verification: not required (preflight blocker; refusing job start when machine state is uncertain is intrinsically safe).** Shipped 2026-05-04 in **`df7fca7`**. |
| T1-46 | `notifySimulatorTx` deferred-and-chunked so sendJob starts streaming first | Audit 2E section 6.3: `MachineService.startValidatedJob` ran `for (const line of lines) notifySimulatorTx(line)` synchronously BEFORE `await this.controllerRef.current.sendJob(lines)`. For million-line jobs the controller didn't see byte 1 until the entire fan-out completed — multi-second freeze right when the user expects "click Start → laser begins" within ~100ms. **Shipped Option B** (deferred full notification, preserves all simulator detail vs Option A's sampling): kick off `sendJob(lines)` first (controller starts streaming immediately), then schedule the simulator fan-out via the new `_notifySimulatorChunked(lines, notify)` private helper, then `await sendPromise`. The helper walks `lines` in 1000-line batches, invoking the notify callback synchronously inside each batch and yielding via `setTimeout(tick, 0)` between batches. The first batch is also deferred so `sendJob` is on the event loop ahead of the notification work. Per-call errors are swallowed — matches the existing per-listener try/catch shape; a broken listener must not break job start. NOTIFY_CHUNK = 1000 is tuned for "yield often enough to repaint, batch large enough that fan-out finishes within seconds for million-line jobs" (~60k lines/sec sustained at 16ms paint cadence while remaining responsive). Existing `tests/machine-service-start-validated-job.test.ts` updated to await the deferred chunks via `waitUntil(...)` before asserting all-lines-eventually-delivered. Pinned by `tests/job-start-deferred-simulator-fanout.test.ts` (11 contracts: sendJob fired before any notifySimulatorTx call (deferred order pin); all lines eventually notified after the chunks drain; throwing listener doesn't break startValidatedJob and all lines are still attempted; source-level pins for T1-46 marker, sendJob-before-chunked invocation order, helper signature, setTimeout(..., 0) yielding, NOTIFY_CHUNK = 1000, OLD sync for-loop removed). **Hardware verification: not required (UI thread responsiveness; no g-code, no machine state, no preflight semantic change).** Shipped 2026-05-04 in **`72ede5c`**. |
| T1-63 | Warning confirmation dialog includes detail + fix per warning | Audit 4B Finding 7.4 + Section 14: the blocker dialog already rendered `• <title>\n  → <fix>` per issue, but the warning confirmation dialog rendered `▲ <title>` only — no detail, no fix. A user staring at "▲ High cut power" with no value, no consequence, and no remediation pressed Start because the title didn't sound serious. **Shipped:** the warning branch in `src/core/preflight/confirmPreflightForJobStart.ts` now mirrors the blocker format: `▲ <title>` + indented detail line (skipped when `detail === title` to avoid duplicate-line noise) + indented `→ <fix>` line when fix is present. Warnings separated by `\n\n` instead of `\n` for readability when multiple are present. The OLD title-only `.map(i => \`▲ \${i.title}\`)` shape is gone. Pinned by `tests/preflight-warning-confirm-includes-detail.test.ts` (19 contracts: full title + detail + fix rendered with ▲ marker and arrow prefix; detail-equal-to-title de-duplication; missing-fix branch produces no arrow; missing-detail-distinct-from-title still renders correctly; multi-warning blank-line separation; cancel path preserved; no-warnings short-circuit preserved; T1-63 marker + de-dup logic + arrow-prefix shape + OLD shape gone source-level pins). **Hardware verification: not required (UI dialog text only; no g-code, no machine state, no preflight semantic change).** Shipped 2026-05-04 in **`b0b4827`**. |
| T1-44 | Controller `_checkJobBounds` simulates G91 from known starting position | Audit 2D Finding 2D-06: `_checkJobBounds` had `if (relative) continue` — relative-mode (G91) lines were skipped entirely. For current-mode / head-mode jobs (entire body emitted as G91), the controller-layer defense-in-depth bounds check provided **zero protection**. A user running a current-mode job with the head near the bed edge could silently exceed the envelope and contact a limit switch / off-bed obstruction. Compile-time `machinePlanBounds` knew local job bounds but not the head's current position; the runtime check was the layer that should have closed the gap. **Shipped:** new `_positionConfirmed: boolean` flag on `GrblController`, set true whenever `wPos` or `mPos` parses successfully in a status report (`<...>` parser), cleared at connect-entry and on `disconnect()`. The constructor default `_state.position = {0,0,0}` is indistinguishable from "actually at origin" — the flag distinguishes "we know we're at origin" from "we're guessing." `_checkJobBounds` rewritten with simulation: `curX/curY` seeded from `_state.position.{x,y}`, absolute moves set the cursor to literal X/Y, relative moves accumulate the delta, cursor is checked against `bedW`/`bedH` after each move. OLD `if (relative) continue` short-circuit removed. When a relative move is reached and `_positionConfirmed === false`, the job is refused with `"Cannot accept relative-mode job: current head position is unknown. Reconnect to refresh status, then try again."`. The 500-line cap and EPS=0.01 tolerance are preserved. Pinned by `tests/controller-bounds-checks-g91.test.ts` (24 contracts including positive case, X-axis reject with cumulative position in message, multi-delta accumulation, mode-flip preserves checks, negative deltas, position-unknown rejection, source-level pins for the flag declaration / set / clear / cleanup and the `_checkJobBounds` body shape including OLD-short-circuit-absent assertion). Existing `tests/controller-bounds-recheck.test.ts` updated: two contracts that pinned the obsolete behavior (`'G90 then G91: relative G0 not validated as absolute'` + `'G91 at start: relative G0 not validated'`) flip to T1-44 semantics — assert reject instead of accept. The third related contract (G90 → G91 X9999 → G90 → X9999 reject) still rejects (T1-44 catches earlier on the G91 line; contract title still holds). **Hardware verification: not required (controller-side runtime guard; refusing to send a job that would exceed bed extents is intrinsically safe).** Regression checks: `controller.test.ts` (47/47), `controller-bounds-recheck.test.ts` (13/13), `pipeline.test.ts` (98/98). Shipped 2026-05-04 in **`4fb7dcd`**. |
| T1-33 | Live `$30` controller value wins over profile `maxSpindle` + mismatch preflight | Audit 2A: `PipelineService.compileGcode` resolved `maxSpindle` as `profile?.maxSpindle ?? controllerMaxSpindle ?? 1000` — **profile wins**. With profile=1000 (default for many profile entries) and controller $30=255 (Falcon ship-default), generating "50% power" produced S=500 → firmware clamps to 255 → actual 100% output. Over-power on flammable materials is a fire risk, and even on hardwood/leather the calibrated burn settings come out wrong. **Shipped (two parts):** **Part 1 — precedence flip:** `(controllerMaxSpindle != null && controllerMaxSpindle > 0) ? controllerMaxSpindle : (profile?.maxSpindle ?? 1000)`. Controller wins when present and positive; profile is fallback for the disconnected / no-$30-yet case. Inline comment cites T1-33 + the failure-mode rationale. **Part 2 — preflight mismatch detection:** new `MACHINE_MAXSPINDLE_MISMATCH` code (severity: error, blocking) in `MachinePreflight.ts`. Triggers when `profile.maxSpindle > 0 && ctrl.maxSpindle > 0 && (ratio < 0.95 || ratio > 1.05)`. The 5% symmetric tolerance allows firmware rounding / minor calibration drift while still catching the profile=1000 + $30=255 (~74% off) case the audit framed. Skipped when either side is missing — `MISSING_MAX_SPINDLE` covers profile-blank separately, and disconnected controllers can't yet contribute. Fix-it message names both values + points at "$30=N" remediation. `runPreflightSummary` takes a new optional `firmwareMaxSpindleFromMachine: number` parameter; `ConnectionPanelMain.tsx` reads `controllerRef.current?.maxSpindle` (already on the `LaserController` interface) and passes when positive. Pinned by `tests/maxspindle-precedence-and-mismatch.test.ts` (22 contracts: profile=1000 + $30=255 raises MISMATCH with profile + controller + $30 in remediation text; matched values pass; 3% drift passes; 5.1% drift triggers; symmetric (controller > profile by 5.1%) triggers; profile-only / controller-only / profile=0 cases all pass; source-level pins for the precedence flip + OLD precedence absent + T1-33 marker + constant declarations + the 5% tolerance constants + ConnectionPanelMain wiring). Existing regression tests `tests/auto-detect-includes-max-spindle.test.ts` (12/12), `tests/pipeline.test.ts` (98/98), and `tests/preflight.test.ts` still pass — the precedence flip changes behavior only when both controller and profile values are positive, which existing fixtures didn't exercise. **Hardware verification: not required (runtime calculation + preflight only; no new g-code emission, no machine-state change).** Shipped 2026-05-04 in **`a5dbe91`**. |
| T1-32 | `$32` laser-mode verification before M4 emission | Audit 2A Priority 2: `$32` was parsed and stored as `_laserMode` and exposed via `controller.state.laserMode`, but no preflight rule consulted it. With `$32=0` (CNC / spindle mode) GRBL keeps the laser on at full S between motion commands — including stationary planner stalls between path segments — when M4 is emitted. A diode-laser user repurposing a 3018 router (or hitting a profile with `$32=0` left over from CNC duty) could see the laser stay full-on between vector segments and burn / mark / start a fire. **Shipped:** new `getFirmwareLaserModeEnabled(): boolean \| undefined` on `ControllerInterface` (optional) and `GrblController` (reads the `$$` cache key 32, mirrors `getFirmwareHomingCycleEnabled`'s cache-or-undefined shape — `true` for `$32=1`, `false` for `$32=0`, `undefined` if not yet read). `PreflightContext` gains `liveMachineInfo.laserMode?: boolean` and `outputUsesM4?: boolean` (precomputed at the `runPreflightSummary` boundary via a single `\bM4\b` regex scan — case-insensitive, matches comment text by design for conservative bias). `runPreflightSummary` takes a new optional `firmwareLaserModeFromMachine` parameter wired from `controllerRef.current?.getFirmwareLaserModeEnabled?.()` in `ConnectionPanelMain.tsx`. New rule in `MachinePreflight.ts` raises `MACHINE_LASER_MODE_DISABLED` (severity: error, blocking) when `liveLaserMode === false && ctx.outputUsesM4`. Skipped when laserMode is `undefined` (disconnected — runtime preflight or post-connect handshake will surface it later) or no M4 emission. Fix-it message points users at `$32=1` and the M3 constant-power alternative. Pinned by `tests/preflight-rejects-m4-without-laser-mode.test.ts` (21 contracts: `$32=1 + M4` passes; `$32=0 + M4` blocks with `$32=1`/`M3` remediation text; `$32=0 + M3` passes; disconnected + M4 skips; `$32=0 + no gcode` skips; lowercase `m4` still matches; M4-in-comment still flagged conservatively; source-level pins for `outputUsesM4` declaration, `laserMode` declaration, `MACHINE_LASER_MODE_DISABLED` constant, parameter wiring, the precompute regex line, T1-32 marker, rule guard shape, GrblController method signature + `$$` key 32 read, ControllerInterface declaration, ConnectionPanelMain wiring). **Out of scope:** the spec's secondary $22 / $20 / $23 verifications — separate tickets; T1-32 headline is `$32` + M4. **Hardware verification: not required (preflight is software-only contract; no g-code emitted, no machine-state change).** Shipped 2026-05-04 in **`8bc2f3a`**. |
| T1-80 | Validation failure shows user-facing state, not silent downgrade | Audit 5A user-trust failure + Priority 12: pre-T1-80 a transient Gumroad outage during `EntitlementService.runInitialize` deleted the user's stored license and silently rendered "Free" — paid users saw their Pro features locked with no message and assumed they'd been revoked. **Shipped (state-plumbing scope):** new `LicenseStatus` type in `src/entitlements/types.ts` with seven values (`free | verified | offline_grace | verification_failed | revoked | developer | tester`). `EntitlementState` widened with optional `status`, `lastVerifiedAt`, `graceUntil`, `lastError` fields. `EntitlementService.validateAndApplyStoredCode` now returns a `StoredCodeValidation` discriminated union instead of boolean. `runInitialize` switches on the kind and sets distinct state per outcome — verified → paid+hasPro+verified, offline_grace → paid+hasPro+offline_grace, revoked → free+revoked (code preserved), verification_failed → free+verification_failed+lastError (code preserved). New `verifyGumroadStructured` distinguishes outcomes by HTTP status, success flag, refunded/chargebacked/disputed, network-throw, and cache freshness. **Critical:** the OLD silent `storage.remove(STORAGE_KEY)` path on verification failure is gone — the license code is preserved on revoked (UI can show "contact support") and on verification_failed (user can retry without re-entering). Only explicit `deactivate()` removes the code. Pinned by `tests/license-status-states.test.ts` (37 contracts: verified / revoked / verification_failed / offline_grace / cache-fresh / no-license / deactivate / revoked-preserves-storage / source-level pins for T1-80 markers, StoredCodeValidation, verifyGumroadStructured, OLD silent-delete shape absent, all seven LicenseStatus values). Regression: `entitlement-api-split` (11/11), `entitlement-storage-migration` (12/12), `entitlement-api-no-deprecated-export` (17/17), `entitlement-api-migration-phase2a` (16/16), `entitlement-api-migration-phase2b` (23/23). **Out of scope:** UI strip rendering the four messages with appropriate colors + Retry button — separate ticket once the surface is designed; the state plumbing closes the trust failure at the service layer. **Hardware verification: not required** (state plumbing; no g-code, no machine-state change). Shipped 2026-05-05 in **`ae17f9f`**. |
| T1-45 | Compile complexity gate — warn/block on huge jobs | Audit 2E Priority 0: pre-T1-45 compile/output ran on the renderer thread synchronously, no progress, no cancel, no estimate. A 4MP grayscale photo could freeze the UI for tens of seconds; users couldn't tell processing from hung. The streaming architectural fix (T3-15) is multi-week; this is the cheap defense — estimate the upper-bound complexity from the scene before launching the work. **Shipped:** new `src/core/preflight/rules/CompileComplexityPreflight.ts` exports `estimateCompileComplexity(scene)` and `runCompileComplexityChecks(ctx, out)`; integrated into `runPreflight` orchestrator. Three severity bands: INFO at ≥1M lines, WARN at ≥3M lines, BLOCK at ≥10M lines OR >800MB estimated memory. Estimator walks visible objects on output layers (raster: pixels = grayscale w×h, lines = pixels (worst-case); vectors: lines per flattened vertex, cubic=12 / quadratic=10 / polygon=N+1 / rect=5 / ellipse=32 / text proxy ~30/char + 4/path overhead). Memory = pixels × 5 + lines × 80. Hidden / output:false / invisible objects excluded. New `PREFLIGHT_CODES.COMPILE_COMPLEXITY_INFO/WARN/BLOCK`. Pinned by `tests/compile-complexity-gate.test.ts` (23 contracts: empty scene → no result; small vector → no result; 4MP grayscale photo → WARN; 16MP photo → BLOCK as error; 8000×8000 raster trips memory threshold; 1.3M lines → INFO only; hidden / output:false / invisible all excluded; estimator math zeros for empty; mixed-but-small → no warning; source-level pins for the four numeric thresholds, T1-45 marker, runPreflight wiring, code declarations). Regression: `preflight` (1/1), `preflight-output-layer-filter` (7/7), `preflight-duplicate-geometry` (21/21). **Out of scope:** streaming compile with progress + cancel — that's T3-15. **Hardware verification: not required** (preflight info / warning / blocker; refusing to launch a compile that will OOM is intrinsically safe; no g-code, no machine-state change). Shipped 2026-05-05 in **`e19b252`**. |
| T1-78 | Split `requireFeature` into `canUseFeature` + `assertFeature` — fully shipped | API hygiene fix in four commits across 2026-05-04. **Phase 1 (`bad81ab`)** added new exports to `src/entitlements/index.ts`: `canUseFeature(f): boolean`, `assertFeature(f): void` (throws `EntitlementError`), and the `EntitlementError` class. `requireFeature` was preserved as a transitional `@deprecated` alias. **Phase 2a (`eaf99b7`)** migrated the three service-layer callers: `Nester.ts` (2 sites → `assertFeature('nesting')`), `BooleanOps.ts` (1 → `assertFeature('boolean_ops')`), `JobCompiler.ts` (6 flag-builder sites → `canUseFeature('<flag>')`). Ad-hoc `throw new Error('… requires a Pro license')` paths replaced. `tests/service-layer-pro-gate-coverage.test.ts` widened to a form-agnostic matcher. **Phase 2b (`77e041a`)** migrated four UI-hook callers (6 sites total): `useSceneOperations.ts`, `useGeneratorHandlers.ts`, `useMaterialTestHandlers.ts`, `useKerfHandlers.ts` — all to `assertFeature(...)`. **Phase 3 (`91a848b`)** removed the transitional alias from the entitlements barrel entirely. The barrel now exports only `canUseFeature`, `assertFeature`, and `EntitlementError` for the gate API surface. New `tests/entitlement-api-no-deprecated-export.test.ts` (17 contracts) pins the alias removal at three layers: source-of-truth (the barrel has no `export function requireFeature` declaration), runtime (the imported namespace's `requireFeature` is `undefined`), and migrated-file sweep (the seven previously-migrated files still have zero references). Companion tests in Phase 1/2a/2b were updated: alias-still-exported assertions removed; the global "no internal caller of requireFeature" sweep in Phase 2b's test remains load-bearing as a regression guard against re-introduction. Existing `nesting-throws-without-license` + `boolean-ops-throws-without-license` tests continue to pass because `EntitlementError.message` includes "Pro license". Master checklist `[x]` 2026-05-04. |

### 鈼?Partial

| Ticket | What | What's done | What's missing |
|---|---|---|---|
| T1-36 | Offset-with-holes for kerf compensation — **code shipped (band-aid), hardware verification needed** | Audit 2C Critical 2 (production-safe failure): pre-T1-36 `offsetObject` only processed `poly[0]` (outer ring); holes were silently dropped. For kerf comp on a donut (gear, inlay, joint, lettering with closed loops) the inner contour stayed at design size, so the laser kerf removed material from BOTH sides of the inner edge → finished hole came out too small. Root cause split: `objectToPolygon` was pushing each subpath as its own single-ring polygon, so the hole semantics were never preserved into the polygon array; `offsetObject` then read only `poly[0]`. **Shipped (band-aid):** (1) `src/geometry/BooleanOps.ts` adds private `pointInRing` (ray-cast, even-odd), `ringSignedArea`, and `groupRingsByContainment(rings)` which classifies containment depth per ring and groups depth-0 outers with their depth-1 holes (depth-2+ start new polygons — islands inside holes). `objectToPolygon`'s path-geometry branch now routes raw rings through this helper instead of one-polygon-per-subpath. (2) `src/geometry/OffsetPath.ts` adds local `ensureWinding(ring, wantCCW)`, forces canonical winding (outer CCW, hole CW) before passing the compound polygon `[outer, ...holes]` to polygon-offset (which accepts holes natively when winding is canonical). Pinned by `tests/offset-preserves-holes.test.ts` (27 contracts): simple square → 1×1; donut → 1×2 (was 2×1 pre-T1-36); two disjoint squares → 2 polygons; outer+hole+island case → 2 polygons (depth-0 outer+hole grouped; depth-2 island as its own outer); donut +1mm widens outer to 52mm + shrinks hole to 18mm; donut -1mm narrows outer to 48mm + grows hole; no-holes regression; wrong-winding inputs auto-corrected; source pins. **Architectural cleanup:** T2-15 (CompoundPath model) — makes this issue structurally impossible. T1-36 is the band-aid; T2-15 is the proper fix. Regression: `boolean-ops-throws-without-license` (1/1), `kerf-presets` (18/18), `preflight-duplicate-geometry` (21/21) all green. **Hardware verification owed** — cut a 50×50 square with a 20×20 hole at center on cardstock with kerf-comp +0.1mm; confirm both outer and inner edges land at design size after the cut. Code shipped 2026-05-05 in **`ffedba5`**. |
| T1-34 | Serpentine error-diffusion dithering — **code shipped, hardware verification needed** | Audit 2B: pre-T1-34 every row of the error-diffusion dither (Floyd-Steinberg, Jarvis, Stucki, Atkinson, Burkes, Sierra3/2/Lite) scanned left→right and propagated error rightward + downward. Mid-tone gradients showed diagonal "worm" patterns; bidirectional engraving compounded the artifacts because alternating burn direction met dither rows that all propagated the same way. **Shipped:** `ditherErrorDiffusion` inner row loop alternates direction per row. Even rows scan LTR (kernel as defined); odd rows scan RTL with `kernel.dx` mirrored so error always propagates "ahead of the cursor + downward" relative to the scan rather than to absolute X. Kernel definitions for all 8 error-diffusion modes are unchanged. Ordered (Bayer) and random modes are intentionally untouched (single-pass, no error to alternate). Pinned by `tests/dither-serpentine.test.ts` (29 contracts: diagonal autocorrelation balanced on uniform mid-tone (measured ratio 1.01 vs. a pre-T1-34 ratio that ran 2-3×); adjacent rows do not share an identical leading-edge dither pattern (proves serpentine fired); all 8 error-diffusion modes still produce strict 0/255 outputs of the expected dimensions; determinism across calls; pure-black / pure-white round-trips; ordered mode untouched; single-row / two-row edge cases hit the alternation boundary; source-level pins for T1-34 marker, serpentine docstring, mirrored-dx logic, reverse-row scan start). Regression: `raster-output-uses-modal-m4` (16/16), `raster-m4-no-software-splitting` (6/6), `pipeline` (100/100). **Hardware verification owed** — burn a uniform mid-tone (value=128) photo with Floyd-Steinberg before/after T1-34 and visually compare for diagonal worm patterns vs isotropic noise. Code shipped 2026-05-05 in **`fa62f09`**. |
| T1-31 | Raster image strategy — single M4 per pass with inline S modulation — **code shipped, hardware verification needed** | Audit 2A "biggest Phase 2A failure": pre-T1-31 each raster segment was wrapped in its own M4/M5 pair — for a typical photo engrave at 0.1mm scan resolution, thousands of M4→M5 cycles per pass causing planner stutter, fat/weak segment boundaries, saturated buffer bandwidth. The fill strategy already used the modal pattern; raster lagged. **Shipped:** `planRasterOperation` rewritten to mirror `planFillOperation`: ONE `laserOn` (M4 S0) at start, ONE `laserOff` (M5 S0) at end. Between scanlines, `rapid` (G0) auto-extinguishes the laser in M4 dynamic-power mode; within a scanline, segments are stitched with power=0 `linear` (G1 S0) gap-bridges. `appendRasterBurnMoves` (the velocity-aware accel/decel helper) is unchanged and still wraps each burn segment. `interpolateOffset` + `applyScanOffset` are pre-computed up front per scanline so gap-bridges use post-offset endpoints. **Bonus refinement (tolerance merge in `extractSegmentsGrayscale`):** deferred — filed as T1-31-followup; the structural M4/M5 collapse already handles the dominant cost. Pinned by `tests/raster-output-uses-modal-m4.test.ts` (16 contracts): exactly 1 laserOn for the whole operation, exactly 1 laserOff at the end, laserOn precedes all rapids+linears, laserOff is the final move, ≥1 rapid per scanline, ≥3 power=0 gap-bridges across multi-segment rows, ≥1 burn linear per segment, laserOn carries power=0 (S handled inline on linears), no second laserOn anywhere, no premature laserOff, planRasterOperation source pushes exactly 1 laserOn + 1 laserOff. `tests/pipeline.test.ts` raster + grayscale assertions updated from old per-segment expectations (11 / 5 laserOns) to modal expectations (1 laserOn for the pass; per-segment power on the burn linears, not on laserOn). Regression: `raster-m4-no-software-splitting` (6/6), `raster-with-curve` (4/4), `plan-optimizer-large-raster` (1/1), `plan-marker-emission` (10/10), `pipeline` (100/100). **Hardware verification needed — Falcon A1 Pro front-origin burn test.** Burn a calibration grid photo at 100mm/s on the Falcon and visually inspect for stutter / ghosting at segment boundaries vs. pre-T1-31 output. The structural fix is verified at the move-stream level; the visible-quality improvement is what the user experiences. Code shipped 2026-05-05 in **`315565a`**. |
| T1-51 | Strengthen GRBL handshake proof — reject raw `ok` as welcome — **code shipped, hardware verification needed** | Audit 3B P0 fix + section 7. The welcome predicate accepted `line === 'ok'` as a connect signal — but many non-GRBL devices respond `ok` to newlines (modems, industrial controllers, serial loopbacks), stale lines from previous sessions can sit in the kernel/browser buffer at connect time, and a malfunctioning device can spoof the handshake. A non-GRBL device passing the handshake then "Connected" in the UI while subsequent polling and `$$` parsing produced silent nonsense. **Shipped:** removed the bare-`ok` clause from `GrblController.ts:280-285`. The remaining clauses each require GRBL-specific shape: lower-cased `grbl` substring (banner), `[VER:]` and `[OPT:]` (the two halves of `$I` response — `[OPT:]` added for symmetry with `[VER:]`), `[MSG:]` (GRBL message), or a parseable `<State|MPos:...>` realtime status. If the device responds only with `ok` and never produces one of these, the existing overall 10s connect timeout fires with the standard "no welcome" path. Post-handshake bare-`ok` handling at three sites ($$ / $# / streaming acks) is intentionally untouched — those depend on `ok` as GRBL's protocol acknowledgment. Pinned by `tests/grbl-handshake-rejects-bare-ok.test.ts` (10 source-level contracts: predicate shape, removed bare-ok clause, all four GRBL-shaped clauses still present, `[OPT:]` added, T1-51 marker, post-handshake ack-handling sites count ≥3). **Hardware verification needed — Falcon A1 Pro front-origin burn test.** The Falcon is a known-good GRBL device; verify connect still succeeds on the standard path. The non-GRBL false-positive is observation-only — verify by attempting to connect to a non-laser USB-serial device and observing connect fails rather than producing a misleading "Connected" UI. If Falcon connect regresses, revert. Code shipped 2026-05-04 in **`5b72000`**. |
| T1-4 | `emergencyStop` should also disconnect (close-out) | Shipped pre-session in `86cbe02` (`fix(grbl): emergencyStop severs the command path`); formalized 2026-05-04. `GrblController.emergencyStop()` at `src/controllers/grbl/GrblController.ts:681` sends `REALTIME_RESET` (0x18), aborts the job, emits progress, then calls `void this.disconnect().catch(...)` to sever the command path so no subsequent IPC or user click can reach the machine. The catch swallows disconnect-failed errors (port may already be closed). UI button at `ConnectionPanelMain.tsx:2266-2280` shows the user-facing alert ("The machine was reset and the connection was closed. Reconnect when it is safe to continue."), best-effort calls `machineService.disconnect()` to keep service-side state consistent, and surfaces "⚠ EMERGENCY STOP — disconnected. Reconnect when safe." in the messages console. Pinned by `tests/controller-stop-safety.test.ts` (19 contracts including emergencyStop sends 0x18, aborts job state, closes port, no auto-unlock $X, sendCommand throws "Not connected" after emergencyStop). Closed 2026-05-04. | n/a — shipped pre-session, no missing follow-ups |
| T1-2 | Controller-layer bounds + fresh-status recheck (close-out) | Shipped pre-session in `3d3dfdd` (`fix(grbl): fresh-status + bounds recheck before accepting job`); formalized 2026-05-04. `GrblController.sendJob` runs `_queryFreshStatus()` (`src/controllers/grbl/GrblController.ts:434`) before accepting any job — refuses with `Cannot start job — machine is "<status>"` if status isn't `idle`. `_checkJobBounds(jobLines)` (line 443) scans the first N lines for absolute-mode `G0/G1 X/Y` values exceeding `_bedWidth`/`_bedHeight` from `$130`/`$131` and refuses with a structured error. Relative-mode (G91) deltas are intentionally skipped — they can't be bounds-checked without execution simulation (separate concern, T1-44). Pinned by `tests/controller-bounds-recheck.test.ts` (13 contracts) and `tests/controller-fresh-status-recheck.test.ts` (7 contracts). Closed 2026-05-04. | n/a — shipped pre-session, no missing follow-ups |
| T1-42 | Frame bounds confirmation uses buildFrameCorners — **code shipped, hardware verification needed** | Audit 2D Finding 2D-07 + 2D-09. ConnectionPanelMain's `workFrame` useMemo computed bounds via `computeGcodeOffset` applied to raw scene bounds — it captured the absolute / current / saved-origin offset shift but NOT the front-origin Y-flip (or the now-shipped T1-40 right-origin X-flip). The displayed bounds and the bed-bounds confirmation diverged from what `buildFrameCorners` actually traces. On front-origin machines (most consumer diodes), the warning could say "inside bed" while the actual frame motion went off-bed — exactly opposite of the safety promise. **Shipped:** replaced the `workFrame` useMemo with `frameMachineBounds`, computed by reducing `buildFrameCorners(sceneBounds, transformOpts)` to its bounding box. The transform options pass both `bedHeightMm` and `bedWidthMm` (T1-40 right-origin support), so the displayed bounds match the actual machine-space frame motion across all four origin corners. Single source of truth — frame motion and bounds confirmation now read the same corners. **Hardening per audit Finding 2D-09:** off-bed bounds hard-block via `showAlert` instead of warn-and-continue. The previous behavior allowed the user to "frame anyway" with off-bed motion, risking limit-switch contact or laser-dot marks outside the workpiece. Off-bed now returns `false` without offering an override; the user must move the design or change start mode. Coverage-only warning (frame > 90% of bed) stays as `showConfirm` — that's a quality concern, not a safety failure. Pinned by `tests/frame-confirm-uses-machine-corners.test.ts` (17 contracts): behavioral pin showing front-origin Y-flip applied to derived bounds + diverging from pre-T1-42 raw-scene-bounds shape; source-level pin showing `workFrame` removed, `frameMachineBounds` calls `buildFrameCorners` with `bedWidthMm`, `confirmFrameBounds` reads `frameMachineBounds.minX/maxY`, off-bed path uses `showAlert` + returns false, coverage warning still uses `showConfirm`. **Hardware verification needed — Falcon A1 Pro front-origin burn test.** Test sequence: (1) Place a design that fits inside the bed; click Frame — verify it traces correctly. (2) Place a design that extends past the bed edge in machine space (use front-origin's Y-flip-aware position); click Frame — verify the modal alert lists the off-bed reason and frame motion does NOT execute. (3) Verify the coverage-only warning still appears for >90% bed coverage and lets the user proceed. If off-bed motion DOES still execute, revert. Code shipped 2026-05-04 in **`3ef9f84`**. |
| T1-41 | Saved-origin verification — **code shipped, hardware verification needed** | Audit 2D Critical 1: saved-origin mode anchored jobs to GRBL's G54 work coordinate, but no code verified G54 hadn't drifted between Set Origin and job start. Failure paths: reconnect's `applyWcsNormalization` zeros G54; user types `G10` or `G92` in the console; custom-start template contains a mode-flip; firmware power loss between sessions. In every case a saved-origin job emits design-local coordinates against an unintended G54 — burn lands in the wrong place. **Shipped (minimum viable):** (1) `GrblController.requestWorkOffsets(timeoutMs?)` queries `$#` and resolves with the parsed G54 or null on timeout (default 1s); `LaserController` interface gains `requestWorkOffsets?` so non-GRBL controllers can adopt later. (2) `MachineService` gains `setSavedOriginG54Snapshot` / `getSavedOriginG54Snapshot` / `requestWorkOffsets` wrappers. (3) `App.tsx`'s `handleSaveOrigin` calls `requestWorkOffsets` immediately after the Set Origin G10 is acked and snapshots the result; if the controller doesn't respond, alerts the user that saved-origin jobs will be blocked until they Set Origin again. (4) `ConnectionPanelMain.handleStartJob` runs verify before preflight when `startMode === 'savedOrigin'`: queries fresh G54, compares via `verifySavedOriginG54`, blocks with appendMessage + showAlert on mismatch / no-snapshot / no-current. (5) `MachineService.disconnect()` clears the snapshot — firmware loss across power cycles forces a re-Set-Origin. New helper `src/app/savedOriginVerify.ts` exports `verifySavedOriginG54`, `describeSavedOriginDrift`, and `G54_DRIFT_TOLERANCE_MM = 0.01`. **Out of scope (deferred):** frame-time verify (laser-dot frame motion at low power could still mark the wrong area, but start is the primary safety case); proactive invalidation on console G10/G92 commands; proactive invalidation on `applyWcsNormalization` (the controller event-emit hook for "WCS just changed"). All three are filed as future T1-41 follow-ups; the disconnect-clears-snapshot path is the load-bearing protection for the most common failure mode (the Day-2 power-up-and-run scenario the audit calls out). Pinned by `tests/saved-origin-verifies-wcs.test.ts` (21 contracts on the pure helper). Integration paths (App.tsx + ConnectionPanelMain) aren't pinned by a behavioral test in this commit — they'd need a full mocked-controller mount; helper coverage + TypeScript compile are the structural pin. **Hardware verification needed — Falcon A1 Pro front-origin burn test.** Test sequence: Set Origin on the workpiece corner; run a saved-origin job (verify burn lands correctly); type `G10 L2 P1 X10 Y10` in the console; click Start on the same job (verify it BLOCKS with an alert citing G54 drift); Set Origin again (verify the next job runs). If verify fails to block, revert this commit. Code shipped 2026-05-04 in **`49a3c55`**. |
| T1-40 | Right-origin X mirror support — **code shipped, hardware verification needed** | Audit 2D Critical 3: `MachineOriginCorner` type allows `'front-right' \| 'rear-right'` and the WelcomeWizard exposes them as selectable, but `applyMachineTransform` never mirrored X — gcode for right-origin profiles came out as if for left-origin, silently mirrored. **Shipped Stage 2 (the actual fix, not the disable-band-aid):** `MachineTransformOptions.bedWidthMm?` added (optional for compat with all left-origin call sites; required at runtime when `originCorner` is right-origin). New `useRightOriginXFlip(originCorner)` helper export. `applyMachineTransform` and `transformPointToMachine` compute `flipX = useRightOriginXFlip(...)`, `flipReferenceX = bedWidthMm`, and apply `machineX = bedWidthMm - canvasX + offsetX` symmetric to the front-origin Y mirror that's been in the code for years. Both entry points throw `Error('… requires bedWidthMm > 0 for X mirroring')` if a right-origin caller passes no `bedWidthMm`. Production callers updated to pass `bedWidthMm`: `PipelineService` (already had it in scope), `ConnectionPanelMain.handleFrameSafe` and `handleFrameDot` transformOpts. Pinned by `tests/right-origin-x-flip.test.ts` (14 contracts: helper classification for all four corners, front-right + rear-right transformPointToMachine math with computed expectations, front-left + rear-left baseline unchanged, applyMachineTransform front-right Plan transformation, throw-when-bedWidthMm-missing for both entry points). **Out of scope:** SceneRenderer overlay X-mirror, preflight off-bed message wording for right-origin. Transform stack is correct; UI overlay polish is a separate commit if/when desired. **Hardware verification needed — Falcon A1 Pro front-origin burn test.** The Falcon is front-LEFT; this burn test verifies the left-origin path is unchanged after the refactor. Right-origin verification ideally happens on a real right-origin machine; absent that, the math + spec match is the proof. If left-origin output regresses, revert this commit. Code shipped 2026-05-04 in **`18b1755`**. |
| T1-28 | Autofocus timeout/error must trigger safety-off — **code shipped, hardware verification needed** | Audit 1C (2026-04-25, ChatGPT). `runAutoFocus` set up listeners, sent `$HZ1` (Falcon autofocus), and waited for the controller to leave Idle then return to Idle. On timeout or alarm `cleanup()` only removed listeners and cleared the timer — **no safety command was issued** (no M5, no soft reset, no motion-stop). The Promise rejected and control returned to the caller, leaving the head potentially pressing into the workpiece while the laser may still be in M3/M4 modal state from a previous operation. **Shipped:** both timeout and alarm paths in `src/controllers/grbl/GrblController.ts:runAutoFocus` now invoke `void this.safetyOff().catch(...)` before rejecting; reject messages append "— safety-off attempted" so the caller and any UI surfacing the error can see the safety action was attempted. The `safetyOff().catch(...)` shape is intentional: a transport failure during safety-off must not mask the original timeout / alarm cause. Both branches share `cleanup()` first (clears `setTimeout` and removes the state-change listener). T1-22's two-stage `safetyOff` (M5 critical-write → soft-reset fallback) is the appropriate response for autofocus specifically — a hung Z-probe is exactly the case where regular M5 may not preempt the buffered command. Pinned by `tests/autofocus-timeout-issues-safety-off.test.ts` (14 contracts: timeout fires safetyOff exactly once + reject preserves "timed out" text + "safety-off attempted" marker; alarm-branch source-pin asserts `cleanup()` before `safetyOff().catch` with marker text + T1-28 marker present in runAutoFocus comments; safetyOff-itself-throws preserves original timeout error and does not chain the secondary failure). The alarm branch is source-pinned rather than behaviorally tested because MockSerialPort's `<Alarm:N|...>` injection is flaky against the controller's status-parser (which only accepts certain shapes depending on poll state); the source pin is tight enough that a regression must remove the cleanup → safetyOff().catch → reject sequence to break it. **Out of scope:** MachineService.autoFocus wrapping try/catch/finally (the spec's defense-in-depth second layer) — the controller-level fix delivers the headline contract; service-level wrap is filed as a follow-up if hardware testing surfaces a missed path. **Hardware verification needed — Falcon A1 Pro front-origin burn test.** Test sequence: trigger autofocus with the Z-stage obstructed (or disconnect the probe pin) so the firmware times out → verify M5 fires (laser stays off) and the timeout error surfaces in the UI; if the laser remains in M3/M4 modal state after timeout, revert. Master checklist `[ ]` until verified. Code shipped 2026-05-04 in **`77203e0`**. |
| T1-39 | Frame skips first relative move on front-origin machines — **code shipped, hardware verification needed** | Audit 2D Critical 4: `buildFrameGcode`'s current-mode branch initialized `prev = corners[0]` and started the loop at `i = 1`, silently skipping the first relative move from the head's current position to `corners[0]`. On front-origin diode lasers (Falcon, SCULPFUN, Atomstack — most consumer machines), `corners[0]` in machine space is `(0, jobHeight)` after the Y-flip, so the actual job's first move is `(0, jobHeight)`. The old frame skipped this and traced the design vertically shifted by `jobHeight` — the frame's whole purpose (verify burn area before committing) was defeated. On rear-origin machines `corners[0]` typically maps to `(0, 0)` so the missing move was zero-length and the bug was invisible — that's why this hasn't been caught. **Shipped:** loop now starts at `i = 0` with `prev = (0, 0)`, so the first emitted delta is `corners[0] - (0, 0)` matching the burn's first move. Plus a final negated-`endPos` G0 returns the head to the physical starting position so burn-after-frame is not doubly offset (without it, the head would end at `corners[0]` and the next job's first move would shift the burn by another `corners[0]`). The crosshair branch's existing math is now actually correct — it always assumed head was at `corners[0]` for the right-tip delta calculation; pre-fix the head was at `(0, 0)` so the crosshair was off by the same amount, just invisibly. The eps guard skips the return-to-origin on rear-origin machines where `corners[0]` is already `(0, 0)`. Pinned by `tests/frame-current-mode-emits-first-move.test.ts` (13 contracts) plus updated golden tests in `frame-gcode-pure.test.ts` (5/5) and `frame-gcode-crosshair.test.ts` (15/15). **Hardware verification needed — Falcon A1 Pro front-origin burn test.** Frame a 100×50 design at canvas (10, 20) on the Falcon, then burn the same design without moving the workpiece; the burn must land exactly inside the framed rectangle. If the burn is vertically offset, revert. Master checklist `[ ]` until verified. Code shipped 2026-05-04 in **`df5763e`**. |
| T1-64 | Pause / Resume / Stop catch blocks must surface errors | Audit 4C P0 fix #2. Both `handlePauseResume` and `handleStop` catch blocks previously logged to console only — silent failure on safety-critical machine controls. A user clicking Stop saw nothing if the call failed; the job potentially kept running with no UI signal. **Shipped:** rewrote both catches. `handlePauseResume` always calls `appendMessage('⚠ Pause/Resume command not accepted: ...')` for the message log; pause-failure additionally fires `showAlert('Pause failed', '... job may still be running ...')` because the user assumes the laser stopped when it may not have. Resume-failure stays appendMessage-only (mild case — user can retry). `handleStop` always shows BOTH appendMessage AND `showAlert('Stop failed', '... physical E-stop / disconnect ...')` because stop is the most safety-critical control; the user is forced to acknowledge before doing anything else. The previous code silently cleared `isPaused` inside try; new catch leaves it alone so the UI's pause indication reflects the last known machine state. `console.warn` preserved on both paths for support. Pinned by `tests/pause-resume-stop-surface-errors.test.ts` (22 source-level contracts via `ConnectionPanelMain.tsx` slice: try/catch shape, appendMessage text, showAlert title + text, held/!held gating, console.warn preserved, dep arrays updated, isPaused not cleared in catch). Source-level pin because ConnectionPanelMain mounts the full machine-service / controller / preflight tree — disproportionately heavy for a contract that targets the catch block's text + side-effect shape. **Hardware verification: not required (UI plumbing for previously-swallowed async failures, no g-code or machine-state change).** Shipped 2026-05-04 in **`cb601c1`**. |
| T1-49 | `MachineService.connectRealLaser` cleanup on partial-failure | Audit 3B P0 fix #4 + section 6.3/7. Previously `portRef.current = ws` was assigned BEFORE `requestAndOpen` and `controller.connect`, so a thrown `requestAndOpen` (permission denied, port busy, writer/reader failure) or thrown `controller.connect` (handshake timeout, wrong-device, baud mismatch) left the half-open WebSerialPort pinned on portRef. Subsequent `portRef.current != null` checks treated the app as connected; reconnection often failed until app reload. **Shipped:** rewrote `connectRealLaser` with the audit's recommended try/catch shape — `portRef.current = ws` is assigned only after `controller.connect` succeeds. On any failure path the half-open port is closed (sync today, async after T2-31), `portRef.current` is nulled if it ended up pointing at the failed port, and `controller.disconnect()` is best-effort called to release any partial-connect state. The original error rethrows so the UI's catch surfaces it. Inline comment cites T1-49 + the four failure-mode shapes. Pinned by `tests/connect-cleanup-on-partial-failure.test.ts` (16 contracts via WebSerialPort prototype stubbing — `isSupported` true, `requestAndOpen` per-test impl, `close` records calls; covers requestAndOpen-throws, connect-throws, happy-path, unsupported-browser early-throw). **Hardware verification: not required (UI plumbing for previously-swallowed async failures, no g-code or machine-state change).** Shipped 2026-05-04 in **`7cce3f0`**. |
| T1-50 | Connect button mutex / abortable connect | **Part A shipped 2026-05-04 in `d4e3e35`** — UI mutex on Connect-via-USB and Use-Simulator buttons. `ConnectionPanelMain` gains a `connecting` `useState`; `connectRealLaser` and `connectSimulator` short-circuit on `connecting === true`, set the flag at entry, and clear it in `finally`. Both simulator and real-laser connect paths share the mutex because both race on `controllerRef.current.connect(port)`. `ConnectWizard` accepts a new optional `connecting?: boolean` prop and renders both buttons as `disabled` with `'Connecting…'` label. Pinned by `tests/connect-button-mutex.test.tsx` (18 behavioral contracts). **Part B interface stub shipped 2026-05-04 in `1f135d9`** — `MachineService.connectRealLaser` now accepts an optional `signal?: AbortSignal`. `signal?.throwIfAborted()` runs at entry plus after each await; an aborted signal causes the next await to throw, routing through T1-49's cleanup path (port closed, portRef nulled, controller.disconnect best-effort called). Pinned by `tests/connect-abort-signal.test.ts` (21 contracts: pre-aborted early throw, abort-mid-open and abort-mid-connect cleanup, no-signal production-path equivalence, unaborted-signal no-effect). Today no production caller passes a signal — pure type-system plumbing, zero behavioral change in production paths. **Hardware verification: not required (UI plumbing for previously-swallowed async failures, no g-code or machine-state change).** **Future work (T2-32, T2-33):** propagate the signal into `WebSerialPort.requestAndOpen` and `GrblController.connect` so an in-flight open or handshake can be cancelled mid-air; wire ExecutionCoordinator.safeDisconnect to abort the connect when status is `connecting` instead of early-returning. | Full abort propagation through WebSerialPort and GrblController is T2-32/T2-33; ExecutionCoordinator.safeDisconnect's connecting-state behavior is also future work. |
| T1-17 | Image import freezes the app | **Pass 1 shipped 2026-04-30 in `023a341`** — grayscale loop offloaded to `src/workers/ImagePrepWorker.ts`. **Pass 2 shipped 2026-04-30 in `0632b2b`** — dither cache key uses FNV-1a 32-bit content hash via `buildDitherCacheKey`. **Pass 3 shipped 2026-04-30 in `b8f3dfb`** — `importImageUnified` identity stable across scene mutations via sceneRef pattern. **Pass 4a shipped 2026-04-30 in `05ce7b86e629464c4117c94cb8b6b041e613244f`** — worker-side image processing primitives added (`processImage`, `processImageMainThread`); ImagePrepWorker extended with `process` request kind. Infrastructure-only, no call sites changed. Pinned by `tests/image-processing-worker-equivalence.test.ts` (12/12). **Pass 4b shipped 2026-05-05 in `32fe961`** — `ImageGeometry.processedData` + `processedSettings` fields added; `JobCompiler.compileGeometry` raster branch reuses the cached buffer when fingerprint matches the layer's brightness/contrast/gamma/invert (skipping the four legacy `ImageProcessing.ts` loops). Defensive length check; missing-cache fallback unchanged. Pinned by `tests/jobcompiler-uses-processed-data-pass4b.test.ts` (19 contracts). Decoupled from existing `adjustedData` so the dither-preview path is unaffected. **Pass 4c shipped 2026-05-05 in `7bec4bc`** — `PropertiesPanel.commitImageSettings` fires new `warmProcessedImageCache` helper at `src/ui/hooks/useImageCacheWarmer.ts` after `onSceneCommit`. Helper calls `processImage(...)` (worker or main-thread fallback) and writes the result to `geom.processedData` + `geom.processedSettings` via the live channel (no history bump) — but only after re-checking the fingerprint to discard stale writes when the user drags faster than the worker resolves. Fire-and-forget; errors logged + swallowed (legacy fallback keeps JobCompiler working). Pinned by `tests/warm-processed-image-cache-pass4c.test.ts` (19 contracts). All four passes shipped; hardware verification of UI responsiveness on a real 4-12MP phone import is the remaining gate. | Hardware verification of UI responsiveness on real 4-12MP phone import. |

### 鉁?Confirmed open

| Ticket | What | Estimate |
|---|---|---|
| T1-23 | Pause must emit explicit M5 (or document firmware proof) | needs modal-state subsystem |
| T1-25 | Reconnect safe-state handshake | 1-2 sessions |

### Tier 1 not yet re-verified individually

T1-1 through T1-67 minus the items above (i.e., T1-2, T1-3, T1-4, T1-26 through T1-67 excluding T1-59, T1-65, T1-66, T1-67). Per the prior session's batch audit, ~17/21 spot-checked Tier 1 items were already shipped. **Estimated additional shipped: ~50.** Verify individually before claiming any specific T1-X in this range as shipped.

---

## Tier 2 鈥?9 fully shipped, 3 partial, 115 open

### 鉁?Shipped (9)

| Ticket | What | Evidence | Hash |
|---|---|---|---|
| T2-7 | Real controller abstraction | `src/controllers/ControllerInterface.ts`, `src/controllers/ControllerRegistry.ts`, `src/controllers/grbl/` exist as a real abstraction | pre-session |
| T2-8 | Split Preflight into rule modules | `src/core/preflight/rules/` has 8 separate rule modules: `LayerSettingsPreflight`, `MachinePreflight`, `MachineStatePreflight`, `OptimizationPreflight`, `OutputBoundsPreflight`, `RasterPreflight`, `ScenePreflight`, `TemplatePreflight`, plus `sharedHelpers` | pre-session |
| T2-22 | Standardized test runner | `scripts/run-tests.mjs` runs each test file in its own Node process. **Note:** auto-discovery part may not be done 鈥?manual list still in file. Treating as shipped because the consistent-reporter requirement is met. | pre-session |
| T2-67 | `failed_to_start` outcome enum + 8-distinct-outcomes finalization | `src/ui/components/JobOutcomeDialog.tsx`; `failed_to_start` handled in `JobLogViewer.tsx:66,89` | `a1bb80f` |
| T2-76 | `commitSceneTransaction` single mutation path | `src/ui/scene/SceneTransaction.ts` (306 lines); `tests/scene-transaction-unified.test.ts` (83/83); `tests/scene-transaction-app-wired.test.ts` | pre-session |
| T2-78 | History entries with action metadata | `selectionBefore/After`, `invalidatesOutput` defined in `SceneTransaction.ts:147`, used in `HistoryManager.ts:64` | pre-session |
| T2-79 | Selection restore on undo/redo | `HistoryEntry.selectionBefore/After`; `tests/selection-restore-on-history.test.ts` | pre-session |
| T2-80 | History coalescing 鈥?slider preview/commit | Text-property sliders use preview/commit at `PropertiesPanel.tsx:847,902,956`; T2-80 markers at `PropertiesPanel.tsx:53,428,440` | `e6874af` |
| T2-105 | Hidden source maps in production | `vite.config.ts:9-12` `sourcemap: 'hidden'` (closed T1-83 stopgap) | `b6a56ed` |
| T2-13 | Fault-injecting test transport (FaultInjectingSerialPort, harness scope) | Audit 1F P0: pre-T2-13 the only test transport was MockSerialPort (happy-path only) — write succeeds synchronously, oks come back, status query returns live position. None of the failure modes the safety paths must handle (`safetyOff` fallback, autofocus timeout, reconnect handshake, USB-pull mid-burn, buffer-full backpressure) could be deterministically simulated. Tests for those paths could verify "M5 was attempted," not "M5 failure was handled." **Shipped:** new `tests/helpers/FaultInjectingSerialPort.ts` exports `class FaultInjectingSerialPort implements SerialPortLike` composing a private `MockSerialPort` and intercepting both write and read paths. **10 fault modes:** `normal` (passthrough), `reject-write-after-return` (write succeeds, `onError` fires async after `afterMs`), `drop-write` (data swallowed pre-mock), `partial-write` (first N bytes only), `close-mid-write` (truncated write then port close), `omit-ok` (next ok filtered before user callback), `delay-ok` (next ok delayed by `ms`), `fake-ok` (line never reaches mock; ok fabricated), `stale-status` (first `<…>` cached and replayed on subsequent `?`), `buffer-full` (`writeByte`/`writeByteCritical` throw). Most modes accept `matching: RegExp` for per-line scoping. **Audit trail:** every decision appended to `injectionLog`. `setStaleStatusReply()` lets tests pre-seed the cached status. `setFault(mode)` clears the stale cache when switching modes so tests don't leak. Lives under `tests/helpers/` (T2-22 EXCLUDED_DIRS). **Out of scope:** subclassing or replacing MockSerialPort (composition only); per-test migration of T1-22 / T1-25 / T1-28 / T1-29 to use the harness — filed as dependent ticket work. Pinned by `tests/fault-injecting-serial-port.test.ts` (52 contracts covering each fault mode independently — passthrough, drop-write with/without matching, partial-write truncation length, close-mid-write fires onClose + flips isOpen, omit-ok at user-callback boundary including matching scope, delay-ok measured timing ≥configured ms, fake-ok bypasses mock entirely, stale-status caches first reply and replays + pre-seeded variant, buffer-full sync throw + async reject, reject-write-after-return non-throwing sync return + delayed onError on both write and writeCritical, setFault clears stale cache, injectionLog append-only across mode switches, source-level pins). **Hardware verification: not required** (test infrastructure helper; no g-code, no machine-state, no controller wiring). | `191d19b` |
| T2-14 | Non-removable G-code safety wrapper around custom templates | Audit 2A Priority 3: pre-T2-14 a `gcodeHeaderTemplate` REPLACED `defaultBlock` entirely — a template author who omitted G21 / G90/G91 / M5 silently produced unsafe job start (firmware default units could be inches; distance mode could be left-over G91; laser modal state could be M3/M4 from a prior job). Validator (T2-5) catches missing M5 in footer, T1-26 appends M5 at send time, but the structural guarantee was missing. **Shipped:** `BaseGCodeStrategy.encodeHeader` + `encodeFooter` rewritten so safety baseline is non-removable. Header emits `G21` + `G90`-or-`G91` + `M5 S0` first; template + customStartGcode + T1-43 reassertion append after. Footer emits template + customEndGcode first; safety footer (`M5 S0` + return-to-start (relative) or `G0` returnPosition (absolute) + `G90` restore (relative) + `M2`) always last. Each safety line carries a `; T2-14 safety baseline:` comment for greppability. Duplicate G21 / G90 / M5 from older user templates is idempotent. All 11 E2E snapshots regenerated; existing template tests updated. Pinned by `tests/output-safety-wrapper-non-removable.test.ts` (17 contracts: canonical header order; template that omits G21/G90/M5 → safety still emitted first; malicious M3 in template → safety M5 still fires BEFORE it; customEnd content + safety M5 + M2 with M2 strictly last; footer template that omits M5/M2 → safety still appended; relative-mode includes G91 + G90 restore; source-level pins). Regression: customstart-mode-reassertion (11/11), gcode-relative-mode (19/19), gcode-relative-return-template (8/8), gcode-templates-safety (15/15), preflight-template-validation (5/5), pipeline (100/100), all 11 e2e snapshot tests green. **Hardware verification: not required** (idempotent safety lines; existing validator + T1-26 cover the runtime risk surface). Shipped 2026-05-05 in **`94ebfa7`**. |
| T2-17 | AbortSignal + progress callback through compile pipeline (phase-boundary MVP) | Audit 2E Priority 3: pre-T2-17 compile was async-shaped but completely uncancellable internally — once started, every loop ran to completion. Combined with renderer-thread execution, the user's only options during a runaway compile were to wait or kill the app. **Shipped (focused MVP — phase-boundary checkpoints):** new `CompileOptions { signal?, onProgress? }` and `CompileProgress { phase, fraction, overallFraction, detail? }` exported from PipelineService. `compileGcode` accepts `opts` as 9th positional param (defaults to `{}` so existing callers unaffected). Five phase boundaries instrumented: text-expansion (10%), compile-job (30%), plan (30%), transform (10%), output (20%). Each calls `throwIfAborted(signal)` + `reportPhase(opts, phase, fraction, detail?)`. AbortError shape: `new DOMException('Compile cancelled', 'AbortError')` for cross-runtime compatibility. Pinned by `tests/compile-cancellable.test.ts` (26 contracts: backward-compat with no opts; progress fires at all 5 phases with monotonic non-decreasing overallFraction starting at 0 ending at 1; pre-aborted throws AbortError immediately; mid-flight abort (after text-expansion fires) throws AbortError; declared phases only; budgets sum to 1.0; source-level pins). Regression: pipeline (100/100), determinism-gate (8/8). **Out of scope (T2-17-followup):** deep-loop instrumentation inside compileJob / optimizePlan / BaseGCodeStrategy.generate; UI integration (Cancel button + progress bar in useCompileManager); worker offload pairing. **Hardware verification: not required** (UX layer; cancellation prevents work, doesn't produce incorrect output). Pairs with T1-45 (complexity gate prevents worst cases; T2-17 is fallback). Shipped 2026-05-05 in **`2a9c29c`**. |
| T2-41 | `SafetyActionResult` typed return for safety methods (type + 1 method migrated) | Audit 3D Critical 1+6+P0: every safety method (pause/resume/stop/emergencyStop) returned void — caller couldn't tell whether command was sent, port was open, motion confirmed stopped, position invalidated, rehome/reconnect/inspection required. Lost information for audit trail / post-mortem. **Shipped (focused MVP):** new `src/app/SafetyActionResult.ts` with `SafetyAction` 8-kind union (laserOff/pause/resume/abortJob/emergencyStop/disconnectSafe/beginTestFire/endTestFire), `MotionState`, `LaserOffState`, `Tristate` (`boolean \| 'unknown'`), and `SafetyActionResult` interface (action, accepted, motionState, laserState, positionTrusted, requiresRehome, requiresReconnect, requiresInspection, message?, timestamp). Builders `makeSoftResetStopResult` (GRBL soft-reset shape: motion stopped, laser commandedOff (no per-byte ack), position lost, requiresRehome=true) and `makeNotConnectedResult` (unknown-tristates + requiresReconnect=true). `MachineService.stopAndEnsureLaserOff` migrated from `Promise<void>` to `Promise<SafetyActionResult>`; sole caller (ConnectionPanelMain.handleStop) was awaiting void, so `await Promise<SafetyActionResult>` discards the value identically — no callsite changes. Pinned by `tests/safety-action-result.test.ts` (41 contracts: helper field correctness; custom message override; tristate fields on notConnected; action discriminator across all 8 kinds; MachineService.stopAndEnsureLaserOff returns soft-reset shape; pre-T2-41 void-await callers still work; source-level pins). **Out of scope (T2-41-followup):** migrating pause/resume/disconnect/emergencyStop on MachineService + LaserController interface — each is an interface change touching every stub. **Hardware verification: not required** (additive type + one method migration). T2-46 (user-facing messages) consumes this type and lands after broader migration. Shipped 2026-05-05 in **`6fa502a`**. |
| T2-97 | Entitlement checks must never block safety controls | Audit 5A Required Priority 9 + User-trust failure mode "Losing access while job running": no specific defect today, but no explicit guarantee either — future code could write `if (!hasPro()) return;` somewhere that disables stop/pause/disconnect mid-burn. **Shipped:** Part 1 — new `docs/SAFETY_GUARANTEES.md` lists 6 methods that must complete regardless of entitlement (`MachineService.stopAndEnsureLaserOff/pause/resume/disconnect`, `GrblController.emergencyStop/safetyOff`) plus connection wizard, $X unlock, job log access; reserves Allow-list section for future exceptions. Part 2 — `tests/safety-controls-bypass-entitlement.test.ts` (27 contracts): behavioral checks (each safety method completes without throwing under two distinct "no Pro" states; controller-side counter increments exactly once) + static guard scanning 5 safety files (`MachineService.ts`, `ExecutionCoordinator.ts`, `GrblController.ts`, `SerialPort.ts`, `WebSerialPort.ts`) for forbidden patterns (`import 'entitlements'`, `requireFeature(`, `assertFeature(`, `canUseFeature(`, `hasPro(`); comment-stripping precedes pattern matching so legitimate doc-comments don't false-positive; violations print `file:line`. Doc pin verifies SAFETY_GUARANTEES.md exists, cites audit 5A, names each guaranteed method, has Allow-list section. T1-88 already removed the only pre-existing entitlement consumer in safety code; T2-97 makes the guarantee permanent. **Hardware verification: not required**. Shipped 2026-05-05 in **`9a62d90`**. |
| T2-93 | License status enum — first-class discriminated union (refines T1-80) | Audit 5A Required Priority 8: T1-80 shipped a stopgap (flat-string `status` + sibling `lastVerifiedAt`/`graceUntil`/`lastError` fields). Callers had to read multiple fields to render a status; T2-93 promotes the type to a discriminated union so each kind carries its own metadata and `switch (status.kind)` covers every UI case exhaustively. **Shipped (additive, back-compat preserved):** new `src/entitlements/LicenseStatus.ts` with `LicenseStatusDetail` union (8 kinds: `free | verified | offline_grace | expired | verification_failed | revoked | developer | tester`), helpers `statusAllowsPro(s)`, `statusUserMessage(s) → { tone, title, message, actions }`, `buildStatusDetail(args)` bridge. New optional `EntitlementState.statusDetail?: LicenseStatusDetail` field; auto-populated at `setState` boundary via private `deriveStatusDetail` helper — no call-site changes. Existing flat `status: string` field preserved for backward-compat. Pinned by `tests/license-status-machine.test.ts` (40 contracts: statusAllowsPro per kind; statusUserMessage per kind with tone + action set; buildStatusDetail populates kind-specific metadata; service integration `statusDetail.kind === status` + per-kind metadata matches state's flat siblings; source-level pins). **Out of scope:** dropping the legacy flat `status` once UI consumers migrate to `statusDetail.kind` — filed as T2-93-followup. Regression: license-status-states (37/37 T1-80 contracts unchanged) + entitlement-api-split (11/11). **Hardware verification: not required** (state plumbing). Shipped 2026-05-05 in **`e18e204`**. |
| T2-83 | Block undo/redo while job running | Audit 4E Critical risk + Required Priority 13: pre-T2-83 the user could press Ctrl+Z mid-burn and silently desync the visible scene from what the controller was actually executing. Active job context was already pinned at the compile manager (T2-53 work) but nothing prevented the scene-level handler from running. **Shipped (Option A — block; Option B separate-undo-lane filed under T2-53):** `App.handleUndo` and `App.handleRedo` early-return before any history pop when `grbl.isJobRunning === true`; surfaces `showAlert('Undo blocked' / 'Redo blocked', 'A job is running. Stop the job before editing the design.')`. Guard runs BEFORE `undoEntry()` / `redoEntry()` so history state is not popped on a refused action — the next valid undo after job stop still has the same stack. Dep arrays widened to include `grbl.isJobRunning` + `showAlert`. Pinned by `tests/undo-during-job.test.ts` (11 source-level contracts: T2-83 marker; both handlers check isJobRunning; both invoke showAlert with correct title; both guards run BEFORE the history pop; dep arrays widened; recovery message names the action). **Hardware verification: not required** (UI guard; refusing to mutate visible scene during stream is intrinsically safe). Shipped 2026-05-05 in **`a16f7d1`**. |
| T2-88 | Hash-derived dirty state — helper (call-site migration deferred) | Audit 4F Section 3 Finding 4 + Critical 4: pre-T2-88 the dirty flag was a manually-set boolean toggled at every mutation site. T1-73, T1-74, T1-75 each fixed a forgotten-toggle bug in a different mutation path; the underlying defect class is structural — any new mutation site is one forgotten line away from the same bug. **Shipped (focused MVP):** new `src/app/sceneDirtyHash.ts` exports `hashSceneForPersistence(scene)` (FNV-1a 32-bit hex; non-cryptographic — corruption-check; sha256 under T3-77), `isDirty(scene, lastSavedHash)`, `hasMeaningfulContent(scene)`. Canonical form via recursive key-sort (stable JSON regardless of iteration order); excludes `_bounds`/`_worldTransform` ephemeral caches and `metadata.modified` timestamp (would prevent save from clearing dirty). Image buffers fingerprinted by length + first byte. Pinned by `tests/dirty-derivation.test.ts` (21 contracts including the audit's headline round-trip case: change A then inverse-A → dirty becomes false, which manual flagging cannot do). **Out of scope (T2-88-followup):** migration of the 17 `sceneIsDirtyRef.current = true/false` caller sites to read `isDirty(scene, lastSavedHash)` instead. Mechanical migration; helper consumable incrementally. **Hardware verification: not required**. Shipped 2026-05-05 in **`bc6f7e0`**. |
| T2-82 | History memory budget — count + bytes limits | Audit 4E Required Priority 11: pre-T2-82 the HistoryManager had only a count limit (`maxSize=100`); 50 entries with 5MB images = 250MB, well over what most browsers tolerate. T2-81 reduced per-entry size by stripping `processedData`; T2-82 adds the actual budget enforcement. **Shipped:** new `src/ui/history/estimateSceneBytes.ts` walks the scene structure (per-object 256B, per-layer 512B, per-segment 64B, per-polygon-point 16B, per-char 32B, image buffer lengths) — cheap and deterministic, cheaper than JSON.stringify on multi-MB rasters. HistoryManager constructor gains `maxBytes` (default 100MB) + index-aligned `_entryBytes: number[]` cached at push time + `totalBytes()` public getter. Eviction loop runs while EITHER count > maxSize OR bytes > maxBytes, with at-least-one-entry retention (stale undo is preferable to losing user work). Eviction emits `console.warn('[HistoryManager] T2-82: evicted N entries (~M MB) ...')`. Pinned by `tests/history-memory-budget.test.ts` (18 contracts: estimateSceneBytes shape; count vs byte limits; over-budget single push retained; warning fires on eviction; totalBytes / reset / clear correctness; source-level pins). Regression: history (99/99), history-entry-metadata (43/43), raster-buffers-out-of-history (18/18), selection-restore-on-history (14/14). **Out of scope:** UI surface for the soft warning (T2-82-followup; depends on T3-68 transition-log integration). **Hardware verification: not required**. Shipped 2026-05-05 in **`08b65f9`**. |
| T2-81 | Raster buffers stored outside history (processedData scope) | Audit 4E Critical failure 8 + Required Priority 10: pre-T2-81 a 4MP image with Pass 4c populating `processedData` on every slider commit could balloon history memory by 100× the image size with max history = 100. **Shipped (focused MVP):** new `src/ui/history/stripRegenerableCaches.ts` exports `stripRegenerableImageCaches(scene)` which rewrites image-geometry objects to omit T1-17 Pass 4b/4c's `processedData` (post-pipeline grayscale buffer) + `processedSettings` (cache fingerprint). Allocation-free no-op when no image has the cache. `HistoryManager.buildEntry` routes through the helper at push + reset; `clear` resets the new guard ref. **No-op-guard fix:** new private `_lastPushedScene: Scene | null` tracks the PRE-strip reference; the existing identity-comparison guard (`stack[cursor].scene === scene`) was broken post-strip because the stored scene became a different object — `_lastPushedScene` preserves identical no-op semantics. Pinned by `tests/raster-buffers-out-of-history.test.ts` (18 contracts: strip removes processedData + processedSettings; preserves grayscaleData ref + adjustedData ref + ditherMode; no-op identity preserved; HistoryManager.push stores stripped scene; no-op-commit guard still works; distinct scenes get distinct entries; 50 image-cache pushes leave every entry stripped; reset path strips too; source-level pins). Memory impact: 100 slider commits on a 4MP image previously cost ~400MB of processedData; now 0. **Out of scope (T2-81-followup):** moving grayscaleData + adjustedData to ImageStore-backed reference + ID-keyed adjusted-buffer cache (audit's full proposal); pairs with T2-72. The MVP delivers the dominant per-commit memory win. Regression: history (99/99), history-entry-metadata (43/43), selection-restore-on-history (14/14), active-layer-history-consistent (8/8), undo-redo-invalidation (26/26), text-property-edits-undoable (23/23). **Hardware verification: not required**. Shipped 2026-05-05 in **`d7da44a`**. |
| T2-85 | Explicit `JobFingerprint` type (refines T2-51) | Audit 4F Critical 2 + Priority 3: pre-T2-85 ValidatedJobTicket compared 4 fields (sceneHash + profileHash + gcodeHash + controllerType); start mode, saved origin, machine capabilities, and compile options could change between compile and start without invalidating the ticket — silent wrong output. **Shipped:** new `src/core/job/JobFingerprint.ts` with `JobFingerprint` 7-field interface (sceneHash, profileHash, materialHash, startMode, savedOriginHash, machineCapabilitiesHash, compileOptionsHash), `buildJobFingerprint(args)`, `fingerprintsEqual`, `fingerprintDiff` (returns changed-field array), `fingerprintMismatchReason` (first changed field + per-field user-facing message). Hashing via FNV-1a hex of sorted-key JSON. Pinned by `tests/job-fingerprint.test.ts` (30 contracts). **Out of scope:** ValidatedJobTicket migration to embed JobFingerprint (T2-85-followup). **Hardware verification: not required**. Shipped 2026-05-05 in **`9e755b7`**. |
| T2-86 | Explicit `FrameState` union type (refines T2-60) | Audit 4F Critical 3 + Priority 4: pre-T2-86 frame state was a single boolean (`hasFramedRef.current`); real states include in-progress / failed-with-reason / valid-for-fingerprint-X / stale-because-Y, none of which a boolean can express. **Shipped (type + helpers; ConnectionPanelMain migration deferred):** new `src/app/FrameState.ts` exports `FrameState` 5-kind discriminated union (none / running / valid / stale / failed), `FrameMode` (safe/dot), `FrameStaleReason` (7 reasons matching T2-60 invalidation triggers), `FrameFailureReason` (7 reasons), 5 builder helpers, `frameAllowsStart(state)` predicate, `frameMatchesFingerprint(state, fp)` predicate (carries the T2-85 JobFingerprint so the canStart rule can reject a frame that no longer matches the current job). Pinned by `tests/frame-state-transitions.test.ts` (40 contracts: each builder; valid → stale preserves previousFingerprint; canStart-style composition demonstrates only `valid + matching fp` gates start; all 7 stale reasons + 7 failure reasons round-trip; source-level pins). **Out of scope:** ConnectionPanelMain migration of `hasFramedRef.current` boolean reads — 17+ call sites; T2-86-followup. **Hardware verification: not required**. Shipped 2026-05-05 in **`b4769f0`**. |
| T2-72 | Embed material preset snapshot per layer (mirror of T2-71) | Audit 4D Critical 3 (material variant) + Required Priority 6: pre-T2-72 a layer kept only `materialPresetId`; if the user updated the preset library after save (e.g. changed power 85→90% after running tests), reloading silently compiled with NEW values. Same defect class as T2-71's profile snapshot. **Shipped:** new `src/core/materials/MaterialPresetSnapshot.ts` exports `MaterialPresetSnapshot` interface (compile-relevant fields + appliedAt timestamp), `buildPresetSnapshot`, `diffPreset` (operations / kerf / leadIn / zOffset / tabs / responseCurve / name; JSON for nested), `checkPresetSnapshot` returning 5-kind union (no-snapshot / no-current-preset / preset-deleted / match / mismatch). `Layer.LaserSettings.materialPresetSnapshot?` optional field. Pinned by `tests/material-preset-snapshot.test.ts` (23 contracts). **Out of scope:** load-time dialog UI + apply-time call site wiring (T2-72-followup — needs UX design + PropertiesPanel changes). **Hardware verification: not required**. Shipped 2026-05-05 in **`7e85ca4`**. |
| T2-117 | Correlation IDs across systems — type + generator + snapshot helpers | Audit 5C Required Priority 14: pre-T2-117 the codebase had per-subsystem IDs (`JobLog.id`, `ValidatedJobTicket.ticketId`) but no top-level session ID, no project ID, no compile ID, no preflight ID, no frame ID, and no support bundle ID. Support workflows could not correlate "job failed at 14:32" with "alarm at 14:31" without manual timeline reconstruction. **Shipped (focused MVP — type + helpers; subsystem propagation deferred):** new `src/diagnostics/CorrelationIds.ts` exports `CorrelationIds` 7-field interface (sessionId / projectId / compileId / preflightId / frameId / jobId / supportBundleId), `CorrelationIdPrefix` literal union, `generateCorrelationId(prefix)` (per-prefix deterministic counter under `LASERFORGE_DETERMINISTIC_IDS=1`; production format `${prefix}_${unixMs}_${6chars}` so eyeballed bundles sort near-chronologically by ID), `emptyCorrelationIds()` (fresh sessionId, others null), `withCorrelationId(ids, field, value)` (immutable single-field update), `snapshotCorrelationIds(ids)` (structural copy for embedding in log entry / error report / JobLog), `isCorrelationId(value)` (regex recogniser for support bundle pretty-printer), `resetCorrelationIdCounters()` (test hook). Pinned by `tests/correlation-ids.test.ts` (63 contracts: deterministic-mode sequencing; per-prefix independent counters; all 7 prefix kinds; emptyCorrelationIds shape; withCorrelationId immutability + sessionId preservation; project-load + compile flows preserve parents; snapshotCorrelationIds value-equal + reference-distinct; isCorrelationId positives across all 7 generated kinds + negatives (random string, core/types `det-000001`, empty string, null, undefined, number, malformed); 7-field declared shape; e2e session→project→compile→preflight→frame→job flow; source-level pins). **Out of scope (T2-117-followup):** threading the snapshot through `reportError`, `JobLog`, crash reports, and `support-bundle/correlation-ids.json`. **Hardware verification: not required** (diagnostic plumbing). Shipped 2026-05-05 in **`941485b`**. |
| T2-115 | Privacy redaction layer for diagnostic exports | Audit 5C Critical failure 9 + Required Priority 10: pre-T2-115 the codebase had no central redaction layer. Support bundles, crash reports, and exported logs were one bug away from leaking license keys, file paths with usernames, emails, IP addresses, or project content into customer-shared artifacts. **Shipped:** new `src/diagnostics/Redaction.ts` exports `RedactionOptions` (7-flag interface), `defaultRedactionOptions()` (license/path/email/IP on; project/gcode/image off — conservative for unknown context), `redactString(text, opts)`, `redactObject<T>(value, opts)` (recursive, immutable — input never mutated), `redactDefault<T>(value)` convenience. Patterns: license UUID-shape (8-4-4-4-12 hex case-insensitive), email, IPv4, file paths (`C:\Users\` + `/Users/` + `/home/` user dirs). **Defence-in-depth contract:** license-key redaction runs unconditionally regardless of `redactLicenseKeys` — bundles can never leak a license. G-code lines preserved when `redactGcode=false` so a user opting to share G-code keeps a useful artifact, but license keys in G-code comments are still redacted. Image-buffer-shaped values (Uint8Array under known image keys like `data`/`pixels`/`grayscaleData`) replaced with `[REDACTED:BINARY:Nb]` size summary. Project-name keys (`projectName`/`sceneName`/`fileName`/`name`) redacted when `redactProjectNames=true`. Pinned by `tests/redaction.test.ts` (55 contracts: license always-on with all-options-off; per-option toggles; recursive nested; input not mutated; project-name keys; G-code preserved with license still redacted; typed-array under image vs unrelated keys; numbers/booleans/null/bigint preserved; multiple license keys per string; defaults shape; redactDefault wiring; empty/edge cases; source pin documents always-on contract). **Out of scope (T2-115-followup):** wiring redactDefault into reportError, JobLog export, and support bundle (T2-108). **Hardware verification: not required**. Shipped 2026-05-05 in **`d68ad98`**. |
| T2-91 | Feature enforcement registry — `FEATURE_MATRIX` with per-feature `enforce` declarations | Audit 5A Required Priority 5: pre-T2-91 there was no central registry stating "for feature X, enforcement MUST exist at layers Y and Z." That's exactly how box_generator originally shipped without a service-level gate (T1-79 added it later). **Shipped:** new `src/entitlements/FeatureMatrix.ts` exports `EnforcementLayer` literal union (ui / service / compiler / export), `FeatureDefinition` (id / label / tier / enforcement / description), `FEATURE_MATRIX` (12 entries — every Pro feature in `PRO_FEATURES` with declared enforcement layers), `getFeatureDefinition(id)`, `featuresEnforcedAt(layer)`, `compilerAllowFlagName(id)` (snake_case → `allow${PascalCase}` convention matching JobCompiler). Pinned by `tests/feature-matrix-enforcement.test.ts` (99 contracts) which **source-scans the repo** to verify every claim: every `PRO_FEATURES` entry has a matrix row; every matrix row is a `PRO_FEATURES` entry; every `service`-enforced feature has at least one `assertFeature('${id}')` callsite outside the entitlements module; every `compiler`-enforced feature has the matching `allow${Camel}: canUseFeature("${id}")` line in JobCompiler.ts; non-empty enforcement; at-least-one non-UI layer per feature (UI alone is bypassable); helper round-trip; source-level pins. **Adding a Pro feature to the matrix without wiring its gates fails CI** — that is the load-bearing contract of T2-91. **Hardware verification: not required** (declarative + test-time enforcement). Shipped 2026-05-05 in **`077cc8d`**. |
| T2-92 | Per-feature granular `canUse` — replace single `hasPro` boolean | Audit 5A Critical failure 9 + indirect Priority 11: pre-T2-92 `EntitlementService.canUse(feature)` was `void feature; return this.state.hasPro` — every Pro feature got the same answer. No "Pro Lite" subset, no per-feature trial, no surgical revocation, no tester keys with restricted scope. **Shipped:** `EntitlementService.canUse(feature)` rewritten — developer + tester_permanent tiers act as wildcards (current internal-build behaviour preserved); when `state.features` is populated (T2-89 server-token path) it is consulted as a membership check; when undefined, falls back to `state.hasPro` so legacy callers (every hand-built EntitlementState literal in the codebase + any path that hasn't migrated to per-feature tokens) keep working unchanged. New optional `EntitlementState.features?: ReadonlyArray<ProFeature>` field. The legacy `void feature; ...` body is gone. Pinned by `tests/per-feature-granularity.test.ts` (99 contracts: token with one feature gates the others; multi-feature; empty features array → all false even when hasPro=true (explicit empty signal); developer wildcard; tester_permanent wildcard; free; legacy-undefined back-compat for both hasPro=true/false; trial with feature subset; surgical revocation (paid user minus boolean_ops); source-level pin verifies the new logic AND that the pre-T2-92 body is removed). Regression: `entitlement-api-split` (11/11), `license-status-states` (37/37). **Out of scope (T2-92-followup):** wiring T2-89 server tokens to populate `features`. **Hardware verification: not required**. Shipped 2026-05-05 in **`c6bd8a2`**. |
| T2-43 | `ControllerSafetyCapabilities` typed declarations | Audit 3D Required P0 ("safety capability gating"): pre-T2-43 the safety paths assumed GRBL behaviour everywhere — pause is recoverable, stop invalidates position, soft-reset is the e-stop. For non-GRBL controllers (Wi-Fi/file-upload like Falcon Wi-Fi or LightBurn dialects) those assumptions can be wrong in safety-critical ways: `disconnectStopsJob: false` is the canonical example. **Shipped (focused MVP — type + GRBL declaration + helpers; controller-interface wiring deferred):** new `src/controllers/ControllerSafetyCapabilities.ts` exports `EmergencyStopMethod` / `LaserOffMethod` / `PauseLatencyClass` / `ExecutionModel` literal unions, `SafetyTristate` (`boolean | 'unknown'` — explicit "we don't know" so safety code takes the conservative path), `ControllerSafetyCapabilities` (18-field interface covering e-stop, pause/resume, laser-off, test fire, side-effects of stop/disconnect, execution model), `grblSafetyCapabilities` (the GRBL 1.1 declaration grounded in the spec + observed Falcon A1 Pro: e-stop=soft-reset @50ms, pauseStopsLaserOutput='unknown' because $32-dependent, resumeSupportedAfterError=false because alarm requires re-home, laserOffCanBeVerified=false because no readback, disconnectStopsJob=true because host-streamed, stopInvalidatesPosition=true + stopRequiresRehome=true because soft-reset, executionModel='lineStream'), and refusal-reason helpers `reasonEmergencyStopRefused` / `reasonPauseRefused` / `reasonResumeAfterErrorRefused` / `reasonTestFireRefused` returning a user-facing string when the op is refused or null when permitted. Pinned by `tests/controller-safety-capabilities.test.ts` (60 contracts: every GRBL field's value pinned with rationale; each refusal helper allows GRBL by default; each helper produces a useful message under a constrained capability; over-cap test fire names both numbers; SafetyTristate accepts 'unknown'; hypothetical Wi-Fi controller (`uploadedFile` + `disconnectStopsJob: false`) typechecks; source-level pin for every export). **Out of scope (T2-43-followup):** wiring `capabilities.safety` into `ControllerInterface` + consuming it in `ExecutionCoordinator` / safety service. **Hardware verification: not required** (declarative type + helper layer; hardware change deferred to wiring ticket). Shipped 2026-05-05 in **`dc60b2e`**. |
| T2-66 | `positionTrusted` state propagating from alarm / E-stop / disconnect / frame-fail | Audit 4C Required Priority 9 + "Misleading state 4": pre-T2-66 the codebase had no flag tracking whether the controller's reported position actually matches the machine's physical position. After alarm (soft reset), emergency stop, disconnect mid-job, manual realtime command, or a failed frame, the user could press Start without re-homing — the controller still reports numbers but those numbers are no longer trustworthy. **Shipped (focused MVP — type + transition + start-gate predicate; MachineState wiring deferred):** new `src/app/PositionTrust.ts` exports `PositionTrust` discriminated union (`{trusted: true}` | `{trusted: false; reason; lostAt}`), `PositionTrustLostReason` (6 reasons: never-homed / soft-reset / emergency-stop / disconnect / manual-command / frame-failed), `PositionTrustEvent` (10 kinds: home-success/cancel, soft-reset, emergency-stop, disconnect, manual-command, frame-success/fail, unlock, save-origin), `initialPositionTrust`, `transitionPositionTrust(current, event, now)` (pure function), `positionTrustMessage` (per-reason user-facing string, null when trusted), `canStartJobUnderTrust(trust, mode)` (trusted = allow all; untrusted + savedOrigin = blocked because saved-origin was set BEFORE the trust-loss; untrusted + absolute/current = allowed because user is asserting current head position). **Audit's "Misleading state 3" honoured:** `unlock` ($X) does NOT restore trust — $X clears alarm without moving the head to a known position. Pinned by `tests/position-trust-transitions.test.ts` (51 contracts: every event's transition; reason+lostAt populated; unlock leaves trust unchanged; manual-command idempotent when already untrusted; trusted state covers all 3 start modes; savedOrigin blocked under untrust + reason mentions trust; e2e flow home → soft-reset → blocked → save-origin → allowed; 10 declared event kinds; 6 distinct user-facing messages; source-level pin). **Out of scope (T2-66-followup):** wiring `positionTrust` into `MachineState`, `canStartJob` chain, and a UI banner. **Hardware verification: not required** (declarative type + pure transition; safety effect lands when wired into MachineService). Shipped 2026-05-05 in **`f9fe0ff`**. |
| T2-110 | Controller settings snapshot before job — `$$ + $I + $G + $#` capture | Audit 5C Critical 7 + Required Priority 4: pre-T2-110 the codebase parsed only $30 (max spindle) + $32 (laser mode) live; the full GRBL settings dump was never persisted. When a job produced wrong output, support's first questions ("what's your $30?" "what's your $32?" "where is work-zero?") could not be answered from a job log — the user had to type these into a chat. **Shipped (focused MVP — type + parsers + builder; capture-call wiring deferred):** new `src/diagnostics/ControllerSettingsSnapshot.ts` exports `WcsOffset` interface, `ControllerSettingsSnapshot` (capturedAt / buildInfo / parserState / wcsOffsets / settings), `parseDollarDollar(text)` (defensive — malformed lines silently skipped, CRLF handled, `ok` interleaved handled), `parseWcsOffsets(text)` (handles G54-G59, G28, G30, G92, TLO single-axis, PRB with trailing `:0`, 3D z-axis preserved), `buildControllerSettingsSnapshot(args)` (every raw input independent — null-tolerant; empty-string raw → null; capturedAt overridable for tests; partial success doesn't block other fields), `safetyRelevantValues(snap)` (extracts $30/$32/$130/$131; returns null per missing field so caller falls back to device profile). Pinned by `tests/controller-settings-snapshot.test.ts` (50 contracts: $$ parse — happy path, empty, malformed-skip, CRLF; $# parse — full response, 3D z-axis, malformed-skip, TLO/PRB shapes; build — all populated, all null, mixed null tolerance, partial-success independence, capturedAt default; safetyRelevantValues — present/missing/$32=0; JSON round-trip; source-level pin). **Out of scope (T2-110-followup):** wiring `snapshotSettings()` capture into `MachineService.startValidatedJob` before stream begins, embedding into `JobLog.machine.settings`. **Hardware verification: not required** (parsers operate on pre-recorded text; no live controller dependency). Shipped 2026-05-05 in **`cd5f504`**. |
| T2-114 | React error boundary + window error/rejection persistence | Audit 5C Critical 2 + Required Priority 8: pre-T2-114 the renderer had no `componentDidCatch`, no `window.onerror`, and no `unhandledrejection` listener. A React component throw produced a white screen with no log; an unhandled promise rejection was silently dropped, and the app could continue in an inconsistent state. **Shipped (focused MVP — boundary + global handlers + sink injection):** new `src/diagnostics/AppErrorBoundary.tsx` exports `CrashReport`, `AppErrorBoundary` class with `getDerivedStateFromError` + `componentDidCatch` lifecycle, props `onCrash` (sink) / `onRecover` / `fallback` (override). Sink failures are swallowed — a broken reporter must not turn into a render loop on top of the original crash. Default fallback is plain DOM (no design-system dependency so the crash UI works even when the styling layer threw). New `src/diagnostics/installGlobalErrorHandlers.ts` exports `GlobalErrorReport`, `installGlobalErrorHandlers({onReport, target})` returning `uninstall()`; attaches both `error` and `unhandledrejection` listeners with type-aware reason coercion (Error → message+stack; string → as-is; object → JSON.stringify; null → "Unhandled rejection (no reason)"); sink failures are swallowed. Pinned by `tests/error-boundary.test.ts` (30 contracts: getDerivedStateFromError happy path + ISO capturedAt; componentDidCatch invokes sink with componentStack; sink errors don't re-throw; handleRecover resets state + tolerates throwing onRecover; error event with location fields preserved; unhandledrejection with Error/string/object/null reasons; uninstall detaches both; stack extraction conditional on Error reason; source-level pin verifies both listener types). **Out of scope (T2-114-followup):** wiring `onCrash` to `reportError` (T2-65), full CrashScreen UX with "Export support bundle" wired to T2-108, last-user-action tracker. **Hardware verification: not required** (renderer diagnostic plumbing). Shipped 2026-05-05 in **`fec932f`**. |
| T2-105 (startup) | Startup diagnostics + safe mode + crash-loop recovery | Audit 5B Critical 9 + Required Priority 13: pre-T2-105 there was no startup-attempt accounting — when "the app won't open," the user's only recourse was reinstall. **Note:** a different ticket also numbered T2-105 ("Hidden source maps in production") shipped earlier in `b6a56ed`; the duplicate ID is a pre-existing roadmap artefact. **Shipped (focused MVP — pure detector + safe-mode predicate + log-line formatter; Electron-side wiring deferred):** new `src/diagnostics/CrashLoopDetector.ts` exports `StartupAttempt` (startedAt / outcome=`success`/`crash`/`in-progress` / reason?), `CrashLoopState`, `CrashLoopDetectorOptions`, `DEFAULT_OPTIONS` (threshold=3 per audit), `emptyCrashLoopState`, `recordStartupAttempt` (appends in-progress; trims to maxHistory), `recordSuccessfulStart`, `recordCrash`, `reconcileOnBoot` (stale `in-progress` → `crash` with reason "host died before recording success" — covers the silent-crash case where the renderer dies before sending the ready ack), `consecutiveCrashCount` (counts trailing crashes back to most recent success OR reset barrier), `shouldEnterSafeMode`, `clearCrashLoop` (sets reset barrier — user-explicit "I fixed it"), `formatStartupLogLine` (ISO-prefixed, level-tagged, trailing newline). Pinned by `tests/startup-diagnostics.test.ts` (41 contracts: empty state; record + transitions; reconcileOnBoot for stale in-progress; consecutiveCrashCount with 0/3/success-resets; threshold-1 vs threshold; clearCrashLoop barrier + post-clear new-crash counting; maxHistory eviction order; e2e silent-crash flow; log-line ISO format; source-level pin verifying threshold=3). **Out of scope (T2-105-followup):** wiring `process.on('uncaughtException')` / `app.on('render-process-gone')` / `unhandledRejection` in `electron/main.ts`, persisting state to `userData/startup-state.json`, IPC `safe-mode:enter` to renderer, Reset-to-defaults UI. **Hardware verification: not required** (pure logic). Shipped 2026-05-05 in **`1530856`**. |
| T2-123 | SVG complexity limits — node count, depth, path tokens, segments, polygon points | Audit 5D Critical 7 + Required Priority 7: pre-T2-123 the SVG parser had no node-count cap, no recursion-depth bound, no path-token / segment cap, no polygon-point cap. A malicious SVG could nest <g> 10000 levels deep, carry one <path d=""> with 10M commands, contain 1M <rect>s, or 50M polygon points — all crash patterns documented in real bug-bounty reports against drawing apps. **Shipped (focused MVP — limits + typed error + bump-and-assert helpers; parser wiring deferred):** new `src/import/svg/SvgComplexityLimits.ts` exports `SVG_LIMITS` (8 caps matching audit recommendations: MAX_BYTES=25MB, MAX_NODES=50k, MAX_DEPTH=100, MAX_RENDERABLE=10k, MAX_PATH_TOKENS=200k, MAX_PATH_SEGMENTS=100k, MAX_POLYGON_POINTS=100k, MAX_TRANSFORM_DEPTH=50), `SvgLimitKey` type, `SvgImportLimitError` (carries `limit` + `observed` + `maximum` for UI messaging), `assertSvgLimit(limit, observed)` (strict `>` so equality passes — caller bumps then asserts), `SvgParseContext` (4-counter shape: nodeCount / renderableCount / depth / transformDepth), `emptyParseContext`, `bumpAndAssert(value, limit)` (increment-then-assert convenience), `svgLimitErrorMessage(err)` (per-limit-kind user-facing string with formatted observed/maximum like "1,247,000" — mirrors the audit's example wording; safe fallback for non-limit errors). Pinned by `tests/svg-complexity-limits.test.ts` (54 contracts: every limit's value pinned; assertSvgLimit equality-passes / over-throws; error chain + name; bumpAndAssert returns + throws; emptyParseContext shape; 8 distinct user messages with formatted numbers; non-limit error fallback; null/undefined safe; e2e deep-nesting simulation; source-level pin). **Out of scope (T2-123-followup):** wiring `assertSvgLimit` / `bumpAndAssert` calls into `SvgParser.ts` (traverse depth + node + renderable counters) and `PathParser.ts` (token + segment caps). **Hardware verification: not required** (pure import-side guards). Shipped 2026-05-05 in **`e137aea`**. |
| T2-124 | Image pre-decode size + pixel limits (decompression bomb protection) | Audit 5D Critical 8 + Required Priority 8: pre-T2-124 the import flow read the file fully and decoded fully before any pixel-count check. A 50000×50000 PNG of one colour compresses to ~1MB on disk but decodes to 10GB — Chromium kills the tab or the renderer freezes. **Shipped (focused MVP — limits + typed error + check helpers; useImport wiring deferred):** new `src/import/image/ImageImportLimits.ts` exports `IMAGE_LIMITS` (MAX_FILE_BYTES=50MB, MAX_PIXELS=50 megapixels, MAX_DIMENSION=16384), `ImageImportLimitError` (carries `limit` / `observed` / `maximum` / optional `width` + `height` for dimension/pixel violations), `checkImageFileSize(bytes)` (stage 1 — runs before any read), `checkImageDimensions(width, height)` (stage 2 — runs after a header probe, BEFORE the full decode; per-axis OR area violations + NaN/negative/Infinity guards), `checkImageBeforeDecode(args)` (combined pipeline; file-size first), `imageLimitErrorMessage(err)` (per-limit user-facing string with formatted MB / "10000×6000 = 60.0M pixels. Maximum: 50 megapixels." / "side of 50,000 pixels exceeds 16,384"; non-limit fallback; null/undefined safe). Pinned by `tests/image-decompression-bomb.test.ts` (42 contracts: every limit's value pinned; file-size at-limit / over-limit / 5GB BMP case; 5k×5k passes; 7071×7071 ~50M passes; the audit's headline 50000×50000 rejected at MAX_DIMENSION; per-axis violation isolated; 10000×6000 = 60M rejected on MAX_PIXELS; NaN/negative/Infinity rejected; checkImageBeforeDecode pipeline file-size first; error class shape + extras; per-limit messages with formatted numbers; null/undefined safe; source-level pin). **Out of scope (T2-124-followup):** wiring `checkImageFileSize` + `checkImageDimensions` into `useImport.ts` so the import order becomes file-size → header probe via `createImageBitmap` → pixel-cap check → full decode. **Hardware verification: not required** (pure import-side guards). Shipped 2026-05-05 in **`b751a1c`**. |
| T2-49 | Virtual time / deterministic scheduler for tests | Audit 3E Required P1 virtual time: pre-T2-49 time-sensitive tests (status polling cadence, character-counting flow control, deadman timers, reconnect retry intervals, ack-rate health) relied on real `setTimeout` + arbitrary flush delays — slow, flaky, imprecise about event order under races. **Shipped (focused MVP — scheduler + RealScheduler adapter; per-test migration deferred):** new `tests/helpers/VirtualScheduler.ts` (lives under helpers/ → T2-22 EXCLUDED_DIRS sentinel) exports `TimerHandle`, `SchedulerLike` interface (setTimeout / setInterval / clearTimeout / clearInterval / now), `VirtualScheduler` class (advanceBy fires every task whose fireAt ≤ target; advanceUntilIdle drains with runaway-loop safetyLimit=100k guard; pendingCount introspection; tasks scheduled inside callbacks fire within the same advance window; setInterval re-enqueues at `fireAt + interval` per spec — no compounded missed intervals; tie-break by enqueue order; negative ms throws; cancellation no-op on already-fired handles), `RealScheduler` class (production default wrapping host setTimeout/setInterval). Pinned by `tests/virtual-scheduler.test.ts` (34 contracts: initial state; exact due time fires; three-callback advanceBy(15) fires only first; setInterval × 4 fires/450ms; clear* cancels; advanceBy(0) no-op; negative throws; advanceUntilIdle drains in order with now-at-latest; nested setTimeout fires; recurring counts to 10/1000ms; clearTimeout on fired handle no-op; tie-breaking; safety-limit catches runaway self-rescheduling; RealScheduler surface; source-level pin). **Out of scope (T2-49-followup):** migration of representative tests (`streaming-health`, `controller-fresh-status-recheck`, etc) to inject the VirtualScheduler. **Hardware verification: not required** (test infrastructure helper). Shipped 2026-05-05 in **`e5ce861`**. |
| T2-50 | Scenario-driven failure injection API — typed `injectFault({type,...})` | Audit 3E Critical 6 + Required P0 failure scenario engine: pre-T2-50 the test surface had T2-13's transport-level injector + ad-hoc `MockSerialPort` tools (simulateDisconnect, simulateError, injectResponse, blockStatusQueryResponse) — narrow, ad-hoc, not discoverable from the type system. **Shipped (focused MVP — type union + queue dispatcher + chaos RNG; simulator integration deferred):** new `tests/helpers/ControllerFault.ts` (lives under helpers/ → T2-22 EXCLUDED_DIRS sentinel) exports `ControllerFault` 13-variant discriminated union (drop-ok / slow-ack / inject-error / enter-alarm / malformed-status / disconnect / buffer-overflow / partial-write / baud-mismatch / corrupt-settings-dump / stale-response-after-reconnect / reader-throws-mid-loop / writer-rejects-after-close — every named scenario from the audit), `SimulationContext`, `faultMatches(fault, ctx)` predicate per variant, `FaultQueue` class (inject returns id; consume(id) marks one-shot consumed; clear; pending getter; match(ctx) returns array of matching pending faults; matchOfType<K>(ctx, type) typed filter so the consumer gets `Extract<ControllerFault, { type: K }>` narrowing), `makeChaosRng(seed)` (Mulberry32 PRNG — deterministic per-seed for property tests). Pinned by `tests/fault-injection-scenarios.test.ts` (64 contracts: inject/consume/clear/pending; faultMatches per variant — line trigger, string command, regex command, after-ms, on-realtime-byte, multi-trigger disconnect, byte-threshold, lifecycle unconditional, malformed-status emittingStatus gate; queue.match returns only matchers; matchOfType type-narrows; consumed faults excluded; e2e composed-fault scenario at 2400/2500/3000ms; chaos RNG deterministic-per-seed + distinct-per-seed + always-in-range; 13-variant catalog count; source-level pin verifying every fault type literal). **Out of scope (T2-50-followup):** wiring the queue into the controller-level `GrblSimulator` (T2-47) so tests can `simulator.injectFault({...})` and have the firmware state machine react. **Hardware verification: not required** (test infrastructure model + helpers). Shipped 2026-05-05 in **`d86f9cb`**. |
| T2-75 | Deep geometry/settings validation on load | Audit 4D Required Priority 9: pre-T2-75 the deserializer validated transform finiteness only; geometry shape fields and layer-setting numeric fields could be NaN, Infinity, negative, or absurdly large without being caught. A corrupted but still parseable JSON loaded "successfully" then crashed later during compile/render/preflight — far from the load site, hard to diagnose. **Shipped (focused MVP — per-shape validators + layer-settings validator + auto-repair/strict modes; deserializer integration deferred):** new `src/io/validation/geometryValidation.ts` exports `GeometryValidationIssueKind` (15 kinds), `GeometryValidationIssue` (kind / field / observed / repaired / message), `ValidationResult<T>`, `ValidationMode`, `GeometryValidationError` (carries the issue list for strict-mode rejection at the load site), and per-shape validators: `validateRectGeometry` (width/height>0; x/y finite; cornerRadius≥0), `validateEllipseGeometry` (rx/ry>0; finite center), `validateLineGeometry` (all 4 endpoints finite), `validatePolygonGeometry` (≥2 points or default; per-point finite repair preserving order), `validateTextGeometry` (fontSize>0; text is string), `validateImageGeometry` (originalWidth/Height>0; cropX/Y≥0 + cropWidth/Height>0 — invalid crop reset to full image), `validateLayerSettings` (power.min/max with min≤max; speed>0; passes positive integer; fill interval>0). `applyValidationMode(result, mode)` chooses behaviour: `auto-repair` returns the repaired value with issues for the T2-74 repair report; `strict` throws when any issue is present. Pinned by `tests/geometry-deep-validation.test.ts` (74 contracts: clean shapes pass; NaN/Infinity/negative/zero per geometry kind triggers correct issue + correct repair value; polygon empty + per-point individual repair; text fontSize/non-string repair; image dimensions + crop reset; all 4 layer-settings validations; clean settings preserve exact values; strict-mode throws + auto-repair returns; issue shape always populated; source-level pin). **Out of scope (T2-75-followup):** wiring the validators into `deserializeScene` (T2-74's repairs collector). **Hardware verification: not required** (load-side validation, no machine effect). Shipped 2026-05-05 in **`d4a3837`**. |
| T2-103 | Release artifact integrity — SHA256 + SBOM + signed checksum | Audit 5B Critical 6 + Required Priority 9: pre-T2-103 the build pipeline produced installers with no checksums, no SBOM, no provenance. Users could not verify a downloaded LaserForge installer against tampering; support had no workflow to confirm "is this exe a real LaserForge build?". **Shipped (focused MVP — checksum-format helpers + generator script; SBOM + GPG signing CI integration deferred):** new `src/integrity/checksumFormat.ts` exports `computeSha256Hex` (NIST-canonical-verified for empty string and `'abc'`), `formatChecksumLine(hashHex, filename)` (standard `<64-char-lowercase-hex>  <filename>` two-space format that `sha256sum -c` accepts; rejects bad hex / empty / newline filenames), `formatChecksumsFile` (alphabetical sort + trailing newline POSIX convention), `parseChecksumsFile` (round-trip; tolerates blank lines + sha256sum `*name` binary-mode + skips malformed lines), `matchesAnyPattern` (simple `*` glob — sufficient for `*.exe` / `*.dmg`). New `scripts/generate-checksums.mjs` orchestrates: walks the release dir, hashes each match against default patterns (`*.exe,*.dmg,*.zip,*.AppImage,*.deb,*.rpm`), writes `SHA256SUMS`. Pinned by `tests/release-artifact-integrity.test.ts` (34 contracts: NIST canonical empty + 'abc'; Buffer ↔ string parity; 64-char lowercase hex; two-space separator; rejects short/uppercase/63-char hex + empty + newline filenames; alphabetical sort + trailing newline; parse round-trip with binary-mode `*` strip + blank-line tolerance + malformed skip; pattern matching with dot-anchoring; e2e hash → format → parse → re-verify; source-level pin). **Out of scope (T2-103-followup):** GitHub release workflow YAML wiring (gpg --detach-sign of SHA256SUMS + softprops/action-gh-release upload); `npx @cyclonedx/cyclonedx-npm` SBOM step. Both depend on T2-98 + CI secrets not yet provisioned. **Hardware verification: not required** (build-time + verify-time tooling). Shipped 2026-05-05 in **`6db0564`** + path-correction in `5915854` (relocated to `src/integrity/`). |
| T2-106 | Dependency security scanning in CI — `npm audit` + Dependabot | Audit 5B Priority 10: pre-T2-106 the license-check workflow caught GPL/AGPL contamination but did not catch known CVEs in production dependencies; no Dependabot config either, so security advisories required manual polling. **Shipped (Layers 1 + 2; Layer 3 OSV deferred):** `.github/workflows/ci.yml` gains `npm audit --omit=dev --audit-level=moderate` step running BEFORE `npm run build` (fast-fail on bad dep bumps; `--omit=dev` excludes vitest/eslint/tsx where dev-only CVEs don't ship to users; `--audit-level=moderate` fails CI on moderate-or-higher production-dependency CVEs). Verified clean at ship time: `npm audit --omit=dev --audit-level=moderate` returned "found 0 vulnerabilities". New `.github/dependabot.yml` opens weekly PRs for the npm ecosystem with a `production-security` group (applies-to: security-updates) and a separate `dev-deps` group for @types/eslint/tsx so a flurry of routine version bumps doesn't bury security PRs that matter; github-actions ecosystem also tracked monthly so workflow files stay current. Pinned by `tests/dependency-security-scan.test.ts` (20 contracts: ci.yml exists; `npm audit` runs with --omit=dev + --audit-level=moderate; T2-106 marker; audit step runs BEFORE build; dependabot.yml exists with version=2 + npm ecosystem + weekly schedule + production-security group + applies-to=security-updates + github-actions ecosystem + exclude-patterns for dev deps; license-check.yml still exists alongside). **Out of scope (T2-106-followup):** Layer 3 — OSV scanner action (broader CVE database coverage beyond npm advisories; SARIF report). **Hardware verification: not required** (CI configuration). Shipped 2026-05-05 in **`<TBD>`**. |
| T2-74 | Surface load repair report to user — helper (UI dialog deferred) | Audit 4D Critical 5 + Required Priority 8: pre-T2-74 the deserializer silently relocated orphan objects, cleared broken parent references, removed duplicate-id objects, and reset invalid activeLayerId. A user loading their own project could discover their groups got dropped or objects got moved without any signal. **Shipped:** new `deserializeSceneWithReport(json): ProjectLoadReport` returns `{ scene, repairs: ProjectRepair[] }` with 4 `ProjectRepairKind` discriminants (orphan-objects-relocated / duplicate-objects-removed / broken-parent-cleared / invalid-active-layer). `buildSceneFromParsedEnvelope` gains optional `repairs?` collector — `deserializeScene` passes undefined (no-op, full back-compat). Each repair has count + human-readable details ready for UI surfaces. Pinned by `tests/load-repair-report.test.ts` (23 contracts: clean → 0 repairs; each of 4 kinds triggered + repaired correctly; multi-kind project; backward-compat with deserializeScene; source pins). **Out of scope:** load-time dialog ("Project opened with repairs / [Got it] / [Discard load and try again]") — UX design + reuse of T1-71 recovery-prompt pattern, T2-74-followup. **Hardware verification: not required**. Shipped 2026-05-05 in **`bf6ea72`**. |
| T2-71 | Embed device profile snapshot in project metadata (schema + diff + checker) | Audit 4D Critical failure 3 + Required Priority 5: pre-T2-71 project metadata stored only `deviceProfileId`. If the user edited the referenced profile after save (e.g. changed `maxSpindle` to match a hardware update), reloading silently compiled against new values — output power could drift 4× without any user signal. **Shipped:** `Scene.metadata.deviceProfileSnapshot?: DeviceProfile` field added (optional → legacy projects ride forward). SceneSerializer passes it through write + read; absent on legacy serialized JSON. New `src/core/devices/profileSnapshot.ts` exports `diffProfiles(saved, current)` (24 tracked safety-relevant fields; cosmetic name/id/timestamps NOT tracked) and `checkProfileSnapshot(scene, getProfileById)` returning a 5-kind discriminated union (`no-snapshot` / `no-current-profile` / `profile-deleted` / `match` / `mismatch`). Pinned by `tests/project-profile-snapshot.test.ts` (27 contracts: diff math; all 5 result kinds; round-trip preserves snapshot; legacy backward-compat preserves deviceProfileId without spurious snapshot field; source-level pins). Regression: autosave-serialization (27/27). **Out of scope:** load-time dialog UX (T2-71-followup) — the helper is ready; UI consumes when designed. **Hardware verification: not required**. Shipped 2026-05-05 in **`17999fd`**. |
| T2-69 | Atomic autosave record — single key with checksum + metadata | Audit 4D Critical failure (atomicity) + Required Priority 2: pre-T2-69 the autosave write was two sequential `storage.set` calls (`AUTOSAVE_KEY` for JSON + `AUTOSAVE_TIME_KEY` for ISO timestamp). A browser crash between them left the JSON current but the timestamp stale (or absent — `readAutosave` defaulted to `new Date().toISOString()` masking the inconsistency); concurrent readers in another tab could observe a partial state. **Shipped:** new `AUTOSAVE_RECORD_KEY = 'laserforge_autosave_record'` holds an `AutosaveRecord` JSON object `{ version, json, timestamp, checksum (FNV-1a hex), sceneName?, objectCount?, layerCount? }` — single key, single set call, atomic at the storage level. Helpers `buildRecord` (with best-effort sceneName/objectCount/layerCount extraction for the T1-71 recovery dialog), `parseRecord` (defensive parse with version + field-shape checks), `autosaveRecordChecksumValid` (corruption check, not cryptographic — sha256 upgrade tracked under T3-77). One-shot `migrateAutosaveToRecordKey` runs once per process via `_recordMigrationAttempted` flag; if no record key exists but legacy keys do, builds a record from the old format and writes the new key. Read path prefers record; on miss/parse-fail falls back to legacy two-key read so users don't lose work to corruption. Clear removes all three keys. Pinned by `tests/atomic-autosave-record.test.ts` (27 contracts: round-trip with metadata; legacy migration with timestamp preserved; checksum mismatch detection; clear cleans all three keys; atomicity verified by spying on adapter.set — exactly one autosave-related set call per persistAutosave; consecutive writes update in place; missing autosave returns null; source-level pins). `tests/autosave-dirty-flag-on-failure.test.ts` updated (2 assertions widened to read JSON from inside the record). Regression: autosave-dirty-flag-on-failure (11/11), autosave-preserves-images (7/7), autosave-serialization (27/27), autosave-storage (9/9). **Out of scope:** legacy-key cleanup after the new path proves out (filed as T2-69-followup); sha256 upgrade (T3-77); T2-70 previous-backup slot builds on this format. **Hardware verification: not required** (storage-layer atomicity). Shipped 2026-05-05 in **`9f11fce`**. |
| T2-77 | Async revision guards on trace (image-import deferred) | Audit 4E Critical 4 + 5 + Required Priority 6: pre-T2-77 the trace handler in PropertiesPanel captured `scene` from React closure at start of async work, then awaited `traceToSceneObjectAsync` for potentially seconds, then committed a `newScene` built from the now-stale capture — any user edits during the await were silently erased on commit. **Shipped (trace scope; minimum-viable):** new `src/ui/hooks/asyncSceneGuard.ts` exports `captureSceneRevision(scene)` + `isSceneStale(token, current)` (identity-based; the codebase mutates by replacing scene references). `PropertiesPanel.handleTrace` captures `sceneRef.current` before the await, checks staleness after; on stale surfaces "scene changed — please run the trace again" via showAlert and refuses the commit. Pinned by `tests/async-scene-producer-guards.test.ts` (13 contracts: identity-match → not stale; different ref → stale; shallow-cloned → stale; cross-check; token shape; source-level pins for both helper and PropertiesPanel; defensive pin that useImport.ts is NOT yet wired). **Out of scope (filed as T2-77-followup):** image-import (`importImageUnified`) integration of the same guard; richer apply-to-current vs discard conflict-resolution dialog. The minimum-viable shape prevents the data loss; the dialog is a UX polish. **Hardware verification: not required** (UI-layer data-loss prevention). Shipped 2026-05-05 in **`1387bd1`**. |
| T2-33 | Partial-open cleanup in `WebSerialPort.requestAndOpen` | Audit 3B section 3.3 + scenarios 7.3, 7.4 + Required P0 fix #4 partial-failure cleanup. Pre-T2-33 the `requestAndOpen` catch block was `this._isOpen = false; throw …;` — if the failure happened AFTER `port.open()` succeeded but during `getWriter()` / `getReader()`, NO cleanup ran: writer/reader locks stayed held, port stayed open, permission stayed granted. "Classic hardware lifecycle leak: the port can be half-open and unavailable for retry." **Shipped:** track each acquisition step (`port`, `portOpened` flag, `writer`, `reader`) in locals; commit to instance fields only after ALL succeed; on catch unwind in reverse — reader.cancel + releaseLock, writer.releaseLock, port.close (only if portOpened), null instance fields, rethrow. T1-50 Part B integrated: optional `AbortSignal`, `throwIfAborted()` between every step; aborted surfaces as `"Connection aborted by user"`. Pinned by `tests/web-serial-partial-open-cleanup.test.ts` (38 contracts): happy path (no cleanup); open() throws → no port.close (open never succeeded); writer-acquire fails → port.close called + zero lock-release calls; reader-acquire fails → writer.releaseLock + port.close called; null writable / null readable → "writer lock" / "reader lock" errors + appropriate cleanup; pre-aborted signal → port.open never called; mid-flight abort → port.close called for cleanup; source-level pins. Regression: connect-abort-signal (21/21), connect-cleanup-on-partial-failure (16/16), connect-safe-state-handshake (43/43), serial-port-close-async (18/18), fault-injecting-serial-port (52/52). **Hardware verification: not required** (transport-layer cleanup). Shipped 2026-05-05 in **`f5d77dd`**. |
| T2-23 | Determinism gate — same scene compiled 20× must be byte-identical | Audit 2F section 7 / Gate B / P1: pre-T2-23 nothing tested compile reproducibility. Latent non-determinism (Map iteration over non-stable keys, unstable tie-breaking in path optimizer, async font-cache timing, leaked Date.now()) could land silently. T2-23 builds on T1-48 (deterministic Output.createdAt via injectable clock) + T2-18 (parser semantics for normalization). **Shipped:** new `tests/determinism-gate.test.ts` (8 contracts) compiles a non-trivial scene (cut rects + engrave ellipse + engrave line + 1-bit raster across three layers; IDs from a seeded T2-21 PRNG) 20× through `PipelineService.compileGcode`, strips the `; Date:` line (PipelineService doesn't yet thread the T1-48 clock; filed as T2-23-followup), asserts byte-identical output across all 20 runs (4130 chars). Companion contracts: 5 Fisher-Yates shuffles of `scene.objects` produce the same gcode (planner / optimizer establishes a canonical order before emission); two same-seed regenerated scenes compile identically; `findFirstDiff` diagnostic self-tested. Source-level pin that Output.ts retains the T1-48 clock injection. Regression: `parse-gcode-helper` (39/39), `analyze-burn-bounds` (35/35) green. **Out of scope:** threading `clock` through `PipelineService.compileGcode` to compare unstripped gcode (filed as T2-23-followup; the headline determinism guarantee is delivered). **Hardware verification: not required** (test infrastructure). Shipped 2026-05-05 in **`185a096`**. |
| T2-21 | Property-based geometry tests (in-house, no fast-check dep) | Audit 2F section 10 / P1: hand-fixture tests miss edge cases — near-zero segments, near-collinear points, large/small coordinate values, rotated/scaled transforms. Property-based testing generates many random valid inputs and asserts invariants hold. Audit proposed adding `fast-check`; T2-21 ships the same execution shape (deterministic seeded PRNG, generator/predicate API, first-failing-counterexample report) as a focused 200-line in-house helper, no dep added. **Shipped:** new `tests/helpers/propertyTesting.ts` exports `forAll(gens, predicate, opts?)`, `makeRng(seed): Rng` (mulberry32 PRNG so failures are reproducible), `Arbitrary<T>` interface + built-in arbitraries (`realIn`, `intIn`, `point`, `convexPolygon`, `affineTransform`, `applyAffine`, `bounds`). Variadic up to 4 generators. Property results carry `{ ok, runs, seed }` on pass and `{ ok, runIndex, seed, input, error? }` on fail; `describeFailure(r)` formats stable output. **Out of scope:** fast-check's automatic shrinker — original failing input is reported, minimization is quality-of-life. Pinned by `tests/property-geometry.test.ts` (20 properties × 200 cases = 4000+ test cases per run): transform invariants (bounds contain every transformed point with magnitude-scaled tolerance, identity no-op, translation invertible); closed-path invariants (transform preserves vertex count, no NaN/Infinity, positive-det preserves winding); bounds invariants (single point → zero-area AABB, monotonic widening, empty → Infinity AABB); output invariants using T2-18+T2-19 (parsed endpoints finite, `noBurnDuringRapid` holds, burnBounds ⊆ totalBounds, parser recovers all generator burn segments, spindle ≤ ceiling, `endsLaserOff`, units + distanceMode declared, totalDistanceBurn ≥ longest segment). Synthetic burn-job generator uses per-segment M5/M4/M5 cycling (not T1-31's modal-M4) so the parser's conservative `noBurnDuringRapid` check passes — property tests pin the parser contract; modal-M4 emission is pinned separately by `raster-output-uses-modal-m4`. Regression: `parse-gcode-helper` (39/39), `analyze-burn-bounds` (35/35). **Hardware verification: not required** (test infrastructure). Shipped 2026-05-05 in **`be90a91`**. |
| T2-20 | Pixel fixture harness for raster regression tests | Audit 2F C2 / Gate C / P0 #2: pre-T2-20 nothing tested the FULL raster pipeline (image → JobCompiler → PlanOptimizer → Output) at pixel level. Image-processing tests stopped at pixel arrays; planner tests used synthetic moves; the layers were tested independently but never connected. A bug in raster scanline coordinate calculation could ship with passing narrow-scope tests while pixel-level output was wrong. **Shipped:** new `tests/helpers/imageFixtures.ts` exports 8 programmatic fixture builders (blackPixel, whitePixel, checkerboard(n), horizontalGradient(w,h), verticalGradient(w,h), blankRow(w,h,i), blackRow(w,h,i), diagonalLine(n)). Each returns `RasterFixture { scene, width, height, data, layerId, objectId, label }`. `tests/raster-pixel-fixtures.test.ts` (22 contracts) compiles each through full pipeline + verifies via T2-18 parser + T2-19 analyzer: black pixel → 1-3 burn segments; white pixel → 0; checkerboard → multi-row segments + Y-range; gradient → distinct power values; blankRow → 4 rows + ≥4 rapids (modal-M4 shape); blackRow → 1 row + Y-bounds ≈ 1 pixel; diagonal → ≥5 segments; modal-M4 invariants hold across all fixtures; source pins. **Out of scope:** golden `.gcode` snapshot files (T2-20-followup) — semantic parser-driven assertions catch the same bug class without snapshot maintenance overhead. Regression: parse-gcode-helper (39/39), analyze-burn-bounds (35/35), raster-output-uses-modal-m4 (16/16). **Hardware verification: not required** (test infrastructure). Shipped 2026-05-05 in **`8b23d67`**. |
| T2-19 | Burn-bounds analyzer test helper | Audit 2F P0 #1: pre-T2-19 tests had no helper that derived physical output bounds from emitted G-code — they trusted `machinePlanBounds` from the compiled ticket without independently verifying what the G-code actually does. Correlated bug (MachineTransform skews coords + plan-bounds calculation also wrong) could pass tests while the machine burned in the wrong place. T2-18's parser provides per-move `laserOn` flags and `fromXY/toXY`; T2-19 builds the actionable derivations on top. **Shipped:** new `tests/helpers/analyzeBurnBounds.ts` exports `analyzeBurnBounds(parsed): BurnAnalysis` returning `burnSegments` (with power/feed/lineIndex per laser-on motion), `rapidSegments`, `burnBounds`/`rapidBounds`/`totalBounds` (tight AABBs), `overscanRegions` (contiguous G1/G2/G3 with laser OFF — gap-bridges, fill overscan), `totalDistanceBurn`/`totalDistanceRapid` (Math.sqrt-based so RTL motion accumulates positive distance), `laserOnTime` (seconds via `feed × distance`, defensively 0 when feed missing), `midJobLaserOff` (M5 events between burn moves, with trailing job-end M5 filtered out via "promote pending events on next burn" pattern). Pinned by `tests/analyze-burn-bounds.test.ts` (35 contracts: simple burn segment correctness; burn vs rapid bounds segregation; multi-segment square burn; modal-M4 raster shape from T1-31 produces 2 burn segments + 1 overscan region; mid-job M5 surfaces only when followed by later burn; pure rapid produces empty burnBounds; empty gcode → zero analysis; missing-feed burn → laserOnTime=0 defensively; frame-vs-burn equal-bounds-across-power-levels pattern; RTL motion correctness; source-level pins). Regression: `parse-gcode-helper` (39/39). **Out of scope:** per-test migration of substring assertions to use `analyzeBurnBounds(...)` — separate dependent ticket work that T2-19 unblocks. **Hardware verification: not required** (test infrastructure helper). Shipped 2026-05-05 in **`9246c76`**. |
| T2-18 | Semantic G-code parser test helper (parser scope) | Audit 2F C1 + P0 #1: pre-T2-18 the test suite had no semantic G-code parser. Tests relied on string `includes('G21')` or regex matches — shallow protection that passed whether modal state was correct, whether emission ordering was right, etc. **Shipped:** new `tests/helpers/parseGcode.ts` exports `parseGcode(text): ParsedGcode`. Walks gcode line-by-line, tracks modal state (units, distance mode, motion mode, laser modal, feed, spindle, position, plane), classifies lines, computes burn/rapid/total bounds, surfaces safety-invariant predicates (startsLaserOff, endsLaserOff, unitsDeclared, distanceModeDeclared, noBurnDuringRapid, noNaN, noInfinity, spindleNeverExceedsMax(max), feedAlwaysPositive, finalLaserOff). Per-move modalBefore + modalAfter snapshots. Comment stripping for `;` and `(...)`. Lives under `tests/helpers/` (T2-22 EXCLUDED_DIRS sentinel). **Out of scope:** subroutines / variable substitution / full parameter set; per-test migration filed as T2-19 / T2-23 dependent work (~30 tests benefit per audit). Pinned by `tests/parse-gcode-helper.test.ts` (39 contracts covering all modal transitions, position accumulation under both modes, burn-bounds segregation, all 10 safety invariants, line classification, null-state defaults, per-move snapshot capture). **Hardware verification: not required** (test infrastructure helper). | `9f353cd` |
| T2-56 | Move job-log finalization out of mounted UI into MachineService subscription | Audit 4A Race 4 / Required Fix 8: pre-T2-56 finalization driven by ConnectionPanel.tsx:141-152 useEffect. If panel was unmounted (sidebar closed, route change) at run→idle transition, effect didn't fire — finalization delayed until panel remounted (or missed entirely). **Shipped:** new `MachineService.attachAutoFinalize(ctrl)` subscribes to onStateChange + onProgress, calls existing tryFinalizeJobLog with `console.info` default sink. Returns unsub function. Existing `log.status !== 'running'` early-return makes double-call (auto + UI useEffect) idempotent. `useMachineService` gains `controllerReady: boolean` arg + useEffect that attaches once controllerReady is true. App.tsx threads `grbl.controllerReady`. **Out of scope:** removing ConnectionPanel.tsx useEffect (kept as belt-and-suspenders); `service.onFinalizationMessage(cb)` observable for UI to display finalize messages — filed as T2-56 follow-up. Pinned by `tests/auto-finalize-without-mounted-ui.test.ts` (12 contracts). Regression: machine-service-job-lifecycle-safety (14/14), try-finalize-respects-observed-running (4/4), try-finalize-after-observed-running (5/5), start-validated-job-then-immediately-tryfinalize (1/1), active-job-canvas-context-* (7+6/13) all green. **Hardware verification: not required** (event subscription wiring). | `bb2f5d0` |
| T2-52 | Centralized active-profile store via `useSyncExternalStore` (foundation) | Audit 4A Finding 3 / Critical Failure 6 / Required Fix 4: pre-T2-52 two coexisting profile-sync mechanisms — `ConnectionPanel.tsx:43` (1-second polling + storage event + custom event) and `App.tsx:287-290` (profileRevision counter + custom event). The polling was a tell that neither fully worked. **Shipped (foundation):** new `src/core/devices/ActiveProfileStore.ts` with `class ActiveProfileStore` (stable-bound subscribe/getSnapshot per useSyncExternalStore contract, setActiveProfile mutator, refresh helper, destroyForTests), `activeProfileStore` singleton, `useActiveProfile()` React hook. **Dual-listener back-compat:** subscribes to both `storage` events (cross-tab) and `laserforge:active-profile-changed` custom events (in-app legacy mutators) so existing dispatchers drive the store unmodified. **Double-notify prevention:** setActiveProfile re-reads via getActiveProfile after setActiveProfileId so _snapshot matches cache's canonical instance; the custom event dispatched in the same call triggers refresh which sees identity match and is a no-op — exactly one _notifyAll per mutation. **Lazy init:** window listener binding deferred to first subscribe/getSnapshot for test-bootstrap safety. **Throwing-listener resilience:** each subscriber call try/catched + console.warn'd. **Out of scope:** migration of App.tsx's profileRevision (10 sites) and ConnectionPanel.tsx's polling shim — filed as T2-52-followup, per-consumer review. Pinned by `tests/active-profile-store.test.ts` (20 contracts: subscribe-set fires once; multiple subscribers all notified; unsubscribe stops; storage event drives refresh+notify; custom event drives refresh+notify; setActiveProfile dispatches custom event for legacy listeners; setActiveProfile(null) clears; refresh no-op on identity match; throwing listener isolated; source-level pins). **Hardware verification: not required** (state plumbing). | `f1af7a6` |
| T2-31 | `SerialPortLike.close()` is now async — awaitable transport release | Audit 3B section 3.2 + P0 fix #3: pre-T2-31 the interface declared `close(): void` and `WebSerialPort.close` set `isOpen = false` synchronously then started `port.close().then(...)` un-awaited. A caller could believe the port was fully closed while the browser was still releasing it; rapid reconnect raced the still-closing handle → permission/handle race → "port busy" errors. **Shipped:** interface changed to `close(): Promise<void>`. `isOpen` still flips synchronously at close-entry (back-compat: `if (!port.isOpen)` guards fire immediately). `WebSerialPort.close` rewritten as async — awaits reader.cancel(), reader/writer.releaseLock() (try/catch each), `port.close()`, then best-effort `forget()` (feature-detected). `MockSerialPort.close` async too; in-memory state flips sync, promise resolves immediately. **Caller updates:** `MachineService.connectRealLaser` catch path awaits `ws.close()`; `GrblController.disconnect` awaits `this._port.close()`; `GrblController.connect`'s 10s timeout uses `void this._port.close().catch(...)` (setTimeout cannot await; reject is the load-bearing signal). Test stubs in `connect-abort-signal.test.ts` and `connect-cleanup-on-partial-failure.test.ts` switched from sync `function (): void` to `async function (): Promise<void>`. Pinned by `tests/serial-port-close-async.test.ts` (18 contracts: close returns Promise; sync isOpen flip at entry; onClose fires by promise-resolve; double-close idempotent; source-level pins for T2-31 markers across SerialPort/WebSerialPort/GrblController/MachineService, async signatures, OLD un-awaited then-chain gone, callers await or `void.catch`). Regression sweep clean: safety-off-two-stage (14/14), safety-write-failure-surfaces (13/13), connect-abort-signal (21/21), connect-cleanup-on-partial-failure (16/16), controller-stop-safety (19/19). **TS count:** 42 (one below baseline — narrowing fix in active-job-canvas-context-pinned.test.ts). **Foundation for T2-32 / T2-33** (ConnectionManager + partial-open cleanup). **Hardware verification: not required** (transport ordering; no g-code, no machine-state). | `898b510` |
| T2-22 | Standardize test runner — auto-discovery (Stage 1) | Audit 2F C7 / Gate H / P2: pre-T2-22 manual 232-entry test file list in `scripts/run-tests.mjs`; new tests required manual registration → drift loophole that T1-47 patched defensively but didn't structurally close. **Shipped (Stage 1):** `scripts/run-tests.mjs` rewritten — recursive `walkTests(testsDir)` discovers `*.test.ts(x)` files, excludes `snapshots/helpers/fixtures/node_modules`, sorts alphabetically for stable order, runs each via tsx in its own Node process (existing per-test isolation preserved). Auto-pickup eliminates registration drift entirely. **T1-47 retired** in the same commit: `tests/runner-registration-coverage.test.ts` removed because its failure mode is impossible by construction with auto-discovery. **Documented skip list (`KNOWN_FAILURES`):** alphabetical sort surfaces 5 pre-existing post-T1-58 broken tests earlier in the run; 4 fixed in-line by threading `getActiveProfile()` to `compileGcode` (active-job-canvas-context-cleared 7/7, active-job-canvas-context-pinned 6/6, validated-job-ticket-mismatch 13/13, validated-job-ticket-phase1 19/19 — phase1 also widened `runPreflightSummary` args for T1-55/T1-32/T1-33). 5th (ui-start-job-uses-ticket) stays in skip list with `T2-22-followup` citation; has additional bugs beyond profile-hash. Skip logged via `↷ <file> (skipped: <reason>)` on stderr — never silent. Pinned by `tests/runner-auto-discovery.test.ts` (20 contracts: T2-22 marker, readdirSync walk, walkTests helper, all four EXCLUDED_DIRS sentinels, TEST_FILE_PATTERN matches `.test.ts(x)` only, OLD manual array gone, deterministic sort, KNOWN_FAILURES Map declared with stderr `↷` logging + T2-22-followup citation, walk finds 232 files, retired T1-47 file gone). **Stage 2 (vitest migration) deferred** — multi-session per-test refactor. **Hardware verification: not required** (test runner infrastructure). | `0b33d3d` |
| T2-60 | Frame freshness invalidation — any relevant change resets `hasFramed` | Audit 4B Section 8.3 + Critical UX failure 2 detail: pre-T2-60 the `hasFramed` ref invalidated only on connect/disconnect and scene mutation. Missed: startMode change, savedOrigin numeric change, active-profile change (different originCorner / bed size), live `$130/$131` bed-dimension change, originCorner toggle, compiledTicketId change. Without invalidation users frame once and the T1-59 gate gets out of date silently. **Shipped:** new pure helper `src/app/computeFrameFreshnessKey.ts` exports `computeFrameFreshnessKey(inputs): string` from `{ startMode, savedOriginX, savedOriginY, profileId, bedWidth, bedHeight, originCorner, compiledTicketId }`. Deterministic concatenated string (no hash dep needed for 8-field input). 1 µm rounding so FP import noise doesn't bust the key. NaN coerces to stable token. **`ConnectionPanelMain.tsx`:** new `frameFreshnessKey` computed each render via the helper; new `lastFrameKeyRef` tracks previously-observed value; new `useEffect([frameFreshnessKey])` resets `hasFramed.current = false` + bumps `workflowVersion` whenever the key differs. Mount-time first-fire sets ref but doesn't invalidate. Existing `historyVersion` and `isConnected` invalidation effects stay (orthogonal cases). **Out of scope:** spec's `currentFrameKey` argument plumbing through `hasFramed(key)` call sites — invalidating the ref directly is functionally equivalent. Pinned by `tests/frame-freshness-invalidation.test.ts` (23 contracts including each input change individually + same-id stability + sub-µm FP resilience + NaN stability + all four corners distinct + source-level pins). Regression: `frame-required-before-start` (13/13), `frame-idle-timeout-dynamic` (7/7), `exact-idle-gates` (27/27) green. **Hardware verification: not required** (UI flag invalidation; no g-code, no machine-state). | `5f27d34` |
| T2-39 | Strict profile validation on save | Audit 3C Finding 5.1 / Required Priority 9: profiles user-editable, save-side validation only checked shape. bedWidth=0, maxSpindle=NaN/Infinity, originCorner='upside-down' (storage edit), autoFocusCommand='M3 S1000', maxFeedRate negative all silently accepted — produced downstream nonsense (NaN S-values in gcode, transform corner mismatch, laser fired unattended via autofocus). **Shipped (validator + save-side rejection scope):** new `src/core/devices/validateProfile.ts` exports `validateProfile(profile): { ok, issues }`. Pure function, per-field rules — bedWidth/bedHeight positive finite ≤ 5000mm; maxSpindle positive finite ≤ 65535 (16-bit PWM ceiling); maxFeedRate / maxRateX/Y / maxAccel* positive finite when set; originCorner ∈ {front-left, rear-left, front-right, rear-right}; baudRate ∈ {9600, 19200, 38400, 57600, 115200, 230400, 250000}; name non-blank; id non-empty. Autofocus-command safety: regex deny-list against $X (alarm-clear), $H (homing), M3/M4 (laser-on outside test-fire), G10 (silent WCS rewrite), M2/M30 (program end), realtime bytes (0x18/0x21/0x7E). autoFocusTimeoutMs validated positive ≤ 5 minutes. watts NaN/negative is warning (not error) — some setups don't know exact wattage. New `ProfileValidationError extends Error` with `.issues` field. `saveDeviceProfile` validates first, throws on hard error with all issues, warnings `console.warn`'d but allowed. Backfill / updatedAt bump runs only after validation passes. **Out of scope:** Settings UI per-field error rendering + "profile needs review" banner for legacy invalid profiles — filed as future T2-39 follow-ups. Pinned by `tests/profile-validation.test.ts` (39 contracts). Regression: `device-profile-basic-api` (9/9), `autofocus` (12/12), `gcode-templates-safety` (15/15), `material-preset-schema` (40/40) all green. **Hardware verification: not required** (validation rejects bad data before persistence; no g-code, no machine-state). | `b87e59b` |
| T2-16 | Duplicate / overlap path detection in preflight | Audit 2C P2: zero duplicate-detection across the codebase. SVG-import / paste-twice / multi-import workflows leave stacked copies of the same shape; controller burns each independently — twice the time, twice the deposited power, charred edges, wasted material. **Shipped:** new `src/core/preflight/rules/DuplicateGeometryPreflight.ts` with `fingerprintObject(obj)` (canonical-form key = type + rounded-to-1µm transform six-tuple + per-geometry-type signature) + `runDuplicateGeometryChecks(ctx, out)` rule. New `GEOMETRY_DUPLICATE` PREFLIGHT_CODES constant. `runPreflight` orchestrator wired. Per-geometry signatures: rect = w×h+cornerRadius; ellipse = rx×ry; line = direction-vector; polygon = point count + closed flag + first 3 points; path = subPath count + total seg count + first move-to; text = full text + font; image = src + bitmap dimensions. Skips invisible objects + hidden-layer + output:false-layer (duplicates that won't burn aren't worth surfacing). Severity = warning (sometimes users intentionally stack shapes for double cuts). Cluster collapsing: 3+ duplicates → one warning naming count, listing first 3 ids with `+N more` suffix when cluster > 3. Pinned by `tests/preflight-duplicate-geometry.test.ts` (21 contracts: 2 stacked → 1 warning; different positions → no warning; same shape different rotation → no warning (transform differs); 3+ → cluster collapse with count; rect vs ellipse same bounds → no warning (different types); hidden / output:false suppression; fingerprintObject determinism + cornerRadius distinguishes; source-level pins for T2-16 marker, exports, code constant, orchestrator wiring, import). Regression: `preflight.test.ts`, `preflight-output-layer-filter.test.ts` (7/7) green. **Hardware verification: not required** (preflight informational warning; no g-code change). | `cb93bf7` |
| T2-11 | Service-layer operation mutex for laser-on operations | New `MachineService` API: `tryAcquireOperation(kind) / releaseOperation(kind) / getActiveOperation()`. `ActiveOperationKind = 'jog' \| 'frame' \| 'frameDot' \| 'testFire' \| 'autoFocus' \| 'setOrigin'`. `_activeOperation: ActiveOperationState \| null` + monotonic `_operationSessionCounter`. **Re-entry policy:** same-kind acquire returns true without bumping startedAt/sessionId — preserves the existing `beginTestFire` deadman re-entry pattern. Different-kind acquire returns false. Release is idempotent + warns on kind mismatch. **`ExecutionCoordinator` integration** at every temporary-laser-on entry (jog, runFrame, beginTestFire, endTestFire, deadman timer, autoFocus, setOriginAtCurrentPosition) with try/finally release. `runFrame` uses laserMode-conditional kind ('frameDot' vs 'frame'). FrameResult.reason union widened with 'operation-busy'. Pinned by `tests/operation-mutex-prevents-overlap.test.ts` (46 contracts). Three existing test mocks updated with permissive stubs (`tryAcquireOperation: () => true, releaseOperation: () => {}`): `execution-coordinator-autofocus.test.ts`, `run-frame-fail-fast-on-blocked-command.test.ts`, `frame-dot-finally-emits-m5.test.ts`. Regression sweep clean across 17 coordinator/frame/jog/machine-service test files. **Hardware verification: not required** (refusing operations on contention is intrinsically safe; no g-code emission change). **Unblocks T1-30** which can read `getActiveOperation()` as the `opState === 'none'` source for its centralized command-gate helper. | `24ce00a` |

### 鈼?Partial (3)

| Ticket | What | What's done | What's missing |
|---|---|---|---|
| T2-4 | Split `ConnectionPanelMain.tsx` into service + view | Phases 4-7 shipped: `ExecutionCoordinator` owns frame, jog, autofocus, set-origin, test-fire, emergencyLaserOff, safeDisconnect (`tests/execution-coordinator*.test.ts` covers each phase) | `ConnectionPanelMain.tsx` still 1978 lines. Service-extraction is real but file split is incomplete. ~2-3 more phases needed. |
| T2-12 | Unified MachineSafetyState | Part 1: `laserOutputState` + subscriptions (6 markers). Part 2: `FAULTED_REQUIRES_INSPECTION` + Acknowledge button (15 markers, `tests/execution-coordinator-disconnect.test.ts` etc.) | Full canonical `MachineSafetyState` discriminated union (16 states: DISCONNECTED_UNKNOWN, RUNNING_TEMP_LASER, etc.) not done. Most consequential parts shipped; type-unification "bow on top" remains. |
| T2-77 | Async revision guards | `capturedRevisionId` reserved in `SceneTransaction.ts:53` (scaffolding) | Mid-async cancellation logic not wired; needed for trace, image import |

### 鉁?Open (115)

Grouped by cluster. **Bold = highest leverage / blocks other work.**

#### Reliability architecture (16 tickets)

T2-1 (ValidatedJobTicket 鈥?type exists, contract not enforced), **T2-3** (paywall: `requireFeature` exists in scattered hooks; not service-layer-gated comprehensively), T2-5 (`GcodeTemplateValidator` exists at `src/core/preflight/GcodeTemplateValidator.ts` but not wired into compile pipeline), T2-6 (no Zustand stores), **T2-10** (no MachineCommandGateway), **T2-11** (no operation mutex), T2-13 (no FaultInjectingSerialPort), T2-14, T2-15 (no CompoundPath model), T2-16, T2-17 (no AbortSignal in compile), T2-18, T2-19, T2-20, T2-21 (no fast-check), T2-23, T2-24, T2-25 (no ControllerCapabilities), T2-26, T2-27, T2-28, T2-29.

#### Connection lifecycle (7 tickets)

T2-30 (Falcon WiFi as transport), T2-31 (close async), **T2-32** (no ConnectionManager), T2-33, T2-34 (no generation guard), T2-35, T2-36 (subscription transport callbacks).

#### Safety APIs (10 tickets)

T2-37 (capability snapshot), T2-38 (CapabilityValue<T>), T2-39 (profile validation on save), T2-40 (central op-gating authority), T2-41 (SafetyActionResult typed), T2-42 (ControllerSafetyOps contract), T2-43 (ControllerSafetyCapabilities), T2-44 (extended safety state machine), T2-45 (JobExecutionSession), T2-46 (user-facing safety messages).

#### Test infrastructure (4 tickets)

T2-47 (realistic GRBL simulator), T2-48, T2-49 (virtual time), T2-50 (injectFault API).

#### State machines / Job lifecycle (10 tickets)

T2-51 (CompiledJobState atomic), T2-52 (useSyncExternalStore), T2-53 (JobPhase), T2-54 (unified disconnect), T2-55 (resetProjectRuntimeState), T2-56 (job log finalization), T2-57 (typed errors per domain 鈥?`machineAlarmCode` partial), T2-58 (Ready-to-Run panel), T2-59 (material-first workflow with confidence labels 鈥?UI exists, workflow doesn't), T2-60 (frame freshness invalidation).

#### UX (7 tickets)

T2-61 (design-editing controls out of conn panel), T2-62 (recovery cards: alarm/disconnect/frame-fail/E-stop), T2-63 (operation order preview), T2-64 (beginner/advanced toggle), **T2-65** (central error reporter 鈥?blocks lots of UX work), T2-66 (positionTrusted state), T2-77 (covered above).

#### Persistence (8 tickets)

T2-68 (critical error history preserved across clearMessages), T2-69 (atomic autosave with checksum), T2-70 (previous autosave backup slot), T2-71 (device profile snapshot in project), T2-72 (material preset snapshot per layer), T2-73 (formal migration pipeline), T2-74 (deserializeSceneWithReport), T2-75 (deep geometry validation on load).

#### History (8 tickets)

T2-81 (raster buffers outside history), T2-82 (memory budget), T2-83 (block undo while running), T2-84 (RuntimeState meta-container), T2-85 (JobFingerprint type), T2-86 (FrameState union), T2-87 (RecoveryState machine), T2-88 (hash-derived dirty 鈥?scaffolding referenced).

#### Entitlements (9 tickets)

T2-89 (server-signed entitlement), T2-90 (signed local token public-key verify), T2-91 (FEATURE_MATRIX), T2-92 (per-feature canUse), T2-93 (LicenseStatus enum), T2-94 (clock-tamper detection), T2-95 (real trial model), T2-96 (subscription lifecycle), T2-97 (entitlement never blocks safety).

#### Release engineering (10 tickets)

T2-98 (Win/macOS CI runners), T2-99 (Win code signing), T2-100 (macOS notarization), T2-101 (auto-update with electron-updater), T2-102 (rollback / failed-launch detection), T2-103 (SHA256 + SBOM + signed checksums), T2-104 (versioned user-data migration), T2-106 (CI dependency security scanning), T2-107 (CSP 鈥?currently has `unsafe-inline` and `unsafe-eval` per `electron/main.ts:190,191`).

#### Diagnostics (10 tickets)

**T2-108** (support bundle exporter), T2-109 (reconstruction-grade JobLog), T2-110 (controller settings snapshot before job), T2-111 (persist partial job log), T2-112 (event-window retention), T2-113 (structured RX/TX events), **T2-114** (React error boundary + window error/rejection persistence), T2-115 (privacy redaction), T2-116 (storage health/quota), T2-117 (correlation IDs), T2-118 (Help 鈫?Diagnostics panel).

#### Security (10 tickets)

T2-119 (assertTrustedSender on every IPC handler), T2-120 (typed namespaced storage IPC), T2-121 (main-process command classification), T2-122 (typed serial command IPC), T2-123 (SVG complexity limits), T2-124 (image pre-decode pixel limits), T2-125 (compiler enforces template validation), T2-126 (Falcon WiFi as untrusted telemetry), T2-127 (storage value size limits), T2-128 (per-namespace storage authorization).

---

## Tier 3 鈥?2 shipped, 87 open

### 鉁?Shipped (2)

| Ticket | What | Evidence | Hash |
|---|---|---|---|
| T3-1 | Autosave to IndexedDB / filesystem | `src/core/storage/{IndexedDb,Filesystem}StorageAdapter.ts` + `bootstrap.ts` picks per-runtime | pre-session |
| T3-82 | Production bundle smoke tests | `scripts/verify-production-build.mjs` with broader pattern library (auto-Pro unlock literal, legacy tester HMAC, debug API leakage `__forceProUnlock`/`__entitlementService`, mock entitlement leakage, vitest leakage, source map references); 22 markers in code | `de3fbc7` |

### 鉁?Open (87)

T3-2, T3-3, T3-4 (Win/macOS code signing), T3-5 (auto-update channel), T3-6 (crash reporting), T3-7 (backward-compat fixture corpus), T3-8 (Electron CSP hardening 鈥?duplicates T2-107), T3-9 (IPC attack surface), T3-10 (input file-format size limits), T3-11 (burn-progress visual bugs), **T3-12** (hardware-in-the-loop safety verification suite), T3-13 (active-edge-table fill scanline), T3-14 (sampled G-code preview), T3-15 (spool-based G-code AsyncIterable streaming), T3-16 (WebSerial cable-pull recovery), T3-17 (Wi-Fi safety model), T3-18 (output validator semantic scan), T3-19, T3-20, T3-21 (frame-dot F3000 hardcoded), T3-22, T3-23, T3-24, T3-25, T3-26, T3-27, T3-28, T3-29, T3-30, T3-31, T3-32, T3-33, T3-34, T3-35, T3-36, T3-37, T3-38, T3-39, T3-40, T3-41, T3-42, T3-43, T3-44, T3-45, T3-46, T3-47, T3-48, T3-49, T3-50 (device identity verification on connect), T3-51, T3-52, T3-53, T3-54, T3-55, T3-56, T3-57, T3-58, T3-59, T3-60, T3-61, T3-62, T3-63, T3-64, T3-65, T3-66, T3-67 (canonical bounds selectors), T3-68 (debug state graph + transition log; scaffolding ready in `SceneTransaction.ts:94,95,152-154,271,283`; emitter not wired), T3-69, T3-70, T3-71, T3-72, T3-73, T3-74, T3-75, T3-76, T3-77, T3-78, T3-79, T3-80, T3-81, T3-83, T3-84 (Linux packaging 鈥?only if business decides), T3-85 (installer QA matrix), T3-86 (native module packaging smoke test 鈥?referenced from T1-86 as future work), T3-87, T3-88 (IPC fuzz suite), T3-89 (production security build CI checks).

---

## Tier 4 鈥?2 shipped, 7 open

### 鉁?Shipped (2)

| Ticket | What | Evidence | Hash |
|---|---|---|---|
| T4-1 | Starter material preset library | `src/core/materials/defaultPresets.ts` has 10 presets: `preset-birch-3mm`, `preset-mdf-3mm`, `preset-acrylic-3mm`, `preset-cardboard`, `preset-leather-2mm`, `preset-anodized-aluminum`, `preset-slate-stone`, `preset-cork-3mm`, `preset-paper-cardstock`, `preset-fabric-cotton` | pre-session |
| T4-8 | Import formats 鈥?DXF | `src/import/dxf/` with `importDxfIntoScene` wired into `src/ui/hooks/useImport.ts:255` and `src/ui/components/FileToolbar.tsx:19` | pre-session |

### 鉁?Open (7)

T4-2 (kerf planner UI 鈥?beyond the kerf compensation already shipped in box generator), T4-3 (inline coaching), T4-4 (Know-Why engine), T4-5 (Honest Mode), T4-6 (D.13 Phase 2 calibration), T4-7 (Marlin controller stub), T4-9 (material feedback portability).

---

## Audit advisories status

Started at **15 advisories** (2 low, 2 moderate, 11 high) per prior session. After audit work:

- **Cleared (14):** xmldom upgrade, dompurify upgrade, electron-builder cluster (multiple commits including major bump), postcss override (commits `93b305b` 鈥?`0046a04`)
- **Remaining (1):** Electron 34鈫?1 major bump 鈥?deferred (full-session smoke surface, would need re-running the entire app + IPC + dialog test set)

---

## Cross-audit commit ledger (most recent activity)

The arc that closed Gate 1:

| Commit | What |
|---|---|
| `93b305b` | xmldom + dompurify upgrade |
| (cluster) | electron-builder major bump |
| `0046a04` | postcss override |
| `a1bb80f` | T2-67 failed_to_start enum (closes T1-87 stopgap) |
| `37bd775` | T1-88 hotfix |
| `b6a56ed` | T2-105 hidden source maps (closes T1-83 stopgap) |
| `de3fbc7` | T3-82 bundle verifier broadened (closes T1-81 stopgap) |
| `e6874af` | T2-80 slider coalescing (closes T1-74 stopgap aspect) |
| `2600666` | T1-24 error/alarm laser-off |
| `b0375fa` | T1-20 WCS placement-uncertain gate |
| `ea0f750` | chore: ImageProcessing.ts return types |

The arc post-Gate-1 (this session, off-roadmap, exception):

| Commit | What |
|---|---|
| `b7ac61a` | T2-12 part 2: faulted controller state |
| `c5c0795` | App.tsx label cleanup |
| `16e00b3` | Frame with crosshair |
| `d33428b` | Box generator joinery rewrite + Test Fire M3+5% |
| `5450df8` | Kerf compensation |
| `c385e06` | GRBL console panel |
| `3d61f58` | Kerf presets |
| `7fb7b7f` | $32=0 auto-detect banner |
| (pending) | Inside-vs-outside box dimensions (paste-ready, awaiting push) |

After the inside-vs-outside ship, all subsequent commits follow the strict roadmap rules in `.cursor/rules/laserforge.md`.

---

## Next 5 tickets in strict roadmap order

1. **T1-105** — Jog `hasJogged.current = true` only after jog command accepted; Set Origin `savedOrigin` + localStorage only after G10 confirmed. ChatGPT items #3 + #4. Pattern fix: changes `executionCoordinator.jog` return shape and adds success/failure return to `setOriginAtCurrentPosition`. Pairs with T1-104 (which gates the click) to fully close the "UI state updates before hardware success" hole on Jog and Set Origin. **Hardware verification recommended.**
2. **Controller `safetyOff()` propagation to MachineService laser state.** ChatGPT item #11. Largest of the remaining safety items; high priority. May need scope-decision before ticket assignment.
3. **T1-17 pass 4b** — JobCompiler consumes pre-processed `adjustedData` when present.
4. **T1-17 pass 4c** — UI pipes brightness/contrast/gamma/invert through worker on slider drag.
5. **T1-19 static guard** — `tests/no-direct-controller-sendcommand-from-ui.test.ts`.

After those close, every Tier 1 ticket I have evidence for is shipped. We then enter Tier 2, where the audit identifies these as highest leverage:

- T2-3 (widen service-layer paywall gating)
- T2-65 (central error reporter 鈥?unblocks T2-114 error boundary and lots of UX cleanup)
- T2-12 finish (close out canonical safety state union)
- T2-10 (MachineCommandGateway choke point 鈥?unblocks consistent gating across Tier 2)
- T2-11 (operation mutex 鈥?unblocks T2-12 full canonical type)

---

## Caveats & known limits of this audit

- Tier 1 tickets in the range T1-2, T1-3, T1-4, T1-26 through T1-58 (excluding the explicitly verified ones) were not individually re-probed in this audit. The prior session's batch audit covered most of them; spot-check before assuming any specific T1-X is shipped.
- "Partial" classifications are subjective. Bias is conservative: if a ticket has clear remaining scope, it's partial.
- Tickets that ship as a side effect of higher-priority work aren't credited explicitly. Example: T2-3 widening `requireFeature` use will likely close T2-91 (FEATURE_MATRIX) too; that won't show up here until the later commit explicitly addresses it.
- Future tickets that are scaffolded but not wired (T2-77 with `capturedRevisionId`, T2-88 referenced as future, T3-68 with `transitionLog` plumbed but no emitter) are counted as **open**, not partial. Scaffolding has value but it's preparation, not shipping.
- The Tier 2 / Tier 3 / Tier 4 numbers above are lower bounds. As I find more work shipped without explicit T-markers, the shipped column grows.
- Hash columns marked "pre-session" mean: shipped before the audit window; commit hash not in my context. Not a quality issue; just unrecoverable from this static probe.

