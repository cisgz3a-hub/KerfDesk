# Image, crop and adjustment remediation

Base: `ccaa3064d9efe904821307f0603ce842d903b586`.
Branch: `codex/feature-quality-images-20260905`.
Scope: F11-01, F12-01, F12-02, F12-03, F12-04, F13-01 and F13-02 from the September 5 feature code-quality audit. All seven are addressed in this lane.

| Finding | Result                                                                                                                                                                                                                                                                                                                                                    | Acceptance evidence                                                                                                                                                                                                                                                                                                                                                      |
| ------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| F11-01  | Crop hydrates paged luminance with the existing leased reader, crops the actual pixels and removes obsolete asset metadata. Embedded sources missing valid luminance are decoded; unavailable or inconsistent source pixels produce an error instead of a white crop. Existing source/mask/document ownership checks still enclose all asynchronous work. | `image-pixel-authority.test.ts`: paged pixel values, dimensions, physical bounds, lease release, missing-page failure, project undo/redo and save/reopen. `crop-image.test.ts`: embedded recovery, missing source, inconsistent dimensions and ordinary crop geometry. Existing `image-command-actions.test.ts`: stale source, mask and document results cannot publish. |
| F12-01  | Successful editor Undo and Redo mark the session dirty since Apply; no-op history actions preserve the original session.                                                                                                                                                                                                                                  | `editor-session.test.ts` and `image-editor-lifecycle.test.ts`: Apply → Undo → Apply → Redo → Apply produces the intended three pixel revisions and three project undo entries.                                                                                                                                                                                           |
| F12-02  | Eraser honors the background color chosen by the store rather than overriding it with white.                                                                                                                                                                                                                                                              | `editor-session.test.ts` and `image-editor-store.test.ts`: selected RGB background reaches the pixel buffer and round-trips through undo/redo. Existing Quick Mask tests pass.                                                                                                                                                                                           |
| F12-03  | Apply transitions a paged raster to one authoritative embedded bitmap. The original paged image remains referenced by project undo; ready asset deletion policy is unchanged.                                                                                                                                                                             | `image-pixel-authority.test.ts`: original and resized dimensions, no obsolete asset metadata, hydration bypass, source-file reopening, `.lf2` round trip, processed-preview and emitted-G-code equivalence to an independent embedded fixture, undo/redo with retained page references.                                                                                  |
| F12-04  | PNG input and derived luminance use the same copied pixels synchronously, before either encoding await can admit a later editor revision.                                                                                                                                                                                                                 | `image-editor-decode.test.ts`: hold `toBlob`, mutate the original document, then complete encoding; both recorded PNG input and emitted luminance retain the original pixels. Existing lifecycle tests preserve later edits as dirty and reject stale owners.                                                                                                            |
| F13-01  | Enter on Cancel, Reset and OK retains each focused button's native action. Enter from a numeric field commits once and prevents the default action.                                                                                                                                                                                                       | `AdjustDialog.test.tsx`: rendered production dialog, focused button keydown followed by native-click simulation, numeric-field Enter and Escape. Cancel/Reset leave pixels and history unchanged.                                                                                                                                                                        |
| F13-02  | Adjustment preview always passes through the shared layer compositor, including singleton layers with opacity, visibility or blend changes.                                                                                                                                                                                                               | `editor-adjust-session.test.ts`: expected half-opacity/hidden/multiply pixels equal the committed composite while preview leaves the working document unchanged.                                                                                                                                                                                                         |

## Verification

Dependencies installed with `pnpm install --offline --frozen-lockfile --ignore-scripts`.

Focused command, exit 0: **48 files, 324 tests passed**.

```text
pnpm exec vitest run src/ui/image-editor src/ui/raster/crop-image.test.ts src/ui/raster/image-pixel-authority.test.ts src/ui/state/image-edit-actions.test.ts src/ui/state/image-mask-actions.test.ts src/ui/import/paged-raster-hydration.test.ts src/ui/import/paged-raster-source.test.ts src/ui/import/paged-raster-asset-lifecycle.test.ts src/ui/commands/image-command-actions.test.ts
```

Additional checks passed: `pnpm typecheck`; ESLint for all changed TypeScript/TSX files; Prettier for all changed source, tests and this note; `git diff --check`.

The original audit probes asserted observed defects. The committed regressions assert the corrected invariants. The first acceptance run corrected three test-harness assumptions: jsdom File uses FileReader rather than `.text()`, the existing brightness operation maps white at -100 to 127, and the bake snapshot case uses ordinary opaque source pixels. No product behavior was changed to satisfy those assumptions.

## Boundaries

This is code, jsdom and deterministic output evidence. jsdom does not perform a real PNG encode or synthesize native Enter-to-click activation; those seams are explicitly captured/simulated in the tests. No live-browser, material, air-cut or hardware qualification was performed in this lane. No Start/Frame policy, output gates, provider, deployment, asset garbage collection or large-file persistence schema changed. Existing unedited paged `.lf2` files retain their prior origin-local storage limitation; edited images become embedded at Crop/Apply.
