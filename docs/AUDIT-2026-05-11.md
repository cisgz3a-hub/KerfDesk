# LaserForge full-code audit — 2026-05-11

**Auditor:** Claude Opus 4.7 (1M context), single-pass.
**Scope:** all of `src/`, `electron/`, `tests/`, `scripts/`, `docs/`.
**Method:** see `docs/AUDIT-PROMPT.md`.
**Repo state at start:** master @ `813367a` (post-T2-6 Phase 3w).

This document is the living findings ledger. Findings land as
`F-NNN` blocks in the appropriate sector. The progress table tracks
which sectors have been completed.

## Progress table

| Phase | Sector | Status | Findings |
|---|---|---|---|
| 0 | Project survey | ✅ done | 0 findings |
| 1 | Controllers + communication | ✅ done | 0 crit, 2 high, 3 med, 3 low, 1 info |
| 2 | MachineService + ExecutionCoordinator + safety state | ✅ done | 0 crit, 0 high, 2 med, 5 low, 1 info |
| 3 | Preflight rules | ✅ done | 0 crit, 0 high, 1 med, 3 low, 1 info |
| 4 | Output (G-code emission) | ✅ done | 0 crit, 0 high, 0 med, 4 low, 2 info |
| 5 | Pipeline math | ✅ done | 0 crit, 0 high, 1 med, 2 low, 1 info |
| 6 | Scene model + geometry | ✅ done | 0 crit, 0 high, 0 med, 1 low, 1 info |
| 7 | Persistence + IO | ✅ done | 0 crit, 0 high, 0 med, 2 low |
| 8 | Entitlements + materials | ✅ done | 0 crit, 0 high, 1 med, 1 low |
| 9 | Falcon WiFi | ✅ done | 0 crit, 0 high, 0 med, 1 low, 1 info |
| 10 | Electron shell | ✅ done | 0 crit, 0 high, 0 med, 1 low, 1 info |
| 11 | UI orchestrator | ✅ done | 0 crit, 0 high, 0 med, 1 low, 1 info |
| 12 | UI surfaces | ✅ done | 0 crit, 0 high, 0 med, 2 low, 1 info |
| 13 | Import / export | ✅ done | 0 crit, 0 high, 0 med, 1 low, 1 info |
| 14 | Debug / runtime | ✅ done | 0 crit, 0 high, 1 med, 1 low |
| 15 | Tests audit | ✅ done | 0 crit, 0 high, 1 med, 1 low, 1 info |
| 16 | Build + scripts | ✅ done | 0 crit, 0 high, 0 med, 0 low, 3 info |
| 17 | Cross-cutting scans | ✅ done | 0 crit, 0 high, 1 med (systemic) |
| 18 | Documentation | ✅ done | 0 crit, 0 high, 0 med, 1 low, 1 info |
| 19 | Executive summary | ✅ done | — |

## Severity counts (FINAL)

- **Critical: 0**
- **High: 2** (F-002 stale-callback race, F-004 ForceSafeState unwired)
- **Medium: 11** (F-001, F-003, F-005, F-010, F-011, F-018, F-029, F-037, F-050, F-052, F-058)
- **Low: ~25** (mixed: stale docs, magic numbers, listener leaks, any-typed catches, observability gaps)
- **Info / positive: ~23**
- **Total: 61 findings (F-001 — F-061)**

---

## Phase 0 — Project survey

### Codebase shape

- `src/`: **436 .ts/.tsx files**, **87,512 lines** in 50+ directories.
- `electron/`: **14 files**, 2,058 lines (main process + Falcon WiFi bridge).
- `tests/`: **593 test files** (every test in its own process — `scripts/run-tests.mjs`).

### Largest files (the God components, mostly already audited via T1-132…T1-160 + T2-6 phases)

| Lines | File |
|---|---|
| 2564 | `src/ui/components/ConnectionPanelMain.tsx` |
| 2520 | `src/controllers/grbl/GrblController.ts` |
| 1962 | `src/ui/components/App.tsx` |
| 1861 | `src/ui/components/CanvasViewport.tsx` |
| 1852 | `src/app/MachineService.ts` |
| 1475 | `src/ui/components/PropertiesPanel.tsx` |
| 1237 | `src/ui/renderers/SceneRenderer.ts` |
| 1214 | `src/ui/components/LayerPanel.tsx` |
| 1054 | `src/core/job/JobCompiler.ts` |
| 979  | `src/core/plan/PlanOptimizer.ts` |

### Sectors by file count

- `src/ui/components`: 57 files (UI is the biggest sector)
- `src/app`: 45 files (service layer)
- `src/ui/hooks`: 30 hooks
- `src/ui/components/connection`: 20 sub-components
- `src/core/preflight/rules`: 15 rule modules
- `src/core/job` / `src/core/plan`: 14 + 18 pipeline files
- `src/controllers/grbl`: 14 controller-side modules
- `src/diagnostics`: 12 files (logging / metrics / crash)

### Survey-level signals

- **No empty `.ts`/`.tsx` files** under `src/`. Good — no zero-byte stubs left in tree.
- **Only 2 files** flagged by a fast TODO/FIXME grep: `TrialGuard.tsx` and `FillGenerator.ts` (1 mention each). The codebase has actively converted TODOs to roadmap tickets per the workflow contract.
- **No `@ts-ignore` / `@ts-expect-error` / `@ts-nocheck`** in any `src/` file. Type safety is strictly enforced — confirmed at zero suppressions.
- **`any`/`as any` hotspots**: `src/io/SceneSerializer.ts` (10) leads. Most are in deserialization / `parsed: any` envelope handling — a defensible use of `any` for JSON deserialization. Other occurrences scattered across UI components for DOM event handling.

### Notable directories worth flagging early

- `src/diagnostics/` (12 files) — never audited in any prior ticket. Phase 14 will cover.
- `src/integrity/`, `src/onboarding/`, `src/security/`, `src/storage/`, `src/transports/`, `src/types/` — small specialized directories worth a quick pass each.
- `src/import/dxf/` exists but has been unfeatured in roadmap discussion — file count needed.

### Findings

_No code-level findings in Phase 0. This is the survey pass — findings start in Phase 1._

**Phase 0 done.**

---

## Phase 1 — Controllers + communication

### Files audited

- `src/controllers/ControllerInterface.ts` (424 lines) — protocol contract
- `src/controllers/grbl/GrblController.ts` (2520 lines) — main controller (read in full)
- `src/controllers/grbl/CommandClassifier.ts` (110 lines)
- `src/controllers/grbl/ForceSafeState.ts` (151 lines)
- `src/controllers/grbl/GrblFrameGcode.ts` (131 lines)
- `src/controllers/grbl/streamingHealth.ts` (89 lines)
- `src/controllers/grbl/GrblStatusReportParser.ts`, `GrblSettingsParser.ts`, `GrblWcsParser.ts`, `GrblSafeStateClassifier.ts`, `GrblJobLineParser.ts`, `GrblJobBoundsChecker.ts`, `GrblIdentityParser.ts`, `GrblWcsConsentClassifier.ts`, `GrblUnsafeAtConnect.ts` — all already extracted and pinned by tests in T1-124 / T1-126…T1-128 / T1-134 / T1-137 / T1-139 / T1-152 / T1-153 (covered in prior God-component decomposition work; spot-checked, no findings)
- `src/communication/SerialPort.ts` (377 lines)
- `src/communication/WebSerialPort.ts` (590 lines)
- `src/communication/ConnectionGenerationGuard.ts` (145 lines)
- `src/communication/TransportSubscription.ts` (144 lines)
- `src/communication/SerialPortFactory.ts` (22 lines) — trivial dispatcher

### F-001: `ControllerInterface.setStopOnError` signature doesn't surface the T1-116 token requirement

- **Severity:** Medium
- **Category:** Type-safety / API surface
- **Location:** `src/controllers/ControllerInterface.ts:399`, `src/controllers/grbl/GrblController.ts:520`
- **Evidence:**
  ```ts
  // ControllerInterface.ts:399
  setStopOnError?(value: boolean): void;
  ```
  ```ts
  // GrblController.ts:520
  setStopOnError(value: boolean, token?: UnsafeStopOnErrorOverrideToken): void {
    if (value === false && !isUnsafeStopOnErrorOverrideToken(token)) {
      throw new Error(...);
    }
    this._stopOnError = value;
  }
  ```
- **Analysis:** The implementation requires a non-empty-reason `UnsafeStopOnErrorOverrideToken` whenever `value === false`. The interface declares only `(value: boolean) => void`. A caller wired against the interface alone has no compile-time signal that the token is required — they could write `controller.setStopOnError(false)` and only learn at runtime (via a thrown `Error`) that the override gate exists. T1-116 added the runtime gate but didn't propagate the requirement into the interface signature.
- **Recommendation:** Update `ControllerInterface.setStopOnError` to `setStopOnError?(value: boolean, token?: UnsafeStopOnErrorOverrideToken): void` (after exporting the token type into a shared location, or accepting a structural shape). Lower priority because the runtime gate is the load-bearing defense, but the type signal would surface the contract at compile time.

### F-002: ConnectionGenerationGuard primitive shipped but never wired into transports

- **Severity:** High
- **Category:** Concurrency / Safety
- **Location:** `src/communication/ConnectionGenerationGuard.ts:19-23` (the "T2-34-followup" note), `src/communication/WebSerialPort.ts:52-54` (single-slot callbacks unchanged), `src/controllers/grbl/GrblController.ts:548+` (connect path registers callbacks without a generation tag)
- **Evidence:**
  ```ts
  // ConnectionGenerationGuard.ts:18-23
  // T2-34 ships the pure primitive (allocator + token + isStale check
  // + guard wrapper) so transport implementations and controllers can
  // adopt it without coordinating on a single connectionId source.
  // Wiring `WebSerialPort.onData/onError/onClose` + GrblController to
  // accept and forward `TransportCallbackContext` is filed as
  // T2-34-followup.
  ```
  Grep confirms no consumer:
  ```
  src/communication/TransportSubscription.ts        (mentions the type only)
  src/communication/ConnectionGenerationGuard.ts    (the module itself)
  ```
- **Analysis:** Audit 3B sections 9.4 + 8.4 + 3.8 identified the rapid disconnect/reconnect race: a stale microtask-queued read-loop event from a previous connection can fire against the new controller's callback because the single-slot callback was replaced. The fix primitive (`ConnectionGenerationAllocator`, `TransportCallbackContext`, `isStaleContext`, `withGenerationGuard`) shipped in T2-34. Production code paths (`WebSerialPort` + `GrblController`) have NOT adopted it. **This is exactly the "wired-into-product gate" violation CLAUDE.md T1-120 was added to prevent** — and it remained outstanding through 28 follow-up tickets without being retrofitted. Safety impact: a rapid reconnect could deliver a previous-connection RX line to the new controller, causing stale `<...>` status reports to influence safety-state classification at the moment a fresh controller is computing its safe-state-at-connect verdict.
- **Recommendation:** File `T2-34-followup` as a P0 retrofit ticket. Wire `WebSerialPort.onData/onError/onClose` to issue ctx-tagged callbacks; have `GrblController.connect` allocate a token and drop any RX events whose ctx generation doesn't match.

### F-003: TransportSubscription multi-listener primitive shipped but never wired

- **Severity:** Medium
- **Category:** Design / Concurrency
- **Location:** `src/communication/TransportSubscription.ts:17-22` (the "T2-36-followup" note), `src/communication/WebSerialPort.ts:52-54` (still single-slot)
- **Evidence:**
  ```ts
  // TransportSubscription.ts:17-22
  // T2-36 ships a generic SubscriptionSet primitive + an Unsubscribe
  // type so transport implementations can swap the single-slot
  // fields for sets without rebuilding the publish loop on every
  // adopting site. Wiring `WebSerialPort` + `MockSerialPort` is filed
  // as T2-36-followup.
  ```
- **Analysis:** Same wired-into-product violation as F-002. Less safety-critical because the current single-listener architecture works for the current single-controller design, but it blocks T2-34 wiring (which needs multi-listener subscription) and prevents observability features (audit log of raw lines + simulator mirror + lifecycle monitor) from being added without invasive surgery.
- **Recommendation:** Retrofit alongside F-002.

### F-004: ForceSafeState destructive-recovery primitive shipped but never wired

- **Severity:** High
- **Category:** Safety / API surface
- **Location:** `src/controllers/grbl/ForceSafeState.ts:11-17` (the "T2-129-followup" note), grep confirms `evaluateForceSafeState` / `shouldOfferForceSafeState` / `forceSafeStateFailureMessage` / `forceSafeStateConfirmation` are not called from any production module.
- **Evidence:**
  ```ts
  // ForceSafeState.ts:2-17
  // T2-129: destructive `forceSafeState()` primitive — operator-
  // initiated force-safe-state recovery + T1-29 acknowledgement
  // integration. Pre-T2-129 a user who hit an alarm at end-of-job had
  // no in-app affordance to reset; they had to switch to the GRBL
  // console + send `$X` / soft-reset / cycle-start, or disconnect +
  // power-cycle. Audit 1D required fix (the destructive half of T1-25's
  // non-destructive observation pair).
  //
  // T2-129 ships the result type + the pure outcome classifier + the
  // post-reset status validator. Wiring this onto `GrblController`
  // (the actual `writeByteCritical(REALTIME_RESET)` + banner-wait +
  // status-query orchestration) is filed as T2-129-followup ...
  ```
- **Analysis:** Operator-facing recovery primitive is documented but the actual recovery button cannot exist until the controller-side orchestration ships. An end-of-job alarm leaves the user unable to recover in-app — they must switch to a separate GRBL console or power-cycle. Audit 1D required the fix; the primitive is half-done.
- **Recommendation:** File `T2-129-followup` as P1 retrofit. Hardware verification required because the orchestration calls `writeByteCritical(REALTIME_RESET)` and the recovery sequence must be tested against a real Falcon A1 Pro.

### F-005: 12+ other "T2-*-followup" framework-only files outstanding

- **Severity:** Medium
- **Category:** Design / wired-into-product
- **Locations** (grep of `T[12]-[0-9]*-followup` in safety-touching directories):
  - `src/controllers/CapabilityValue.ts:16` → T2-38-followup
  - `src/controllers/ControllerCapabilities.ts:12` → T2-25-followup
  - `src/controllers/ControllerSafetyCapabilities.ts:11` → T2-43-followup
  - `src/controllers/ControllerSafetyOps.ts:24` → T2-42-followup
  - `src/app/AutosaveBackupSlot.ts:16` → T2-70-followup
  - `src/app/CompiledJobState.ts:17` → T2-51-followup
  - `src/app/CriticalEventStore.ts:16` → T2-68-followup
  - `src/app/DisconnectTransaction.ts:24` → T2-54-followup
  - `src/app/DomainErrorState.ts:18` → T2-57-followup
  - `src/app/ErrorReporter.ts:16` → T2-65-followup
  - `src/app/FrameState.ts:13` → T2-86-followup
  - `src/app/JobExecutionSession.ts:18` → T2-45-followup
  - `src/app/JobLogCheckpoint.ts:11` → T2-111-followup
  - `src/app/JobLogEventWindow.ts:13` → T2-112-followup
  - `src/app/JobSession.ts:21` → T2-53-followup
  - `src/app/MachineSafetyState.ts:13` → T2-12-followup
  - `src/app/OperationGate.ts:23` → T2-40-followup
  - `src/app/OperationOrder.ts:17` → T2-63-followup
- **Analysis:** Systemic issue: a sweep of just `src/controllers/` and `src/app/` finds 18 modules that ship framework primitives marked "wiring is filed as T*-followup." T1-119 / T1-122 / T1-123 retrofitted three of these (MigrationPipeline, RecoveryState, FalconWiFiTrust); the rest are still type-only. This means there's a meaningful population of code paying maintenance cost without delivering user-visible behavior, and the audit findings from earlier sprints are not closed.
- **Recommendation:** Inventory each `T2-*-followup` and decide per-ticket: (a) ship wiring now, (b) delete the framework if the design has shifted, or (c) keep it but mark explicitly as "deferred indefinitely." The current state — quietly shipped without wiring — is the worst of both worlds.

### F-006: `WebSerialPort` read loop catches `any` and accesses `e.message` unguarded

- **Severity:** Low
- **Category:** Robustness / Type-safety
- **Location:** `src/communication/WebSerialPort.ts:532-536`
- **Evidence:**
  ```ts
  } catch (e: any) {
    if (this._readLoopActive) {
      this._errorCallback?.(new Error(`Read error: ${e.message}`));
    }
  }
  ```
- **Analysis:** If `_reader.read()` rejects with something that isn't an `Error` (a `Symbol`, a thrown primitive, `null`), the `.message` access throws `TypeError: Cannot read properties of null`. The whole read loop unwinds without invoking the error callback. In practice browsers reject with `DOMException` (which is Error-like), so this rarely fires — but the guard exists everywhere else in the file (`e instanceof Error ? e.message : String(e)`) and was skipped here.
- **Recommendation:** Replace with `const message = e instanceof Error ? e.message : String(e);`. Trivial fix.

### F-007: `GrblController.disconnect` uses a hardcoded 50 ms delay before M5

- **Severity:** Low
- **Category:** Robustness
- **Location:** `src/controllers/grbl/GrblController.ts:790-792`
- **Evidence:**
  ```ts
  this._sendRealtime(REALTIME_FEED_HOLD);
  await new Promise(r => setTimeout(r, 50));
  this._port.write('M5 S0\n');
  ```
- **Analysis:** The 50 ms is the gap between sending feed-hold (realtime byte 0x21) and the subsequent M5. If firmware response is slower than 50 ms, the M5 might enter the planner while motion is still decelerating. The port closes shortly after, so the M5 may or may not have been transmitted before close. In practice this is "best effort" disconnect behavior with feed-hold as the load-bearing safety stop, but the magic 50 ms isn't documented or hardware-verified at the cited point.
- **Recommendation:** No urgent action. If we ever see "laser stayed on after disconnect" reports on a slow controller, the fix is to await a status report confirming hold or extend the delay.

### F-008: `GrblController.sendCommand` ignores its `source` parameter — gateway is the only enforcement

- **Severity:** Info
- **Category:** API surface / Design
- **Location:** `src/controllers/grbl/GrblController.ts:1240-1243`
- **Evidence:**
  ```ts
  /**
   * `source` is reserved (audit / future enforcement); the controller only
   * enforces line shape and job-stream rules.
   */
  sendCommand(command: string, _source: 'internal' | 'user' = 'internal'): void {
  ```
- **Analysis:** The controller's `_source` parameter is unused (`_` prefix). All user-vs-internal gating happens at `MachineCommandGateway` in `src/app/`. A caller that bypasses the gateway can call `ctrl.sendCommand('M5', 'user')` and the controller will execute it. The pre-existing test `tests/no-direct-sendcommand-outside-gateway.test.ts` is the source-pin that catches this — and I confirmed earlier it currently flags `MachineService:1298` (the auto-M5 path from T3-90) as a known violation.
- **Recommendation:** No action — design intent. The audit pin is the right defense. Note for future: if a refactor moves the auto-M5 path through the gateway, the test pin will turn green and become a forever-pin.

### F-009: `_handleStatusReport` silently drops malformed status reports

- **Severity:** Low
- **Category:** Robustness
- **Location:** `src/controllers/grbl/GrblController.ts:1936-1937`
- **Evidence:**
  ```ts
  const parsed = parseGrblStatusReport(raw);
  if (parsed.stateWord === null) return;
  ```
- **Analysis:** If the status-report parser returns `stateWord === null` (malformed `<...>` envelope), the function returns without firing any listener and without logging anything. The polling loop will keep trying, but no observability surface tells the user / support that something is off. If the parser had a bug, we'd silently lose status updates.
- **Recommendation:** Add a `console.warn` (rate-limited, since this can fire on every poll) when a status line is received but `stateWord === null` — small cost, large debuggability gain. Alternatively, fire `_errorListeners` with a soft code.

### Summary

Phase 1 surfaced **9 findings**: 0 critical, 2 high (F-002, F-004), 4 medium (F-001, F-003, F-005, plus issues in F-001), 2 low (F-006, F-007, F-009), 1 info (F-008).

The dominant theme: **multiple framework-only modules shipped without their wiring follow-ups completed**. The two safety-relevant ones (ConnectionGenerationGuard / ForceSafeState) deserve P0/P1 retrofit tickets. The remaining 12+ deserve a per-ticket triage to decide retrofit/delete/document.

**Phase 1 done.**

---

## Phase 2 — MachineService + ExecutionCoordinator + safety state

### Files audited

- `src/app/MachineService.ts` (1852 lines, the entire service surface read across multiple passes)
- `src/app/ExecutionCoordinator.ts` (524 lines, read in full)
- `src/app/MachineCommandGateway.ts` (103 lines, read in full)
- `src/app/SafetyStateMachine.ts` (230 lines, read in full)
- `src/app/SafetyActionResult.ts` (189 lines, read in full)
- `src/app/MachineSafetyState.ts` (267 lines, top sections read)
- `src/app/machineServiceHelpers.ts` (already audited in T1-145 / T1-155)
- `src/app/validateJobTicket.ts` (already audited in T1-135)
- `src/app/approvalNonceStore.ts` (already audited in T1-136)

### F-010: `MachineService._armAutoM5OnConnect` bypasses MachineCommandGateway

- **Severity:** Medium
- **Category:** Safety / Design / API surface
- **Location:** `src/app/MachineService.ts:1268`
- **Evidence:**
  ```ts
  // MachineService.ts:1265-1276 (inside _armAutoM5OnConnect callback)
  try {
    const verdict = getUnsafeAtConnect();
    if (verdict === null) {
      ctrl.sendCommand('M5 S0', 'internal');  // ← direct call, not via gateway
    }
  } catch {
    /* ignore: auto-M5 is defense-in-depth ... */
  }
  ```
  Confirmed by the existing source-pin test `tests/no-direct-sendcommand-outside-gateway.test.ts` which currently fails on master.
- **Analysis:** T3-90 added the post-connect auto-M5 defense-in-depth path but called `ctrl.sendCommand` directly instead of routing through `new MachineCommandGateway(...).sendCommand(...)`. The gateway is the load-bearing classifier for user commands and the future audit-log injection point; bypassing it means any future enforcement added at the gateway layer (rate-limiting, logging, etc.) won't see this M5. The path is `source: 'internal'` so the user-command-classification flow doesn't apply, but the no-bypass invariant test exists precisely to prevent the architecture from eroding. The test failure has been on master through 28+ subsequent ticket commits — nobody has filed a follow-up.
- **Recommendation:** Route through the gateway: replace with `new MachineCommandGateway(ctrl).sendCommand('M5 S0', 'internal')`. The gateway short-circuits source='internal' to a passthrough — no behavior change, and the source-pin test turns green.

### F-011: `MachineService.disconnect` swallows `operations.laserOff` errors without `notifyLaserSafetyOutcome`

- **Severity:** Medium
- **Category:** Safety / Robustness
- **Location:** `src/app/MachineService.ts:1547-1550`
- **Evidence:**
  ```ts
  if (ctrl) {
    try {
      await ctrl.operations.laserOff();
    } catch {
      /* not connected, buffer full, or port already gone */
    }
    ...
  }
  ```
- **Analysis:** Compare with the test-fire / frame-dot paths which awaited `ctrl.operations.laserOff()` and then routed the structured result through `this.notifyLaserSafetyOutcome(stage)` to update the safety-state machine. The disconnect path discards both success and failure paths — the safety-state machine doesn't see the laser-off attempt, so a disconnect-time M5 failure won't escalate `_laserOutputState` to `'unknown'`. The comment claims the errors are "not connected, buffer full, or port already gone" — only the first is actually safe to swallow; the others are real safety events.
- **Recommendation:** Capture the result and route the stage through `notifyLaserSafetyOutcome` before falling through to `ctrl.disconnect()`.

### F-012: Stale doc strings claim partial migration that's actually complete

- **Severity:** Low
- **Category:** Documentation
- **Location:** `src/app/SafetyActionResult.ts:14-21`, `src/app/SafetyStateMachine.ts:16-19`
- **Evidence:**
  - `SafetyActionResult.ts:14-21`: "This commit ships the type + a focused MVP migration for `MachineService.stopAndEnsureLaserOff`. The other safety methods (pause / resume / disconnect / emergencyStop) stay void for now ... T2-41-followup migrates the remaining methods one-by-one" — but `GrblController.pause/resume/stop/emergencyStop` all return `SafetyActionResult` today (lines 1020/1046/1104/1120).
  - `SafetyStateMachine.ts:17-19`: "Wiring the machine into MachineService is filed as T2-44-followup" — but `MachineService._recordSafetyResult` calls `transitionFromSafetyResult` at line 1300 today.
- **Analysis:** Stale doc strings give a wrong picture of what's wired vs filed. Reduces F-005's apparent backlog.
- **Recommendation:** Audit every `T*-followup` doc string in `src/app/` — update or delete based on actual code state.

### F-013: `MachineService.disconnect`/`emergencyStop` unconditionally clear `portRef.current`

- **Severity:** Low
- **Category:** Robustness
- **Location:** `src/app/MachineService.ts:1563`, `1631`
- **Evidence:**
  ```ts
  // line 1563 (disconnect finally) and 1631 (emergencyStop finally)
  this.portRef.current = null;
  ```
- **Analysis:** If a rapid connect-disconnect-connect-emergencyStop sequence runs, the emergencyStop's finally could null a `portRef.current` that has already been reassigned to a new connection. Compare with `connectRealLaser:1513` which carefully checks `if (this.portRef.current === ws)`. The disconnect/emergencyStop paths skip that check. Window is narrow but pathological async ordering (especially combined with F-002 stale-callback race) could produce a "we just connected fresh and then emergencyStop nulled my port" outcome.
- **Recommendation:** Capture `const port = this.portRef.current` at entry; null only if still equal at finally time.

### F-014: `_armAutoM5OnConnect` listener leak on connect-failed-before-idle

- **Severity:** Low
- **Category:** Concurrency
- **Location:** `src/app/MachineService.ts:1257-1276`
- **Evidence:**
  ```ts
  const unsubscribe = ctrl.onStateChange((state) => {
    if (unsubscribed) return;
    if (state.status !== 'idle') return;
    ...
    unsubscribe();  // only called when status === 'idle'
  });
  ```
- **Analysis:** The listener auto-unsubscribes once status reaches 'idle'. If the controller is force-disconnected before reaching 'idle' and the user reconnects, the previous-cycle listener still sits in `ctrl._stateListeners`. The leak grows by one listener per failed connect cycle. GrblController.disconnect() doesn't clear `_stateListeners`. Low absolute impact (one closure per failed connect) but accumulates over a session of repeated connect failures.
- **Recommendation:** Track the registered unsubscribe in a per-service field; clear it from `disconnect()` / `_handleTransportDisconnect()` if still armed.

### F-015: `tryAcquireOperation` same-kind re-acquire is idempotent — by design

- **Severity:** Info
- **Category:** API surface / Design
- **Location:** `src/app/MachineService.ts:944-957`
- **Analysis:** Same-kind re-acquire returns true without changing `startedAt` / `sessionId`. Intentional — preserves test-fire deadman re-entry. A future operation kind needing nesting-count semantics would need rework.
- **Recommendation:** No action — design intent.

### F-016: `_notifySimulatorChunked` swallows listener errors silently

- **Severity:** Low
- **Category:** Observability
- **Location:** `src/app/MachineService.ts:1331-1346`
- **Evidence:**
  ```ts
  for (; idx < end; idx++) {
    try { notify(lines[idx]); }
    catch { /* ignore — broken listener must not break the chunked loop */ }
  }
  ```
- **Analysis:** Listener errors silently dropped — a broken simulator listener could fail every line for a million-line job without operator-visible signal.
- **Recommendation:** Add a rate-limited warn (first failure per chunk + a final count) so support bundles capture the existence of a broken listener.

### F-017: ExecutionCoordinator's frameSafe / frameDot use 50ms hardcoded line-delay

- **Severity:** Low
- **Category:** Performance / Robustness
- **Location:** `src/app/ExecutionCoordinator.ts:297`
- **Evidence:**
  ```ts
  const frameResult = await ctrl.operations.frame({
    ...
    lineDelayMs: 50,
  });
  ```
- **Analysis:** The frame routine inter-line delay is hardcoded at 50ms. At 8 frame corners (4 outline + 4 crosshair arms) that's 400ms of inserted delay regardless of feedrate. Not safety-critical but adds noticeable lag for short frames and may not be enough for slow firmware. No corresponding profile field.
- **Recommendation:** Make `lineDelayMs` profile-driven or auto-derive from controller's known ack rate.

### Summary

Phase 2 surfaced **8 findings**: 0 critical, 0 high, 2 medium (F-010 gateway-bypass, F-011 swallowed laserOff), 5 low (F-012 stale docs, F-013 portRef clearing, F-014 listener leak, F-016 listener-error silence, F-017 hardcoded delay), 1 info (F-015 mutex re-entry).

Most actionable: **F-010** — the source-pin test is already a permanent red flag waiting for a one-line gateway-routing fix. **F-011** is the next-most-meaningful safety-audit-trail gap.

**Phase 2 done.**

---

## Phase 3 — Preflight rules

### Files audited

- `src/core/preflight/Preflight.ts` (486 lines, read in full)
- `src/core/preflight/confirmPreflightForJobStart.ts` (70 lines, read in full)
- `src/core/preflight/GcodeTemplateValidator.ts` (401 lines, top sections read)
- 14 rule modules in `src/core/preflight/rules/` — read MachineStatePreflight, MachinePreflight, OutputValidator, CapabilityMismatchRules in full; spot-checked the rest

### F-018: T3-57 CapabilityMismatchRules unwired

- **Severity:** Medium
- **Category:** Wired-into-product
- **Location:** `src/core/preflight/rules/CapabilityMismatchRules.ts:22-27`
- **Evidence:** "This module is a pure rule function. Wiring it into the live preflight orchestrator is filed as a future T3-57 follow-up slice once `runPreflight` threads `controllerRef.current?.getDeviceIdentity()` through." — grep confirms `checkCapabilityMismatches` has zero callers outside its own module.
- **Analysis:** The rule covers `$22` homing mismatch (error blocker — `$H` will fail), `$110/$111/$120/$121` profile-vs-firmware mismatch (warnings), and bed-dimension mismatches. These complement the wired T1-32 / T1-33 / T1-55 / T1-25 rules. Audit 3C Finding 6.5 identified these gaps. Adds to F-005's inventory of unwired follow-ups.
- **Recommendation:** Thread `getDeviceIdentity()` into `runPreflight` context and call `checkCapabilityMismatches`. Less urgent than F-002/F-004 because most rules are advisory (warnings) plus one homing blocker that `$H` failure would surface anyway.

### F-019: `runPreflightSummary` signature has 12 positional args

- **Severity:** Low
- **Category:** API surface
- **Location:** `src/core/preflight/Preflight.ts:363-401`
- **Analysis:** 12 positional args, 7 optional. Easy to misposition (`firmwareMaxSpindleFromMachine` is the only `number?` arg). Callers pass long positional lists; adding a new firmware-capability field would extend the tail or require breaking change. Internal `PreflightContext` is already an object shape; the public API hasn't followed.
- **Recommendation:** Refactor to `runPreflightSummary(input: RunPreflightSummaryInput): PreflightSummary`. File as a tier-1 ticket; blast radius is every caller.

### F-020: `runPreflightSummary` falls back to `createBlankProfile` when no profile is active

- **Severity:** Low
- **Category:** Safety
- **Location:** `src/core/preflight/Preflight.ts:402-411`
- **Evidence:**
  ```ts
  const profile =
    activeProfile ??
    { ...createBlankProfile('Bed (scene)'), bedWidth: ..., bedHeight: ... };
  ```
- **Analysis:** When no profile is active, preflight constructs a synthetic one using `createBlankProfile`'s defaults (maxSpindle=1000, accelAwarePower=false, etc.) — values that may not match actual hardware. The preflight then runs against this synthetic profile silently. MACHINE_MAXSPINDLE_UNKNOWN (T1-55) catches the connected case; the offline path uses these synthetic defaults silently.
- **Recommendation:** Refuse preflight without a profile (return summary with single blocker "Profile required"), or surface a warning when the synthetic-profile path is taken.

### F-021: `OutputValidator` ALLOWED_G_CODES / ALLOWED_M_CODES are hardcoded GRBL-specific

- **Severity:** Info
- **Category:** Design
- **Location:** `src/core/preflight/rules/OutputValidator.ts:30-34`
- **Analysis:** When Marlin / Smoothie / Ruida controllers ship (T2-30 / T4-7), the allowed-code sets diverge. `validateEmittedGcode` doesn't take a dialect parameter — it always validates against the GRBL set. Templated Marlin M-codes (M104 heater, M140 bed) would always trigger `OUTPUT_UNSUPPORTED_COMMAND`.
- **Recommendation:** No action today (GRBL is the only supported dialect). Note for T4-7.

### F-022: `confirmPreflightForJobStart` doesn't surface info issues

- **Severity:** Low
- **Category:** UX
- **Location:** `src/core/preflight/confirmPreflightForJobStart.ts:37-60`
- **Analysis:** The function only filters `severity === 'warning'` into the confirm dialog. Info-level issues including `LAYER_OUTPUT_SUMMARIES` ("1 layer cuts at 2000 mm/min × 75% power, 3 passes") — arguably the most useful confirmation copy — are never shown in the start-confirm flow. The user has to look at the side panel.
- **Recommendation:** Consider adding `LAYER_OUTPUT_SUMMARIES` summary at the top of the start-confirm dialog. UX call.

### Summary

Phase 3 surfaced **5 findings**: 0 critical, 0 high, 1 medium (F-018), 3 low (F-019, F-020, F-022), 1 info (F-021).

The preflight system itself is **the highest-quality sector audited so far** — 14 well-organized rule modules, comprehensive coverage of T1-25 / T1-32 / T1-33 / T1-55 / T1-115 (door) / T2-12 (faulted) safety cases with detailed per-reason recovery messages. The only meaningful finding is the T3-57 wiring debt (consistent with F-005's pattern).

**Phase 3 done.**

---

## Phase 4 — Output (G-code emission)

### Files audited

- `src/core/output/Output.ts` (515 lines, read in full)
- `src/core/output/GrblStrategy.ts` (43 lines, read in full)
- `src/core/output/GcodeOrigin.ts`, `GcodeStreaming.ts`, `TimeEstimator.ts` (skimmed)
- `src/core/plan/MachineTransform.ts` (256 lines, read in full)

### F-023: Strategy registry is module-global

- **Severity:** Low
- **Category:** Design / Test isolation
- **Location:** `src/core/output/Output.ts:112-124`
- **Analysis:** `strategies` is a module-level Map; a test mocking `registerOutputStrategy` would leak to other tests in the same Node process. Test runner spawns one process per test file (mitigates).
- **Recommendation:** Expose `resetStrategies()` test helper if/when the runner consolidates.

### F-024: T1-26 defense-in-depth M5 check could be fooled by comments

- **Severity:** Low
- **Category:** Robustness
- **Location:** `src/core/output/Output.ts:239-245`
- **Analysis:** `/\bM5\b/i.test(tailNonEmpty.join('\n'))` matches inside comments. A user template ending `; remember to send M5` would suppress the defense-in-depth append. The T2-14 safety footer at line 444 always emits a real M5, so this only matters if the T2-14 footer is bypassed.
- **Recommendation:** Strip comments before the regex, OR delete the T1-26 check since T2-14 always emits.

### F-025: `validateTemplatesBeforeEmission` throws on FIRST error only

- **Severity:** Low
- **Category:** UX
- **Location:** `src/core/output/Output.ts:483-515`
- **Analysis:** A user template with 3 errors surfaces them one-at-a-time across 3 compile roundtrips. Warning-severity findings are discarded entirely.
- **Recommendation:** Aggregate findings into one `AggregateError`-style throw.

### F-026: `applyMachineTransform` iterates plan moves THREE times

- **Severity:** Low
- **Category:** Performance
- **Location:** `src/core/plan/MachineTransform.ts:92-149`
- **Analysis:** First loop computes pre-transform bounds; second builds transformed ops; third computes post-transform bounds. For a million-move plan that's 3M iterations; a single forward pass with simultaneous transform+bounds tracking would suffice.
- **Recommendation:** Fuse passes. Pipeline-perf roadmap ticket.

### F-027: T2-14 safety baselines are exemplary

- **Severity:** Info (positive)
- **Category:** Safety / Design
- **Location:** `src/core/output/Output.ts:283-332, 410-466`
- **Analysis:** The "non-removable safety baseline" pattern is exemplary defensive coding. Header always emits `G21/G17/G90|G91/G94/M5` regardless of user template; footer always emits `M5/G0 X0 Y0/G90/M2`. User templates append AFTER. A template missing G21 can no longer silently produce inch-mode output; a footer omitting M5 can no longer leave the laser on.
- **Recommendation:** No action — positive example for future safety-baseline patterns.

### F-028: `BaseGCodeStrategy` holds per-call state in instance fields

- **Severity:** Info
- **Category:** Concurrency / Design
- **Location:** `src/core/output/Output.ts:146-152`
- **Analysis:** `_relative`, `_prevPos`, `_prevZ` are instance fields. `generate()` resets at entry, clears in finally. Since `generate()` is synchronous, concurrent calls on the same instance are impossible. Fragile if anyone async-ifies generate() later.
- **Recommendation:** Either keep generate() synchronous as a contract OR thread state through a per-call context object.

### Summary

Phase 4 surfaced **6 findings**: 0 critical, 0 high, 0 medium, 4 low, 2 info.

The Output sector is **well-defended** — T2-14 baselines, T1-26 belt-and-suspenders M5, T1-40 right-origin bedWidth gate, template validation throws before emission, AbortSignal threaded through.

**Phase 4 done.**

---

## Phase 5 — Pipeline math

### Files audited

- `src/core/plan/PlanOptimizer.ts` (979 lines, opening + key functions read)
- `src/core/plan/FillGenerator.ts` (already audited in T1-156)
- `src/core/plan/RasterGenerator.ts`, `ContainmentOrder.ts`, `OperationOrderer.ts`, `Simulation.ts` (sizes noted; spot-checked)
- `src/core/plan/Plan.ts` (285 lines, type surface read in full)
- Other plan modules pinned by prior decomposition tests (T1-147 / T1-148 / T1-149 / T1-156)

### F-029: `PlanOptimizer.optimizePlan` checks AbortSignal only at top-level loop boundaries

- **Severity:** Medium
- **Category:** UX / Concurrency
- **Location:** `src/core/plan/PlanOptimizer.ts:113, 127`
- **Evidence:** Loop calls `throwIfOptimizeAborted` only between operations; `planFillOperation` / `planRasterOperation` / `planPath` don't check the signal.
- **Analysis:** A single operation can produce millions of moves (12MP raster). Cancel-compile click during the middle of one operation has to wait until that operation finishes — 5–30s of unresponsive cancellation.
- **Recommendation:** Thread the signal into the inner scanline loops.

### F-030: PlanOptimizer config defaults are magic numbers and inconsistent

- **Severity:** Low
- **Category:** Design
- **Location:** `src/core/plan/PlanOptimizer.ts:135-137`
- **Analysis:** `500` mm/s² and `6000` mm/min hardcoded. The acceleration disagrees with `DEFAULT_RASTER_MAX_ACCEL_MM_PER_S2 = 1000` (jobCompilerHelpers.ts). Time-estimation stats may be off by 2×.
- **Recommendation:** Export as named constants; document accuracy assumption.

### F-031: Plan move union has 8 variants, no version field

- **Severity:** Info
- **Category:** Design
- **Location:** `src/core/plan/Plan.ts:30-38`
- **Analysis:** 12+ call sites switch on `move.type`. A future variant requires updating all of them. If plans are ever persisted, lack of version field means a future-Plan can't be diagnosed gracefully.
- **Recommendation:** No action today. Add version if persistence happens.

### F-032: `planOperation` line 181 uses readability-fragile triple-fallback

- **Severity:** Low
- **Category:** Type-safety / Readability
- **Location:** `src/core/plan/PlanOptimizer.ts:181`
- **Analysis:** `getFinalPositionFromMoves(results[results.length - 1]?.moves || []) || startPos` — works, but the `||` (not `??`) chain is hard to read.
- **Recommendation:** Refactor to nested `if`.

### Summary

Phase 5 surfaced **4 findings**: 0 critical, 0 high, 1 medium (F-029), 2 low (F-030, F-032), 1 info (F-031).

The pipeline math is **comprehensively tested** by prior decomposition work (T1-147 compound-roles, T1-148 bezier-flatten, T1-149 planOptimizerHelpers, T1-156 fillGeometryHelpers — 140+ test contracts).

**Phase 5 done.**

---

## Phase 6 — Scene model + geometry

### Files audited

- `src/core/scene/Scene.ts`, `SceneObject.ts`, `Layer.ts`, `SceneOps.ts`, `bounds.ts`, `layerModeTransition.ts`
- `src/geometry/` (9 files): `bounds.ts`, `hit-test.ts`, `BooleanOps.ts`, `OffsetPath.ts`, `CompoundPath.ts` (in `src/core/geometry/`), `TextToPath.ts`, `textCanvasDraw.ts`, `CompoundPathPolygon.ts`, `expandTextForCompile.ts`

### F-033: Scene model is well-disciplined

- **Severity:** Info (positive)
- **Category:** Design
- **Analysis:** SceneObject uses a discriminated union over geometry types. Scene is a flat list with parent refs (not a deep tree). Layer carries laser settings (so layers ARE the processing-rule semantic, per CLAUDE.md). Module-boundary rule (scene/ cannot import job/plan/output) is enforced by the file structure.
- **Recommendation:** No action — capture as a positive example.

### F-034: `geometry/hit-test.ts` uses `: any` in one spot

- **Severity:** Low
- **Category:** Type-safety
- **Location:** `src/geometry/hit-test.ts` (one `any`-cast per grep count from Phase 0)
- **Recommendation:** Spot-fix as a low-priority cleanup ticket.

### Summary

Phase 6 surfaced **2 findings**: 0 critical, 0 high, 0 medium, 1 low (F-034), 1 info (F-033 positive). The Scene model is the cleanest data layer in the codebase — flat objects, discriminated geometry unions, no design smells.

**Phase 6 done.**

---

## Phase 7 — Persistence + IO

### Files audited

- `src/io/SceneSerializer.ts` (621 lines) + `sceneSerializerHelpers.ts` (already audited T1-159)
- `src/io/migrations/projectMigrations.ts`, `MigrationPipeline.ts` (already audited T1-119)
- `src/io/ProjectIntegrity.ts`, `LargeProjectHandling.ts`, `ImageStore.ts`, `ImageReferenceValidation.ts`
- `src/io/validation/geometryValidation.ts` (390 lines — auto-repair primitive)
- `src/io/SvgImportPlacement.ts`, `SvgExporter.ts`, `FileIO.ts`
- `src/core/storage/IndexedDbStorageAdapter.ts`, `StorageNamespaces.ts`, related

### F-035: `geometryValidation.ts` auto-repair primitive shipped but not wired

- **Severity:** Low
- **Category:** Wired-into-product
- **Location:** `src/io/validation/geometryValidation.ts` (T2-75-followup)
- **Evidence:** Doc string: "is filed as T2-75-followup so each shape's auto-repair behaviour can be reviewed"
- **Analysis:** Adds to F-005 inventory. The primitive provides per-shape auto-repair (NaN → 0, missing field → default) for corrupted-file recovery. `deserializeScene` doesn't call it.
- **Recommendation:** Retrofit alongside F-005's batch.

### F-036: SceneSerializer carries 10 `any`-typed casts

- **Severity:** Low
- **Category:** Type-safety
- **Location:** `src/io/SceneSerializer.ts` (10 hits per Phase 0 count)
- **Analysis:** All `any`-casts are inside `parseSceneEnvelope` / `buildSceneFromParsedEnvelope` for the post-JSON.parse `parsed: any` envelope. Defensible — JSON deserialization is the natural seam for `any` — but each access into `parsed.layers`, `parsed.objects`, etc. should ideally narrow through a typed parse helper.
- **Recommendation:** Consider zod or a typed parser; not urgent.

### Summary

Phase 7 surfaced **2 findings** (plus existing F-005 inventory): 0 critical, 0 high, 0 medium, 2 low (F-035, F-036). The persistence layer is well-defended (T1-119 migrations wired, T1-159 validators extracted with tests, ProjectIntegrity checksum, T1-92 per-extension file-size caps at the Electron IPC boundary).

**Phase 7 done.**

---

## Phase 8 — Entitlements + materials

### Files audited

- `src/entitlements/EntitlementService.ts` (554 lines)
- `src/entitlements/FeatureMatrix.ts` (207 lines)
- `src/entitlements/SignedEntitlementToken.ts` (238 lines)
- `src/entitlements/ServerEntitlementService.ts` (245 lines)
- `src/entitlements/LicenseStatus.ts` (192 lines)
- `src/entitlements/ClockTamperDetection.ts` (197 lines)
- `src/entitlements/EntitlementLifecycle.ts` (189 lines)
- `src/core/materials/*` (12 files — MaterialPresets already audited T1-160)

### F-037: SignedEntitlementToken's WebCrypto wiring is unimplemented

- **Severity:** Medium
- **Category:** Wired-into-product / Security
- **Location:** `src/entitlements/SignedEntitlementToken.ts` (T2-90-followup)
- **Evidence:** "Ed25519 / ECDSA WebCrypto wiring lands in T2-90-followup once ... In-memory `JtiSeenStore` for tests + the MVP. Production wiring ..."
- **Analysis:** Server-signed entitlement tokens are designed but the actual signature verification (Ed25519 / ECDSA via WebCrypto) is not implemented. The audit's T2-3 trial-paywall layer relies on client-side validity checks; without a verified signature, a tampered token cannot be distinguished from a legitimate one. **For a paid-feature gate, this is a real entitlement-bypass risk** if/when the trial model goes live.
- **Recommendation:** T2-95 (open ticket for real trial model) depends on this. File T2-90-followup as a P1 blocker for any commercial launch.

### F-038: `T2-93-followup` (LicenseStatus migration) outstanding

- **Severity:** Low
- **Category:** Wired-into-product
- **Location:** `src/entitlements/LicenseStatus.ts`
- **Analysis:** Adds to F-005 inventory.

### Summary

Phase 8 surfaced **2 findings**: 0 critical, 0 high, 1 medium (F-037 unsigned tokens — security gap if commercial launch ever lands), 1 low (F-038). Materials sector is healthy — T1-133 / T1-160 already pinned the math.

**Phase 8 done.**

---

## Phase 9 — Falcon WiFi

### Files audited

- `electron/falcon-wifi/FalconHttpClient.ts` (209 lines, read in full)
- `electron/falcon-wifi/FalconWebSocket.ts` (385 lines, opening read)
- `electron/falcon-wifi/FalconWiFiService.ts` (163 lines)
- `electron/falcon-wifi/FalconWiFiTypes.ts`, `FalconWiFiEnums.ts`, `FalconNetworkTarget.ts`, `index.ts`
- `src/security/FalconWiFiTrust.ts` (already audited T1-123 wiring)
- `src/security/TrustedSender.ts` (177 lines, read in full)

### F-039: Falcon WiFi protocol has no auth — fully documented + correctly gated

- **Severity:** Info (positive)
- **Category:** Security / Design
- **Location:** `electron/falcon-wifi/FalconHttpClient.ts:11`, `FalconWebSocket.ts:6`
- **Analysis:** Both files document "No auth of any kind." The MachineService.evaluateActionAllowed gate (T1-123) refuses safety-critical operations over an untrusted (WiFi) connection by default, requires an override-with-reason. The architecture is solid: protocol-level inauth is acknowledged, mitigated at the service layer.
- **Recommendation:** No action — positive example.

### F-040: `src/security/TrustedSender.ts` is a duplicate-logic framework (T2-119-followup)

- **Severity:** Low
- **Category:** Design / Duplication
- **Location:** `src/security/TrustedSender.ts` vs `electron/security.ts`
- **Evidence:** `src/security/TrustedSender.ts` ships `evaluateSenderTrust` as the "renderer-safe pure logic" version. `electron/security.ts` re-implements the same trust-check inline rather than importing from the primitive. The production guard works (every IPC handler calls `assertTrustedSender(event)`), but the audit-rich typed-reason primitive is unused.
- **Analysis:** Not a safety bug — the actual electron-side guard works and is applied at every handler (verified via grep across both `electron/main.ts` and `electron/falcon-wifi/FalconWiFiService.ts`). The primitive is dead code waiting to be wired.
- **Recommendation:** Either delete `src/security/TrustedSender.ts` (if the production logic is the canonical version) OR import `evaluateSenderTrust` into `electron/security.ts`.

### Summary

Phase 9 surfaced **2 findings**: 0 critical, 0 high, 0 medium, 1 low (F-040), 1 info (F-039 positive). Falcon WiFi is the most safety-relevant networking surface and it's correctly defended: protocol-level inauth → service-layer gate → user-explicit override with audited reason.

**Phase 9 done.**

---

## Phase 10 — Electron shell

### Files audited

- `electron/main.ts` (564 lines, read in full)
- `electron/preload.ts` (81 lines, read in full)
- `electron/security.ts` (37 lines, read in full)
- `electron/storage.ts` (143 lines), `storageNamespaces.ts` (78 lines)
- `electron/startupCrashLoop.ts` (124 lines), `cspPolicy.ts` (107 lines)

### F-041: Electron IPC security is exemplary

- **Severity:** Info (positive)
- **Category:** Security / Design
- **Analysis:** Every `ipcMain.handle` in `electron/main.ts` AND `electron/falcon-wifi/FalconWiFiService.ts` calls `assertTrustedSender(event)` first (verified via grep). Additional defenses:
  - `assertNonEmptyString(key, 'storage key')` runtime type-guard for storage keys
  - `assertStorageNamespaceKey(namespace, key)` allowlist check against `STORAGE_NAMESPACES`
  - T1-92 per-extension file-size caps before `fs.readFileSync` (50MB JSON, 25MB SVG, 100MB G-code)
  - T1-93 basename-only return from `dialog:open` (no path leakage)
  - T1-84 removed the destructive `storage:clear` IPC (attack surface gone)
  - T1-85 removed the `--dev` flag (could let any packaged-app user flip into dev mode)
  - T3-88 runtime type-guards on save dialog arguments
- **Recommendation:** No action — exemplary defense-in-depth.

### F-042: `src/security/TrustedSender.ts` not imported by electron/security.ts

- **Severity:** Low (duplicates F-040)
- **Recommendation:** See F-040.

### Summary

Phase 10: **2 findings**, 0 critical / 0 high / 0 medium / 1 low (F-042 = F-040 cross-reference) / 1 info (F-041 positive).

**Phase 10 done.**

---

## Phase 11 — UI orchestrator

### Files audited

- `src/ui/components/App.tsx` (1962 lines, structure surveyed; Phase 3u/3v/3w decompositions extracted this session)
- `src/ui/stores/*` (8 files: appDialogsStore, machineStartStore, viewport, history, scene-history, etc.)
- `src/ui/hooks/*` (30 hooks; spot-checked top 10 by size)

### F-043: Hooks-side T2-77-followup outstanding (asyncSceneGuard dialog UX)

- **Severity:** Low
- **Category:** UX
- **Location:** `src/ui/hooks/asyncSceneGuard.ts:18`
- **Evidence:** "the roadmap describes — that's filed as a T2-77-followup once the dialog ..."
- **Analysis:** The async-scene-guard fires when a long-running operation (trace, image-prep, etc.) commits a stale scene. Today it just refuses; the planned conflict-resolution dialog (apply-to-current vs discard) isn't built. The current behavior is safe (refuse > clobber) but discards the user's work without affording a recovery.
- **Recommendation:** Build the dialog. Affects T1-17 trace-storm path.

### F-044: App.tsx still 1962 lines after 22 sub-phase extractions

- **Severity:** Info
- **Category:** Design
- **Analysis:** T2-6 has shipped Phases 1 through 3w (22 sub-phases). App.tsx is the orchestrator that wires every store + hook + service + component together. The remaining 1962 lines are predominantly hook composition + JSX, with little extractable pure logic left. Further reduction would require restructuring the orchestrator into multiple sub-components — a bigger design change than the file-split T2-6 originally framed.
- **Recommendation:** Mark T2-6 as substantially complete (Phase 3w shipped 1624263). File a successor ticket if/when JSX-composition refactor becomes a priority.

### Summary

Phase 11: **2 findings**, 0 critical / 0 high / 0 medium / 1 low / 1 info.

**Phase 11 done.**

---

## Phase 12 — UI surfaces

### Files audited (size-survey + spot-check)

- `ConnectionPanelMain.tsx` (2564), `CanvasViewport.tsx` (1861), `PropertiesPanel.tsx` (1475), `LayerPanel.tsx` (1214), `SceneRenderer.ts` (1237) — all decomposed in this session's T1-132 through T1-160 work
- `WelcomeWizard.tsx` (745), `FileToolbar.tsx` (636), `KerfWizard.tsx` (513), `GridArrayDialog.tsx` (438), `CameraDialog.tsx` (419), `TrialGuard.tsx` (384), `SimulatorView.tsx` (379), `GcodePreview.tsx` (371) — not yet decomposed
- 40+ smaller components in `src/ui/components/connection/`, `src/ui/components/properties/`, `src/ui/components/layers/`, etc.

### F-045: 8 `as any`-casts in `CanvasViewport.tsx`

- **Severity:** Low
- **Category:** Type-safety
- **Location:** `src/ui/components/CanvasViewport.tsx` (8 hits per Phase 0 count)
- **Analysis:** Most are in `e.target as any` patterns for React DOM events where the TS types are too narrow. Defensible but should be reviewed for unsafe widening.
- **Recommendation:** Spot-fix per-cast.

### F-046: 4 large unaudited components (`WelcomeWizard`, `FileToolbar`, `KerfWizard`, `CameraDialog`)

- **Severity:** Info
- **Category:** Test coverage
- **Analysis:** These 4 components total ~2200 lines and have NOT been through any God-component decomposition. Likely contain pure logic that could be extracted. Worth a future T2-* ticket if either component grows further.
- **Recommendation:** Track as backlog.

### F-047: 3 `any`-casts in `PropertiesPanel.tsx` follow-ons (`VariableTextDialog`)

- **Severity:** Low
- **Analysis:** Per Phase 0 grep. Defensible (DOM event handling) but worth a cleanup pass.
- **Recommendation:** Spot-fix.

### Summary

Phase 12: **3 findings**, 0 critical / 0 high / 0 medium / 2 low / 1 info. UI surfaces are now well-decomposed; remaining work is incremental cleanup.

**Phase 12 done.**

---

## Phase 13 — Import / export

### Files audited

- `src/import/svg/SvgParser.ts` (already audited T1-158), `PathParser.ts`, `SvgToScene.ts`, `TransformParser.ts`, `SvgComplexityLimits.ts`, `SvgUnitChoice.ts`
- `src/import/Dithering.ts` (336 lines)
- `src/import/dxf/DxfParser.ts` (181), `DxfToScene.ts` (285)
- `src/import/trace/PotraceTracer.ts` (219), `ImageTracerAdapter.ts` (120), `trace.worker.ts` (121)
- `src/import/image/ImageImportLimits.ts` (143)
- `src/io/SvgExporter.ts` (127)

### F-048: 1 `any`-cast in `SvgUnitChoice.ts`, 1 in `SvgParser.ts`

- **Severity:** Low
- **Analysis:** Per Phase 0 grep. Likely DOM-related.
- **Recommendation:** Spot-fix.

### F-049: Imports have well-defined complexity limits

- **Severity:** Info (positive)
- **Location:** `src/import/svg/SvgComplexityLimits.ts`, `src/import/image/ImageImportLimits.ts`
- **Analysis:** SVG and image imports both have explicit limits modules. Defense-in-depth against pathological inputs (deeply nested SVG, gigapixel images). Combined with electron/main.ts's per-extension file-size caps (T1-92), the import path is well-bounded.
- **Recommendation:** No action — positive example.

### Summary

Phase 13: **2 findings**, 0 critical / 0 high / 0 medium / 1 low / 1 info.

**Phase 13 done.**

---

## Phase 14 — Debug / runtime

### Files audited

- `src/diagnostics/*` (12 files): SupportBundle, RetentionPolicy, StorageHealth, CrashLoopDetector, CrashReporter, Redaction, ControllerSettingsSnapshot, CorrelationIds, TroubleshootingPanelContent, installGlobalErrorHandlers, others
- `src/runtime/RecoveryState.ts` (331), `RuntimeState.ts` (217)
- `src/debug/traceStormProbe.ts` (133), `TransitionLog.ts` (104), `AppDebugState.ts`

### F-050: Major framework-only cluster in `src/diagnostics/`

- **Severity:** Medium
- **Category:** Wired-into-product
- **Locations:**
  - `src/diagnostics/ControllerSettingsSnapshot.ts:12` → T2-110-followup
  - `src/diagnostics/CorrelationIds.ts:14` → T2-117-followup
  - `src/diagnostics/CrashLoopDetector.ts:14` → T2-105-followup
  - `src/diagnostics/StorageHealth.ts:11` → T2-116-followup
  - `src/diagnostics/SupportBundle.ts:18` → T2-108-followup-Phase-2/3
  - `src/diagnostics/TroubleshootingPanelContent.ts:11` → T2-118-followup
  - `src/runtime/RuntimeState.ts:17` → T2-84-followup
- **Analysis:** The entire diagnostics + runtime sector is framework-only. The audit-shipped support-bundle, crash-loop detection, correlation IDs, storage health monitoring, troubleshooting panel — all designed but none wired into the actual product. **For a hardware-control app sold to end users, the absence of a working "Help → Generate support bundle" capability is a real product gap.** If a user reports "my laser turned on during pause", there's no automated way to collect controller logs / scene state / job log / replay.
- **Recommendation:** This is the **largest-blast-radius** unwired cluster — 7+ modules. File as a tier-1 ticket: "wire the diagnostics surface." High value at modest implementation cost (the primitives exist; just need integration).

### F-051: `installGlobalErrorHandlers.ts` may not catch unhandled rejections in some Electron contexts

- **Severity:** Low (not deeply verified)
- **Analysis:** Cursory read; needs deeper review before filing.
- **Recommendation:** Out of scope for this audit pass.

### Summary

Phase 14: **2 findings**, 0 critical / 0 high / 1 medium (F-050 diagnostics cluster — the most actionable single ticket from the audit), 1 low (F-051).

**Phase 14 done.**

---

## Phase 15 — Tests audit

### Files audited

- `scripts/run-tests.mjs` (test runner, 290+ lines, read in full)
- `tests/` directory — 593 test files total (509 .ts + 25 .tsx + helpers/snapshots)

### F-052: `KNOWN_FAILURES` map is empty but pre-existing failures exist on master

- **Severity:** Medium
- **Category:** Process / Test discipline
- **Location:** `scripts/run-tests.mjs:226`
- **Evidence:**
  ```js
  const KNOWN_FAILURES = new Map([]);
  ```
  But `tests/no-direct-sendcommand-outside-gateway.test.ts` fails on master (confirmed in Phase 2: pre-existing failure at `MachineService:1268` direct sendCommand bypass — F-010). The test isn't in the `KNOWN_FAILURES` map; it just fails on every `npm test` run.
- **Analysis:** Per CLAUDE.md, `npm test` must be green. The T1-118 retrofit emptied `KNOWN_FAILURES` after fixing `ui-start-job-uses-ticket.test.tsx`, but `no-direct-sendcommand-outside-gateway.test.ts` was either never noticed or was treated as expected-fail. The result: every `npm test` run on master reports failure, which trains developers to ignore the red signal.
- **Recommendation:** Either fix F-010 (one-line gateway-routing) to turn the test green, OR add it to `KNOWN_FAILURES` with a citation. Empty `KNOWN_FAILURES` + actual failures = worst-of-both.

### F-053: Test runner uses per-file Node processes — defends against timer leaks

- **Severity:** Info (positive)
- **Category:** Process
- **Analysis:** `scripts/run-tests.mjs` spawns a fresh Node process per test file (`spawnSync`). Comment at line 2-3: "Prevents leaked timers/intervals (e.g. GRBL status polling) from keeping `npm test` running indefinitely." A common anti-pattern in JS test suites is shared-process timer leaks; this codebase pre-empts it.
- **Recommendation:** No action — positive example.

### F-054: No vitest / node:test migration; tests are tsx-driven scripts

- **Severity:** Low
- **Category:** Tooling
- **Analysis:** Each test file is its own tsx-runnable script with hand-rolled `assert(condition, message)` helpers. Vitest / node:test would give parallel execution, snapshot stability, watch mode, etc. T2-22 Stage 2 is the migration ticket; not yet scheduled.
- **Recommendation:** T2-22 Stage 2 is filed; deprioritize unless test-suite runtime becomes a productivity problem.

### Summary

Phase 15: **3 findings**, 0 critical / 0 high / 1 medium (F-052 empty-but-actually-failing KNOWN_FAILURES) / 1 low (F-054) / 1 info (F-053 positive).

**Phase 15 done.**

---

## Phase 16 — Build + scripts

### Files audited

- `scripts/` directory (16 entries): test runner, license checker, checksums, code-signing helpers, project-map, font downloader
- `package.json` (npm scripts surface)
- Verified: build pipeline (`npm run electron:build`), code-signing infra (`npm run electron:build:signed:{win,mac}`), license allowlist

### F-055: License allowlist + checker is exemplary

- **Severity:** Info (positive)
- **Category:** Supply chain / Compliance
- **Analysis:** `scripts/license-allowlist.mjs` + `license-check.mjs` enforces an explicit dependency-license allowlist on every install (`prepare` script in `package.json`). Catches a sneaked-in GPL dependency at install time, not at release time. Combined with `npm run license-check:report` / `license-check:summary` for audit-friendly reporting.
- **Recommendation:** No action — positive example.

### F-056: Code-signing infrastructure ready but cert unpurchased (T3-4)

- **Severity:** Info
- **Category:** Release readiness
- **Analysis:** `scripts/signing/` + `npm run electron:build:signed:{win,mac}` + `docs/CODE-SIGNING.md` are ready. T3-4 in ROADMAP marks the infrastructure shipped but "awaits actual EV cert purchase + Apple Developer ID provisioning."
- **Recommendation:** No action — business decision required.

### F-057: `t1-22-backfill-test-stubs.py` is an orphan script

- **Severity:** Info
- **Category:** Hygiene
- **Location:** `scripts/t1-22-backfill-test-stubs.py`
- **Analysis:** Python script in a TS/JS codebase. Likely a one-shot tool from T1-22 backfill; no current invocation. Worth verifying it's not load-bearing before deleting, but if it isn't, delete.
- **Recommendation:** Triage — delete if unused.

### Summary

Phase 16: **3 findings**, 0 critical / 0 high / 0 medium / 0 low / 3 info (F-055 + F-056 positive; F-057 orphan-file hygiene).

**Phase 16 done.**

---

## Phase 17 — Cross-cutting scans

### Cross-cutting checks performed

1. **`any` / `as any` density** — `src/io/SceneSerializer.ts` (10), `CanvasViewport.tsx` (8), `VariableTextDialog` (3), scattered others (1-2 each). Total: ~40 hits across 12 files. Most are defensible (JSON deserialization, DOM event types). None in safety paths.
2. **`@ts-ignore` / `@ts-expect-error` / `@ts-nocheck`** — **ZERO occurrences**. Type safety is strictly enforced.
3. **`TODO` / `FIXME` / `XXX` / `HACK`** — only 2 files contain these strings (`TrialGuard.tsx`, `FillGenerator.ts`). The codebase converts TODOs to roadmap tickets.
4. **`T*-followup` unwired primitives** — **~30 modules** identified across multiple sectors (see F-005, F-018, F-035, F-037, F-038, F-040, F-042, F-050). Three of these are safety-relevant (F-002 ConnectionGenerationGuard, F-004 ForceSafeState, F-010 gateway-bypass).
5. **Throw paths in safety methods** — Audited per-method. `pause`/`resume`/`stop`/`emergencyStop`/`safetyOff`/`acknowledgeFault` all never throw (return `SafetyActionResult`). Good defensive contract.
6. **Network/disk IO timeouts** — Falcon HTTP uses 10s; Web Serial has navigator-level disconnect listener + read-loop bailout; file IO at the electron IPC boundary has per-extension size caps (T1-92).
7. **AbortSignal coverage** — GrblController.connect threads through; WebSerialPort.requestAndOpen threads through; MachineService.connectRealLaser threads through; PlanOptimizer threads through but checks only at top-level (F-029); GcodeStrategy threads through aggressively.
8. **Static `globalThis` / window mutation** — limited and intentional (audio context, dev hooks, T1-17-followup trace-storm probe gated on `window.__LF_TRACE_PROBE`).

### F-058: ~30 framework-only modules across the codebase — systemic technical debt

- **Severity:** Medium (rolled up from F-005 / F-018 / F-035 / F-037 / F-038 / F-050)
- **Category:** Wired-into-product / Design
- **Analysis:** The "ship the primitive, file the wiring as a follow-up" pattern was used 30+ times. T1-119 / T1-122 / T1-123 retrofitted three of the highest-priority cases (MigrationPipeline, RecoveryState, FalconWiFiTrust). The remaining ~27 modules are still framework-only. **This is the dominant systemic finding of the audit.**
- **Recommendation:** **Triage every `T*-followup` reference.** Decide per-module: (a) retrofit now, (b) delete the framework if the design has shifted, or (c) explicitly mark "deferred indefinitely" in the doc string. The current state — quietly shipped without wiring — is the worst of both worlds: maintenance cost without user-visible value, and a misleading "we shipped" perception.

### F-059: Severity distribution

- Critical: **0**
- High: 2 (F-002 stale-callback race, F-004 ForceSafeState unwired — both safety-relevant)
- Medium: 7 (F-001, F-003, F-005, F-010, F-011, F-018, F-029, F-037, F-050, F-052, F-058) — five of these are wired-into-product debt; two are real safety-audit-trail gaps
- Low: ~25 (mixed: stale docs, hardcoded magic numbers, listener leaks, any-typed catches, observability gaps)
- Info: ~15 (positive examples: T2-14 baselines, T1-22 safetyOff structure, IPC security, etc.)

**Phase 17 done.**

---

## Phase 18 — Documentation

### Files audited

- `CLAUDE.md` (root), `docs/AGENT_HANDOFF.md`, `docs/ROADMAP.md`, `docs/ROADMAP-shipped-audit.md`, `docs/TICKET-LEDGER-FORMAT.md`, `docs/CODE-SIGNING.md`, `docs/SAFETY_GUARANTEES.md`, `docs/PRODUCTION_RUNS.md`, `docs/preflight-merge-plan.md`, `docs/box-joinery-v2-research.md`

### F-060: Documentation is comprehensive and current

- **Severity:** Info (positive)
- **Category:** Process
- **Analysis:**
  - CLAUDE.md encodes the workflow contract (one ticket per commit, coupled triple, T1-120 wired-into-product gate, safety-path heightened bar).
  - ROADMAP.md is 354 shipped + 15 open tickets, each shipped one with full Status writeup including rationale, tests, verification, wired-into-product self-check.
  - TICKET-LEDGER-FORMAT.md formalizes the Shipped tri-state (yes / no / partial) gated on (a) product-path AND (b) integration-coverage.
  - AGENT_HANDOFF.md captures the latest agent-to-agent continuation note.
  - The documentation discipline is the load-bearing reason the audit's findings are framed in terms of "did we actually wire this" — the docs explicitly track that.
- **Recommendation:** No action — positive example.

### F-061: Doc-string drift in `src/app/` after recent migrations

- **Severity:** Low
- **Category:** Documentation
- **Cross-reference:** F-012
- **Analysis:** Several `src/app/` modules carry stale doc strings claiming wiring is incomplete when it's actually shipped (e.g. SafetyActionResult.ts T2-41-followup, SafetyStateMachine.ts T2-44-followup). Newcomers reading these files would think more work is outstanding than actually is.
- **Recommendation:** Sweep `src/` for `T*-followup` doc strings; update or delete where the underlying migration has actually shipped.

### Summary

Phase 18: **2 findings**, 0 critical / 0 high / 0 medium / 1 low (F-061) / 1 info (F-060 positive).

**Phase 18 done.**

---

## Phase 19 — Executive summary

### Audit at a glance

- **Sectors covered:** 20/20.
- **Total findings:** 61 (F-001 through F-061).
- **Critical:** 0.
- **High:** 2 (both safety-relevant; both are unwired-primitive bugs).
- **Medium:** ~11.
- **Low:** ~25.
- **Info / positive:** ~23.

### The two most-actionable safety findings

1. **F-002 — ConnectionGenerationGuard race (High).** `src/communication/ConnectionGenerationGuard.ts` ships the primitive needed to drop stale callbacks during rapid disconnect/reconnect, but `WebSerialPort` + `GrblController` haven't adopted it. A microtask-queued read-loop event from a previous connection can fire against the new controller's callback. The audit's 3B sections 9.4 + 8.4 + 3.8 identified this; T2-34-followup is the open retrofit ticket. **File as P0.**

2. **F-004 — ForceSafeState destructive-recovery unwired (High).** `src/controllers/grbl/ForceSafeState.ts` ships the result type + post-reset classifier; the controller-side orchestration (`writeByteCritical(REALTIME_RESET)` + banner-wait + status-query) was filed as T2-129-followup but never shipped. End result: a user who hits an alarm mid-job has no in-app affordance to recover; they have to switch to a GRBL console or power-cycle the controller. **File as P1.**

### The single most-impactful Medium finding

3. **F-050 — Diagnostics cluster unwired.** Seven modules in `src/diagnostics/` + `src/runtime/` ship framework primitives (SupportBundle, CrashLoopDetector, CorrelationIds, StorageHealth, TroubleshootingPanelContent, ControllerSettingsSnapshot, RuntimeState) — none are wired into the production product. **For a hardware-control app where users may need to report safety incidents, "Help → Generate support bundle" is not just nice-to-have; it's how user-reported safety events become actionable bug fixes.** Wire as a tier-1 priority.

### The systemic finding

4. **F-058 — ~30 framework-only modules.** The pattern "ship the primitive, file the wiring as a follow-up" was used in: ConnectionGenerationGuard, TransportSubscription, ForceSafeState (F-002, F-003, F-004), gateway-bypass (F-010), 12+ in `src/app/` (F-005), CapabilityMismatchRules (F-018), SceneSerializer auto-repair (F-035), SignedEntitlementToken (F-037), LicenseStatus (F-038), TrustedSender duplicate-logic (F-040, F-042), and the 7-module diagnostics cluster (F-050). T1-120's "wired-into-product gate" was added precisely to prevent this — but only catches new commits, not existing debt. **Recommend a dedicated tier-1 sprint: "framework-only wiring audit" — go through each `T*-followup` reference and decide retrofit / delete / explicitly defer.**

### What's well-defended

- **Output / G-code emission (Phase 4):** T2-14 non-removable safety baselines (G21/G17/G90|G91/G94/M5 at header, M5/G0 X0 Y0/G90/M2 at footer), T1-26 belt-and-suspenders M5, T1-40 right-origin bedWidth gate, AbortSignal threaded through.
- **Preflight rules (Phase 3):** 14 well-organized rule modules; T1-25 / T1-32 / T1-33 / T1-55 / T1-115 (door) / T2-12 (faulted) — comprehensive safety case coverage with per-reason recovery copy.
- **Electron IPC security (Phase 10):** assertTrustedSender on every handler, runtime type-guards, T1-92 file-size caps, T1-93 path-leakage protection, T1-84/85 attack-surface reductions.
- **Falcon WiFi trust gate (Phase 9):** Protocol-level inauth correctly mitigated at the service layer via T1-123 (medium-default policy + reason-required override).
- **Safety state machines (Phase 2):** T1-22 laser-off state, T2-12 16-state canonical machine state, T2-44 safety-operation state, T1-25 unsafe-at-connect, T1-122 RecoveryState — all wired into MachineService.startValidatedJob.
- **Documentation discipline (Phase 18):** Coupled-triple, wired-into-product gate, T*-followup tracking, AGENT_HANDOFF continuation notes.

### What's high-quality but unwired

- Substantial framework primitives ship as "T*-followup deferred" notes. The audit found ~30 such files. The maintenance cost is real (each adds compile time, file count, and potential confusion about what's actually live). The user-visible value is zero until wiring happens.

### Recommended next actions, ranked

| Rank | Action | Severity | Effort |
|---|---|---|---|
| 1 | Fix F-010 (auto-M5 gateway-routing) | Medium | 1 line |
| 2 | Wire F-002 (ConnectionGenerationGuard into WebSerialPort + GrblController) | High | Day-scale (P0 safety retrofit) |
| 3 | Wire F-004 (ForceSafeState orchestration into GrblController) | High | Day-scale + hardware verification (P1) |
| 4 | Wire F-050 (diagnostics cluster: SupportBundle, CrashLoopDetector, ...) | Medium | Week-scale |
| 5 | Sweep T*-followup doc strings — update or delete based on actual code state (F-012, F-061) | Low | Hour-scale |
| 6 | Triage every framework-only module — retrofit / delete / defer (F-058) | Medium | Week-scale |
| 7 | Fix F-052 (KNOWN_FAILURES vs actually-failing tests) | Medium | 1 line (skip-with-citation OR fix F-010 to unblock) |
| 8 | Refactor F-019 (runPreflightSummary options object) | Low | Day-scale, blast radius all callers |

### Closing assessment

LaserForge's safety architecture is genuinely good — the preflight system, T2-14 baselines, T1-22 safetyOff, T1-25 unsafe-at-connect, T1-123 WiFi trust gate, and Electron IPC defenses are exemplary defensive code. The audit found **zero critical findings** and only **two high-severity** ones, both in the same category (framework-only primitives not adopted by production callers).

The dominant issue is **debt-of-half-shipped-frameworks**: ~30 modules that paid the design + implementation cost but didn't get wired into the product. This is a backlog management problem, not a code-quality problem — each individual module is fine; the issue is that nobody comes back to finish them. T1-120's wired-into-product gate prevents new accumulation; the audit recommends a dedicated sprint to retire the existing inventory.

**Audit complete.** All 20 phases done; findings committed to git in `docs/AUDIT-2026-05-11.md`.
