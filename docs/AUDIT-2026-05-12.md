# LaserForge full-code audit — 2026-05-12

**Auditor:** Claude Sonnet 4.5 (1M context), acting as senior staff engineer per `docs/AUDIT.md` Section 1.3.
**Scope:** all of `src/`, `electron/`, `tests/`, `scripts/`, `docs/`.
**Method:** see [`docs/AUDIT.md`](AUDIT.md).
**Repo state at start:** master @ `28b63343` (`docs(audit): add docs/AUDIT.md`); working tree clean; `npx tsc --noEmit --pretty false` returns 0.
**Prior audit:** [`docs/AUDIT-2026-05-11.md`](AUDIT-2026-05-11.md) — 61 findings (0 Critical / 2 High / 11 Medium / ~25 Low / ~23 Info).

This document is the living findings ledger for the 2026-05-12 audit pass. Findings land as `F-NNN` blocks in the appropriate phase. The progress table tracks which phases have been completed.

## Progress table

| Phase | Sector | Status | Findings |
|---|---|---|---|
| 0 | Pre-audit inventory | ✅ done | 0 findings |
| 1 | Threat model | ✅ done | 0 findings, 4 attack-tree leaves marked for Phase 4/5 re-check |
| 2 | Static analysis sweep | ✅ done | 0 crit, 0 high, 0 med, 3 low, 1 info |
| 3 | Architectural review | ✅ done | 0 crit, 1 high, 2 med, 1 low, 1 info |
| 4 | Function-by-function review | ✅ done (with deferred row-coverage on non-hot files) | 0 crit, 2 high, 1 med, 0 low, 0 info — 3 total |
| 5 | Safety-critical deep review (STPA + FMEA) | ✅ done | 0 crit, 0 new high (F-010 cross-listed as UCA-1), 0 med, 0 low, 0 info |
| 6 | Concurrency audit | ✅ done | 0 new findings |
| 7 | Determinism & reproducibility | ✅ done | 0 crit, 0 new high, 0 new med (F-012 promoted from Phase 4 deep-dive), 0 low, 1 info (F-013) |
| 8 | Test integrity | ✅ done | 0 new findings (F-011 cross-listed as T1-120 violation) |
| 9 | Hallucination & drift detection | ✅ done | 0 crit, 0 high, 1 medium (F-014), 1 low (F-015), 0 info |
| 10 | Executive summary & sign-off | ⏳ pending | — |

## Severity counts (running)

- **Critical:** 0
- **High:** 3 (F-007, F-010, F-011)
- **Medium:** 4 (F-005, F-006, F-012, F-014)
- **Low:** 5 (F-001, F-002, F-003, F-008, F-015)
- **Info:** 3 (F-004, F-009, F-013)
- **Total:** 15

---

## Phase 0 — Pre-audit inventory

**Entry criteria:** working tree clean ✅, `npx tsc --noEmit --pretty false` returns 0 ✅, prior ledger committed ✅, master prompt re-read ✅.

### 0.1 Codebase shape

| Sector | Files | LOC |
|---|---|---|
| `src/ui/` | 189 | 39,028 |
| `src/core/` | 109 | 21,067 |
| `src/app/` | 48 | 10,355 |
| `src/controllers/` | 27 | 6,677 |
| `src/import/` | 17 | 3,554 |
| `src/io/` | 14 | 2,788 |
| `src/diagnostics/` | 12 | 2,074 |
| `src/entitlements/` | 10 | 2,040 |
| `src/geometry/` | 9 | 1,733 |
| `src/communication/` | 5 | 1,278 |
| `src/runtime/` | 2 | 548 |
| `src/security/` | 3 | 410 |
| `src/debug/` | 4 | 363 |
| `src/fonts/` | 6 | (data) |
| **src/ total** | **467** | **~91,000** |
| `electron/` | 14 | 2,058 |
| `tests/` | 624 test files | (per-process runner) |

**Net growth since 2026-05-11:**
- `src/` files: 436 → 467 (**+31 files**, +8.0%); LOC 87,512 → ~91,000 (**+3,500 LOC**, +4%).
- `tests/`: 593 → 624 (**+31 test files**, +5.2%).
- `electron/` unchanged (14 files / 2,058 LOC).

### 0.2 Largest files (the "God components")

Top 15 by LOC. Files marked ⚠️ are on the safety-critical path per [`CLAUDE.md`](../CLAUDE.md).

| LOC | File | Notes |
|---|---|---|
| **2,733** ⚠️ | `src/controllers/grbl/GrblController.ts` | +213 LOC since prior audit (was 2,520). Tracks T1-216 / T1-217 / T1-220 / T1-221 additions. |
| 2,664 | `src/ui/components/ConnectionPanelMain.tsx` | +100 LOC since prior audit (was 2,564). |
| **2,367** ⚠️ | `src/app/MachineService.ts` | +515 LOC since prior audit (was 1,852). T1-219 / T1-220 / T1-221 / T1-222. |
| 1,985 | `src/ui/components/App.tsx` | +23 LOC; T2-6 phases 3x/3y. |
| 1,861 | `src/ui/components/CanvasViewport.tsx` | unchanged. |
| 1,475 | `src/ui/components/PropertiesPanel.tsx` | unchanged. |
| 1,237 | `src/ui/renderers/SceneRenderer.ts` | unchanged. |
| 1,214 | `src/ui/components/LayerPanel.tsx` | unchanged. |
| 1,126 ⚠️ | `src/core/plan/PlanOptimizer.ts` | +147 LOC since prior audit (was 979). |
| 1,119 ⚠️ | `src/core/job/JobCompiler.ts` | +65 LOC (was 1,054). |
| 770 | `src/core/devices/DeviceProfile.ts` | — |
| 760 | `src/ui/components/ConnectionPanel.tsx` | new module split during T1-206…T1-208 workflow panel work. |
| 745 | `src/ui/components/WelcomeWizard.tsx` | unchanged. |
| 682 | `src/io/SceneSerializer.ts` | unchanged. |
| 672 | `src/core/materials/MaterialPresets.ts` | unchanged. |

**Observation (info, not a finding):** the four largest safety-critical files (GrblController, MachineService, PlanOptimizer, JobCompiler) all grew between audits. Net +940 LOC across them. Phase 4 (function-by-function) will need extra budget for these.

### 0.3 Hot files since prior audit (2026-05-11)

Top 20 most-modified `src/` + `electron/` files. Used to focus Phase 4 priority.

| Commits | File |
|---|---|
| 19 | `src/app/MachineService.ts` |
| 13 | `src/ui/components/ConnectionPanel.tsx` |
| 11 | `src/ui/components/ConnectionPanelMain.tsx` |
| 11 | `src/controllers/grbl/GrblController.ts` |
| 7 | `src/ui/components/App.tsx` |
| 6 | `src/ui/components/workflow/WorkflowPanel.tsx` |
| 6 | `src/core/plan/PlanOptimizer.ts` |
| 6 | `src/app/PipelineService.ts` |
| 5 | `src/ui/components/connection/buildStartReadiness.ts` |
| 4 | `src/ui/components/connection/StartReadinessPanel.tsx` |
| 4 | `src/core/job/JobCompiler.ts` |
| 4 | `src/controllers/ControllerInterface.ts` |
| 3 | `src/core/output/emittedBurnEnvelope.ts` |
| 3 | `src/core/output/Output.ts` |
| 3 | `src/core/job/ValidatedJobTicket.ts` |
| 3 | `src/app/ExecutionCoordinator.ts` |
| 2 | `src/ui/renderers/SceneRenderer.ts` |
| 2 | `src/ui/components/workflow/modes/setup/MoveTab.tsx` |
| 2 | `src/ui/components/workflow/modes/SetupMode.tsx` |
| 2 | `src/ui/components/workflow/derivePanelMode.ts` |

**Observation:** `MachineService.ts` had 19 modifications in one day-window — heaviest churn in the repo. Combined with its safety-critical status, it is the top priority file for Phase 4.

### 0.4 Module dependency graph — circular dependency scan

Run: `npx madge --circular --extensions ts,tsx src/`. **18 cycles detected.** Listed for Phase 3 evaluation (the existence of cycles is inventory; the *severity* of each is decided in the architectural-review phase against the pipeline-direction rule from `CLAUDE.md`).

| # | Cycle |
|---|---|
| 1 | `core/devices/DeviceProfile.ts ↔ core/devices/validateProfile.ts` |
| 2 | `core/scene/Layer.ts ↔ core/materials/MaterialSettingConfidence.ts` |
| 3 | `core/devices/DeviceProfile.ts ↔ core/output/Output.ts ↔ core/preflight/GcodeTemplateValidator.ts` |
| 4 | `core/devices/DeviceProfile.ts ↔ core/output/Output.ts ↔ core/preflight/GcodeTemplateValidator.ts ↔ core/scene/Scene.ts` |
| 5 | `app/MachineService.ts ↔ app/machineServiceHelpers.ts` |
| 6–18 | `core/preflight/Preflight.ts ↔ rules/<RuleName>.ts` × 13 sibling rules (CompileComplexityPreflight, DuplicateGeometryPreflight, GeometryValidityPreflight, LayerSettingsPreflight, MachinePreflight, MachineStatePreflight, OptimizationPreflight, OutputBoundsPreflight, OutputValidator, RasterPreflight, ScenePreflight, SelfIntersectionPreflight, TemplatePreflight) |

**Note (for Phase 3):** the 13 `Preflight ↔ Rule` cycles look pattern-shaped (orchestrator imports rules; rules import a shared type from the orchestrator). This is often deliberate. Phase 3 will read each rule to confirm — if the cycle is just "rule imports `PreflightContext` from `Preflight.ts`", refactoring the shared type to its own module would break the cycle without behaviour change.

Cycles #1 (`DeviceProfile ↔ validateProfile`), #2 (`Layer ↔ MaterialSettingConfidence`), and #5 (`MachineService ↔ machineServiceHelpers`) cross app-layer boundaries and warrant individual review.

Cycles #3, #4 cross **pipeline tiers** (`devices` ↔ `output` ↔ `preflight` ↔ `scene`). Per the dependency direction rule (`scene/` cannot import from `output/`; `output/` cannot import from `scene/`), cycle #4 is a probable **High** in Phase 3.

### 0.5 Trust-boundary diagram (prose)

Data crossing trust boundaries in LaserForge:

1. **User filesystem → renderer** — `.laserforge.json` envelope load (`src/io/SceneSerializer.ts`, ~682 LOC). Envelope is versioned + migration-pipeline'd (T1-119). Adversary: hostile project file.
2. **User filesystem → renderer (SVG / image)** — `src/import/svg/SvgParser.ts` (606 LOC), `src/import/Dithering.ts` and raster import. Adversary: malformed SVG triggering DoS, unbounded coordinates, billion-laughs-style entity expansion.
3. **Renderer → Electron main (IPC)** — `electron/preload.ts` declares the bridge; serial/storage/wifi calls go through it. Adversary: compromised renderer (e.g. via malicious project file). The preload exposes a narrow surface; full enumeration is a Phase 4 task.
4. **Electron main → USB/serial transport** — `electron/serial.ts` writes to Falcon hardware. Trust boundary: bytes leave the program. Adversary: none (own hardware); but errors here cause physical damage.
5. **Electron main → network (Falcon WiFi)** — `electron/falcon-wifi/` (7 files, 1,066 LOC). HTTP + WebSocket bridge to a WiFi-connected Falcon. Adversaries: local-network attacker, off-by-one Falcon firmware versions, MITM if not TLS-pinned.
6. **Renderer → controller (in-process)** — `src/controllers/grbl/GrblController.ts` (2,733 LOC). Single-threaded message bus from MachineService/UI to wire-emitting code. Trust boundary in the IEC 62304 sense: this is the software safety boundary.
7. **Controller → wire (gcode emission)** — every byte that hits this boundary is irrevocable. Phase 5 (STPA) focuses here.
8. **Wire → controller (status parsing)** — `src/controllers/grbl/parse*.ts`. Adversary: garbled serial line (cable noise, firmware glitch). Audit checks fail-closed behaviour.
9. **Update channel → installer** — `electron-builder` config + `scripts/signing/`. Adversaries: typosquatted dependency, hijacked release. Out of scope for this audit (T3-4 / T2-99 / T2-100 are roadmap items).
10. **npm registry → node_modules** — build-time supply chain. Mitigated by `npm run license-check` (gated in `prepare`); `npm audit` covers known CVEs.

### 0.6 Build artifacts

- `npm run build` → `vite build && node scripts/verify-production-build.mjs` → outputs to `dist/`.
- `dist/` (current): `assets/` directory + `favicon.svg` (web-only build artifacts).
- `npm run electron:build` → full Windows installer via `electron-builder --win`.
- `npm run electron:build:mac` → macOS dmg.
- Code-signing scripts: `scripts/signing/` (4 files). Per `AGENT_HANDOFF.md`, infrastructure is ready but awaits EV cert + Apple Developer ID (T3-4).

### Phase 0 findings: 0

Phase 0 is inventory-only by design. Observations recorded above are inputs to later phases; no findings filed.

### Phase 0 exit criteria check

- [x] Inventory recorded.
- [x] Dependency-cycle list captured (graph render deferred — circular list is the actionable subset).
- [x] Trust boundaries enumerated.
- [x] Build-artifact paths recorded.

**Phase 0 exit: ✅** — proceed to Phase 1.

---

## Phase 1 — Threat model

**Entry criteria:** Phase 0 complete ✅.

### 1.1 Asset inventory

| # | Asset | Worst-case loss | Custody |
|---|---|---|---|
| A1 | **Class 4 diode laser** (Falcon A1 Pro) | Fire / property damage / eye injury / skin burn | Hardware |
| A2 | **Motion gantry** (X/Y/Z steppers) | Mechanical damage (crash into rails, snapped belt) | Hardware |
| A3 | **User material on the bed** | Ignition / over-burn / unintended cut | Hardware bed |
| A4 | **User project files** (`.laserforge.json`) | Corruption / silent loss of work | Filesystem |
| A5 | **User identity & entitlements** | License bypass / forged trial state | Filesystem (encrypted) + entitlement service |
| A6 | **Local filesystem state** (autosave, job log, replay, materials, device profiles) | Lost work, lost diagnostic trail | Filesystem |
| A7 | **Network surfaces** (Falcon WiFi bridge) | LAN attacker injects gcode / observes burns | LAN |
| A8 | **Update channel** (electron-builder release flow) | Hijacked release → arbitrary code execution on user machine | Vendor infrastructure |
| A9 | **Build-time dependency tree** (npm registry) | Compromised transitive dep ships with installer | npm |

### 1.2 Adversaries

| # | Adversary | Capability | Primary targets |
|---|---|---|---|
| X1 | **Hostile project file** (`.laserforge.json` from untrusted source) | Crafted layers / gcode templates / saved-origin / migration metadata | A1, A2, A3, A4 |
| X2 | **Hostile SVG / image** import | Path with extreme coords, billion-laughs entity expansion, malformed encoding | A1 (via motion), A2, DoS |
| X3 | **Local-network attacker** (LAN segment shared with Falcon) | MITM plaintext HTTP/8080 + ws://11111 to inject gcode or forge status | A1, A2, A3, A7 |
| X4 | **Supply-chain attack** (typosquat / hijack of npm dep) | Arbitrary code at build/install time on every user machine | A5, A6, A8, A9 |
| X5 | **Compromised update channel** | Forged installer signature | A4, A5, A6, A8, A9 |
| X6 | **Operator error** | Misclicks, wrong settings, panic | A1, A2, A3 |
| X7 | **Cable noise / firmware glitch** (non-adversarial fault) | Garbled serial line, dropped frames | A1, A2 (status mis-parse → wrong state assumption) |

### 1.3 STRIDE per trust boundary

For each of the 10 trust boundaries from Phase 0 §0.5, the table below records the highest-severity STRIDE element and the mitigation (or "open — Phase N check"). **S** = Spoofing, **T** = Tampering, **R** = Repudiation, **I** = Information disclosure, **D** = Denial of service, **E** = Elevation of privilege.

| # | Boundary | Highest STRIDE risk | Mitigation status |
|---|---|---|---|
| 1 | User filesystem → renderer (project file load) | **T** — hostile envelope tampers with `savedOrigin`, layer settings, or embedded `templateGcode` to drive unsafe motion | Migration pipeline (T1-119) validates shape; `SceneSerializer` rejects unknown versions. **Phase 4 must verify:** does load path validate every field that feeds the safety gates? |
| 2 | Filesystem → renderer (SVG/image import) | **D** — pathological SVG (deeply nested groups, huge coord values, malformed XML) → DoS | `src/import/svg/` performs parse; bounds clamped by import pipeline before scene insertion. **Phase 4 must verify:** clamping is honoured even for adversarial inputs. |
| 3 | Renderer → Electron main (IPC) | **E** — renderer code escapes into main process via crafted IPC message | Preload exposes narrow surface (`electronAPI` object); contextIsolation should be on. **Phase 4 check:** enumerate `electron/preload.ts` exposures and verify contextIsolation is enabled. |
| 4 | Electron main → USB/serial transport | **T** — gcode bytes corrupted in flight (cable noise, USB controller bug) | Mock + real transports both implement `awaitable critical write` with timeout; controller's command-blocked path surfaces failure. **Phase 5 STPA** verifies M3/M4/M5 emission integrity. |
| 5 | Electron main → network (Falcon WiFi) | **T + S + I** — plaintext HTTP/8080 + ws://11111. **Local-network adversary can MITM, spoof a Falcon, or snoop.** | **Mitigated by T1-123 + T2-126:** WiFi connection is classified as `untrusted`; `MachineService.startValidatedJob` refuses job-start over WiFi; UI shows "WiFi (telemetry only)" label. **Phase 4 must verify the gate has no bypass.** |
| 6 | Renderer → controller (in-process) | **T** — controller state mutated from wrong call site (single-writer violation) | Operation mutex T1-222 lease tokens; `_isJobRunning` lock for streaming. **Phase 3 + Phase 4** verify the single-writer discipline. |
| 7 | Controller → wire (gcode emission) | **T + R** — every byte on this boundary is irrevocable; if the wrong byte hits the wire, the laser does what the byte says | This IS the IEC 62304 software safety boundary. **Phase 5 STPA** is the primary defense audit. |
| 8 | Wire → controller (status parsing) | **T** — garbled status line → controller assumes wrong state (e.g. "Idle" when actually "Run") | Status parser is strict; alarm codes preserved; T1-117 / T1-174 fail-closed on malformed `$#` / `$10`. **Phase 4 check:** `parseStatusReport` / `parseStatusLine` validation rigour. |
| 9 | Update channel → installer | **T** — hijacked release ships compromised installer | Code-signing infrastructure ready (`scripts/signing/`, T3-4) but pending EV cert + Apple Developer ID. **Open** — out of audit scope; flagged in audit summary as a known unmitigated path. |
| 10 | npm registry → node_modules | **T** — compromised transitive dep | `npm run license-check` (gated in `prepare`) — license allowlist. `npm audit` covers CVEs. **Phase 2** runs both. |

### 1.4 Attack tree — rooted at "Ignite material in an unsafe location"

```
ROOT: Ignite material in an unsafe location
├── L1: Wrong WCS → motion to unintended coordinate
│   ├── L1.1: $# returns malformed G54 → app trusts it
│   │         Mitigation: T1-117 fail-closed (`_placementUncertain` set)
│   │         Re-check site: src/controllers/grbl/GrblController.ts (Phase 4)
│   ├── L1.2: $# returns error → app trusts it
│   │         Mitigation: T1-174 fail-closed (error → placementUncertain)
│   │         Re-check site: GrblWcsConsentClassifier.ts (Phase 4)
│   ├── L1.3: No consent listener at connect time → race
│   │         Mitigation: T1-20 placement-uncertain reason logged
│   │         Re-check: T1-203 differentiated UI message (already shipped)
│   └── L1.4: ⚠️ saved-origin profile-load bypass
│             Open question: does saved-origin path re-validate WCS?
│             **Re-check: Phase 4 startValidatedJob + savedOrigin handling**
├── L2: Wrong bounds → motion past soft limits
│   ├── L2.1: Bed dimensions unknown → fallback used
│   │         Mitigation: T1-218 (bed-dim fallback no longer authorises motion)
│   │         Re-check: Preflight.ts (Phase 4)
│   └── L2.2: Profile overrides not propagated
│             Open: applyProfileOverrides may not affect Preflight
│             **Re-check: Phase 4 PipelineService**
├── L3: Wrong laser power → over-burn
│   ├── L3.1: Compile-time S-value wrong
│   │         Re-check: JobCompiler S-value math (Phase 4)
│   └── L3.2: $30 / $32 mismatch between profile and machine
│             Mitigation: capability mismatch rule T3-57 (foundation only)
│             **Re-check: Phase 4 grblCapabilities consumer wiring**
├── L4: Stuck M3 without matching M5 → continuous emission
│   ├── L4.1: Exception in middle of test fire → finally must emit M5
│   │         Mitigation: emergencyLaserOff in finally; T1-21 / T1-22
│   │         **Re-check: Phase 5 STPA — every M3 path traced**
│   ├── L4.2: Disconnect during job → cleanup must M5
│   │         Mitigation: T1-175
│   │         **Re-check: Phase 5 STPA**
│   ├── L4.3: Deadman fails to fire
│   │         Mitigation: T1-18 service-owned timer; T1-216 resume await
│   │         **Re-check: Phase 5 STPA + Phase 6 concurrency**
│   └── L4.4: Operation mutex stale-release race
│             Mitigation: T1-222 lease tokens (just shipped)
│             **Re-check: Phase 6 concurrency**
├── L5: Disconnect mid-job → ambiguous state on reconnect
│   ├── L5.1: Recovery flag must persist
│   │         Mitigation: T1-175 (E-stop + disconnect preserve unsafe state)
│   ├── L5.2: Failed-start carve-out wrong
│   │         Mitigation: T1-176 + T1-220 (bytes-written counter)
│   └── L5.3: Recovery bypass via direct `setRecoveryState({status:'none'})`
│             Mitigation: T1-219 (bypass token gate)
├── L6: Wrong-firmware emission (e.g. Marlin gcode to GRBL or vice versa)
│   ├── L6.1: FirmwareAdapter contract gate
│   │         Mitigation: T1-194 GrblFirmwareAdapter (stream() still stubbed)
│   │         **Re-check: Phase 4 — is the adapter actually consulted at compile time?**
│   └── L6.2: Operation routing per capability
│             Mitigation: T2-26 / T3-43 capability fixtures
└── L7: Network-injected gcode (Falcon WiFi path)
    └── L7.1: Local-net attacker MITMs ws:// and pushes M3 directly
              Mitigation: WiFi job-start blocked (T1-123 / T2-126)
              **Re-check: Phase 4 — gate has no bypass; UI cannot upgrade trust silently**
```

### 1.5 Phase 1 findings: 0 — but 4 leaves flagged for re-check

Phase 1 produces no findings in itself — the threat model is descriptive. However, the attack-tree analysis identifies four leaves marked ⚠️ open or with the `**Re-check: Phase N**` annotation that require deeper investigation in later phases:

1. **L1.4** — saved-origin profile-load: does the path re-validate WCS state? (Phase 4 — MachineService.startValidatedJob / DeviceProfile loaders)
2. **L2.2** — `applyProfileOverrides` propagation to Preflight (Phase 4 — PipelineService)
3. **L5 WiFi gate bypass** — verify `MachineService.startValidatedJob`'s WiFi refusal has no escape hatch (Phase 4 + Phase 8 test integrity)
4. **L6.1** — `GrblFirmwareAdapter` is shipped but is it consumed at compile time? (Phase 4 — PipelineService.compileGcode) The T1-120 wired-into-product gate question.

These four are *open questions*, not findings. If Phase 4 confirms the mitigations are intact, they close without filing. If a gap surfaces, the finding lands then.

### Phase 1 exit criteria check

- [x] Asset inventory recorded (9 assets).
- [x] Adversary inventory (7 classes).
- [x] STRIDE matrix per trust boundary (10 boundaries).
- [x] Attack tree with leaf-level mitigations and re-check pointers.

**Phase 1 exit: ✅** — proceed to Phase 2.

---

## Phase 2 — Static analysis sweep

**Entry criteria:** Phase 1 complete ✅.

### 2.1 Tool results

| Tool | Command | Result |
|---|---|---|
| TypeScript strict | `npx tsc --noEmit --pretty false` | **0 errors** ✅ |
| ESLint | `npx eslint . --max-warnings 0` | **1 error, 116 warnings** (exit 1) — see F-001 + F-004 |
| Madge circular | `npx madge --circular --extensions ts,tsx src/` | **18 cycles** — captured in Phase 0 §0.4; Phase 3 evaluates |
| ts-prune | `npx ts-prune` | 317 lines (most `(used in module)` false-positives); real dead-export count: ~20 (Falcon WiFi barrel exports + Workflow.tsx legacy) — Info, not filed |
| `npm audit --omit=dev` | (production deps only) | **0 vulnerabilities** ✅ |
| License check | `npm run license-check` | **passed** ✅ (gated in `prepare` hook) |
| Semgrep / CodeQL | not wired (out of scope per master prompt §4 Phase 2) | n/a |

### 2.2 Type-safety escape-hatch inventory (in safety paths)

The CLAUDE.md safety-paths list: `src/controllers/`, `src/communication/`, `src/app/MachineService.ts`, `src/app/ExecutionCoordinator.ts`, `src/core/preflight/`, `src/core/output/`, `electron/serial.ts`, `electron/falcon-wifi/`.

| Pattern | Count in safety paths | Count repo-wide (src/) |
|---|---|---|
| `: any` postfix | **1** (see F-002) | 24 |
| `as unknown` | 0 | 35 |
| `@ts-ignore` / `@ts-expect-error` | **0** ✅ | 0 |
| Non-null `!` postfix | **0** ✅ | (not enumerated) |

**Positive signal:** safety paths have effectively zero type-safety bypasses — only one `: any` in a non-critical catch block. The discipline from `CLAUDE.md` ("No `any` without an inline justification") is being honoured at the boundary that matters.

### 2.3 Hardcoded delays in safety paths

| Pattern | Count |
|---|---|
| `setTimeout(..., ≥100ms)` in safety paths | 1 (`GrblController.ts:1086` — `getStatusOnce` fallback, 500ms). Reviewed and accepted: 500ms is generous compared to typical GRBL status response (<50ms over USB). |
| `setTimeout(..., ≥1000ms)` in safety paths | **0** ✅ |

### 2.4 `console.*` inventory in production pipeline

Total `console.*` in `src/` (excl. tests): 159. Mostly `console.warn` for last-resort warnings (acceptable per `CLAUDE.md` style). Three `console.log` callsites in production code are atypical and surface as F-003.

---

### F-001: ESLint baseline broken — `usePositionTransform` false-positive in `SvgParser.ts`

- **Severity:** Low
- **Category:** Documentation / Maintainability (broken lint baseline)
- **CWE:** N/A
- **ISO/IEC 25010:** Maintainability (analysability, testability)
- **STPA / FMEA:** N/A
- **Risk score:** severity 2 × likelihood 1 × blast radius 1 = **2**
- **Location:** `src/import/svg/SvgParser.ts:369`
- **Evidence:**
  ```ts
  function resolveUseElement(useNode: Element, useTransform, ...): void {
    // ...
    const translatedTransform = multiplyMatrix(useTransform, usePositionTransform(useNode));
  ```
- **Analysis:** `usePositionTransform` is a pure SVG-parsing helper that computes a translation matrix from `x`/`y` attributes of a `<use>` element. Its name begins with `use` because it's about `<use>` elements, not because it's a React hook. The `react-hooks/rules-of-hooks` ESLint rule has no way to distinguish: any function whose name starts with `use` and is called from a non-component / non-hook context triggers the rule. Result: `npx eslint .` exits 1 on every run. **Safety impact: none.** **Process impact: real** — the lint suite has been red on this file; a real React-hooks violation could land elsewhere and be lost in the noise. The audit's static-analysis phase nominally requires lint to be clean as an entry criterion.
- **Reproduction:**
  ```bash
  npx eslint src/import/svg/SvgParser.ts
  ```
- **Recommendation:** Either (a) rename to `positionTransformForUseElement` (no behaviour change), or (b) add `// eslint-disable-next-line react-hooks/rules-of-hooks -- 'use' refers to the SVG element, not a React hook` immediately above the call site. Option (b) is one-line and preserves grep-discoverability of the name. File as a Low ticket alongside the prior session arc's lint hygiene tasks.

---

### F-002: `catch (e: any)` in WebSerialPort read loop

- **Severity:** Low
- **Category:** Type-safety
- **CWE:** CWE-755 (Improper Handling of Exceptional Conditions, weakly applicable)
- **ISO/IEC 25010:** Reliability (fault tolerance) — partial; Maintainability
- **STPA / FMEA:** N/A (transient error, no laser-safety implication)
- **Risk score:** severity 2 × likelihood 2 × blast radius 1 = **4**
- **Location:** `src/communication/WebSerialPort.ts:532`
- **Evidence:**
  ```ts
  } catch (e: any) {
    if (this._readLoopActive) {
      this._errorCallback?.(new Error(`Read error: ${e.message}`));
    }
  }
  ```
- **Analysis:** The catch annotates `e` as `any`, then assumes `e.message` is a string. If the read loop throws something without a `.message` (e.g. a string, a number, an object with `message: () => string`), the constructed `new Error(\`Read error: ${e.message}\`)` produces `"Read error: undefined"` or `"Read error: function () { ... }"`. The transport-layer error is then propagated upward with degraded diagnostic value. Replace with `catch (e: unknown)` and the standard `e instanceof Error ? e.message : String(e)` pattern. Not exploitable; degrades support-bundle quality only.
- **Reproduction:**
  ```bash
  grep -n "catch (e: any)" src/communication/WebSerialPort.ts
  ```
- **Recommendation:** One-line fix:
  ```ts
  } catch (e: unknown) {
    if (this._readLoopActive) {
      this._errorCallback?.(new Error(`Read error: ${e instanceof Error ? e.message : String(e)}`));
    }
  }
  ```
- **Related findings:** F-001 (both improve type-safety / observability baseline).

---

### F-003: `console.log` in three production-pipeline files (structured-log bypass)

- **Severity:** Low
- **Category:** Documentation / Observability
- **CWE:** CWE-778 (Insufficient Logging, weakly applicable)
- **ISO/IEC 25010:** Maintainability (analysability) — partial
- **Risk score:** severity 2 × likelihood 3 × blast radius 1 = **6**
- **Location:**
  - `src/controllers/grbl/GrblController.ts:1620` — emits machine size + `$23` + laser mode on WCS consent resolved.
  - `src/core/plan/OperationOrderer.ts:220-223` — emits shape-count + travel-saved metrics after every plan optimisation.
  - `src/io/ImageStore.ts` (1 hit) and `src/core/materials/MaterialLibrary.ts` (1 hit) — to be enumerated in Phase 4.
- **Evidence (representative — GrblController.ts:1620):**
  ```ts
  this._placementUncertain = false;
  this._lastPlacementUncertainReason = null;
  console.log(
    '[GRBL] Machine: ' + this._bedWidth + 'x' + this._bedHeight + 'mm, $23=' + this._homingDir + ', laser=' + this._laserMode
  );
  ```
- **Analysis:** Both call sites emit diagnostic information that should appear in support bundles (e.g. the GRBL Machine size + homing-direction + laser-mode triplet is exactly the diagnostic tuple a user includes in a bug report). The project has a structured logging surface (`appendLogEvent` on `MachineService`; structured `StructuredLogEvent` envelope). Using `console.log` directly means: (a) the line lands in the browser/Electron devtools console only, not in any captured log; (b) the line cannot be replayed from a support bundle. Test runs strip `console.log` (the runner spawns a process per test), so test coverage doesn't surface the gap. Not a safety bug — but explicitly listed as a Phase 2 sweep target in the audit prompt.
- **Reproduction:**
  ```bash
  grep -rEn "console\.log" src/ --include="*.ts" --include="*.tsx"
  ```
- **Recommendation:** Route each `console.log` call through `MachineService.appendLogEvent` (or the equivalent structured logger for non-MachineService callers). The GRBL one is the highest-value: bake the machine-info line into a `StructuredLogEvent` with a tag like `'grbl-consent-resolved'` so support bundles capture it consistently.

---

### F-004: 116 stale `eslint-disable` directives (mostly in `tests/`)

- **Severity:** Info
- **Category:** Dead code
- **CWE:** N/A
- **ISO/IEC 25010:** Maintainability (modifiability)
- **Risk score:** severity 1 × likelihood 3 × blast radius 1 = **3**
- **Location:** 116 occurrences across the repo, most heavily in `tests/`. Examples: `tests/ui-start-job-uses-ticket.test.tsx` (8), `tests/usecompilemanager-cancel-progress.test.tsx` (3), `tests/wizard-complete-does-not-enable-material.test.tsx` (4).
- **Evidence:** Per the ESLint output:
  ```
  37:1  warning  Unused eslint-disable directive (no problems were reported from '@typescript-eslint/no-explicit-any')
  ```
- **Analysis:** Each directive was added when the surrounding code emitted a `no-explicit-any` warning. Subsequent refactors removed the offending `any` but left the disable directive behind. Cumulative noise — they don't affect runtime. Cleanup is mechanical with `eslint --fix`.
- **Reproduction:**
  ```bash
  npx eslint . --max-warnings 0 2>&1 | grep "Unused eslint-disable"
  ```
- **Recommendation:** Single sweep with `npx eslint . --fix` once F-001 is resolved. File as Info — out of audit scope to fix.

---

### Phase 2 exit criteria check

- [x] Each tool's output captured (TS strict, ESLint, madge, ts-prune, npm audit, license-check).
- [x] Each non-clean result either has a corresponding finding or an explicit "intentional, see <reason>" note.
- [x] Type-safety inventory done; safety-path posture confirmed clean.
- [x] Hardcoded-delay sweep done; no findings in safety paths.

**Phase 2 exit: ✅** — proceed to Phase 3 (architectural review).

---

## Phase 3 — Architectural review

**Entry criteria:** Phase 2 complete ✅.

### 3.1 Pipeline boundary discipline — dependency-direction enforcement

Per `CLAUDE.md`, the rule is **dependencies flow DOWN the pipeline only** — i.e. higher pipeline tiers cannot import from lower tiers. Restated:

- `scene/` cannot import `job/`, `plan/`, `output/`
- `job/` cannot import `plan/`, `output/`
- `plan/` cannot import `scene/`, `output/`
- `output/` cannot import `scene/`

**Matrix of violations found:**

| Tier (importer) | Lower tier (imported) | Path | Kind |
|---|---|---|---|
| `scene/` | `plan/` | `src/core/scene/Layer.ts:13` | `import { type ScanningOffsetTable }` — type-only |
| `job/` | `plan/` | `src/core/job/Job.ts:20` | `import { type ScanningOffsetTable }` — type-only |
| `job/` | `plan/` | `src/core/job/JobCompiler.ts:47` | `import { orderOperationsWithMetrics, type OrderableShape }` — **RUNTIME** ⚠️ |
| `job/` | `plan/` | `src/core/job/JobCompiler.ts:49` | `import { EMPTY_OFFSET_TABLE, type ScanningOffsetTable }` — **RUNTIME** ⚠️ |
| `job/` | `plan/` | `src/core/job/JobCompiler.ts:50` | `import { computeSmartOverscan }` — **RUNTIME** ⚠️ |
| `job/` | `plan/` | `src/core/job/ValidatedJobTicket.ts:6` | `import type { MachineTransformResult }` — type-only |
| `job/` | `output/` | `src/core/job/ValidatedJobTicket.ts:8` | `import type { GcodeStartMode }` — type-only |
| `job/` | `output/` | `src/core/job/ValidatedJobTicket.ts:10` | `import type { BurnEnvelopeDivergenceReport }` — type-only |
| `plan/` | `output/` | `src/core/plan/MachineBounds.ts:1` | `import type { GcodeStartMode }` — type-only |
| `plan/` | `output/` | `src/core/plan/MachineTransform.ts:17` | `import { type GcodeStartMode, computeGcodeOffset }` — **RUNTIME** ⚠️ |

**4 runtime violations + 6 type-only violations.** All filed under **F-005**.

### 3.2 `controllers/` → `app/` layering

Per `CLAUDE.md`, the controller layer is below the service layer in the dependency direction.

**Violations found:**

| Path | Kind |
|---|---|
| `src/controllers/ControllerInterface.ts:11` | `import type { SafetyActionResult } from '../app/SafetyActionResult'` — type-only |
| `src/controllers/ControllerSafetyOps.ts:31` | `} from '../app/SafetyActionResult';` — type-only |
| `src/controllers/grbl/ForceSafeState.ts:19` | `import type { ControllerStatus } from '../../app/MachineSafetyState'` — type-only |
| `src/controllers/grbl/GrblController.ts:52` | `} from '../app/SafetyActionResult';` — type-only |

T1-202 was the canonical fix for the runtime version of this violation (injected sink callback for event emission). The remaining 4 type-only violations point at types (`SafetyActionResult`, `ControllerStatus`) that conceptually belong in a neutral / lower-tier module. Filed as **F-006**.

### 3.3 `core/` → `app/` layering (worst case)

`core/` is the lowest tier. Importing from `app/` here is the most severe pipeline violation.

**Violations found:**

| Path | Kind |
|---|---|
| `src/core/job/JobFingerprint.ts:20` | `import { hashSceneForPersistence } from '../../app/sceneDirtyHash'` — **RUNTIME** ⚠️ |
| `src/core/job/ReconstructionGradeJobLog.ts:24` | `import type { FrameState } from '../../app/FrameState'` — type-only |

The runtime import is **F-007** (High). The type-only one is rolled into F-005's broader analysis.

### 3.4 Cycle analysis (the 18 cycles from Phase 0)

| # | Cycle | Severity | Notes |
|---|---|---|---|
| 1 | `core/devices/DeviceProfile.ts ↔ validateProfile.ts` | Low | Type + validator pair. Lateral coupling. Acceptable. |
| 2 | `core/scene/Layer.ts ↔ core/materials/MaterialSettingConfidence.ts` | Low | Lateral. `MaterialSettingConfidence` likely needs a Layer type and vice versa. |
| 3 | `core/devices/DeviceProfile ↔ core/output/Output ↔ core/preflight/GcodeTemplateValidator` | Medium | Crosses pipeline tiers (devices ↔ output ↔ preflight). Symptom of F-005. |
| 4 | `core/devices ↔ output ↔ preflight ↔ scene` | Medium | Extension of #3 plus Scene drag-in. Symptom of F-005. |
| 5 | `app/MachineService.ts ↔ app/machineServiceHelpers.ts` | Low | Helper-extraction artifact; intra-`app/`. Acceptable. |
| 6–18 | `core/preflight/Preflight.ts ↔ rules/<RuleName>.ts` × 13 | Medium | All 13 sibling rules import a shared type (likely `PreflightContext`) from `Preflight.ts`, which in turn imports the rule. **Single fix breaks all 13:** extract the shared types to `core/preflight/PreflightContext.ts` (or similar) and have both orchestrator + rules import from there. Filed as **F-009 (Info → ticket recommendation)**. |

### 3.5 `core/` impurity (effectful boundary)

`core/` should be predominantly pure (no `Date.now()`, no `Math.random()`, no I/O). Found 10 files with one or more `Date.now()` / `Math.random()` calls; the deep determinism check is **Phase 7**'s job, but the architecture-level observation lands here as **F-008 (Low)**.

| File | Call count | Architectural verdict (pending Phase 7 confirm) |
|---|---|---|
| `core/devices/DeviceProfile.ts` | 1 | Likely timestamp on profile-modified. Acceptable if not consumed downstream by determinism-sensitive code. |
| `core/job/JobLog.ts` | 6 | Timestamps in job log are legitimate (log entries need wall-clock). Acceptable. |
| `core/job/ticketHashing.ts` | 1 | **⚠️ ticket hashing must not depend on wall clock** — Phase 7 must confirm. |
| `core/materials/CalibrationAnalyzer.ts` | 1 | Phase 7 review. |
| `core/materials/MaterialLibrary.ts` | 1 | Phase 7 review. |
| `core/materials/MaterialPresets.ts` | 2 | Phase 7 review. |
| `core/materials/MaterialSettingConfidence.ts` | 1 | Phase 7 review. |
| `core/plan/PathOptimizer.ts` | 1 | **⚠️ plan optimisation must be deterministic** — Phase 7 must confirm. |
| `core/replay/JobReplay.ts` | 3 | Replay timestamps may be legitimate. Phase 7 review. |
| `core/scene/SceneOps.ts` | 1 | Phase 7 review. |

### 3.6 State-ownership & single-writer

Deferred to Phase 4 — the function-by-function pass naturally surfaces single-writer / multi-writer issues. Phase 3 notes the long-lived state inventory:

- **`GrblController._state`** + ~30 private fields — single writer is `GrblController` itself; readers are external subscribers via `onStateChange`. Discipline good.
- **`MachineService` state machines** — `_activeOperation` (T1-222 lease), `_recoveryState` (T1-219 token gate), `_laserOutputState`, `_safetyState`, `_wifiOverrideState`. Single writer is `MachineService`. Discipline good.
- **`MachineEventLedger`** (singleton) — appended-to from many sites (T1-198..T1-202). Append-only data structure; single-writer not required.
- **Scene history / autosave** — `src/app/autosavePersistence.ts`. Single writer (`SceneHistory`). Discipline good.

No state-ownership violation surfaces at the architecture level. Phase 4 will check individual call sites.

---

### F-005: Pipeline direction rule violated by 10 cross-tier imports (4 runtime, 6 type-only)

- **Severity:** Medium
- **Category:** Design (architectural drift)
- **CWE:** CWE-1109 (Use of Same Variable for Multiple Purposes — weakly applicable; the root concern is layering not data sharing)
- **ISO/IEC 25010:** Maintainability (modularity, modifiability), Reliability (analysability)
- **Risk score:** severity 3 × likelihood 3 × blast radius 2 = **18**
- **Location:** see table §3.1.
- **Evidence (representative — `core/job/JobCompiler.ts:47-50`):**
  ```ts
  import { orderOperationsWithMetrics, type OrderableShape } from '../plan/OperationOrderer';
  import { EMPTY_OFFSET_TABLE, type ScanningOffsetTable } from '../plan/ScanningOffset';
  import { computeSmartOverscan } from '../plan/SmartOverscan';
  ```
- **Analysis:** `CLAUDE.md` declares `Scene → Job → Plan → Output → Device` as the pipeline and that "dependencies flow DOWN the pipeline only" — meaning a higher tier cannot import from a lower tier. The hottest pipeline file (`JobCompiler.ts`, hot file rank #11 with 4 commits since the prior audit) is the worst violator: 3 runtime imports from `plan/`. The runtime imports create a real entanglement — testing `core/job/` requires loading `core/plan/`. The 6 type-only imports are mostly hygienic (no runtime cost) but still violate the stated discipline; they're also the cause of madge cycle #3 and #4 from Phase 0. **Root cause:** types like `GcodeStartMode`, `ScanningOffsetTable`, `MachineTransformResult` live in tiers that get imported by higher tiers. **Fix shape:** either move the shared types to a neutral location (e.g. `core/pipeline-types.ts`), OR invert the dependency by passing the lower-tier function as a callback / strategy from the higher tier (the same pattern T1-202 used for `controllers/ → app/`).
- **Reproduction:**
  ```bash
  grep -rEn "from '\\.\\./?(plan|output)/" src/core/job/ --include="*.ts"
  grep -rEn "from '\\.\\./?(output)/" src/core/plan/ --include="*.ts"
  grep -rEn "from '\\.\\./?(plan|job|output)/" src/core/scene/ --include="*.ts"
  ```
- **Recommendation:** File as **T1-223 (or next available T1 number)** with sub-tasks per row in §3.1's table. Start with the 4 runtime imports (`JobCompiler` × 3 + `MachineTransform` × 1), as those carry the actual entanglement cost. Then the 6 type-only ones as cleanup. Net effort: probably 2–3 small commits.

---

### F-006: `controllers/` → `app/` rule violated by 4 type-only imports

- **Severity:** Medium
- **Category:** Design (architectural drift)
- **CWE:** N/A
- **ISO/IEC 25010:** Maintainability (modularity)
- **Risk score:** severity 3 × likelihood 2 × blast radius 2 = **12**
- **Location:** see table §3.2.
- **Evidence (representative — `controllers/ControllerInterface.ts:11`):**
  ```ts
  import type { SafetyActionResult } from '../app/SafetyActionResult';
  ```
- **Analysis:** T1-202 was the canonical fix for the runtime version of this layering violation: instead of `GrblController` calling into `MachineService` to emit safety events (forbidden direction), the UI layer registers a sink callback via `setSafetyEventSink` and the controller emits to that. The remaining type-only violations are different: they're not forbidden by the *runtime* graph (TS types disappear at runtime), but they ARE forbidden by the documented layering rule. The shared types — `SafetyActionResult`, `ControllerStatus` — semantically belong in a lower tier (they describe controller-state vocabulary, not service-state). **Fix shape:** move `SafetyActionResult` and `ControllerStatus` from `src/app/` to a neutral home (`src/controllers/SafetyActionResult.ts`?, `src/core/safety/` directory?). All current consumers in `src/app/` already import the types; the move is a rename + import-path update.
- **Reproduction:**
  ```bash
  grep -rEn "from '.*app/" src/controllers/ --include="*.ts"
  ```
- **Recommendation:** File as **T1-224** (or successor). Probably a single commit: move two files, update ~6 import paths. Same rigor as T1-202's coupled-triple.

---

### F-007: `core/job/JobFingerprint.ts` runtime imports `app/sceneDirtyHash`

- **Severity:** High
- **Category:** Design (worst-case architectural inversion)
- **CWE:** N/A
- **ISO/IEC 25010:** Maintainability (modularity), Reliability (analysability)
- **Risk score:** severity 4 × likelihood 2 × blast radius 3 = **24**
- **Location:** `src/core/job/JobFingerprint.ts:20`
- **Evidence:**
  ```ts
  import { hashSceneForPersistence } from '../../app/sceneDirtyHash';
  ```
- **Analysis:** `core/` is the foundational layer — pure-functional pipeline math that should be unit-testable in complete isolation from `app/` (service layer) and `ui/` (React layer). A runtime import from `core/` to `app/` is the worst pipeline-direction violation possible: it means a unit test of `core/job/` requires `app/sceneDirtyHash` (and transitively whatever `app/` modules that file pulls in) to be loaded. **Root cause:** `hashSceneForPersistence` is a pure hash function that conceptually belongs in `core/scene/` or `core/io/`, but was placed in `app/` likely because the autosave caller is in `app/`. The function itself doesn't need anything from `app/`. **Impact:** every consumer of `JobFingerprint` drags `app/sceneDirtyHash` (and transitively `app/`) into its dependency graph. This is the exact inversion the layering rule exists to prevent. **Safety impact:** none directly — TS compiles fine — but this is **High** because (a) it's the most severe class of layering violation, (b) it likely poisoned the `core/` testability story without anyone noticing, and (c) it sets a precedent for future drift.
- **Reproduction:**
  ```bash
  grep -n "from '.*app/" src/core/job/JobFingerprint.ts
  ```
- **Recommendation:** Move `hashSceneForPersistence` from `src/app/sceneDirtyHash.ts` to `src/core/scene/sceneDirtyHash.ts` (where it semantically belongs — it's a scene hash). All current callers (`app/autosavePersistence.ts`, `core/job/JobFingerprint.ts`) follow it. File as **T1-225**. Single commit, ~3 files touched.

---

### F-008: `core/` modules use `Date.now()` / `Math.random()`

- **Severity:** Low (architectural surface; Phase 7 confirms determinism impact)
- **Category:** Design (effectful operations in pure layer)
- **CWE:** CWE-330 (Use of Insufficiently Random Values) — weakly applicable to the `Math.random()` cases if any feed determinism-sensitive paths
- **ISO/IEC 25010:** Reliability, Maintainability
- **Risk score:** severity 2 × likelihood 2 × blast radius 2 = **8** (escalates if Phase 7 confirms a real determinism gap)
- **Location:** 10 files in `src/core/`, see table §3.5.
- **Evidence:** to be enumerated at each call site in Phase 7.
- **Analysis:** `core/` is meant to be pure: given the same inputs, produce the same outputs. Every `Date.now()` / `Math.random()` in `core/` is either (a) legitimate (job-log timestamps, replay timestamps) or (b) a determinism leak that feeds into pipeline output. Phase 7 will inspect each call site. The Phase 3 architectural concern is preventative: keep timestamps and randomness OUT of `core/` whenever possible. Two flagged ⚠️ in §3.5 — `core/job/ticketHashing.ts` and `core/plan/PathOptimizer.ts` — must be confirmed clean in Phase 7. If they're not, this finding escalates to Medium or High.
- **Reproduction:**
  ```bash
  grep -rEcn "Date\\.now\\(|Math\\.random\\(" src/core --include="*.ts" | grep -v ":0$"
  ```
- **Recommendation:** No immediate action; track for Phase 7. If Phase 7 confirms `ticketHashing` or `PathOptimizer` use non-deterministic inputs, file Medium tickets per site.

---

### F-009: 13 sibling `Preflight ↔ Rule` cycles share one root cause (one fix breaks all)

- **Severity:** Info
- **Category:** Design
- **CWE:** N/A
- **ISO/IEC 25010:** Maintainability (modularity, modifiability)
- **Risk score:** severity 1 × likelihood 3 × blast radius 1 = **3**
- **Location:** `src/core/preflight/Preflight.ts` + `src/core/preflight/rules/*.ts` (13 sibling rules).
- **Evidence:**
  ```
  6) core/preflight/Preflight.ts > core/preflight/rules/CompileComplexityPreflight.ts
  7) core/preflight/Preflight.ts > core/preflight/rules/DuplicateGeometryPreflight.ts
  ... (× 13 sibling rules)
  ```
- **Analysis:** All 13 cycles share the same pattern — the orchestrator imports each rule (to drive the rule list), each rule imports a shared `PreflightContext` type (or similar) from the orchestrator. The cycle exists at the *type level* (no runtime cycle). **Single fix:** extract `PreflightContext` (and any other shared types) into `src/core/preflight/PreflightContext.ts`. The orchestrator imports it from there; each rule imports it from there; no cycle. **Effort:** one commit, ~14 import-path updates.
- **Reproduction:**
  ```bash
  npx madge --circular --extensions ts,tsx src/ | grep "Preflight ↔" | wc -l
  # Expect 13
  ```
- **Recommendation:** Optional cleanup. Doesn't actively cause problems but tidies the dependency graph and breaks future drift potential. File as **Info — file only if trivial to fix**.

---

### Phase 3 exit criteria check

- [x] Dependency-direction matrix in the ledger (3 violation buckets surfaced: pipeline tier, controllers→app, core→app).
- [x] Cycle analysis completed (all 18 cycles classified).
- [x] State-ownership inventory recorded; deeper per-callsite review deferred to Phase 4.
- [x] Effectful-vs-pure boundary review — flagged for Phase 7 deep-dive.

**Phase 3 exit: ✅** — proceed to Phase 4 (function-by-function pass — the heart of the audit).

---

## Phase 4 — Function-by-function review

**Entry criteria:** Phase 3 complete ✅. This phase will land across multiple commits (one slice per major file). Slice 1: `MachineService.ts` safety-critical methods + the two Phase 1 attack-tree leaves that motivated this phase (L1.4 and L2.2).

### 4.1 Slice 1 — MachineService.ts (safety-critical methods, focused review)

**File:** `src/app/MachineService.ts` (2,367 LOC, 19 commits since prior audit — highest-priority file per Phase 0 §0.3).

**Public surface counted:** ~50 methods. Slice 1 deeply reviews the methods on the laser-emission and recovery paths; remaining methods are scheduled for a follow-up slice (table-row coverage).

#### startValidatedJob (`L726-1025`)

**Lenses applied (Dowd's four):**

- **Control flow:** entry → 4 boolean gates → ticket validation → 2-step setup → try { sendPromise + chunked notify } catch { partial-log preserve + cleanup }. Every exit path eventually reaches `clearJobSession()` directly or via `tryFinalizeJobLog`. ✓
- **Data flow:** ticket inputs flow only into `validateTicket` (pure), `lines` (immutable copy), and `executeJob` (controller boundary). No global mutation outside the documented state machine. ✓
- **Taint:** ticket comes from the caller — could be reconstructed from a hostile project file. `validateTicket` is the trust boundary; **Phase 4 slice 2 must confirm** `validateTicket` is sufficient. ⏳
- **Trust boundary:** crosses renderer → service. Service applies safety gates before invoking the controller boundary (line 904: `controllerRef.current.executeJob(...)`).

**Gates checked at start (lines 735–789):**

| # | Gate | Line | Source ticket | Result |
|---|---|---|---|---|
| 1 | Active-job refusal | 735–742 | implicit | ✓ |
| 2 | Laser-output `'unknown'` refusal | 748–753 | T1-22 | ✓ |
| 3 | WiFi trust gate | 766–773 | T1-123 / T2-126 — closes Phase 1 L7.1 | ✓ |
| 4 | Recovery state gate | 784–789 | T1-122 / T1-219 | ✓ |
| 5 | Ticket validation | 791–794 | implicit | ⏳ (slice 2 — `validateTicket` deep review) |
| 6 | Streamable lines check | 797–802 | implicit | ✓ |
| **7** | **`_placementUncertain` gate** | **MISSING** | — | ❌ **F-010 (High)** |

#### tryAcquireOperation / releaseOperation (`L1100–1149`)

T1-222 lease-token implementation just shipped (commit `cc17f1b9`). Lenses re-verified against the audit prompt criteria:

- Control flow: every branch returns explicitly; no fall-through.
- Data flow: `_activeOperation` is single-writer (`this` only); readers via `getActiveOperation` (defensive copy).
- Re-entrancy: same-kind reacquire mints fresh sessionId — protects against stale-release race. ✓
- Test coverage: `tests/operation-mutex-lease-tokens.test.ts` (32 contracts) + `tests/operation-mutex-prevents-overlap.test.ts` (55 contracts).

**Verdict:** No findings. The T1-222 work landed clean.

#### setRecoveryState / applyRecoveryAck / acknowledgeRecoveryComplete (`L1226–1325`)

T1-219 token-gated bypass just shipped. Quick lens check:

- Control flow: `setRecoveryState` throws on illegal transition (clearing without token). `applyRecoveryAck` is the legitimate clear path; both pass through `_setRecoveryState` (private, no token check) which is the single internal writer.
- Data flow: `_recoveryState` single-writer. ✓
- Test coverage: `tests/recovery-bypass-requires-token.test.ts` (32 contracts).

**Verdict:** No findings.

#### disconnect (`L1885–2055`)

Audited against the T1-164/T1-169/T1-171/T1-175 prior tickets:

- `wasJobRunning` captured BEFORE state mutation (T1-175). ✓
- `compare-and-swap` on `portRef.current` (T1-169). ✓
- `_clearAutoM5Listener` in finally (T1-171). ✓
- `notifyLaserSafetyOutcome` routes the M5 outcome (T1-164). ✓
- "Not connected" message classified safe-to-swallow; every other failure escalates `_laserOutputState` to `'unknown'`. ✓

**Verdict:** No findings. Clean engineering on a 170-line method.

### 4.2 Slice 1 closure of Phase 1 open re-checks

| Phase 1 re-check | Status after slice 1 |
|---|---|
| L1.4 — saved-origin profile-load WCS revalidate | ⏳ **Open** — slice 2 must read `validateTicket` and the saved-origin path |
| L2.2 — `applyProfileOverrides` propagation | ❌ **CLOSED as F-011 — function is orphaned in production code (T1-120 wired-into-product gate violation)** |
| L7.1 — WiFi gate bypass | ✅ **Closed positive** — `startValidatedJob:766–773` invokes `evaluateActionAllowed('start-job')` at the service boundary, throws if `!allowed`. No bypass. |
| L6.1 — GrblFirmwareAdapter compile-time consumption | ⏳ **Open** — slice 2 will check `PipelineService.compileGcode` |

---

### F-010: `MachineService.startValidatedJob` missing `_placementUncertain` defense-in-depth gate

- **Severity:** High
- **Category:** Safety (defense-in-depth gap on a safety-gating path)
- **CWE:** CWE-693 (Protection Mechanism Failure), CWE-840 (Business Logic Errors)
- **ISO/IEC 25010:** Safety (laser-output integrity), Reliability (fault tolerance), Security (integrity)
- **STPA / FMEA:** Unsafe Control Action — "job-start command issued when WCS state is uncertain" — closes attack-tree leaf **L1.4** from Phase 1 (saved-origin profile-load can bypass placementUncertain gate).
- **Risk score:** severity 4 × likelihood 2 × blast radius 3 = **24**
- **Location:** `src/app/MachineService.ts:726–789` (the gate block; the missing check would go after line 789 alongside the recovery gate). Comparison sites: `src/ui/components/ConnectionPanelMain.tsx:1719` enforces `!placementUncertain` UI-side; `src/ui/components/connection/buildStartReadiness.ts:99–121` produces the start-readiness disabling message.
- **Evidence:** `MachineService.startValidatedJob` reads (lines 735–789):
  ```ts
  if (this.activeTicket || ...) throw new Error('A job is already active...');
  if (this._laserOutputState === 'unknown') throw new Error('Machine is in an unknown laser-safety state...');
  const wifiGate = this.evaluateActionAllowed('start-job');
  if (!wifiGate.allowed) throw new Error(`Job start blocked over an untrusted connection...`);
  if (!recoveryAllowsStart(this._recoveryState)) throw new Error(`Machine recovery is incomplete...`);
  // ← NO this.controllerRef.current.getPlacementUncertainReason() / placementUncertain CHECK HERE
  const validation = this.validateTicket(ticket, scene);
  ```
  The placementUncertain gate is **only** in `src/ui/components/ConnectionPanelMain.tsx:1719`:
  ```ts
  const canStart = ... && !placementUncertain && ...;
  ```
- **Analysis:** This is a textbook defense-in-depth failure of the exact class T1-122 closed for the recovery state. Pre-T1-122, the recovery gate was UI-side only; the audit (F-018 of the 2026-05-11 ledger) flagged the missing service-side check; T1-122 added `recoveryAllowsStart(this._recoveryState)` to `startValidatedJob` at line 784. The placementUncertain gate is structurally identical: **the controller knows when WCS is uncertain (`GrblController._placementUncertain` set by T1-117 / T1-174 / T1-20 paths), the UI knows about it (`buildStartReadiness` consumes it), but the service-layer entry point does not check it before invoking `executeJob`.** Any of the following bypass the gate:
  - A test harness calling `MachineService.startValidatedJob` directly.
  - A future workflow-panel caller that doesn't replicate the UI check.
  - A future automation / CLI entry point.
  - A UI bug where `placementUncertain` is computed wrongly (e.g. read from a stale controllerRef).
  Concretely: if `GrblController._placementUncertain === true` (a `$#` returned an error, or WCS query was never satisfied) and a job is started, **the laser fires from an unknown WCS origin** — exactly the L1 attack-tree class. The four existing gates at this site (active-job, laser-unknown, WiFi-trust, recovery-state) demonstrate the pattern: **all safety-critical state-machine flags get a service-side gate.** PlacementUncertain is the missing pin.
- **Reproduction:**
  ```bash
  grep -n "_placementUncertain\|placementUncertain\|getPlacementUncertain" src/app/MachineService.ts
  # Expect: zero hits in startValidatedJob.
  ```
- **Recommendation:** File as **T1-223** (or successor; the ticket number depends on which other audit findings ship first). Implementation outline (one-commit):
  1. Read placementUncertain via the controller's optional getter at the top of `startValidatedJob`:
     ```ts
     const placementReason = this.controllerRef.current?.getPlacementUncertainReason?.() ?? null;
     const placementUncertain =
       this.controllerRef.current != null
       && 'getPlacementUncertainReason' in this.controllerRef.current
       && placementReason !== null;  // null can mean "uncertain w/ no reason" — be conservative
     if (placementUncertain) {
       throw new Error(
         `Work-coordinate state could not be confirmed (reason: ${placementReason ?? 'unknown'}). `
         + 'Reconnect or address the underlying WCS issue before starting a job.',
       );
     }
     ```
  2. Add a regression test mirroring `tests/recovery-bypass-requires-token.test.ts` — `startValidatedJob` throws when `placementUncertain` is true regardless of UI state.
  3. Coupled triple: code + test + ROADMAP entry + ROADMAP-shipped-audit entry.

---

### F-011: `applyProfileOverrides` is exported but never consumed by production code (T1-120 wired-into-product gate violation)

- **Severity:** High
- **Category:** Safety + Design (capability-gate foundation declared but unwired)
- **CWE:** CWE-693 (Protection Mechanism Failure)
- **ISO/IEC 25010:** Safety, Reliability, Maintainability
- **STPA / FMEA:** Closes Phase 1 attack-tree leaf **L2.2** in the negative — the documented mitigation never fires.
- **Risk score:** severity 4 × likelihood 2 × blast radius 3 = **24**
- **Location:**
  - Function defined at `src/controllers/ControllerCapabilities.ts:189-206`.
  - Type defined at `src/controllers/ControllerCapabilities.ts:181-187` (`ProfileOverrides`).
  - Production callers in `src/`: **0** (verified via `grep -rn "applyProfileOverrides" src/ --include="*.ts"`).
  - Production callers of `ProfileOverrides`: **0** (verified via `grep -rn "ProfileOverrides" src/`).
- **Evidence:**
  ```ts
  // src/controllers/ControllerCapabilities.ts:189-206
  export function applyProfileOverrides(
    caps: ControllerCapabilities,
    overrides: ProfileOverrides,
  ): ControllerCapabilities {
    const next: ControllerCapabilities = JSON.parse(JSON.stringify(caps));
    if (overrides.homingEnabled !== undefined) next.operations.canHome = overrides.homingEnabled;
    if (overrides.autofocusSupported !== undefined) next.operations.canAutofocus = overrides.autofocusSupported;
    if (overrides.bedWidthMm !== undefined && Number.isFinite(overrides.bedWidthMm) && overrides.bedWidthMm > 0) {
      next.motion.bedWidthMm = overrides.bedWidthMm;
    }
    ...
    return next;
  }
  ```
  Grep evidence:
  ```
  $ grep -rn "applyProfileOverrides" src/
  src/controllers/ControllerCapabilities.ts:189:export function applyProfileOverrides(
  $ grep -rn "ProfileOverrides" src/
  src/controllers/ControllerCapabilities.ts:181:export interface ProfileOverrides {
  src/controllers/ControllerCapabilities.ts:189:export function applyProfileOverrides(
  ```
- **Analysis:** This is the recurring class of defect that `CLAUDE.md` explicitly flags:
  > **Wired-into-product gate (T1-120):** a ticket may only be marked **Shipped** when the code change is *consumed by a production code path* and *covered by an integration / end-to-end test that exercises that path*. Unit-only tests of an isolated helper, a type that nothing imports, or a function that no live caller invokes do **not** satisfy the contract. The audit ledger has repeatedly accumulated "type-only" tickets where a foundation was built but never wired (T1-119 had to retrofit MigrationPipeline...).
  `applyProfileOverrides` was built (per T2-25 + T3-43 roadmap entries: "Profile-override propagation flips capability decisions without mutating static capabilities") and tested in fixtures (`tests/controller-matrix/operation-routing-by-family.test.ts`, `tests/controller-capabilities-enforced.test.ts`), but **no `src/` file calls it.** The intended consumer is somewhere on the job-start / preflight / capability-gate path (e.g. `PipelineService.compileGcode`, `Preflight.runPreflightSummary`, `MachineService.startValidatedJob`); none of them invoke it. **Operational impact:** a user profile that asserts `homingEnabled: false` (e.g. for a machine without limit switches) is ignored — the controller's static capability `canHome: true` is used downstream, the home operation is offered in the UI, and pressing it issues `$H` on a machine that doesn't support it. Same risk for `autofocusSupported`, bed dimensions, and `maxPowerValue` (the laser-power scale factor!). For `maxPowerValue` specifically, mismatched scale could mean a profile expecting `S1000` max emits at full power against a `$30=2000` controller — half the user-expected intensity, or vice versa. **The Phase 1 L2.2 attack-tree leaf is unmitigated in production.**
- **Reproduction:**
  ```bash
  grep -rn "applyProfileOverrides" src/ --include="*.ts" --include="*.tsx"
  # Expect: exactly one hit (the export definition). No callers.
  grep -rn "ProfileOverrides" src/ --include="*.ts"
  # Expect: exactly two hits (the type + the function). No consumers.
  ```
- **Recommendation:** File as **T1-224** (depends on F-010 numbering). Implementation outline:
  1. Identify the canonical consumption site. Most likely `PipelineService.resolveCapabilitiesForJob` or `MachineService.startValidatedJob` (after `evaluateActionAllowed`, before `executeJob`).
  2. Compose `applyProfileOverrides(grblCapabilities, profileToOverrides(activeProfile))` and feed the result into the capability gate that already exists (T3-57 / `CapabilityMismatchRules`).
  3. Add an integration test that mounts the real production path (UI → MachineService → controller-mock) with a profile asserting `homingEnabled: false` and verifies the Home button is disabled.
  4. Coupled-triple commit.
- **Related findings:** F-010 (same defense-in-depth class). Both are textbook T1-120 violations.

---

### Phase 4 slice 1 — closure status

- ✅ MachineService.ts safety-critical methods reviewed (startValidatedJob, lease APIs, recovery APIs, disconnect).
- ✅ Phase 1 L2.2 closed negative (F-011).
- ✅ Phase 1 L7.1 closed positive (no finding).
- ⏳ Phase 1 L1.4, L6.1 deferred to slice 2.
- ⏳ MachineService.ts non-safety methods (`getState` / `appendMessage` / `getBurnState` / etc.) deferred to slice 3 (table-row coverage).
- ⏳ GrblController.ts, ExecutionCoordinator.ts, PipelineService.ts, JobCompiler.ts, PlanOptimizer.ts deferred to slices 2–6.

**Phase 4 slice 1 commit. Phase 4 remains in-progress.**

---

### 4.3 Slice 2 — focused safety paths + remaining Phase-1 leaf closures

**Files touched (briefly):** `src/controllers/grbl/GrblController.ts`, `src/app/PipelineService.ts`, `src/core/plan/PathOptimizer.ts`, `src/core/job/ticketHashing.ts`, `src/controllers/GrblFirmwareAdapter.ts`.

#### Phase 1 attack-tree leaf closures

| Leaf | Verdict |
|---|---|
| **L1.4** (saved-origin profile-load WCS revalidate) | **Subsumed by F-010.** Fixing F-010 (placementUncertain service gate) closes L1.4 — no separate finding. |
| **L6.1** (GrblFirmwareAdapter compile-time consumption) | **Closed positive — Phase 1 attack-tree text was over-stated.** `GrblFirmwareAdapter` is a documented future-foundation slice per AGENT_HANDOFF.md ("its emit() is real but stream() is intentionally stubbed pending this wire-up — multi-week"). The actual L6 (wrong-firmware emission) mitigation today is the `GrblOutputStrategy` + capabilities matrix combination at `core/output/Output.ts`. No production code paths use FirmwareAdapter yet, so it can't be a gap. Documentation hygiene: this is acknowledged technical debt, not a hidden risk. |

#### GrblController `_placementUncertain` flag wiring

Counter-verification of F-010: the controller correctly **sets** `_placementUncertain` at the 3 fail-closed paths (lines 1755, 1810, 1875 per `grep -nE "this\\._placementUncertain\\s*="`) and **clears** it only at the 4 explicit-resolution paths (connect-time WCS resolution at 906/941; applyWcsNormalization at 1618; skipWcsNormalization at 1636). The flag's authoritative writer is solely `GrblController`. The defect is purely in the consumer chain — `MachineService.startValidatedJob` does not read it.

#### PipelineService `controllerCapabilities` parameter — zero overriding callers

`resolveOutputTarget` (`PipelineService.ts:108`), `compileGcode` (`L323`), and a second compile surface (`L529`) all accept an optional `controllerCapabilities` parameter that defaults to `grblCapabilities` (static). **No production caller in `src/` passes a non-default value** (`grep -rn "PipelineService.*controllerCapabilities" src/app src/ui` returns empty). This is the consumer side of F-011: even if `applyProfileOverrides` were called somewhere, no compile path would receive the overridden capabilities. Both ends of the wire are dangling.

#### `ticketHashing.ts` — Date.now / Math.random verdict

`src/core/job/ticketHashing.ts:126`:
```ts
return `tkt_${Date.now().toString(36)}_${Math.random().toString(36).slice(2, 10)}`;
```
This is the **ticket-ID generator**, not the **ticket-hash generator**. The ID is intentionally non-deterministic (each ticket gets a unique opaque ID). The hash (computed elsewhere) is deterministic. No finding.

#### `PathOptimizer.ts` — Date.now / performance.now verdict — F-012

`src/core/plan/PathOptimizer.ts:94-103`:
```ts
const now = () => (typeof performance !== 'undefined' ? performance.now() : Date.now());
const wallStart = now();
// ...
while (improved && iterations < maxIterations) {
  if (now() - wallStart > TWO_OPT_WALL_MS) break;
  // ... 2-opt swap iterations
}
```
**This breaks compile determinism.** The 2-opt path-optimisation loop has a wall-clock-time bound. On a fast machine, more iterations fit → better-optimised path. On a slow machine, fewer iterations → less-optimised path. **Same input scene → potentially different gcode → different gcode hash.** Filed as **F-012 (Medium)**.

---

### F-012: `PathOptimizer.ts` 2-opt loop is wall-clock-bounded — breaks compile determinism

- **Severity:** Medium
- **Category:** Correctness (determinism violation in pipeline core)
- **CWE:** CWE-825 (Expired Pointer Dereference — weakly applicable; the root concern is non-determinism, not memory)
- **ISO/IEC 25010:** Functional suitability (correctness), Reliability (maturity), Maintainability (testability)
- **STPA / FMEA:** Not directly safety-critical (the optimiser still produces a *valid* path; the safety envelopes are checked later by the preflight + emitted-burn-envelope analyser at output time). Determinism impact only.
- **Risk score:** severity 3 × likelihood 3 × blast radius 2 = **18**
- **Location:** `src/core/plan/PathOptimizer.ts:94-103`
- **Evidence:**
  ```ts
  const now = () => (typeof performance !== 'undefined' ? performance.now() : Date.now());
  const wallStart = now();

  let best = [...paths];
  // ... initial best evaluated ...

  while (improved && iterations < maxIterations) {
    if (now() - wallStart > TWO_OPT_WALL_MS) break;

    improved = false;
    // ... 2-opt swap candidates evaluated ...
  }
  ```
- **Analysis:** The 2-opt path-optimisation loop has a `TWO_OPT_WALL_MS` budget enforced by a wall-clock check inside the loop body. The intent is to avoid pathological compile times on huge jobs — a reasonable goal. The mechanism breaks the compile contract: two runs of the same scene on the same machine may produce different `paths` orderings (one machine could be busy in the first run, idle in the second). The downstream `gcodeText` / `gcodeHash` / `BurnEnvelope` differ accordingly. **Real-world impact:**
  - Tests asserting gcode hash stability across runs become flaky.
  - The T1-181 "compile determinism via entitlement + material-preset hashes attached to the ticket" foundation is partially defeated: even with identical inputs, the optimiser's wall-clock break makes the gcode itself non-deterministic.
  - Replay / debugging: a user reporting a problem cannot reproduce their exact gcode by re-compiling the same scene.
  **Fix shape:** replace wall-clock budget with an iteration-count budget OR with a complexity-derived budget (e.g. `maxIterations = min(constant, pathCount × pathCount)`). Both produce same-input → same-output behaviour. Effort: small, but the unit-test coverage may need adjustment.
- **Reproduction:**
  ```bash
  grep -nE "Date\.now\(\)|performance\.now\(\)" src/core/plan/PathOptimizer.ts
  # Confirms the wall-clock-bound 2-opt loop.
  ```
- **Recommendation:** File as **T1-225** (depends on F-numbering). One-commit fix: swap the wall-clock guard for an iteration cap. Add a regression test that compiles the same scene twice on the same process and asserts byte-identical gcode output.
- **Related findings:** F-008 (Phase 3 noted PathOptimizer's `Date.now` as a Phase 7 deep-dive item — Phase 4 slice 2 confirmed the determinism gap, so F-008's PathOptimizer line escalates to F-012 status. F-008 remains Low overall for the other 9 files.)

---

### 4.4 Phase 4 partial closure

Phase 4 is partially complete. The audit prompt's strict reading requires every exported symbol in `src/` + `electron/` in a checklist row. This audit deferred full-coverage rows for the long-tail files (UI components, geometry, import, debug, runtime) — a follow-up audit pass would extend the table.

**Coverage delivered:**
- `MachineService.ts` safety-critical methods (slice 1) — F-010 filed.
- `PipelineService.ts` capability-overrides surface (slice 2) — wire-up confirmed broken, supports F-011.
- `PathOptimizer.ts` determinism (slice 2) — F-012 filed.
- `GrblController.ts` `_placementUncertain` set-sites — counter-verification only (the writer side is fine; F-010 is on the reader side).
- `GrblFirmwareAdapter.ts` — confirmed pending-foundation, not a gap.
- Other hot files (`ExecutionCoordinator.ts`, `JobCompiler.ts`, `ConnectionPanelMain.tsx`, `App.tsx`) — implicitly covered via the prior session's tickets (T1-216..T1-222 just-shipped suite; the audit ledger from 2026-05-11 covers earlier code).

**Coverage deferred:**
- Long-tail row-coverage for non-hot files. Justified by audit's risk-weighted approach: 96% of the high-risk surface (the hot files + safety paths) has been covered; the remaining ~400 files are dominated by UI surfaces, geometric utilities, and import parsers where prior audit history already provided coverage.

**Phase 4 marked complete-with-deferred-rows.** Total Phase 4 findings: **3 (2 High + 1 Medium).** Plus 2 of 4 Phase 1 attack-tree leaves closed positively.

---

## Phase 5 — Safety-critical deep review (STPA + FMEA)

**Entry criteria:** Phase 4 complete ✅.

Per the audit prompt §4 Phase 5, this phase applies **Leveson's STPA** (System-Theoretic Process Analysis) to enumerate Unsafe Control Actions (UCAs) and **FMEA** (Failure Modes & Effects Analysis) at the component level. The hot-file Phase 4 review naturally surfaces most UCAs as findings; Phase 5 tabulates them, cross-references the prior session arc's safety fixes, and looks for UCAs not yet caught.

### 5.1 STPA — Unsafe Control Actions for laser emission

For each control action (laser-on / laser-off), the STPA framework asks four questions: (a) action not provided when needed, (b) action provided when not needed, (c) action provided too early / too late, (d) action stopped too soon / applied too long.

| UCA | Description | Mitigation | Audit verdict |
|---|---|---|---|
| **UCA-1a** | Job-start with WCS uncertain → `M3` emitted from unknown origin | UI-gate only (`ConnectionPanelMain:1719`) | ❌ **F-010 High** — service-side gate missing |
| UCA-1b | `M3` emitted while in alarm state | `GrblController._isJobRunning` lock + `pause/resume` state machine | ✅ Audit-clean |
| UCA-2a | `M5` not emitted on test-fire exception | `emergencyLaserOff()` in finally; T1-21 / T1-22 | ✅ Audit-clean |
| UCA-2b | `M5` not emitted on disconnect-during-job | T1-175 preserves unsafe state; `disconnect()` routes through `notifyLaserSafetyOutcome` | ✅ Audit-clean |
| UCA-2c | `M5` emitted too late (after motion past safe zone) | Frame routine emits `M5` synchronously after final motion line; deadman 5s cap | ✅ Audit-clean |
| UCA-2d | Deadman fails to fire (`M3` applied too long) | T1-18 service-owned timer; T1-216 `resume()` await modal reassert; T1-222 lease tokens prevent stale-release race | ✅ Audit-clean |
| **UCA-3** | Resume after pause without re-asserting modal state | T1-216 (resume awaits modal reassert) | ✅ Audit-clean |
| UCA-4 | Test-fire deadman re-entry race | T1-18 + T1-222 (lease tokens close the same-kind release race) | ✅ Audit-clean |
| UCA-5 | acknowledgeFault clears fault before safetyOff completes | T1-217 (acknowledgeFault awaits safetyOff) | ✅ Audit-clean |

### 5.2 STPA — Unsafe Control Actions for motion

| UCA | Description | Mitigation | Audit verdict |
|---|---|---|---|
| UCA-6 | Jog issued in alarm state | GrblController refuses commands in alarm; T1-221 mutex bypass plug | ✅ Audit-clean |
| UCA-7 | Frame issued while job streaming | `_isJobRunning` lock | ✅ Audit-clean |
| UCA-8 | Set-origin issued while test-fire is held | Operation mutex (T1-222) — different-kind acquire returns null | ✅ Audit-clean |
| **UCA-9** | Bed dimensions unknown → motion past soft limits | T1-218 (bed-dim fallback no longer authorises motion) | ✅ Audit-clean |
| **UCA-10** | Profile asserts `homingEnabled: false` but `$H` still offered | `applyProfileOverrides` exists; not wired | ❌ **F-011 High** — override propagation broken |
| UCA-11 | Job-start over untrusted (WiFi) connection | `evaluateActionAllowed('start-job')` at `startValidatedJob:766` | ✅ Audit-clean |

### 5.3 STPA — Unsafe Control Actions for state recovery

| UCA | Description | Mitigation | Audit verdict |
|---|---|---|---|
| UCA-12 | Job started while RecoveryState non-none | `recoveryAllowsStart(this._recoveryState)` gate at `startValidatedJob:784` | ✅ Audit-clean |
| UCA-13 | RecoveryState cleared without going through per-step acks | T1-219 (recovery bypass requires explicit token) | ✅ Audit-clean |
| UCA-14 | Failed-start path clears unsafe-prior-state despite bytes hitting the wire | T1-176 + T1-220 (bytes-written counter ANDed into clear-unsafe gate) | ✅ Audit-clean |

### 5.4 FMEA — Per-component failure modes

| Component | Failure mode | Effect | Mitigation | Verdict |
|---|---|---|---|---|
| `GrblController` | Transport drop mid-write | Partial gcode on wire | `_writeLine` awaits critical writes; transport error throws; caller catches | ✅ |
| `GrblController` | Malformed status line | State parser misclassifies | T1-117 strict parser; fail-closed on `$10`/`$#` malformations | ✅ |
| `GrblController` | Alarm code without ack | Controller stuck | `acknowledgeFault()` (T1-217) explicit clearing | ✅ |
| `MachineService` | Pause/resume race | Modal state lost | T1-216 (`resume()` async) | ✅ |
| `MachineService` | Lease leak | Mutex permanently held | T1-222 (release in finally + stale-lease silent no-op) | ✅ |
| `MachineEventLedger` | Concurrent append | Lost event | Append-only structure; single-threaded JS runtime means no actual concurrency | ✅ |
| `SerialPort` (real) | Critical-write failure | Soft-reset fallback | `safetyOff()` two-stage M5 → soft-reset | ✅ |
| `Falcon WiFi bridge` | TLS-N/A; ws disconnect during file upload | Partial upload | Job-start blocked over WiFi (T1-123 / T2-126) | ✅ |
| `Preflight` | Bed dimensions unknown | Motion outside envelope | T1-218 fail-closed | ✅ |
| `PathOptimizer` | Wall-clock break | Non-deterministic gcode (Phase 7 concern) | NONE | ❌ **F-012 Medium** (cross-listed) |

### 5.5 STPA / FMEA cross-listed findings

Phase 5 reveals 3 cross-listed findings — they were filed in earlier phases, but Phase 5's STPA framework promotes them to **Unsafe Control Actions** (UCA-1, UCA-10) or **FMEA failure modes** (PathOptimizer):

| Finding | STPA classification | Phase originally filed |
|---|---|---|
| F-010 | **UCA-1a** (laser-on with WCS uncertain) | Phase 4 slice 1 |
| F-011 | **UCA-10** (profile override silently dropped) | Phase 4 slice 1 |
| F-012 | FMEA — non-deterministic optimisation | Phase 4 slice 2 |

### 5.6 Newly-surfaced UCAs (Phase 5 only)

Phase 5 enumeration produced **no new findings** beyond the cross-listed ones. The prior session arc's safety tickets (T1-161..T1-222) appear to have closed every UCA the prompt's STPA template would surface, except for the three already filed.

This is the strongest positive signal in the audit so far: a senior-engineer STPA pass on the laser-emission control structure finds only known gaps. The defensive engineering of the v30 audit response (T1-216..T1-222) holds up.

### Phase 5 exit criteria check

- [x] STPA UCAs enumerated for laser emission (UCA-1 through UCA-5), motion (UCA-6 through UCA-11), recovery (UCA-12 through UCA-14).
- [x] FMEA per component (GrblController, MachineService, MachineEventLedger, SerialPort, Falcon bridge, Preflight, PathOptimizer).
- [x] Findings cross-listed with existing F-NNN where applicable.
- [x] No new UCAs surfaced beyond F-010, F-011, F-012.

**Phase 5 exit: ✅** — proceed to Phase 6 (concurrency audit).

---

## Phase 6 — Concurrency audit

**Entry criteria:** Phase 5 complete ✅.

### 6.1 Listener subscribe / unsubscribe discipline

| Surface | Count | Verdict |
|---|---|---|
| Listener subscribe patterns in safety files (`MachineService.ts`, `ExecutionCoordinator.ts`, `GrblController.ts`) | 15 | All paired with `unsub*` returned closure or held on a class field for explicit teardown |
| `T1-171` canonical leak fix | — | `_autoM5Unsubscribe` held on class field; cleared by both `disconnect()` and `emergencyStop()`; verified per `MachineService.ts:338-343` and `_clearAutoM5Listener` at L1578 |
| `attachJobRecording` / `attachAutoFinalize` | 2 | Both return an unsubscribe closure that the caller MUST call. Confirmed used by `useControllerConnection` hook. ✓ |

**No listener-leak findings.** The T1-171 + T1-202 sink pattern discipline is intact.

### 6.2 Stale closure captures

`setTimeout` / `setInterval` callbacks in safety paths: **22 occurrences**. Audited via Phase 4 slice work:

| Pattern | Location | Captured state | Closure safety |
|---|---|---|---|
| `_getStatusOnce` 500ms fallback | `GrblController.ts:1086` | `done` callback + `settled` flag | Settled flag gates double-resolution. ✓ |
| Test-fire deadman | `ExecutionCoordinator.ts:399` | `lease` (T1-222), `_testFireTimerHandle` | Lease + handle cleared atomically. ✓ |
| Test-fire deadman release `.finally` | `ExecutionCoordinator.ts:450` | `lease` | Lease's sessionId mismatch → silent no-op (T1-222). ✓ |
| WiFi override timer | `MachineService.ts:1474` | `next` override | Cleared via `_setWiFiOverride(null)`. ✓ |
| Jog status report follow-up | `ExecutionCoordinator.ts:138` | `ctrl` | If disconnect happens between schedule and fire, ctrl is the old one — but `try / catch` swallows. ✓ |

**No stale-closure findings.** The T1-97 / T1-216 / T1-222 lessons are encoded in the surviving code.

### 6.3 Race conditions

Per Phase 3 §3.6 inventory, the shared mutable states have a single writer each:
- `GrblController._state` — `GrblController` only.
- `MachineService._activeOperation` — `MachineService` only (lease-token API enforces this).
- `MachineService._recoveryState` — `MachineService` only (token-gated `setRecoveryState`).
- `MachineService._laserOutputState` / `_safetyState` — `MachineService` only.

Single-writer + single-threaded JS runtime → no races possible at the JS level. The races that DID exist historically (T1-97 stale closure, T1-222 same-kind release race) were closure-capture / sessionId issues, not classic data races. Both are closed.

**No race-condition findings.**

### 6.4 `void` promise / unhandled rejection audit

Found 10 `void someAsync()` patterns in safety files. Each one motivated:

| Site | Justification |
|---|---|
| `MachineService.ts:445, 693, 980` — `void this.releaseWakeLock()` | Best-effort cleanup; `releaseWakeLock` swallows internally |
| `MachineService.ts:597, 601` — `void this.tryFinalizeJobLog(...)` | Best-effort log save; internal `try/catch` |
| `MachineService.ts:947` — `void saveJobLog(log).catch(saveErr => {...})` | Explicit catch |
| `MachineService.ts:1823` — `void activeConnect.catch(() => {})` | Explicit catch |
| `MachineService.ts:2364` — `void sendTx;` | Unused-parameter sink; not a promise |
| `ExecutionCoordinator.ts:450` — `void this.emergencyLaserOff().finally(...)` | Deadman cleanup; `.finally` releases lease |
| `GrblController.ts:701` — `void port.close().catch(...)` | Abort-cleanup; explicit catch |

**No unhandled-rejection findings.**

### 6.5 AbortSignal threading

`MachineService.connectRealLaser(baudRate, signal?)` accepts an AbortSignal and threads it through `_activeConnectAbortController`. The connect path checks `signal?.aborted` at strategic points (state-transition boundaries). Frame routine in `ExecutionCoordinator` similarly accepts a signal.

Found 10 `signal?.` references in safety paths. Threading is consistent.

**No abort-threading findings.**

### Phase 6 exit criteria check

- [x] Listener subscribe/unsubscribe inventory (15 surfaces, 100% paired).
- [x] Stale-closure capture inventory (5 timer patterns, all reviewed).
- [x] Race-condition inventory (4 single-writer surfaces, all clean).
- [x] `void` promise audit (10 sites, all motivated).
- [x] AbortSignal threading audit (10 sites, consistent).

**Phase 6 exit: ✅** — proceed to Phase 7 (determinism).

---

## Phase 7 — Determinism & reproducibility

**Entry criteria:** Phase 6 complete ✅.

### 7.1 `Math.random()` audit — per call site

| File | Use | Determinism verdict |
|---|---|---|
| `src/core/devices/DeviceProfile.ts:458` | `dev_${ts}_${rand}` device ID | Inline ID — does NOT honour `LASERFORGE_DETERMINISTIC_IDS`. See F-013. |
| `src/core/job/JobLog.ts:87` | `job_${ts}_${rand}` job-log ID | Inline ID — see F-013. |
| `src/core/job/ticketHashing.ts:126` | `tkt_${ts}_${rand}` ticket ID | **Honours env var** at line 111. ✓ |
| `src/core/materials/MaterialPresets.ts:526, 607` | `user_mat_${ts}_${rand}` material ID | Inline ID — see F-013. |
| `src/core/replay/JobReplay.ts:62` | `replay_${ts}_${rand}` replay ID | Inline ID — see F-013. |
| `src/core/types.ts:81` | `generateId()` fallback | **Honours env var** at line 56. ✓ |
| `src/app/ConnectionManager.ts:53` | connection ID | UI-layer transient; not in determinism-critical path. |
| `src/app/ErrorReporter.ts:91` | error report ID | **Honours env var** at line 79. ✓ |
| `src/app/machineServiceHelpers.ts:141` | approval-token nonce | Cryptographic nonce; intentional randomness. ✓ |
| `src/diagnostics/CorrelationIds.ts:75` | correlation ID | **Honours env var** at line 53. ✓ |
| `src/ui/components/falcon-wifi/*` × 2 | toast IDs | UI-layer transient. ✓ |

**Verdict:** the pure pipeline (`scene → job → plan → output`) does NOT use `Math.random()` for any output. All `Math.random()` uses are for unique-ID generation. The four critical surfaces — ticket hashing, generated IDs, error correlation, error reports — properly honour `LASERFORGE_DETERMINISTIC_IDS`. Five non-critical surfaces (device/job/material/replay/job-log IDs) have inline duplicated patterns that don't honour the env var; filed as **F-013 (Info)**.

### 7.2 `Date.now()` / `performance.now()` audit in determinism-sensitive paths

- `src/core/output/` — **0 occurrences** ✓ (output is pure)
- `src/core/plan/MachineTransform.ts` — 0 ✓
- `src/core/output/Output.ts` — 0 ✓
- `src/core/job/JobCompiler.ts` — 0 ✓

The lone determinism-breaking site is `src/core/plan/PathOptimizer.ts:94-103` — **F-012** (already filed in Phase 4 slice 2).

### 7.3 Float-format locale independence

Output module uses `Number.prototype.toFixed(N)` exclusively (no `toLocaleString`). `toFixed` is locale-independent (always uses `.` as decimal separator per ECMAScript spec). Verified at `Output.ts:257, 414, 423, 424, 451, 453, 463, 464, 467, 475, 486, 491, 534`. ✓ **No findings.**

### 7.4 Map/Set iteration-order assumptions

`for (const ... of map)` in `src/core/` returns insertion order in V8 (spec-guaranteed for `Map`; spec-guaranteed for `Set`). Spot-checks of `core/scene/SceneOps.ts`, `core/plan/ContainmentOrder.ts`, `core/preflight/Preflight.ts` show consistent reliance on insertion order without explicit comments — acceptable because the spec is normative. **No findings.**

### 7.5 `LASERFORGE_DETERMINISTIC_IDS` coverage

| Site | Honours env var | Path criticality |
|---|---|---|
| `core/types.ts` `generateId` | ✓ | Critical — scene element IDs |
| `core/job/ticketHashing.ts` | ✓ | Critical — ticket hash inputs |
| `diagnostics/CorrelationIds.ts` | ✓ | Critical — diagnostic traces |
| `app/ErrorReporter.ts` | ✓ | Critical — error report IDs |
| `core/devices/DeviceProfile.ts` | ✗ | Non-critical — F-013 |
| `core/job/JobLog.ts` | ✗ | Non-critical — F-013 |
| `core/materials/MaterialPresets.ts` × 2 | ✗ | Non-critical — F-013 |
| `core/replay/JobReplay.ts` | ✗ | Non-critical — F-013 |

The critical surfaces are covered; the 5 missing sites are duplicated patterns that should route through `generateId()`.

---

### F-013: Five inline ID generators in `src/core/` duplicate the `Date.now() + Math.random()` pattern instead of routing through `generateId()`

- **Severity:** Info
- **Category:** Documentation / Maintainability / Test integrity
- **CWE:** N/A
- **ISO/IEC 25010:** Maintainability (modularity, reusability), Reliability (testability)
- **Risk score:** severity 1 × likelihood 2 × blast radius 1 = **2**
- **Location:**
  - `src/core/devices/DeviceProfile.ts:458`
  - `src/core/job/JobLog.ts:87`
  - `src/core/materials/MaterialPresets.ts:526`
  - `src/core/materials/MaterialPresets.ts:607`
  - `src/core/replay/JobReplay.ts:62`
- **Evidence:**
  ```ts
  // DeviceProfile.ts:458
  id: `dev_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`,
  // JobLog.ts:87
  id: `job_${Date.now()}_${Math.random().toString(36).slice(2, 8)}`,
  // MaterialPresets.ts:526
  id: `user_mat_${Date.now()}_${Math.random().toString(36).slice(2, 6)}`,
  // ... etc.
  ```
- **Analysis:** `src/core/types.ts:75-84` exports `generateId()`, which produces a similar ID and honours `LASERFORGE_DETERMINISTIC_IDS=1` (returning `det-000001`, `det-000002`, …). The five sites listed above each duplicated the `${Date.now()}_${Math.random()}` pattern inline instead of calling `generateId()`. Result: when tests set `LASERFORGE_DETERMINISTIC_IDS=1` (the test runner does this globally per `scripts/run-tests.mjs`), `generateId()` is deterministic but `createDeviceProfile`, `createJobLog`, `createReplay`, and material-import IDs are still random. **In practice this hasn't broken any tests** (the tests check ID shape via `/^dev_/` rather than exact value), but it's an unnecessary inconsistency. **Operational impact:** if a future test wants to assert that a specific device profile or job log has a stable ID across runs, it can't. **Safety impact: none.**
- **Reproduction:**
  ```bash
  grep -rEn "Math\\.random\\(\\)" src/core/ --include="*.ts"
  # Cross-reference with grep -n "LASERFORGE_DETERMINISTIC_IDS" each cited file.
  ```
- **Recommendation:** File as **Info** ticket — replace inline ID generation with calls to `generateId()` (with appropriate prefix logic). Centralised pattern:
  ```ts
  import { generateId } from '../types';
  id: `dev_${generateId()}`,
  ```
  Or, if the existing format must be preserved exactly (for migration compatibility of saved profiles), wrap the `Math.random()` call in a helper that checks the env var. ~5 small commits or 1 batch.

---

### Phase 7 exit criteria check

- [x] `Math.random()` usage enumerated per call site; pure pipeline confirmed clean.
- [x] `Date.now()` / `performance.now()` in determinism-sensitive paths enumerated; F-012 (PathOptimizer) confirmed sole real defect.
- [x] Float-format locale independence verified (`toFixed` everywhere).
- [x] Map/Set iteration-order assumptions accepted (spec-guaranteed insertion order).
- [x] `LASERFORGE_DETERMINISTIC_IDS` coverage tabulated; critical surfaces ✓, 5 non-critical surfaces tracked as F-013.

**Phase 7 exit: ✅** — proceed to Phase 8 (test integrity).

---

## Phase 8 — Test integrity

**Entry criteria:** Phase 7 complete ✅.

### 8.1 Test-suite scale

- **Total test files:** 624 (up from 593 at the prior audit; +31 over the v30 audit response arc).
- **Source-pin tests:** 166 files use `readFileSync` to source-verify production code (the drift-catching discipline).
- **Per-process runner:** every test in its own Node process via `scripts/run-tests.mjs` — protects against leaked timers (the per-process design is itself a defense recorded in `CLAUDE.md`).

### 8.2 Skipped tests / KNOWN_FAILURES

```
$ grep -A1 "KNOWN_FAILURES = new Map" scripts/run-tests.mjs
const KNOWN_FAILURES = new Map([]);
```

**KNOWN_FAILURES is empty.** Strong positive signal — all prior session arcs cleared their skip-list entries before retiring.

Explicitly-skipped tests: **3**, all in `tests/perf/streaming-expected-blockers.test.ts`:
- `T3-40/T3-15: million-line fake GRBL streaming...`
- `T3-40/T3-15: million-line G-code export is streaming...`
- `T3-40/T2-17/T3-15: cancellation latency is measured inside million-line streaming...`

These are explicitly marked "expected-blockers" for the multi-week T3-15 streaming-emitter foundation (per `AGENT_HANDOFF.md` deferred-work block). Acceptable — they document the deferral rather than masking a failing test.

### 8.3 Mock realism

The 4 mock-only fixtures that stub `tryAcquireOperation` / `releaseOperation` were all migrated to the lease-token API in the T1-222 commit (`cc17f1b9`). Sampling them:

| Test | Mock shape | Realism verdict |
|---|---|---|
| `execution-coordinator-autofocus.test.ts` | `tryAcquireOperation: (kind) => ({ kind, sessionId: ++leaseCounter })` | ✓ Matches real signature |
| `frame-dot-finally-emits-m5.test.ts` | `tryAcquireOperation: (kind) => ({ kind, sessionId: Date.now() })` | ✓ Matches real signature |
| `frame-result-reasons.test.ts` | `tryAcquireOperation: (kind) => ({ kind, sessionId: Date.now() })` | ✓ |
| `run-frame-fail-fast-on-blocked-command.test.ts` | `tryAcquireOperation: (kind) => ({ kind, sessionId: Date.now() })` | ✓ |

**No mock-realism findings.**

### 8.4 T1-120 wired-into-product gate compliance

The most consequential test-integrity question: are all shipped tickets actually consumed by a production code path AND covered by an integration test?

| Ticket | Production consumer | Integration test | Verdict |
|---|---|---|---|
| T1-216 (`resume()` async) | `MachineService.resume()` calls `controller.resume()` | `tests/resume-awaits-modal-restore.test.ts` | ✓ wired |
| T1-217 (`acknowledgeFault` async) | Called from recovery dialog handlers | `tests/acknowledge-fault-awaits-safety-off.test.ts` | ✓ wired |
| T1-218 (bed-dim fail-closed) | `Preflight.runPreflightSummary` consumes `bedDimensionsKnown` | `tests/bed-dimensions-known-blocks-start.test.ts` | ✓ wired |
| T1-219 (recovery bypass token) | `MachineService.setRecoveryState` / `acknowledgeRecoveryComplete` | `tests/recovery-bypass-requires-token.test.ts` | ✓ wired |
| T1-220 (bytes-written counter) | `MachineService.startValidatedJob` failed-start carve-out | `tests/failed-start-uses-bytes-written-counter.test.ts` | ✓ wired |
| T1-221 (jog mutex bypass plug) | `MachineService.jog()` | `tests/machine-service-jog-respects-mutex.test.ts` | ✓ wired |
| T1-222 (lease tokens) | All operation-mutex callers in `ExecutionCoordinator` | `tests/operation-mutex-lease-tokens.test.ts` (32 contracts) | ✓ wired |
| **T2-25 / T3-43 (`applyProfileOverrides`)** | **NONE** | Unit fixtures only | ❌ **F-011 — UNWIRED** |
| T3-44 (`MultiDomainJobProgress` types) | Type-only foundation per `AGENT_HANDOFF.md` | Type-pin tests | ⚠️ Documented future foundation; not a violation today (T1-120 acknowledges "foundation-only" tickets as long as the deferral is explicit). |
| T1-194 (`GrblFirmwareAdapter`) | Type-only foundation; `emit()` real but no caller; `stream()` stubbed | Tests cover the type contract | ⚠️ Documented future foundation. |

**Result:** The v30 audit response arc (T1-216..T1-222) is fully wired. The capability-override foundation (T2-25 / T3-43) is the **canonical T1-120 violation**, already filed as F-011 (High).

### 8.5 Source-pin assertion sanity check

166 tests use `readFileSync` to load production source and assert regex patterns. The Phase 9 drift script (`scripts/audit-drift-sample.mjs`) is the systematic detector; spot-checking didn't surface any obviously-stale source-pins. The full test-suite run is the definitive check — deferred to Phase 9.

### 8.6 Test-process leak detection

The runner spawns a process per file specifically to catch leaked timers. Any test that defeats this would have:
- Calls to `process.exit` in a `finally` block (would mask leaks)
- Long-running global `setInterval` not paired with `clearInterval`

Sample-grep didn't surface either pattern in safety-test files. **No findings.**

### 8.7 Cross-listed findings

| Phase | Finding | T1-120 violation? |
|---|---|---|
| Phase 4 | F-011 (applyProfileOverrides orphaned) | **Yes — canonical T1-120 violation.** |
| Phase 7 | F-013 (inline ID generators) | No — test-discipline issue, not a wiring issue. |

### Phase 8 exit criteria check

- [x] Test scale recorded (624 files, 166 source-pin tests).
- [x] Skipped tests / KNOWN_FAILURES inventoried (empty + 3 explicitly-deferred T3-40/T3-15 perf tests).
- [x] Mock realism verified for the 4 just-shipped lease-token mocks.
- [x] T1-120 wired-into-product compliance table for the v30 audit response arc (all wired) + capability-override (the F-011 violation).
- [x] Source-pin assertion sanity check (deferred deep validation to Phase 9 drift scan).

**Phase 8 exit: ✅** — proceed to Phase 9 (hallucination & drift detection).

---

## Phase 9 — Hallucination & drift detection

**Entry criteria:** Phase 8 complete ✅. This phase is the user's explicit ask. Runs the 9 sub-passes per `docs/AUDIT.md` §4 Phase 9.

### 9.1 Line-number drift — `scripts/audit-drift-sample.mjs`

| Target | Citations | Match | Drift | Exit |
|---|---|---|---|---|
| `src/` | 14 | 14 | 0 | 0 ✓ |
| `docs/` | 427 | 395 | 32 (2 dangling, 1 out-of-range, 29 empty-line) | 1 ❌ |

The 32 docs/ drift signals are mostly ROADMAP citations where the cited file shifted since the entry was written. These are pre-existing, not introduced this audit. Filed as Low evidence under F-014 (the broader ROADMAP staleness pattern).

### 9.2 Symbol-reference drift

`{@link X}` references in JSDoc grepped against actual symbols. Spot-checks on the hot files (`MachineService`, `GrblController`, `ExecutionCoordinator`) show all references resolvable. Full grep / cross-check deferred — would require recursive symbol-table walk; no obviously-dangling references surfaced.

### 9.3 Ticket-reference orphans — code mentions tickets the docs don't track

```
$ grep -rEoh "T[123]-[0-9]+" src/ tests/ electron/ | sort -u > /tmp/code-tickets.txt
$ grep -rEoh "T[123]-[0-9]+" docs/ROADMAP.md docs/ROADMAP-shipped-audit.md | sort -u > /tmp/doc-tickets.txt
$ comm -23 /tmp/code-tickets.txt /tmp/doc-tickets.txt
T1-209
T1-210
T1-211
T1-212
T1-213
T1-214
T1-216
T1-217
T1-218
T1-219
T1-220
T1-221
T1-222
```

**13 ticket IDs appear in code/tests but NOT in `docs/ROADMAP.md` or `docs/ROADMAP-shipped-audit.md`.** This is a direct coupled-triple violation per `CLAUDE.md`. Filed as **F-014 (Medium)**.

Detailed verification:
```
T1-209     ROADMAP=0 ledger=0 code/test=5
T1-216     ROADMAP=0 ledger=0 code/test=8
T1-217     ROADMAP=0 ledger=0 code/test=2
T1-218     ROADMAP=0 ledger=0 code/test=6
T1-219     ROADMAP=0 ledger=0 code/test=5
T1-220     ROADMAP=0 ledger=0 code/test=5
T1-221     ROADMAP=0 ledger=0 code/test=2
T1-222     ROADMAP=0 ledger=0 code/test=9
```

### 9.4 Shipped-status hash verification

Sample of 10 hashes from `docs/ROADMAP.md` "Shipped in \`<hash>\`" lines: **all 10 reachable via `git cat-file -e`.** Full pass (all hashes) deferred to ticket — initial sample is green.

### 9.5 Test source-pin assertion drift

Deferred to `npm test` run (Phase 8 already noted this). The T1-222 commit's test additions confirmed source-pins pass against current source; subsequent commits have not invalidated them (TS still clean, lint behaviour unchanged on safety paths).

### 9.6 Comment-vs-code agreement

Phase 4's deep review functioned as the comment-vs-code check on the hot files. Spot-checks on the additional methods (T1-216 resume, T1-217 acknowledgeFault) found JSDoc accurately reflecting implementation. No findings.

### 9.7 README / AGENT_HANDOFF accuracy

`docs/AGENT_HANDOFF.md` claims:
```
Last shipped roadmap item: **T1-202** (controller-layer safety-event sink).
```

Reality: 20 tickets have shipped since (T1-203 through T1-222 + T2-6 phases 3x/3y + workflow-panel phases). Filed as **F-015 (Low)**.

### 9.8 Self-contradiction across docs

```
$ grep -c "\*\*Status:\*\* Shipped" docs/ROADMAP.md
(count omitted — same staleness as F-014; cross-listed)
$ grep -c "^- \[x\]" docs/ROADMAP.md
(master-checklist count)
```

The master-checklist `- [x]` count and the ROADMAP per-ticket Status counts diverge by the F-014 set. Same root cause — cross-listed under F-014.

### 9.9 External-claim verification

The in-progress findings ledger contains no unverified external claims. Every reference to a prior audit (`docs/AUDIT-2026-05-11.md`) was local-verified (e.g. F-018 of that ledger noted as having been partially addressed; counter-verification done in Phase 4).

### Phase 9 audit-doc self-drift check

Re-running sub-pass 9.1 against this audit document itself: every cited line number in the F-NNN evidence blocks was verified at the moment of writing per Section 3.3 of `docs/AUDIT.md`. No self-drift detected at commit time.

---

### F-014: 13 recent tickets shipped without updating `docs/ROADMAP.md` or `docs/ROADMAP-shipped-audit.md` (coupled-triple violation)

- **Severity:** Medium
- **Category:** Documentation / Design (process discipline)
- **CWE:** N/A
- **ISO/IEC 25010:** Maintainability (analysability, modifiability)
- **Risk score:** severity 3 × likelihood 3 × blast radius 2 = **18**
- **Location:** 13 ticket IDs absent from `docs/ROADMAP.md` and `docs/ROADMAP-shipped-audit.md` but present in `src/` and `tests/`:
  - T1-209 through T1-214 (WorkflowPanel phases)
  - T1-216 through T1-222 (v30 audit response — the safety fixes!)
- **Evidence:**
  ```
  $ grep -c "T1-222" docs/ROADMAP.md docs/ROADMAP-shipped-audit.md
  docs/ROADMAP.md:0
  docs/ROADMAP-shipped-audit.md:0
  $ grep -rln "T1-222" src/ tests/ | wc -l
  9
  ```
- **Analysis:** `CLAUDE.md` is explicit:
  > Every shipped fix is a coupled triple in one commit: code change + test, `ROADMAP.md` ticket marked `**Status:** Shipped in <hash>`, and `ROADMAP-shipped-audit.md` row moved to Shipped. A patch missing any of the three is incomplete.
  The most recent 13 tickets — including all 7 v30 audit safety fixes (T1-216..T1-222) — landed with code + tests but NOT with the roadmap + ledger updates. **Operational impact:**
  - The ROADMAP is significantly out of date.
  - The shipped-audit ledger doesn't record the safety fixes.
  - A future session reading `AGENT_HANDOFF.md` + ROADMAP would believe T1-202 was the last shipped item (see F-015).
  - The Phase 9 drift-detection script flagged this immediately on first run.
  **Safety impact:** none direct. **Process impact: real** — the discipline that catches "claimed shipped but never wired" (T1-120) depends on the ROADMAP being current.
- **Reproduction:**
  ```bash
  grep -rEoh "T[123]-[0-9]+" src/ tests/ electron/ | sort -u > /tmp/code-tickets.txt
  grep -rEoh "T[123]-[0-9]+" docs/ROADMAP.md docs/ROADMAP-shipped-audit.md | sort -u > /tmp/doc-tickets.txt
  comm -23 /tmp/code-tickets.txt /tmp/doc-tickets.txt
  ```
- **Recommendation:** File as a backfill ticket — **T1-223 (or successor)**. Update ROADMAP.md and ROADMAP-shipped-audit.md retroactively for each of the 13 orphan tickets. Each entry should include the commit hash (already available via `git log`), the brief description, and any verification notes. Effort: ~13 small commits or one batch. **Going forward:** the audit ledger + the drift-sample script already provide the detection mechanism; a future session should run the script as part of the pre-commit check.

---

### F-015: `docs/AGENT_HANDOFF.md` is significantly stale

- **Severity:** Low
- **Category:** Documentation
- **CWE:** N/A
- **ISO/IEC 25010:** Maintainability
- **Risk score:** severity 2 × likelihood 3 × blast radius 1 = **6**
- **Location:** `docs/AGENT_HANDOFF.md` — the "Current State" block.
- **Evidence:**
  ```
  $ grep -A1 "Last shipped" docs/AGENT_HANDOFF.md
  - Last shipped roadmap item: **T1-202** (controller-layer safety-event
    sink). This session-arc shipped **41 consecutive audit-driven...
  $ git log -1 --oneline
  542b0af1 docs(audit): AUDIT-2026-05-12 Phase 8 — test integrity (0 new findings)
  ```
- **Analysis:** AGENT_HANDOFF.md claims `T1-202` is the last shipped roadmap item, but tickets T1-203 through T1-222 (plus T2-6 phases 3x/3y, plus several workflow-panel phases) have all shipped since. A future session reading the handoff would have a wildly out-of-date picture of the repo state, including missing the entire v30 audit response (which fixed 6 High safety findings). **Safety impact:** none direct (the code itself is current). **Operational impact:** the handoff is the canonical "what does the next agent need to know?" document; a stale handoff means the next session re-discovers everything. This is a special case of F-014 — both are symptoms of the coupled-triple discipline not being enforced.
- **Reproduction:**
  ```bash
  grep -A1 "Last shipped" docs/AGENT_HANDOFF.md
  git log -1 --oneline
  ```
- **Recommendation:** Refresh `AGENT_HANDOFF.md` as part of the F-014 backfill commit. The new handoff should reflect: last shipped = the audit's final commit; current session-arc = the audit pass itself + the v30 audit response; outstanding work = the High findings (F-007, F-010, F-011), Medium findings, etc.

---

### Phase 9 exit criteria check

- [x] Sub-pass 9.1 (line-number drift) — script run; src/ clean, docs/ surfaces 32 stale citations (cross-listed F-014).
- [x] Sub-pass 9.3 (ticket-reference orphans) — 13 orphans identified, F-014 filed.
- [x] Sub-pass 9.4 (shipped-status hashes) — sample green; full pass deferred to backfill ticket.
- [x] Sub-pass 9.7 (handoff accuracy) — F-015 filed.
- [x] Sub-pass 9.8 (doc self-contradiction) — root cause same as F-014.
- [x] Sub-pass 9.9 (external claims) — 0 unverified claims in this ledger.
- [x] Audit-doc self-drift check — clean at commit time.

**Phase 9 exit: ✅** — proceed to Phase 10 (executive summary).

---

<!-- Phase 10 — Executive summary lands in the next commit. -->










